# What we know, what we believe, and what we got wrong

A living list, audited after every task. Entries are **promoted** (believed → proven) when an
artefact settles them, **retired** when they stop mattering, and **retracted** loudly when they
turn out false. Every proven entry names the artefact that proves it; every believed entry names
the experiment that would settle it. If an entry cannot do that, it does not belong here. The
standing hazards — traps, not claims — were section 4 of this file until 2026-09-26 and live in `docs/HAZARDS.md`
now, by the area each bites (R270); this file wins over that one on any disagreement.

Maintained by whoever is running the loop. Last audited: **2026-09-26 (the Sprint 14 close, `docs/DOC_MAINTENANCE.md` §5 step 2 -- a spot check of the #67, #51 and #31/#33/#40/#53/#56 rows)**. Before that: **2026-09-25 (Sprint 13 R5, the full read)** -- every row of all four sections read; settled section 2 rows moved to section 1 or 3 with their text, open tails of section 1 settled or moved to section 2, fixed section 4 states superseded in place. Before that: 2026-09-25 (the Sprint 11 and Sprint 12 closes, `docs/DOC_MAINTENANCE.md` §5 step 2 — a spot check each), 2026-09-23 (Sprint 10's close) and 2026-09-17 (`docs/audits/2026-09-17-audit-and-code-review.md`) -- rows settled or superseded on any of those days are marked in place.

---

## 1. Proven — with the artefact

| What | Artefact |
|---|---|
| **Dragging the window freezes the game for the length of the drag (2026-09-26, the owner's report).** The Win32 modal move loop (`WM_ENTERSIZEMOVE` to `WM_EXITSIZEMOVE`) runs on the thread that presents, and the guest's frame back-pressure waits on those presents, so the guest clock stops while the title bar is held -- the class `docs/HAZARDS.md` names: a render-thread stall stops the guest's clock rather than lowering its frame rate. Open as issue #67 (area render). Its closing bar: a scripted drag with the pc-sampler's `t=`/`vsync=` advancing through it, the audio counters unchanged, the gate 3/3, and a dragged instance in a control round. | issue #67; `docs/HAZARDS.md` (the render-thread stall); the pc-sampler trace of the closing run, when it exists |
| **A server-to-client record that writes the client's game memory, and one that reads it back, are refused on the client, every time (2026-09-25, Sprint 13 U6).** Found the same day by the project's own read of the recompiled network path (Task U5; the note is private on purpose). `6b30e42e` + `8df7c036`: every function-table entry of both handlers is replaced for both revisions, no knob, one log line at install and "not installed" on failure; the handler returns the library's success so the connection stays up on the client's side (a server awaiting the read record's reply may still time the client out). Our server never sends either record. Details are deliberately not written up here (SECURITY.md). | `ps2x_tests` 899/899 with the synthetic-buffer case (the bytes stay unchanged, the return is the success value); gate `s13_u6_gate` 3/3, PINS MATCH (13) on the relinked exe `7043cdd2…`; the review CLEAN on the second round. ~~Still owed: the r0004 gate and a control round on our server, both in the combined proof (Task 99 checks).~~ Superseded at the Sprint 14 close (2026-09-26): the r0004 gate ran, `s13_proof_gate_r0004` 3/3 PINS MATCH (Sprint 13's combined proof); the control round was Sprint 13 task O1's ladder, `logs/parity/ladder_20260925_194510` 4/4 usable (2026-09-25 23:11Z), with C3's `s13_c3_udp_shift_20260925_191848` PASS beside it; only O1's mixed leg (a console peer) waits for the owner. |
| **Two instances hold the guest clock at real time through a whole ladder round set: the two-instance "speed freeze" re-measured from existing logs (2026-09-25, Sprint 13 H3, R242).** Over the seven streak runs of the ladder (`ladder_20260920_101741`, `ladder_20260921_010147`, `ladder_20260921_025452`, `ladder_20260921_041614`, `ladder_20260922_234417`, `ladder_20260923_001218`, `ladder_20260923_005745`; both instances, 14 run logs), from READY (t >= 330 s) to the end: the guest VBlank rate is a median **59.7/s per instance** (min **54.4**, instance A of `ladder_20260921_010147`, whose 5th-percentile interval is 45.0), the EE clock runs at **1.000x** wall time on every instance, the GL presents a median **51.2/s** (min 41.1, the same instance) and the back-pressure wait takes a median of about **0.5 %** of wall time (max **9.2 %**, the same instance). The 19-21 fps per instance the freeze was set on (Sprint 5, `ours_match_play5`) was the game's own frame rate (`[vu1-stats]` syncv/s); **no `[vu1-stats]` line is in any of the 14 logs** (the ladder sets `PS2X_GS_STATS=1` only), so that number is NOT re-measured here -- what is measured is that neither instance's clock falls behind. A ladder run with `PS2X_VU1_STATS=1` is what gives syncv/s. | `logs/run_{A,B}_20260920_101750`, `_20260921_010154`, `_20260921_025459`, `_20260921_041622`, `_20260922_234446`, `_20260923_001226`, `_20260923_005755` (`[gs-gl stats]` elapsed / `guest_frames=` / `wait_ms=` / `present=`, and the `[pc-sampler]` `t=` / `vsync=` / `ee=` fields); the computation is `tools_py/research/ladder/r242_speed_freeze.py --root <the data root>` |
| **The generated image carries readable names from one sidecar, and renaming changed nothing but identifiers (2026-09-25, Sprint 12).** `recomp/socom2_names.csv` (1,840 rows, 1,771 readable names, a `Pass`/`Score`/`Evidence` on each; r0004 1,705 rows through `carry_names`) is read by the recompiler (`[general] names`) and written only by `tools_py/apply_names.py` from ten levers whose acceptance rules are code. The renamed tree against the pre-rename output: `renamed 1771`, `extents_changed: 0`, `kind_changed: 0`, `only_in_a: 0`, `only_in_b: 0`, `unhandled` and `unmapped` unchanged (`S12-R11 … OK`, `tools_py/recomp_census.py`); the runtime built from scratch and passed the r0001 gate 3/3 with PINS MATCH; the r0004 runtime built with its own sidecar (1,705 rows through `carry_names`) passed the r0004 gate 3/3 with PINS MATCH (`s12_names_r0004_gate`, 2026-09-25 07:40Z). A `FUN_<addr>`/`sub_<addr>` in an older row of this file means the address; the sidecar gives the current identifier (e.g. `FUN_002cc670` is generated as `UIDNASAuthenticate_0x2cc670` now). The counts above are the Sprint 12 reading. **2026-09-25, Sprint 13 N1 and N2:** N1 added 31 `Pass=hand` rows (the runtime's own names, `recomp/names_proposals_hand_2026-09-25.csv`) and N2 14 r0004 `Pass=hand` rows, so the sidecars are 1,871 rows (`socom2_names.csv`) and 1,736 (`socom2_names_r0004.csv`, `Loaded 1736 display names`); census renamed 31 / `extents_changed: 0` for N1, renamed 14 / extents 0 for the r0004 leg; gates `s13_proof2_gate2` and `s13_proof3_gate` (r0001) and `s13_names_r0004_gate` (r0004 exe `d027546d…`), each 3/3 PINS MATCH (the Sprint 13 plan's N1 and N2 rows). | The plan's Task 3 Step 4 RESULT (`docs/superpowers/plans/2026-09-24-sprint-12.md`, `83e9696c`); `logs/parity/gate/s12_names_gate` (exe sha256 `804dd172…`, `PINS MATCH (13 compared)`); the census diff `wt-s12/logs/census_diff.json` (git-ignored) |
| **The r0004 build passes the gate 3/3 under our runtime (2026-09-24, `s11_r0004_probe2`) and plays online on our own server (2026-09-24 evening: login `s11_r0004_online2`, lobby, host, join, a round with two r0004 instances' clocks in step `s11_r0004_round1`): the reboot that stopped it at the credits was two words of PSRewired's capsule baked into our dumped image.** Online, none of the expected divergences happened: r0004 presents r0001's AppId (10472) so Horizon's `muis.json` answers it unchanged, the new `MediusGameInfoRequest` is already modelled, DNAS never appears. **The two revisions cannot join each other's games**: the joiner sees the other revision's game, the join succeeds through Medius and DME, and then the client walks itself back to the online root menu within ten seconds while the host's lobby never shows it — the client-side join filter of research/43b §2, and 43a's Step 2 §2 for the r0001 token (the token, not the patch level), mutual and silent; the server cannot see or fix it. **The round is scored** (`s11_r0004_round2c`, later the same night: `CONTROL-ROUND` exit 0, no divergence, both instances controllable with real numbers, health 1.0 throughout, the two clocks in step to the second) once the online harness's instruments became revision-keyed (`4ab6e9f`: `scripts/parity/env.sh` had hard-coded r0001's `PS2X_PEEK`/`PS2X_CALL_TRACE` and the pre-launch checks and row readers r0001's offsets — the §4 struct-offset row's lesson, three seams deep; r0001's strings pinned byte for byte). Still r0001-only on purpose, unmeasured for r0004: the ladder's own reader constants (`sp_death_probe`, `freeze_trace`'s round-time range). From PSRewired's package (the Ghidra pass, the matcher at 81 %, the config translated through the match report, 2,956 forced entries, the address table's r0004 column, `dist/socom2_r0004.exe`) on the r0004 image `game/disc_r0004/socom2_game.elf`. The night's chain, each link measured before the next: (1) the game rebooted itself on purpose into `dlgAfterErrorReboot` (`FUN_0022ed10` → `LoadExecPS2`; the runtime now exits 74 `reboot-requested`, `a83a1cb`); (2) the loader's `operator new` had returned NULL — not a short heap: `EndOfHeap`/`sbrk` fire 0 times in a run (the game's own pool never walks to `sbrk`), the request was 0x06F97980 bytes, 111 MiB, 245× the run's largest legitimate size; (3) the size was `last − first` of a `std::vector` range copy inside `CButtonSpec::Clone` (`FUN_003A90E0`) reading through `$s1` = this + 0x3AC — a callee-saved register wrong after one call, while the object itself was intact all along (six traced gates); (4) the callee `FUN_002CC5F0` returned with `$s0`/`$s1`/`$sp` still its own because at `0x002CC670` the image held `jr $ra; nop` where its epilogue restores `$s1`/`$s0` — the two words PSRewired's capsule writes there (its r0001-layout stub table, decoded from the capsule's own stack), baked into the overlay we read out of PCSX2's memory with the capsule resident. Second instance of the §4 hazard below. Repaired at the image build as an undo of the capsule's decoded write stack (`tools_py/overlay_repair.py`, `07dc937` → `3bac4a2` → `3eba1b3` → `259bb48`; the r0001 image is byte-identical, a sidecar beside the image records every word and the sha of every input that decided it). Then the gate's own harness had to learn the revision: its guest probes read r0001's addresses and one r0001 struct offset (`91f245f`, the §4 row below), and its pin standard is now per revision (`aea1966`). **`s11_r0004_probe2`: title PASS at r0001's score profile, transition PASS, mission PASS with all four probes (root node, move scale, teleport steps, camera orbit), exe `185cb3b7…`, image `08be8ce7…`** — on the r0001 disc's assets, which is all the gate exercises. Found on the way and fixed for both revisions: `dispatchSyscallOverride` claimed a guest-copied handler (`0x80075000`, syscall 0x5B) it could not run and answered −1, which registered −1 as the handler for the event-flag syscalls 0x55–0x59 on every run, r0001 included (`38161fd`, `1b47be2`; r0001 gate `s11_override_gate` 3/3); `EndOfHeap` answered the image's top instead of the heap limit before `SetupHeap` (same commits; dead code for this title). *(Promoted from section 2 to section 1 on 2026-09-25 at the Sprint 11 close: this is the sprint's headline result with its artefacts, not a belief. The two halves that are genuinely still open are the section 2 row of the same date.)* | The reports beside the ledger: `task-19-reboot-report.md`, `-heap-report.md`, `-node-report.md`, `-register-report.md` (and their reviews); each reviewed to a clean re-review. Gates: `logs/parity/gate/s11_r0004_heap1` (0/3, pre-repair), `s11_r0004_reg2` (2/3, the harness), `s11_r0004_probe2` (**3/3**). |
| **r0004 is a real rebuild, PSRewired serves it, and its ELF is built (2026-09-23).** The r0004 package is in hand and decoded (2026-09-23 evening). PCSX2, driven by the harness through PSRewired's own guide (their DNS, their pnach), was handed the update by their universe the moment it connected — no persona, no lobby — and saved `APACHE00.ZDB` (1,602,296 B, sha256 `51dd9a95…`) to its card. It is a **real rebuild**: both overlays are bigger than r0001's, the banner reads *SOCOM 2 r0004 … Nov 3 2004*, the bare token `r0004` sits at `0x00421998` (the address the capsule's second patch table reads — the "other build" was r0004 all along). The loader's card path (`FUN_001c5b30` → `FUN_001c60b0`) is the disc path minus the signed cdvd container, but its last step is bound to the console identity that saved the package, so the offline decrypt is one number short; the overlays were read out of PCSX2's memory through PINE instead (ftscore 2,416,008 B `410fa485…`, zsealetc 1,721,912 B `4ca2c745…`, identical across two boots) and `scripts/build_revision.sh r0004 --stop-after elf` built the ELF: **5,016,016 B, sha256 `62f4f877…`**. Commits `8d86476` (the card-path decrypt tool) and `e1ee8db` (PINE batched reads). | `game/r0004/card/…/APACHE00.ZDB`, `game/r0004/pine/*.bin`, the r0004 ELF under the build_revision output (all ignored); `.superpowers/sdd/2026-09-23-sprint-11/task-19-download-report.md` and `task-19-decrypt-report.md` with screenshots under `logs/parity/pcsx2_psrewired/`; `tools_py/decrypt_apache.py`'s card path (`8d86476`) |
| **PSRewired's r0004 patch is a DNAS bypass, which this build already carries (2026-09-23).** Task 19 Steps 1–2 (2026-09-23, `agent/r0004`): the capsule's embedded stack decodes (the r0005 cipher) to 491 writes, all of them the resident hook engine; its whole game-visible effect is **one stubbed function, `FUN_002cc670` = the UI command `DNASAuthenticate`**, made to answer "done" on its first poll — a **DNAS bypass and nothing else**. The version token at `0x003F5DD8` ("r0001") is read only by the client-side join filter `VerifyPatchLevel` and never reaches the wire; the capsule leaves it alone, so every PSRewired player advertises r0001 too. The probe at `0x001C5B18` is a fingerprint of the boot ELF's overlay loader (`FUN_001c59c0`), re-checked on every syscall. **Our runtime already replaces that DNAS tick** (`game_overrides_socom2.cpp`, the DNAS override beside the memory-card update override) — so in PSRewired's sense this build *is* r0004 already, and there is no second recompilation to do. | `game/r0004/decoded/{stack.txt,summary.txt}` (ignored) from `tools_py/r0004/capsule.py` on `agent/r0004`; the Step 1 and Step 2 reports under `.superpowers/sdd/2026-09-23-sprint-11/` (on disk); `docs/research/43-r0004-capsule.md`; the r0005 sources in `research/r0005-patch/` for the cipher |
| **PSRewired's r0004 patch is a resident capsule over the r0001 disc, not a code package (2026-09-23).** The patch in hand is **PSRewired's capsule** `r0004v002.elf` (https://psrewired.com/downloads/r0004v002.elf, 67,267 bytes, sha256 `ad0ed7511b2c2d54…`; a git-ignored copy at `game/r0004/`). It is not the memory-card package: a MIPS ELF packed with ps2-packer (one LOAD at `0x01cf3400`, zlib payload at +0x18 → 125,838 bytes that load at `0x00100000`, entry `0x001000e0`), the Based_Skid/Harry62 tooling whose r0005 source is in `research/r0005-patch/` — the same `PasteASM`/`systemHook` shape. Its strings say what it does: loads SIO2MAN/CDVDMAN/PADMAN/MCMAN/MCSERV, hooks a kernel syscall through the vector table, looks for `mc0:UPDATE.DAT` ("Checking mc0 for patch… no update found"), shows "SOCOM II: Server — Patch: r0004", then `LoadExecPS2("cdrom0:\\SCUS_972.75;1")` — it boots the r0001 disc and patches it in memory. The patch body is an encrypted code stack (the high-entropy block at `0x0011c800`; r0005's `update.dat` has the same shape: a version string, then XOR-keyed address/value pairs) applied by the hook once the game is loaded; only 77 constant stores are in the ELF's own code, all its own globals and the GIF/DMAC registers of its splash screen. | `game/r0004/r0004v002.elf` (ignored; sha256 above), its unpacked image `r0004v002.unpacked.bin` and the 77 constant stores in `writes_r0004v002.txt` beside it; `research/r0005-patch/` (the same tooling's source: `README.txt`, `C_Source/059/main.c`, `systemHook.s`, `update.dat`) |
| **The chat receive path is bounded on the client, and the bound installs on every launch (2026-09-23).** Sprint 11 Task 2 (`f2064d3`) wraps the game's chat receive callback and holds a received packet's name and message fields to their declared widths before the game's own code reads them; the wrap announces itself once per process. The first gate on the merged exe carries the line in all three lanes, and the address table of Task 10 (`86c2912`) is in the same exe -- so the table changes nothing at r0001. The server half (Task 3, `4d7e481`: forwarded chat fields clamped) was deployed to the project box at 06:58Z the same day -- restart clean, the four public ports answering (`logs/chain8b.result`). *Narrowed 2026-09-26 (Sprint 13 O2, research/66): the wrap installs on every launch, and in two two-instance rounds it was never entered while game-lobby chat lines crossed both ways (§2, #26) -- "bounded on the client" holds for what reaches `chatFanoutRecv`, and a **game-lobby** chat line (the only room O2 typed in) does not reach it, and briefing-room and in-round chat are untested; whether the game-lobby line passes the server's MLS clamp is not established (its server path was not logged).* | `[socom2] chat receive bound (name 32, message 64)` once each in `logs/parity/gate/s11_open_gate/{title,transition,mission}.game.log`; that gate PASS 3/3 on `dist/socom2.exe` sha256 `ce43934d…` (2026-09-23 05:3xZ); the C++ suite green on the same build (`logs/chain7_cpp.log`); the matcher run against r0001 itself resolves 14,828 of 14,879 functions to themselves (99.66%), the rest being bodies it cannot read (`tools_py/address_matcher.py`, T10's report) |
| **Upstream since our vendored base is exactly PR #244, and #244 has no SPU2 (2026-09-23).** The differential test ran the disc's real `989SND.IRX` on #244's standalone IOP and compared its RPC answers with our `snd989` model: 1,775 of 1,794 calls agree; the 19 that do not are 13 emulator gaps and two model alignments, both landed. #244's `0x1F900000-0x1FA00000` range is a plain register bag, so the streamer half of the real IRX is blind there and a native libsd is not justified (R245) | `docs/research/40-upstream-divergence.md` §5-§6 and §9 with `docs/research/assets/40-irx-differential/` (session socom-pc-09, `agent/upstream` merged at 45e5fce); the two alignments 7b44d70 and 776e423 with their tests |
| **The mission music's DEVICE dips are OURS, not the owner's Bluetooth endpoint: they survive on a wired one (2026-09-23).** The same sixteen-minute briefing capture was taken twice by `scripts/parity/endpoint_ab.sh`, which backs up the per-app routing, points our exe and the recorder at the wired **HyperX QuadCast S** output, refuses to score unless the game's own log names that device, and restores the routing on any exit: **wired 14 DEVICE dips against Bluetooth 11** -- events present in the endpoint recording and absent from the mixer's dump at the aligned time. *(Corrected 2026-09-26, Sprint 15 R1, research/68 §2-§3: four of the wired 14 fall past the dump's end, where there is no dump to compare; the scorer has called those NODUMP since `9d9da69e`, and on today's scorer the A/B reads **wired 10 DEVICE (+4 NODUMP) against Bluetooth 11** -- `python -m tools_py.parity.audio_dips logs/parity/endpoint_ab_20260922_232644_wired/endpoint.wav --dump logs/parity/endpoint_ab_20260922_232644_wired/mix.wav --log logs/run_20260922_232655.log`. The conclusion stands: the dips survive the wired endpoint.)* The dips do not vanish with the Bluetooth path, so whatever loses them sits on our side of the mixer's dump point, after `render()` and before the write, and is endpoint-independent. This retracts the endpoint attribution carried since 2026-09-20 by the "~41 times a minute" row below and by the §4 hazard on naming the endpoint; the measurements themselves stand. **What it does not say:** what the 50 ms hole actually is -- that is the open §2 row. | `logs/parity/endpoint_ab_20260922_232644` (both runs, the per-minute DEVICE tables, and `endpoint_ab.sh`'s `VERDICT: the DEVICE events survive on the wired endpoint (wired 14 vs Bluetooth 11) -- they are ours`), run 02:43Z 2026-09-23. The Bluetooth half is the same instrument as `logs/parity/mission_music_ours_20260922_024457` (11 dips) and the W7 walking capture that preceded it (`logs/parity/mission_music_ours_20260922_224425`: 47 DEVICE events at the JBL, peak 31 in one minute *(today 5 DEVICE, at most 2 in a minute, and 42 NODUMP past the ten-minute dump's end; research/68 §2)*, over **a twelve-minute walk inside a 21-minute capture** -- the drive walks its legs for twelve minutes inside a recording that runs 01:44:22 to 02:05:22) -- W7's lead is superseded by this A/B, which was run the same night |
| **A developer with their own r0001 disc can build and run the game from a fresh clone: one command takes the ISO to the buildable ELF, and the whole way to `dist/socom2.exe` is 29 minutes (2026-09-21).** `scripts/disc_to_elf.sh <iso>` extracts the disc in pure Python, runs the DNAS self-decrypt and the retail loader's own decryption under Unicorn, merges the ELF, and verifies its own output against a tracked expectations file (R230); it is idempotent (a second run is 1 s of checks) and refuses a non-r0001 disc, a wrong image, a missing Unicorn and a bad merged ELF with a sentence and an exit code. | The from-nothing run: empty `game/` in a scratch worktree, `disc_to_elf` 483 s -> `./build.sh recomp` 273 s (`14882 files`) -> `./build.sh runtime` 983 s -> the exe; the owner's six 2026-09-04 hand-run digests reproduced byte-identical and the 349 extracted files matching their tree in name and size; 43 tests (`tools_py/tests/test_disc_to_elf.py`), suite 1712 OK; `docs/archive/sprints-7-12/2026-09-21-sprint-10-disc-to-elf.md`. **Believed, not proven:** any other r0001 dump (a differently laid-out image is allowed by design, R231) and the chain on Linux. *(Settled in part 2026-09-25, Sprint 13 R5: the recompile step runs on Linux -- Sprint 12 ran `ps2_recomp` there in 52 s and reproduced the census (`docs/CURRENT_SPRINT.md`, Sprint 12's task table, row 3a); `disc_to_elf.sh` itself has never run on Linux, and no second r0001 dump has been through it -- both are section 2's row "The disc-to-ELF chain has run on one disc, on Windows".)* |
| **A header that defines its state in an anonymous namespace gives every translation unit its own copy -- and the runtime's stub helpers depended on that (2026-09-21).** `Kernel/Stubs/Helpers/Support.h` was moved to a `.cpp` as build hygiene (one definition instead of nineteen copies of 2041 lines); the C++ suite stayed 764/0 and the gate's mission stage then never reached the HUD: the GL replay latched at t=200 s, pending commands climbed 1-2 -> 18,852, and Q6's stall bound absorbed for the remaining 280 s while the guest ran on. The nineteen stub units had each held their own `g_cdFilesByKey`, `g_cdStreamingLbn`, `g_cdMode`, `g_cdInitialized`, `g_nextPseudoLbn` and `g_iopHeapNext`; sharing one CD cursor and one IOP heap between the CD, MPEG and audio stubs is a behaviour change. Reverted (`955539c`); the same gate then passed 3/3. **Attribution corrected 2026-09-24 (Sprint 11 Task 8b):** "between the CD, MPEG and audio stubs" does not survive a reader scan of today's tree. Every one of the fourteen CD variables (`Kernel/Stubs/Helpers/Support.h`) is referenced from exactly one `.cpp` in the whole runtime, `Kernel/Stubs/CD.cpp`, and so are the eight Support.h helpers that touch them (`registerCdFile`, `readCdSectors`, `readHostRange`, `isResolvableCdLbn`, `cdStreamingEndLbnForStart`, `ensureCdLeafIndex`, `findRegisteredCdFileForLbn`, `tryGetCdImageTotalSectors`); `g_iopHeapNext` is read and written only at `Kernel/Stubs/SIF.cpp:168,184,194`. `MPEG.cpp` and `Audio.cpp` do compile a private copy of the CD group -- they include `Common.h` -- but they never read or write it, so the sharing is compile-time only and there were never two live CD cursors to merge. The mechanism and the artefacts stand; what is not established is which group broke the mission stage, because 955539c collapsed all 2041 lines at once and never isolated one. The row stays open on that basis, not on the attribution. *(Moved out 2026-09-25, Sprint 13 R5: the open half -- which group broke the mission stage -- is section 2's row "Which group of the stub helpers' per-TU state broke the mission stage"; this row keeps the mechanism and its artefacts, and the item's "stays open" below is that row's.)* | `logs/parity/gate/s10_q7_gate/` (FAIL mission, the `bp_pending` series in `mission.game.log`, `[gs-gl] stall bound engaged` once) against `s10_q7b_gate` (PASS 3/3) on the reverted tree; `docs/archive/sprints-7-12/2026-09-21-sprint-10-q7-residuals.md`. The item stays open: one group of state at a time, each with a gate. **Four groups are now out of the anonymous namespace (2026-09-24, Task 8b, `agent/rtstate`):** the stub-warning and PS2-printf throttles, the DMA stub's in-flight model and environment block, the GS GParam and the libc FILE table and rand cursor are named structs owned by `PS2Runtime`, each under a test that two runtimes in one process share none of it. The CD group and `g_iopHeapNext` are deliberately untouched. **The last two followed on 2026-09-26 (issue #51 (closed) 2026-09-26: merged into `main` at `123dd1c5`, PR #65; `agent/x51`: `1f358cb5` the CD group as `CdRuntimeState`, `c69edf76` `g_iopHeapNext` as `IopHeapRuntimeState`; gates `x51_gate_cd` and `x51_gate_iop` both 3/3):** Support.h's anonymous namespace now defines no namespace-scope mutable state, only constants, using-declarations and functions (one function-local log-once flag in `registerCdFile` remains per translation unit, reached from `CD.cpp` alone). Two guest-observable consequences for a gate log: the three 200-line `PS2 printf:` budgets (`Compatibility.cpp`, `LibC.cpp`, `TTY.cpp`) are now one budget per runtime, so a run can carry up to 400 fewer `PS2 printf:` / `PS2 scePrintf:` lines and the "suppressed after 200 lines" notice fires once rather than three times; and `sceDmaReset` now clears the pending-poll map with the environment block, so a channel marked in flight no longer reports busy after a controller reset. |
| **A stranger's environment cannot change the game (Sprint 10 Q2 / Goal 3, 2026-09-21).** Every `PS2X_*` read goes through one accessor over the registry in `ps2x/knobs.h`, whose complete count by class is `docs/KNOBS.md`'s own first lines (generated from the header and held to it by `test_knobs_registry`, both directions -- this row said "151-row" and "20 Shipping" until 2026-09-23, against the generated file's 150 / 18, so it now points instead of counting); the **Shipping** names are `config.json` settings the launcher sends; every Dev knob reads as unset unless `--dev`/`PS2X_DEV=1`; the launcher hands the game none of its inherited `PS2X_*`; eight "X=0 turns it on" switches are flags; a Path knob outside the game folder reads as refused (R207); the login password never reaches a log line (R208). | `s9_g3_poisoned_env` (six probes set: `[knobs] dev=0 ... ignored without --dev: <the six>`, 90 s clean, `done 124`); gates `s9_g3_batches_gate` and `s9_g3_gating_gate` 3/3 with their `[knobs] dev=1` lines and PINS MATCH; the Foxhunt control round `s9_g3_control`; `Knobs` suite, `test_knobs_line`, `test_knobs_registry`; CI green. Believed, not yet proven: the Linux runtime with the flip (the VM's rebuild is deferred, R209; CI builds the library and launcher only). *(Settled 2026-09-25, Sprint 13 R5: the VM rebuilt the runtime with the flip on 2026-09-23 -- `logs/chain9b_runtime.log` `runtime rc=0`, and the knob-line and exit-code cases on `dist-linux/socom2` `Ran 12 tests ... OK` in `logs/chain9b_knobs.log`; the VM's whole-suite result is section 2's issue #25 row, not this one.)* |
| **The intro and briefing music's 300-700 ms dropouts were the MPEG HLE's back-pressure gate: it refused the game's whole demux read once eight decoded pictures were queued, and no audio moved while it was shut; the briefing's low-bitrate video packs 5-10 tiny pictures per 16 KB read, so the gate stayed shut 7-13 frames and the 128 ms PCM ring starved before the next audio burst** (Sprint 10, music round four, 2026-09-21). Found with a per-guest-thread scheduler trace (`PS2X_SCHED_TRACE=1`, `a384dfb`) after the decode-ahead worker (`a6833cc`) had ruled out the ffmpeg decode and the trace had ruled out the drive and the picture waits: the feeder thread was present all along, polling a shut gate ~4000 times a second. Fixed `619c139`: the gate is stream time (~200 ms of pictures ahead), refused video is held in order, audio is re-offered every tick. Run `s10_r4v`: in-stream PCM holes on the briefing 10-11 -> 0, ring underruns 0 for the whole play. | `docs/archive/sprints-7-12/2026-09-20-sprint-10-music-round-four.md` task 4 item 16; `logs/parity/s10_r4v_music_ours/`; `ps2_runtime_expansion_tests.cpp` (the two item-16 cases) |
| **The mission music's pauses are the mission's own design, proven identical on both machines: at stealth level the `.rdr` playlist is a 3 s stinger, ~10 s of rest, a stinger (`rest 9.0 s | MGEN0014 | rest 8.5 s | MGEN0009 | ...`), and only the fight levels (the AI's alert 2/3) play a continuous stem** (Sprint 10, music round four, 2026-09-21). The EE music state machine read to the source (decomp :245347-246077: levels, four weighted playlists per mission, rest entries with PAUSETIME, the alert from the AI's threat float) and polled on both machines through the same walk (`music_state_poll --what music`, ours by the runtime's peek, the console over PINE): the same lists, the cursor through every rest and stem in order on ours, the console starting its list during the flyover. Nothing in the music's decisions differs any more. | `logs/parity/s10_r4s_music_ours/music_state.txt`, `logs/parity/s10_r4t_music_pcsx2/music_state.txt`; the plan's item 15 |
| **Every stereo music stem on ours played its two channels from DIFFERENT positions in the song -- a two-channel VPK is interleaved per 0xb000 streaming buffer (0x5800 bytes of left, then 0x5800 of right), and ours split it per 0x800 chunk** (Sprint 10, music round four, 2026-09-21). Found from the owner's "it doesn't even sound like music": content analysis of the loopback captures (L against R, 4 kHz, lag search +/-2 s) read the console's mission music at +0.26..+0.59 correlation at lag 0 and ours at ~0, with the best match at +61 / +136 / +561 / +674 ms per stem; every level, modulation, flatness, click and spectral-peak measure read the same on both machines, which is why 10 rounds of level instruments passed it. Read in the IRX's stream open (`FUN_00013334`: header word 3 must equal the game's 0xb000 streaming buffer, split `>> 1` per channel). Fixed `c6502ea` with a test; the parity scorer now carries `lr_corr0 / lr_lag_ms / lr_best / side_mid_db` per window and a stereo-desync rule (`37579aa`), which flagged six windows of the pre-fix build. | `docs/archive/sprints-7-12/2026-09-20-sprint-10-music-round-four.md` task 4 (the lead and the cause); `logs/parity/s10_r4k_music_ours/audio_parity.txt` (the six `stereo desync` windows); `tools_py/tests/test_audio_parity_stereo.py` (17) |
| **The music-only capture read ours 31 dB under the console because Windows' per-app session volume for `socom2.exe` on the capture endpoint was at 3 percent -- the loopback tap sits after that slider, and the game, the mixer and the model were never 31 dB low** (Sprint 10, music round four, 2026-09-20). Run 8 (`s10_r4h`) scored 6/177 windows with a median -31 dB on the title ring and the mission stems alike; the fix agent ruled out every stage in code (the mixer's own dump 8 dB ABOVE the console) and named the per-app slider; `app_volume hold` then logged `socom2.exe session first seen: vol 0.03` at the next launch. The capture now holds the launched game's session at 1.0 / unmuted and logs what it found, so a level difference between two captures is the game's. **Every earlier ours-vs-console level reading through this endpoint is suspect by the same amount** (the movie-audio row below included). | `tools_py/parity/app_volume.py` (+4 tests), `scripts/parity/audio_parity.sh` (the hold); `logs/parity/s10_r4j_music_ours/app_volume.log`; the round-four plan task 4 |
| **The real 989snd decompilation (Ziemas, `research/989snd-ziemas/`, git-ignored) overturned the still-playing model of `77d5522`: deactivation clears bit 31 of the handle word and the lookup compares the whole word, so a played-out stream answers 0 on the next poll, and the EE's music manager starts every next stem FRESH (queue 0) on that answer** (Sprint 10, music round four, 2026-09-20). Audit `docs/research/36-989snd-decomp-audit.md`; three model commits with tests (`378a87b` the answer and the slot, `1f6f7be` SetSoundParams answers a live stream, `554972e` the square-law group stage from vol.c:434). Measured on the first VALID music-only capture pair (both machines through AUDIO OPTIONS with SOUND and DIALOG at zero, the same walk and contact): run 8 on the old exe 6/177 windows, stems 60-75 s apart; run 9b on the new exe 77/177, stems 0-2.3 s apart at 8 of 11 boundaries. Residual: three mid-mission gaps of 10-22 s (item 6) and ~6 dB under the console on both music routes (item 7), both under investigation; the owner's ear is the bar. *(Settled 2026-09-25, Sprint 13 R5, from the round-four record's task 4: item 6's gaps are the game's own -- the EE music manager sat in state 3 with an empty cue queue, the stealth rests of the row "The mission music's pauses are the mission's own design"; item 7 on the stems was ours and is fixed (`6ef6419`: a stereo stream is the console's voice pair; run 10 `s10_r4k` 126/177, `D:/socom_archive/parity/s10_r4k_music_ours`); the title ring's -4..-6.5 dB was left unexplained and lives in section 4's movie-audio row. The owner's ear was given on 2026-09-22 (the playthrough: menu music good, the mission music degrading with time) -- that is issue #28's row in section 2.)* The stream engine (stream.c) is signatures only in the decomp, so the streamer is still read from our own IRX disassembly. | `logs/parity/s10_r4h_music_ours/audio_parity.txt` (6/177), `logs/parity/s10_r4j_music_ours/audio_parity.txt` (77/177); `scripts/parity/refs/audio_music_only_mission.pcsx2.json` (run 8, every checkpoint verified); `scripts/parity/music_only_mission.txt` (the eight navigation facts in its header) |
| **OURS joins a game a console client (PCSX2) hosts on the hosted server and the round runs with both moving** (Sprint 10 Goal 3 leg 2, run f, 2026-09-20): the console hosted through the verified flow (`pcsx2_shell host B`: CREATE GAME, Frostfire accepted at row 4 on its own map reference, the lobby), ours joined through `online_login_ours --join` (list, enter, continue, each on the first press), both readied, the match launched; ours moved through 27 positions (341 in-game peek rows), the console through 24 (PINE). Five runs before it each found one thing: the console's CREATE GAME and PLAY LIST titles and its map rows need console references; a shell with no injected pad must post its pad presses; two guests on one host collide on peer UDP 3658 (the console host must be the instance whose pnach shifts it to 3660 -- "Disconnected from Game" otherwise); a host that is READY before the join launches the match under the joiner's lobby check. | `logs/parity/mixed2_pcsx2_hosts_f/`; the Goal 3 plan task 4 |
| **A console client (PCSX2) joins a game OURS hosts on the hosted server and the round runs with both moving** (Sprint 10 Goal 3 leg 1, run d, 2026-09-20): the console's verified join (`pcsx2_shell join B`), both READY, the match launched, the console's camera position moved 509 -> 564 over four stick holds (PINE, 100 in-game rows) while ours' peek moved through 39 positions. The first mixed match to run on the verified flow; three runs before it each found one thing (the console's narrower screen, the READY label edges, the 30 s notice, the stick keys). | `logs/parity/mixed2_ours_hosts_d/` (drive log, pcsx2/, pcsx2_pos.txt); the Goal 3 plan task 3 |
| **A console client (PCSX2) logs into the HOSTED server through the screen-verified flow, with our renderer's references** (Sprint 10 Goal 3 task 2, 2026-09-20): `tools_py.parity.pcsx2_shell login B` -- the same staged steps and detectors as ours over a PCSX2 window at a 640x448 client -- booted, read the main menu after 3 presses (the blind recipe pressed 4 and walked into NEW GAME), created a persona through the verified on-screen keyboard, connected to `US East (Ohio)` and reached the briefing room: `LOBBY class=ok`, every step `verified=True attempt=1`, 264 s, not one new reference cut. The first console login to the project's hosted box. | `logs/parity/s10_pcsx2_login1/` (B_00..B_11, drive.txt); `docs/archive/sprints-7-12/2026-09-20-sprint-10-goal-3-mixed-match.md` |
| **The mission ambience is a CONDUCTOR sound (bank M51_AM 0x31: START/STOP_CHILD_SOUND, TEST_REGISTER on global 2, GOTO_MARKER, LOOP) and our mixer ran none of those grains -- the console's continuous bed at the mission start, silence on ours** (Sprint 9 Q0, 2026-09-20). The audio parity check measured it (s24 69% silent on ours, 0% on the console, every requested stream playing its full disc length at the right level); the disc showed it (`tests/fixtures/audio/m51_am_block.bin`, 33 grains); the game writes the register it branches on every frame (`snd_SetGlobalReg(2, 6)`). Modelled from the open 989snd reference (children, 32 globals + 4 handler registers, markers, RAND_PLAY/PLAY_CYCLE, BRANCH, the tone sentinels); pinned by three tests. The plan's section 6f. | `docs/archive/sprints-7-12/2026-09-20-sprint-9-q0-mission-music-investigation.md` 6f; `logs/parity/s9_q1_parity_ours/audio_parity.txt` (before: FAIL 10/48) and `s9_q1_parity_ours2/` (after: 31/48, no mission window silent); gate `s9_q0_children_gate` 3/3; CI `7de8492`. Still 7-12 dB under the console's bed -- the child volume chain, section 2 *(Settled 2026-09-25, Sprint 13 R5: section 2 never carried that row. The 7-12 dB was read through the capture endpoint before its per-app session was found at 3 percent (the row "The music-only capture read ours 31 dB under the console"), so the figure is unattributed, and the owner's 2026-09-22 playthrough did not name the bed. No issue: re-measured only if a listener reports the bed low.)* |
| **Every stream's first callback starved -- one device period of silence at the start of every stream, 21 in the owner's own run** (Sprint 9 Q0, the owner's second listen, 2026-09-20). The worker fills a new stream's ring on its own 10 ms cadence; the render that landed before its first pass found the ring empty. With the 20 ms device period the hole doubled. A stem on the heels of another opened with a hole; a positioned voice line started late -- the owner's "stuttering, skipping a bit". Fixed: `playStream` decodes the first chunk pair itself, on the caller's thread, before the push | `dist/logs/run_20260920_034239.log` (the owner's run: 21 `UNDERRUN` lines, every one at a `start`, `silent=960` each); `socom2_audio_tests.cpp` "a stream has sound in its very first render call" (RED: three UNDERRUNs at frame 0, peak 0); gate `s9_q0_prefill_gate` |
| **The mission music through the owner's speaker drops out ~41 times a minute, and the pre-device mix does not** (Sprint 9 Q0, 2026-09-20). Same driven mission minute (WAV 340-400 s): the mix as rendered has 5 silences (2 sub-second); what Windows sent to the JBL Flip 6 has 45, 41 of them 55-350 ms. PCSX2 on the same endpoint with the same recorder: 0 in three minutes. The dropouts appear between `render()` and the speaker: the dump is written per callback and cannot see a late callback, the endpoint can. raylib opens the device at miniaudio's low-latency defaults, 10 ms periods x 3 (`raudio.c:474`); under gameplay load the device thread misses that ~40 times a minute. ~~**This is the "skips" the owner reports, on the path only the owner had ever listened on: every earlier measurement ran on a wired 48 kHz endpoint**~~ **The measurement stands; the ATTRIBUTION TO THE ENDPOINT is SUPERSEDED 2026-09-23 by the endpoint A/B (the row "The mission music's DEVICE dips are OURS, not the owner's Bluetooth endpoint"):** the same class of dip survives on a wired endpoint (wired 14 against Bluetooth 11 over sixteen minutes (10 DEVICE + 4 NODUMP on today's scorer, research/68 §3), `logs/parity/endpoint_ab_20260922_232644`), so what is between `render()` and the dump's write is ours, whatever the device is. The 42 -> 2 improvement from the 20 ms x 4 mix device is unaffected -- it was measured on the same endpoint before and after | `logs/parity/s9_q0_m51_trace/` (`mission_audio.wav` the dump, `mission_endpoint_loopback.wav` the JBL endpoint, `mission.game.log` the trace), `tools_py/parity/loopback_record.py` (WASAPI loopback, pyaudiowpatch), `tools_py/parity/audio_envelope.py silences()`; the console: `logs/parity/pcsx2_mission_audio_ref/pcsx2_mission_loopback.wav`. The fix -- the runtime's own playback device at 20 ms x 4 (`runtime/mix_device.h`, `ps2_audio.cpp`) -- is measured by the same run repeated: **`s9_q0_m51_trace2`: endpoint dropouts in the mission minute 42 -> 2 (sub-second), the endpoint now mirroring the dump (3 silences / 4); the run's own mix-open line reads `device Speakers (JBL Flip 6), period 20 ms x 4, engine 48000 Hz`, 960 frames a call; gate 3/3 `s9_q0_device_gate`** |
| **Every music cue played ~2.3 dB to the right, and the game's positioned voice lines could never be raised from silence** (Sprint 9 Q0, 2026-09-20). Two protocol bugs certain from reading and pinned by tests: `snd989.cpp` split `snd_PlayVAGStreamByLoc`'s vol|off and pan|off words with `>> 16` and no sign, so the game's pan 0xffff (-1, the default) reached the host as 65535 and the pan table wrapped it to 105 degrees; and `Mixer::setVolPan` looked up only bank handlers, so `snd_SetSoundParams` on a STREAM handle did nothing -- and the game starts its positioned voice streams at vol 0 and raises them with exactly that call (one per stream in the mission's first 30 s) | `socom2_audio_tests.cpp`: "the 16-bit vol and pan halves are SIGNED on the wire" (RED: "the wire says 65535"), "snd_SetSoundParams reaches a STREAM handle" (RED: "audible once the game raises it (peak 0)"); gate 3/3 `s9_q0_trace_gate` (pan), `s9_q0_device_gate` (setVolPan) |
| **Every stream's start, end and underrun is now stamped on the output-frame clock the audio dump is written on** (Sprint 9 Q0). `snd989::StreamEvent` -- start (the frames rendered before the push), done (the render call where the data ran out, or 0xFFFFFFFF when the game stopped it), UNDERRUN (per render call, with the frames left silent) -- as `[audio] 989snd stream <h> start|done|UNDERRUN frame=N ...` and to a test sink. A VAG stream underrun was counted NOWHERE before this (only the movie ring had a counter); the run that had been called clean was blind to the class. `tools_py/parity/stream_events.py` pairs them into boundary gaps and starvation per stem | `socom2_audio_tests.cpp` (three cases, each RED watched: "done at frame 0", "0 underrun events"); `tools_py/tests/test_stream_events.py` (5); the first traced run `logs/parity/s9_q0_m51_trace/mission.game.log` |
| **The launcher reaches the project's server by NAME, and switching it could not have cost a persona** (Sprint 9 P6, R175, 2026-09-20). The default preset's address is `socom.scotho.com`; a fourth preset "SOCOM Unzipped (by address)" kept `3.143.65.100` on offer for a name that will not resolve until the owner had it removed on 2026-09-20 (Custom takes the address; a config naming `unzipped-ip` heals to `unzipped`). An old `config.json` naming the `unzipped` preset keeps working and simply starts reaching the box by name -- the id is the stable thing, not the address. The switch is safe because the preset string never reaches the game: `loadHosts()` resolves it to a `uint32_t` and maps the seven retail Sony hostnames to that | `nslookup socom.scotho.com` -> 3.143.65.100 (2026-09-20); `socom2_hostnet.cpp:303-316` and `:306-332` read twice, independently; suite 677/677, including four pre-existing assertions moved from the address to the name, and a test that an old config resolves to the name. **Not yet done by a real login:** no driven run has logged in by name -- R175 records why that measurement is a tautology rather than evidence, and the owner's playtest is the first real use *(Settled 2026-09-25, Sprint 13 R5: real logins by name happened on 2026-09-22 -- `dist/logs/run_20260922_143034.log` through `run_20260922_145633.log` carry a `[knobs] dev=0` line with `PS2X_SOCOM2_SERVER=socom.scotho.com` from the launcher, and `run_20260922_143034.log` opens the headset (`lgAudOpen`), which the game does only in an online game session (section 2's voice row).)* |
| **FIXED 2026-09-16 (research/34): the online round that never started on the day's build — every map, Frostfire included — was the GL texture cache exploding on CLUT snapshot serials.** `3d37abc` minted a serial for every palette load whose bytes differed from the previous one and put it in the texture key; SOCOM II alternates palettes on consecutive draws (HUD, skins), so an online round reached 55,483 cache entries and 64k uploads a second, the render thread 2 fps, and the guest clock (timer T0, host-paced minus the render back-pressure wait) 0.04 s per wall second: the STARTING ROUND / OBJECTIVE messages never expired and nobody moved while the network round clock ran. Ids are keyed by palette content now. | Bisect over four launches (`ours_control_frostfire_{regress,bisect1,bisect3}`: only the tree without `3d37abc`/`545b85a` passed); mechanism on `ours_control_frostfire_headstats` (`[gs-gl stats]` textures 414 → 55,483, `[clock]` 2.5 s guest per 42 s host); fix verified `ours_control_frostfire_clutfix` (CONTROL-ROUND exit 0, cache steady at 292, 43–45 fps, guest clock 0.74 s/s); `ps2_gs_tests` 'a CLUT re-loaded with the same bytes re-uses its snapshot id'. |
| **THE ACCEPTANCE TEST PASSED — an automated two-instance online match reached a kill** (Sprint 5 ladder launch 2, Frostfire, pinned harness `171290b`, frozen exe `234b4772cd0a8bf8`): rounds 1–3 KILL on both KillWatch (actor fields) and `verdict_replay` (valves). Round 1: victim B `+0x1044` 1.0 → 0.298 → **0.0** with word 0 `0x6691a0` intact; `+0xF7A` 1 → **2** on the death row (→ 3 at +3.7 s); killer A's single R1 burst 0.59 s before, 27.9–29.2 units 3-D, dy 0; `total_mp_kills` 0→1 **on the killer's instance only**; the victim team's `aiteam_08` 1→0 on A at +0.02 s and on B at +0.25 s; both screens "socomc fragged socome with M4A1" ≤ 0.33 s after. Route v2 arrived all 4 rounds (77–89 s); round 4 missed at −4.1° aim error with 111 bursts. The death readout is now proven online (settles the §2 row and Goal 2) | `logs/parity/s5_t5_ladder2`, `logs/run_[AB]_20260913_230442.log`, `docs/research/22-kill-readout.md` §Ladder launch 2, `docs/research/assets/22-first-kill.png`; independent clause-by-clause re-derivation; evidence archived to `D:/socom_archive/acceptance/s5_ladder2/` (run logs sha256 A `af8b92618dd75f47…`, B `b57e1b26912f5b5b…`, full list in `SHA256SUMS` and tracked manifest `docs/research/assets/22-first-kill-evidence.txt`); pinned by `test_verdict_replay.TestLadderLaunch2Round1Kill` (`d9b5f33`) |
| **The kill REPEATS on the block-pointer exe** (2026-09-15 evening, `s6_ladder8`, harness `d6e417f`, exe `1cfef9af028a90fc…`, host quiet): **4 of 4 rounds KILL on KillWatch** (health `+0x1044` → 0.0 on B's intact actor, t = T+75.2 / 206.6 / 324.4 / 440.5 s), rungs 3,3,1,3, the route arrived every round; **round 2 used the new lead correction** (`lead=+1.5 steps=1 hits=3`: three misses inside tolerance, one lead step, then the kill). `verdict_replay --per-round` (the valve scorer): **rounds 1 and 2 KILL**, round 3 `NO-KILL unattributed` (the fatal burst was fired from 68.2 units 3-D against the pre-registered ≤ 60 bar — the bar stands, the round is not doubly attributed), round 4 `NO-DATA` (the run stops at the last kill before the valve window's rows arrive — harness tail to add). Against Sprint 5's one launch (3 of 4, round 4 never corrected): the aim loop now corrects, and the lobby reached gameplay once every press was verified | `logs/parity/s6_ladder8`, `logs/run_[AB]_20260915_232317.log`, `drive_s6_ladder8.txt`; second launch `s6_ladder9` pending for the plan's 2-of-2 bar *(Settled 2026-09-25, Sprint 13 R5: `s6_ladder9` never started; the second launch on the current exe was `s6_ladder12` (`D:/socom_archive/parity/s6_ladder12`; `docs/STATUS.md` 2026-09-17 afternoon: 4 of 4 rounds usable, kills in 3), and the kill has since repeated on the hosted server (`s8_hosted_kill`, `D:/socom_archive/parity/s8_hosted_kill`, 2 kills in 4 rounds). The plan's strict bar -- both scorers agreeing on two launches -- was never recorded and is not owed: `docs/audits/2026-09-17-audit-and-code-review.md` scored repeatability "effectively met".)* |
| **Spawns are deterministic per map, and a clock round end resets both players to them with the actor block kept**: Frostfire A (796, 100, 614) / B (536, 143, 1254), 2-D gap 691, dy 42, two discrete floors (y ≈ 100 / 142); Vigilance A (540, 160, 1456) / B (1130, 65, 96), gap ~1485, dy 95, continuous terrain. On 3c the two movers swapped floors (A climbed at ~(686, 938), B descended at ~(652, 1230)). Round 1 → 2 on 8c kept B's actor at `0x17935c0` | Broad review `spawns.py`, `ys.py`, `floors.py`, `r2.py` over 5 Frostfire + 4 Vigilance launches and 8c |
| **Mutual standing does not starve either side online**: 3c both pads neutral ~48 s at round start with MoveScale f12 = 1.0 on 384/384 and 310/310 rows, NetIdle peak 1547/1386 ms; kill2 39.6 s; every NetIdle alarm on record falls inside a peer freeze (8c) or a pinned-mover window (Sprint 4 `wtb2`) | Broad review `neutral.py`; Task 3 review |
| **The actor-matrix yaw settles within one 4 Hz row of an `rx` release online** (62/62 holds on 3c, change from release +0.25 s to +2.5 s = 0.00°); the "≈ 2.5 s" caveat is the *walk direction* lagging, not the matrix | Broad review `yawsettle.py` |
| **A clock round end is readable from the valves, and it steps no kill counter** (Task 1 Step 5b, launch 8c, map **VIGILANCE** — also kill2's map): both sides controllable after the vf0 fix (A 84.19/1.52/0.00, B 82.98/1.88/0.00); A's clock string reached `00:00`, then `mp_round_count` 0→1 and `mp_major_game_state` 2→4 on both instances at one aligned moment; `total_mp_kills` 0, `aiteam_00/08` 1, `+0x1044` 1.0, `+0xF7A` 1 throughout and after; no R1 bit in either log. Task 6's negative-control fixture | `logs/run_[AB]_20260913_132843.log`, `control_round.json`; review re-derived |
| **The VU0 vf0 fix restores Frostfire online control** (~~one usable run; a second Frostfire sample and the Medley bar still owed before "fixed"~~ **fixed: Medley/Vigilance bar on 8c, second Frostfire sample on ladder launch 2's precondition, 2026-09-13**): the local ground probe hits from its first call (ProbeEval 1332/1332 A, 1428/1428 B, candidates 1–3), `actor+0x420` tracks the clock within 0.05 s on every row, `+0x1061` bit 0x04 never sets, MoveScale runs at f12 = 1.0 for the whole ~302 s round; movement bar A 51.78/1.65/0.00, B 54.57/3.79/0.00; every Frostfire model gridded by world bounds; starvation peaks 1547/1441 ms. First online valve reads: `aiteam_00/08` 0→1 at round start, `mp_round_count`/`total_mp_kills`/`mp_game_over` 0, clock string counts down | Launch 3c, `logs/run_[AB]_20260913_115809.log`, `logs/parity/frost{A,B}_probe600_vf0.rdram`; review re-derived all with independent scripts |
| **The actor's facing lives in its own transform**: quaternion `actor+0x70` → `FUN_005483d0`/`FUN_00307170` → 4×4 at `+0x80..+0xbc` (row vectors; quaternion angle = −θ of the matrix; row 3 copied from `+0xf54`, not `+0x1c`). Walk direction = (−m[+0xa0], −m[+0xa8]): p90 **1.57°** over 17 live SP forward holds (13 at the spawn heading with a ~1.4° walk bias, 4 elsewhere at 0.0–1.0°), 1.43° over 11 online holds offline. Not valid within ~2.5 s of an `rx` hold | Sprint 5 Task 2, `e685b82`; review re-derived with an independent parser (589/589 quaternion–matrix rows) |
| PCSX2 plays a full online round against **our own** Horizon server, advancing to round 2 | `research/18` §1 + tracked contact sheet `docs/research/assets/18-s0-evidence.png` |
| A playing match and a frozen one produce **byte-identical** DME TCP profiles (11 BROADCAST / 30 SINGLE / 7 TOSERVER / 2 aux-UDP, same opcode histograms) | `research/18` §1; recounted from the raw log by review |
| The peer channel carries **zero** application data all match — every datagram decoded | `research/18` §3 |
| The advertised-port and shared-RSA divergences were real, and their fixes reached the wire | `acb603e`; wire bytes `4C-0E` both slots, distinct keys verified by `(m^17)^d mod N == m` |
| Neither of those fixes moved the symptom | Same runs; 3 hypotheses now falsified by measurement |
| **An online round plays end to end on the project's HOSTED Horizon server over the internet** (AWS Lightsail, 3.143.65.100, the launcher's default preset): two instances behind one home NAT logged in, hosted and joined on Frostfire, reached gameplay and walked the whole five-minute control round with both round clocks in step to the second, ending on the clock. The first online result that is not two instances against a LAN or loopback server | `s8_hosted_control2` 2026-09-19 04:02: `RESULT CONTROL-ROUND round_ended=yes ... health_min=1,1`, `A_LOBBY class=ok`; the server's journals in `logs/parity/s8_hosted_control2/server-side.log`. And a KILL there: `s8_hosted_kill` (the ladder, pinned harness ff514f7) -- `rounds=4/4 usable=4 best_rung=3 kills=2 rungs=1,3,2,3`, round 4 `signal=health on=B` 1.0 -> 0.0, zero starvation alarms, max NetIdle 1.3-1.4 s; four consecutive rounds on one lobby session over the internet. Its one flag is this host's render back-pressure (waits 383 and 133 against the ladder's 100), not the network. Still unexercised: two DIFFERENT networks (the owner's second machine) *(Moved to section 2 2026-09-25, Sprint 13 R5: the row "No round has been played between two different networks".)* |
| **The game keeps saved online personas per server** (the discriminator is one of two things; it is NOT the launcher's preset string): the hosted box's configs are byte-identical to the LAN's apart from the address, yet the login form arrived with no persona there while the same exe and card prefilled "socomc" on the LAN. A stranger's first login on any server is therefore a create-persona login (name keyboard first), and moving the hosted server's IP would orphan every saved persona -- the static address matters. **Narrowed 2026-09-19 (was "keyed, evidently, by the server's address"):** the A/B stands, but it does not distinguish the resolved peer IP from the `DNS` string MUIS hands the client in its universe list (`server/horizon-server/Server.UniverseInformation/MUIS.cs:316`, which the guest feeds straight back into `sceInetName2Address`). Either is consistent with every run. What the row must NOT be read to say: that the LAUNCHER's preset text is an input. It is not -- see the hazard row on `PS2X_SOCOM2_SERVER` in section 4 | `s8_hosted_control` (the failed first round: the harness typed the password into the name), `s8_lan_login_check`, `s8_hosted_login_A1/A2` (created, then prefilled); the harness's first-login path is `ff514f7` |
| **Missions are meant to have music, and our mixer decodes it sample-exact**: 210 stereo 32 kHz one-shot VPK cues in `/RUN/SOUNDS/VAGSTORE.ZAR` (group 1; mono 22050 Hz VAGp files are voice-over), fired adaptively. In a driven mission's dump each cue's offset against the cue decoded from the disc is constant to 2 samples over 30 s, drift under 2.1 ppm, no gaps at chunk resolution, no repeat above 0.71 (musical pitch), no clipping inside a cue; 0.998-1.000 where music plays alone, and elsewhere the low correlation is exactly the mix ratio (music gain 0.21-0.23 under gunfire and VO) | The 2026-09-19 measurement over `logs/s7_audio_mission.wav` and its first pass, with a Python VPK decoder mirroring `decodeChunkPair`; `tools_py/iso_lbn.py` for the sector-to-file map |
| **`snd_AutoVol` was applied instantly and could not reach streams at all** -- the owner's "skips and almost plays two different spliced segments" in a mission. The game ends music cues with `[handle, 0, 0x168|0x1e0, 2]` (to zero over 1.5 or 2 s at the library's 240 Hz tick); our handler ignored the time and went through setVolPan, which only looked at bank sounds. Now a per-handle linear ramp in the mixer's tick, on voices and stream gain; -4 ramps then stops. ~~The fourth argument (always 2) is unverified~~ *(settled 2026-09-25, Sprint 13 R5, from the real decomp: it is `delta_from`, and only the value 3 changes anything -- it retimes a ramp already in flight against its destination (`research/989snd-ziemas/iop/autovol.c`, `snd_AutoVol`) -- so 2 is a plain ramp over the given time, which is what ours runs)* | `54d77a2`; the owner's logs `dist/logs/run_20260918_1411*.log`/`1416*` (5 and 4 calls); the open 989snd reimplementation confirms the tick rate and has no AutoVol. The owner's re-listen is the judge (HUMAN_TASKS) *(the owner listened on 2026-09-22 -- the playthrough; the mission-music question that remains is issue #28's row in section 2)* |
| **A menu stream's first fill was discarded**: the game opens the PCM ring, fills it from its CD stream, then starts it; Open notified nothing, pre-Start writes were dropped and Start zeroed the ring (and R97's fresh flags). Open now allocates, Start only rewinds. The gate's title stage read 23/23 on that run (*corrected 2026-09-25, research/64: that 23/23 was one of fourteen runs where the attract never started before `s22`, on an uncommitted build — not a menu fix; the standing 19/23 is the attract's four captures*): the four captures that always missed sit at the end of the title sequence | `54d77a2`, `s8_audio_mc_gate2` |
| **An empty or missing card folder is an inserted, formatted, empty PS2 card** (the owner's "no memory card was inserted" at the save prompt). SOCOM II uses the EE libmc stubs, not MCSERV, and asks sceMcGetInfo one thing: is the type 2. The stub never created or looked at the folder, reported a constant free size and littered `_slot1` folders for the second port; and a guest path could write OUTSIDE the card root on Windows (`root /= "C:/..."` replaces the root) -- found by the path tests | `cef8d83`; `s8_empty_card_title`: `[MC] GetInfo port=0 type=2 free=8000 format=1`, title 20/23. The save itself, driven on a fresh empty folder (`s8_save_yes`): YES, then slot 1 at "Select MEMORY CARD slot", 12 files / 3020 KB written with every write and close OK (the only failures are the game's own deletes of files that did not exist yet), no error dialog; a second launch on the same folder (`s8_save_second`) shows no prompt and reads the profile and the seven saves back with no writes |
| **The POSIX crash handler read the scheduler snapshot through a mutex and three vector copies** -- a fault inside the allocator or under that lock would have hung the process instead of reporting; the Windows vectored handler is not a signal handler and was never exposed. Now a double-buffered POD thread table flipped by an atomic index, read lock-free | The Linux review of 2026-09-18; the VM smoke: SIGSEGV to the running game exits with the report inside 1.5 s. Tests: the crash table carries every thread the snapshot carries |
| **hostnet's POSIX poll returned EIO on EINTR**, so the Linux SIGPROF sampler (which interrupts poll and is never restarted by SA_RESTART) broke online the moment it was armed | The RED reproduced with a bare poll under a 10 ms timer (n=-1, errno 4); a restarting poll against a monotonic deadline; the same in the connect probe |
| **$ORIGIN/lib was DT_RUNPATH, not RPATH**: dependencies do not inherit it, so the tarball's libavcodec could not find the tarball's libvpx on a host without libvpx -- the VM's self-test passed only because the VM has them all | --disable-new-dtags on the runner and the launcher; readelf shows RPATH. Driver-coupled libraries (libva, libvdpau, libdrm, libgbm, libvulkan, libSDL2, libjack, libpipewire, libsndio) now stay on the host: 125 -> 120 libraries, 109 MB |
| **The parity gate's title stage passes inside the Linux VM at the same 19/23 as Windows**, driven through the latched pad file (at the VM's ~3 fps on software GL neither xdotool key events nor XTEST reach the game's pad poll -- the file does); the launcher started from the unpacked tarball runs the game with a log whose first lines match a Windows run's | `s8_vm_title5` summary in `logs/parity/vm/`, `logs/parity/vm/launcher_*.png`, 2026-09-18 15:50; Sprint 8 Goal 1 Tasks 10-11. The VM's 3 fps is the VMSVGA software rasteriser, not the port: the same exe on a real GPU is the owner's HUMAN_TASKS run *(moved to section 2 2026-09-25, Sprint 13 R5: the row "The Linux client has never run on a real GPU, and no bug report has been sent from Linux")* |
| **A refused stream header closed its file twice** -- Mixer::playStream handed the FILE* to the half-built Stream before parsing the header, so the unknown-magic refusal and ~Stream both fclose'd it; Windows' allocator never said a word, glibc aborted the suite on the first Linux run (the VM and GitHub's runner alike) and AddressSanitizer gave the two stacks | The commit after 095109f on sprint-8 (the stream takes the handle after the header parses; every close through one helper; a test-only close counter makes the Windows RED observable); VM ASan suite 554/554 with no report; Windows gate 3/3 `s8_mixer_gate`. Standing rule: run the C++ suite on Linux after allocator-sensitive changes -- the Linux CI job now does |
| **The VBlank re-anchor tests assumed an unloaded host**: on the VM's loaded virtual clock one binary passed 2 of 3 plain runs, and under ASan the host could not deliver 60 wakes a second at all; the runtime's re-anchor path is plain std::chrono with no platform branch and passes on Windows | The tests now allow one VBlank per wake plus one per further period the wake itself was late by; with the re-anchor sabotaged they still fail ('wake 0 saw 6 in 1 periods of host time'). Two cap-timing cases (R40) and the ~60/s line stay load-sensitive in a loaded VM -- read the C++ suite in the VM on an idle VM |
| **The client runs on Linux: the runner built from the same tree and generated code boots the disc under X and draws the same frame** -- the VM's exported boot frame vs the Windows export of the same screen at 640x448: mean abs diff 0.008 grey levels (bar 3), the GL backend on Mesa 4.1 with the probe passing (clip control absent, noted) | `logs/parity/vm/latest_frame.png` vs `logs/parity/s7_scale_1x.png`, `logs/parity/vm/run.log`, 2026-09-18 13:05, VM `socom-linux`; Sprint 8 Goal 1 Task 8 |
| **The VirtualBox Ubuntu machine gives OpenGL 4.1 core through the VMSVGA driver** (Mesa 25.2, `SVGA3D; build: RELEASE; LLVM`, a bare Xorg + openbox session at 1280x800 started over SSH once `Xwrapper.config` allows non-console users), above the GL probe's 3.3 floor -- the Linux port's GL path is testable in the VM, not only the CPU fallback | `logs/vm_x_start.sh` inside `socom-linux`, 2026-09-18 12:40: `glxinfo -B`. Whether dual-source blending is exposed is the probe's own answer on the first boot (Sprint 8 Task 8). *(Answered at that boot, 2026-09-18: the GL backend ran with the probe passing and clip control the one thing missing (the row "The client runs on Linux"), and the probe drops to the CPU renderer when dual-source blending is missing -- so it is exposed. Settled 2026-09-25, Sprint 13 R5.)* |
| **`sceSifInitRpc` reset the whole IOP model on every call, not only the first** -- loaded 989snd banks, `lastBank`, the VAG-streaming state, all wiped whenever the game re-initialised RPC (it does so as other libraries come up mid-mission); the console's call only sets up the EE's packet queues. This was the owner's "unknown bank" rejects (bank 0xa30000 loaded at log 6688, `sceSifInitRpc` again, `snd_InitVAGStreamingEx` succeeding a SECOND time at 6751 -- only possible on a wiped model -- then 0 entries at the reject) and half of the slot leak. | The test 'a loaded bank survives a second sceSifInitRpc' (RED with the log's exact signature, 0 entries); `RPC.cpp` resets only under the first-init guard. Second-pass commit of Task 12. Any other mid-game oddity that looked like state vanishing is worth re-checking against this. |
| **A VAG stream that plays to its natural end never freed its model slot** (only `snd_StopSound` did, and that only after b3e3797): the game polls `snd_SoundIsStillPlaying` and reuses the slot on "done", so the model now frees the slot with that answer and reaps ended streams before every allocation, asking the mixer per handle. | The test 'a VAG stream that plays to its end is reported done and its slot is reusable'. Residual: in the first-pass run the mixer itself reported two handles playing for the whole mission (7 of 4440 polls said done) -- long-lived streams hold slots by design; whether those two should have ended is a stream-length question, open. *(Superseded 2026-09-25, Sprint 13 R5: that run answered through the still-playing model, which the real decomp replaced on 2026-09-20 -- a played-out stream answers 0 on the next poll (the row "The real 989snd decompilation ... overturned the still-playing model", `378a87b`); the two long-lived handles were never re-read on the new model and are not owed; the live stream question is issue #28's row in section 2.)* |
| **Two instances on one PC with the SAME RSA key reach the lobby and play a control round** (Sprint 7 Task 2b): key sharing between two clients of one account is not what keeps a second machine out | `s7_samekey` 2026-09-17, `PS2X_SOCOM2_RSA_KEY_B` unset: `RESULT CONTROL-ROUND` on Frostfire; `logs/parity/drive_s7_samekey.txt` |
| **Equal-priority time slicing was an ours-only semantic; removed** (`EeScheduler` now expires the slice only for a strictly higher-priority ready thread, as the PS2 kernel does) | `39cd17f`, the test 'two equal-priority threads do not interleave'; with it in: gate 3/3 (`s7_gl_gate2`), 7 of 7 driven control rounds that reached the lobby played to the clock (2026-09-17) |
| **`sceCdRead` moved the CD stream cursor; fixed** (a plain read while a stream was open fed the demux from after that file: the RED test delivered sector 44 where 11 was due) | `5a1b6a8`; `s7_audio_title`: the title loop correlates at 1.000 with the disc PCM, constant offset, no step |
| **The lobby rate on a pinned harness: 6/10 on 2026-09-17, then 10/10 on 2026-09-18 with the injected press latched** (6b7a2b3). Every miss of the first ten was one class, the on-screen keyboard at login typing extra glyphs: the drive's 90 ms wall-clock press fell between two polls of a login screen running at 12-30 fps (dropped, never repeated; the cursor then desynchronised). The latch delivers every press to at least one poll. | `logs/parity/lobby_rate_2026-09-17/` (6/10, `login:keyboard-typing=4`); `logs/parity/lobby_rate_summary.txt` 2026-09-18 02:33 (10/10, harness `9ee5b0c`-era, exe `s7_ring_build`); `lobby_report --bar 0.8`: `RATE 10/10 PASS`. The login screen's slowness stays its own §2 row. |
| **The 3-17 s online freeze (launch 8c) did not reproduce in a quiet or a loaded (four spinning cores) Frostfire control round with the freeze fields in** -- no stall window between 3 and 17 s on either side in either round; the two windows the tracer prints are the pre-round clock at 0 and the 5 s round end | `s7_freeze_quiet` (run logs 215259), `s7_freeze_loaded` (220545), `freeze_trace --peer` both directions; research/29 §0 |
| The player's **feet** are at the right height (−145.875 vs console −145.8672); the defect is the **camera** | `research/17` §0.1, derived from `STATUS.md`'s 2026-09-09 01:30 entry's own camera y values |
| The skeleton root node decays 11.4845 → 0 while its saved copy freezes at the console's 5.50391 | `research/17` §4.1, raw trace `run_20260912_121304.log:3962-6975` |
| `rand()` returned 15 bits over a `_rand_next` frozen at 41 (host CRT's first draw) | `ede2096`; `_rand_next = 0x29` in four of our RDRAM images, live in all three PCSX2 images |
| The disputed mover field could not exceed 4.0000458 under the old mask; console reads 6.3338 | `research/17` §6; two samples a side |
| Five soft-double stubs were bound with the wrong ABI, and were **identity** at 19 of 22 sites (the 3 garbage sites are unreachable) | `db7a992`; delay-slot analysis of every call site |
| The intro-movie macroblocks were a cross-thread race on `m_currentTransfer`, not a byte-accumulator bug | `4a701f1`; deficit 3,748 → 0, MISSING 9 → 0 |
| `--vram-diff` catches a uniform ±1 px shift again after the seam budget (8/15 x, 6/15 y) | `6c017c2`, measured on real renders |
| **One player walking to the other cannot finish inside a round** *(qualified 2026-09-13: measured on Vigilance over 1382 units with the camera-steered loop at ~15 s/step; it does not bound a waypoint route on Frostfire's 691-unit gap — broad review)*. Measured closure efficiency is 39 % (758 units gained for 1961 walked) at ~15 s/step, so 1382 units needs ~450 s against a ~360 s round. Two movers is ~700 units each, ~180 s | Task 7 run `wtb2`, `logs/run_A_20260912_211009.log`; arithmetic checked by review |
| ~~**The two-instance approach closes faster than one:** 1359 units in 127 s against Task 7's 758 in 300 s~~ **SUPERSEDED — derived from the camera+facing reconstruction that was later proven wrong by up to 50 units. Use the actor-row generation below (1485.5 → 50.0 in ~127 s).** The rifle facts stand: 96 R1 injections, ammo 30/30 → 0/30, impacts on the wall ahead of the muzzle | Task 8 run `ours_task8_kill2` |
| ~~**They fought at ~90 units** (median 93, min 34.9, 3 % inside 45)~~ **SUPERSEDED by the actor-row generation below (min 50.0, median 67.9, 0 % inside 45).** Same conclusion, better instrument | Review of `9c28fe0`; superseded once `true_pos()` read the actor's own coordinates |
| **Player health is `actor+0x1044`** (float; 1.0 full, <= 0 dead) and **`actor+0xF7A` is the alive byte** (1 = alive), on SCUS_972.75 **r0001** | `research/19` (two community memory tools agree, one matched to our ELF by patch bytes); decomp compares `+0x1044 <= 0.0` for three teammates in a row, then `< 0.2` and `< 0.5`; every image reads 1.0 and 1, the console's included. **~~Not yet read live in an online match~~ read live online at 1.0 / 1 on launch 3c (976/973 reads); ~~the `<= 0` half is still unread~~ the `<= 0` half read live at three kills on ladder launch 2 (§1's row "THE ACCEPTANCE TEST PASSED")** |
| **The multiplayer round-state object is `*0x437ce8`**: `total_mp_kills`, `mp_round_count`, `mp_game_over`, rounds won and alive per team (each value a short 4 bytes into its record), and the major/minor/"my" round-state bytes at `+0x113..+0x115` | `research/19`; decomp references `DAT_00437ce8` in the round code. Separates a kill from a round ending on its clock without a screenshot |
| **Open-loop aim resolution is floored at ~20-40° by the HARNESS, not the runtime.** The harness's `PAD_AXIS` sends only full deflection (0/255); the shortest usable hold already sweeps 35-40°, against a body that subtends 15-20° at contact range. **The pad file itself accepts 0-255 per axis**, so partial-stick aim is available for the cost of one re-calibration | Task 7 §3.13 (19 timed holds, repeat scatter 99.5 vs 80.8 at one hold length); pad-file range confirmed by the Sprint 5 planning pass |
| **The local player's actor is reachable from a STATIC: `*0x408c58`** (also `0x40d744`, `0x440c38`, and `*0x415ff0+0xbc`). Full chain: `@408c58` word0 = `017941d0`; `@17941d0` word0 = `006691a0` (actor vtable); `*(actor+0xc0)` = mover `006694b0`. The `*0x488de8+0xbc` route is **not** it — that route is recorded in `STATUS`'s 2026-09-09 01:30 entry, not HANDOFF, and `0x488de8` is the *camera* | Offline scan of five of our RDRAM images **and the PCSX2 console image** for vtable `0x6691a0` keeping the actor whose mover is `0x6694b0`; live in an online match (`logs/run_A_20260912_230022.log`, actor `0x17941d0`, 804 rows); `*0x488de8+0xbc` resolved 24 times in 1162 rows and to `0xd9d9d9d9`. `research/18` §4.1; route written into `HANDOFF.md` "Open items" item 0 |
| **Two movers close the map four and a half times faster than one**: `ours_task8_kill2` closed the true 3-D gap **1485.5 → 50.0 units in ~127 s** (11.3 units/s) against Task 7's 757.8 in 300 s (2.5 units/s), both players walking — the first time two online players have been in the same place | `ours_task8_kill2`, `logs/run_A_20260912_231341.log` / `run_B_…` (1172 / 1055 in-game rows). Team-closed over team-walked **61.0 %** vs `wtb2`'s 38.6 %; `kill1` was **36.5 %**, i.e. no better than one mover. Per-side efficiencies double-count the same gap and must not be quoted |
| **The bursts hit nothing because the players were never in range at all, in three dimensions**: on the ACTOR rows the minimum true 3-D separation for the whole run was **50.0 units**, the last 400 rows had a median of **67.9** and a median elevation of **43.3°**, and **0 %** of them were inside 45 units in 3-D, inside 25, or within 10 of each other's height. Range and elevation are co-equal causes; the 77° figure is the single worst row | Same run, actor positions (words 7/8/9). ~~`actor+0x204`/`+0x208` unchanged, so no damage was dealt~~ **Whether damage was dealt is UNKNOWN** — those were not the health field (`research/19`); the geometry conclusion stands on its own |
| **The loop's own distance was wrong by tens of units**, because it reconstructed each player as `camera + 24.9 * facing`: a facing wrong by tens of degrees misplaces a player by up to two orbit radii (~50 units), and placing the OTHER player needed the other side's facing | Reproduced in a **flat** simulated world: reported-best vs ground truth was 40.3/64.3, 23.4/30.7 and 157.6/204.7 before the fix, and **18.1/18.1** after `true_pos()` read the actor's own x/y/z. `research/18` §4.11 |
| **The camera record orbits the actor at ground radius 20.65 (sd 5.17), 19.73 above it** — i.e. the camera looks down on the player from ~44°, which is a free pitch readout | 1172 paired rows of `ours_task8_kill2`; the actor's own position is at `actor+0x1c/+0x20/+0x24` |
| **The rifles DID fire** — this is a geometry failure, not an input one | `buttons=0800` (bit 11, R1) injected **96 times** in `logs/run_A_20260912_231341.log`; HUD ammo `A_fight04.png` 30/30 → `A_fight07.png` **0/30** with bullet impacts on the stone wall in front of the muzzle, `B_final.png` 13/30 1 MAG (~47 rounds fired) |
| **The online movement blocker was `sceInetInterfaceControl(0x200)` returning a constant** — `msSinceNetActivity` never reset, so the movement scale clamped to 0.0 on frame one. Pitch is not among the three scaled axes, which is why RY survived | `abf35bb`; `DAT_0045a1ca` measured 1 (killing the rival candidate), `MoveScale f12 = 1.0` on all 332/331 calls, instance A ~~73 distinct x (539.7→337.9)~~ **65** distinct x (first→last 539.68→471.01; the struck figures were a miscount and min/max) against 1 before, B **80** (1145.28→981.87) — `research/18` §3.12 "Verified" holds the numbers to quote. **Same-binary A/B** (`5ed29ca`, one match, both legs a frame apart): fix ON f12 = 1.0 on 330/330, idle max 1490 ms, activity globals 192/192 distinct, 89 distinct x; fix OFF f12 = 0.0 on 339/339, idle 504,210 ms, globals never written, **0.46 units** of travel. Movement tracks the stick — 1.3 units at neutral vs 28-38 per hold, starting on the hold frame, axes orthogonal. Review verified every figure to 3 dp |
| ~~**The first save on a virgin memory card fails, and the second launch succeeds**~~ **SETTLED 2026-09-22 AND FIXED AT THE ROOT (`152579a`, PR #23 on `main`, gate 3/3).** A driven run to the control-type prompt with an empty card printed `[mc] GetDir REFUSED path '..'` five times: the game enumerates a fresh card with `..`, our path normaliser refused any `..` that would climb past the root, and on a virgin card the current directory IS the root -- so we answered "permission denied" and the game read it as a card it could not use. The owner's second launch worked because the save folder existed by then. A trailing `..` now resolves to the card root while `/../escape.bin` is still refused, and the same run reproduces with the five refusals gone; W10's launch 1 then created a persona on a virgin card, rc=0 (section 2's W10 row). **The instrument that stays** is R238's unconditional `[mc] command <n> FAILED result=<r> (<name>)` line, printed in every build with no knob -- the reason this failure could not be explained the first time. The row as it was written, 2026-09-22 (the owner, at the control-type prompt: "it asks to save, at which time i try and it fails ... I restarted and it works the second time"). No evidence exists from that run: `PS2X_MC_TRACE` was a developer knob, so the build they played recorded nothing about the card at all -- which is the fault R238 fixes, not a fact about the card *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- `152579a` (PR #23), gate 3/3; W10's launch 1 on a virgin card, rc=0 (`logs/parity/w10_virgin_a`). The right-hand cell is the section 2 cell as it was written.)* | **Settles it:** an empty card directory, a run to the control-type prompt, and the `[mc] command <n> FAILED result=<r> (<name>)` line that now prints in every build. The result code names the cause (NoFormat, DeniedPermit, FullDevice); until that line is read, the cause is unknown |
| **The console draws the online screens ~7% narrower than ours, centred** (Sprint 10 Goal 3, 2026-09-20): on a PCSX2 frame at the 640x448 client the GAME LOBBY title's glyphs span x 51..576 (525 px); on ours 31..595 (564 px). **Cause, by arithmetic (2026-09-22, the owner's observation):** the game draws 448 lines, a TV shows them as 480 (4:3), and PCSX2 keeps that 4:3 shape inside its 640x448 window -- width 640 x 448/480 = 597 px, centred, 21 px bars each side. Our span mapped through that is 21 + 31 x 0.933 = 50 and 21 + 595 x 0.933 = 576; measured 51 and 576. Not the CRTC display window, and not a renderer defect: ours is pixel-exact, PCSX2 is the TV-stretched picture letterboxed into a window of the wrong shape. The per-target title references (`title_<name>.pcsx2.png`) stay only because PCSX2 runs at 640x448; a console run at 640x480, or with its aspect set to stretch, should land on our coordinates. The "renderer fix" filler item is withdrawn. *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- the arithmetic against the two frames in the right-hand cell (predicted 50 and 576, measured 51 and 576). The right-hand cell is the section 2 cell as it was written.)* | `logs/parity/mixed2_ours_hosts_b/pcsx2/B_miss_join_enter_1.png` vs `logs/parity/s8_hosted_kill/A_16_game_lobby.png`; the Goal 3 plan *No issue: explained by arithmetic and closed; nothing left to fix.* |
| ~~**Requiem: the joiner fails the control bar on both passes**~~ **SETTLED 2026-09-17:** on the wall-time guest clock (research/34 §6) `ours_control_requiem_clock` is CONTROL-ROUND exit 0, B's hold 0 net 128 u (was 13). It was the slow clock. *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- `ours_control_requiem_clock` (`D:/socom_archive/parity/ours_control_requiem_clock`), CONTROL-ROUND exit 0 (research/34 §6). The right-hand cell is the section 2 cell as it was written.)* | Done. |
| ~~**Foxhunt: the control round's negative control fails on fall damage**~~ **SETTLED 2026-09-17:** the driver's fall guard (a 30-unit height drop: pad neutral 1 s, the side's legs turn back; a health drop within 3 s scored apart as fall damage) -- `ours_control_foxhunt_guard`: B dropped 35 u at T+130 s, turned back, the round ran to its clock, health 1.0/1.0, no counter step, exit 0. Twenty of twenty maps play their control round. *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- `ours_control_foxhunt_guard` (`D:/socom_archive/parity/ours_control_foxhunt_guard`), exit 0. The right-hand cell is the section 2 cell as it was written.)* | Done. |
| ~~**The guest clock runs at 0.5–0.85 of wall time even when a round plays**~~ **FIXED 2026-09-17 (research/34 §6):** ~195 ms of VU1 and ~290 ms of render back-pressure per second were subtracted from the guest clock by the 2026-09-08 exclusion; the scheduler counts wall time by default now (`PS2X_CLOCK_EXCLUDE=1` for an A/B, 100 ms per-gap cap), both online sides read 1.00 s/s (`ours_control_frostfire_clockoff`), gate title/mission PASS. The transition floor was recalibrated (5 → 3) for the now 4 s black screen. *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- `ours_control_frostfire_clockoff` (`D:/socom_archive/parity/ours_control_frostfire_clockoff`), 1.00 s/s on both sides (research/34 §6). The right-hand cell is the section 2 cell as it was written.)* | Done; Requiem (the row "Requiem: the joiner fails the control bar on both passes", settled and in section 1 beside this one) is expected to pass on re-run. |
| **FIXED 2026-09-15 (`a81eb74`), promoted to proven:** the ×8 dropped (`vram_addr & 0x3FFF` is the DBP/SBP field); a seven-region round trip in `ps2_gs_tests.cpp` failed on the old code with every block pointer off by 8 and passes now (455/455); gate PASS 3/3 `s6_blockptr` with the mission stage passing the mission-failure detector after the 3 s turn that ended `s6_depth_m5` in MISSION FAILURE; `motion_pack_check` on the 255 s RAM dump reads `corrupt_chunks=0 of 8` against 5 of 8 on every pre-fix spawn image and identity on the console. Still owed: a live `rx`-hold teleport count from the guest-value probe (`s6_probe`). Row kept as written: ~~**Our `sceGsExecLoadImage`/`sceGsExecStoreImage` HLE multiplies the BITBLTBUF block pointer by 8**~~ (`Kernel/Stubs/GS.cpp` ~:641 and ~:706 computed `vram_addr * 2048 / 256`). At the single-player mission load the game parks 1.75 MB of VRAM in the motion-pack buffer as seven 0x40000 pieces (`FUN_003b1990(0x2400, …, 0x1c0000)` / `FUN_003b18d0`, `vram_addr += 0x400`); with the ×8 the seven block pointers mask to 0x2000/0/0x2000/0…, so the restore writes [6,5,6,5,6] over chunks 0–4 — the motion smear, and so the turn teleport. A block scan of our RDRAM images reproduces the pattern; the stray 0x40000 copy at ~`0x1d2b000` is the load stub's freed `guestMalloc` packet. **Independently CONFIRMED 2026-09-14** (re-derived from the decomp, the runtime source and the dumps): the game's own libgraph writes DBP/SBP unscaled (`FUN_001a2520`/`FUN_001a2708`), so `vram_addr` **is** the block field (256-byte blocks, 14 bits, 0x4000 blocks = 4 MiB) and the ×8 is wrong by exactly 8; 0x2400·256 + 7·0x40000 = 0x400000 exactly; the game hand-patches DBP by +0x280 per 0x28000 bytes (decomp 45108), which fixes both unit and width. Observed smear `[6,5,6,5,6,5,6,7]` on three of our spawn dumps, identity on the console and on `title_ours`; our buffer chunks {0,2,4,6} are byte-identical to each other and {1,3,5} likewise — the two-VRAM-block signature; chunk 0 vs the console's chunk 6 differs only by the −0x2d00 pointer relocation. **Smoking gun:** the leftover load packet in `spawn_ours_vf0_t200` decodes as `BITBLTBUF DBP=0x2000` for the piece whose correct DBP is 0x3C00. Refinements: the **load** parks and the **store** restores (research/25 §9.1 has the direction backwards); chunk 6 is correct by luck always, chunk 5 usually — the odd pieces alias to VRAM block 0, the live framebuffer front, and one run shows 16 KiB drawn over it *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- `a81eb74`, `s6_blockptr` 3/3 (`D:/socom_archive/parity/gate/s6_blockptr`) and the seven-region round trip in `ps2_gs_tests.cpp`; the teleport count it still owed is paid -- `PROBE teleport_steps PASS ours=0 console=0` on every gate since R80's threshold, e.g. `s13_v1_gate` (the first scored gate, `s6_probe`, read `teleport_steps FAIL ours=8` -- the speed-threshold artefact R80 corrected; `D:/socom_archive/parity/gate/s6_probe`). The right-hand cell is the section 2 cell as it was written.)* | research/25 §9. Settle: the paused verification (units at the three sibling call sites, the alias table, dumps vs console), then the two-line fix + a seven-region round-trip test in `ps2_gs_tests.cpp` (the existing single-region test is lossless either way), full gate for blast radius (the same stubs carry loading screens, movies, textures), and an offline descriptor-by-name check vs console, 48 → 0 |
| ~~**Health `+0x1044` and alive `+0xF7A` at a death are still unread live.**~~ **Settled 2026-09-13 by ladder launch 2 (§1): `+0x1044` → 0.0, `+0xF7A` 1 → 2 → 3, word 0 intact online.** Kept as written: SP (Task 2, 3 usable runs): `+0x1044` 1.0 → 0.978 → 0.721 and 1.0 → 0.392, each after a teleport landing; `+0xF7A` 1 on every live row; no death. At run 3's MISSION FAILURE the actor was destroyed (word 0 → base vtable `0x4061c0` from destructor `FUN_0029ed30`, then heap data); KillWatch reads only vtable-intact blocks and did not fire (replays 1588/0, 589/0, 1454/0). Armed as harness defaults *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- `logs/parity/s5_t5_ladder2` (now `D:/socom_archive/parity/s5_t5_ladder2`; section 1's "THE ACCEPTANCE TEST PASSED"). The right-hand cell is the section 2 cell as it was written.)* | The first online death (Task 5 rung 3) |
| **Frostfire's lost control is the online snap-back fed by a ground probe that never hits.** *(Cause fixed by `b625291`; online restoration proven in §1 — this row keeps the mechanism for the record.)* `FUN_00594cf0` (`0x594ec4`) suppresses MoveScale when `0x45a0c1 && 0.6 < clock(0x4365c0) − actor+0x420`. `actor+0x420` is the last ground-hit time, stamped per frame only by `FUN_005b0420`'s hit branch (actor `vtbl[0x20]` `FUN_005506a0` queues via `FUN_005b0840`; `vtbl[0x24]` `FUN_00550570` → `FUN_005b0800` batch → `FUN_005b0420` → `FUN_005b5d40` eval). On launch 1 `+0x420` was never written (A 0x0, B uninitialised 0x102, every row) and `+0x1061` bit 0x04 (the miss branch) was set from the first frames on both sides; MoveScale stopped between clock 0.383/0.616 (A) and 0.550/0.783 (B). Single-player images of ours and PCSX2 stamp `+0x420` = clock; on kill2's map it tracked the clock. R6 firing: **verified, bracketed**; probe miss: **inference, strong**; why it misses: ~~open — leading candidate: the collision grid is exhausted and its cell chains cut~~ **exhausted grid RETRACTED for Frostfire** (launch 2, `logs/run_[AB]_20260913_100624.log` -- now `D:/socom_archive/run_[AB]_20260913_100624.log`, `logs/parity/frost{A,B}_probe600.rdram`, review re-derived: 503 nodes + 7689 free = 8192, free head never 0 over 1988/1964 rows; still stands for `spawn_ours2`'s chain class). **Probe miss PROVEN:** ProbeEval returns 0 with candidate count 0 on 1450/1450 local records while GridQuery returns 1 — the ground is not in the queried cell. The ground models carry T = (960, 0, 800) in their own matrix (e.g. `0x11a4e20`, world quad under A at y 100) but are linked in cells (0,0)/(0,2) — their untranslated bounds — instead of (4,3)/(3,7). The load-time insert (`FUN_0031d6f0` → `FUN_002d7580`) ran on **StartThread thread 7** (`FUN_001ebda0`, pc-sampler). **Cause VERIFIED offline** (`b625291`, M51 census on `logs/parity/spawn_ours_vf0.rdram`: all 48 translated props now by world bounds, e.g. T (3827.66,…) in (21,19) as on `spawn_pcsx2`, 0 by raw; "inserted before placement" excluded); the online chain awaits Frostfire launch 3. Mechanism: the bounds transform `FUN_003085c0` does `vmaddw.xyz vf9, vf7, vf0w` (`0x308608`) and our StartThread contexts have VU0 **vf0.w = 0**; the same signature shows on M51 (48 translated props at (0,0) on ours, by world bounds on the console) *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- `b625291`, the M51 census, and Frostfire launch 3c (section 1's "The VU0 vf0 fix restores Frostfire online control"). The right-hand cell is the section 2 cell as it was written.)* | Fix `R5900Context()` vf0 = (0,0,0,1), then the post-fix M51 census (all 48 translated props by world bounds) and Frostfire launch 3 (ProbeEval count ≥ 1, `+0x420` stamped, MoveScale continuing) | Frostfire launch with traces `ProbeQueue 0x5b0840`, `ProbeBatch 0x5b0800`, `ProbeEval 0x5b5d40` (`[ret] v0 == 0` = miss), `ProbeTake 0x5b0420` and dump `ProbeEval:a1:19,ProbeEval:a1+0x48*:16`: candidate count 0 → collision absent/not loaded; > 0 → the candidate filter rejects them *No issue: the cause is fixed (`b625291`) and the restoration is proven in section 1; this row keeps the mechanism for the record.* |
| **On Frostfire the local move path ran for 0.6 s at round start and never ticked again** — 18 calls (`#0`-`#17`) between t=371.4 and t=372.0 s while 2634 peek rows kept arriving (Medley's kill2 runs ~5560 calls over 293.6 s = 18.9/s), with the pad reaching the guest, the movement scale at 1.0 and the round clock running. Reads as control never being handed over, not as a slow map. **Two leads from the same logs:** both actors read `+0xd0/+0xd4` = **1/1** and `+0x20c` = **0** for the whole run, where every Medley actor reads 8/5 and 4/7 — a *state* difference; ~~and in `FUN_00594cf0` the move-scale call sits inside `if (cVar7 == 0)`, so the multiplayer snap-back — **excluded only on Medley** — would produce exactly this picture.~~ *(2026-09-13, first note: "host wall clock, a coincidence" — itself **retracted the same day by launch 1**: the snap-back did fire at guest clock 0.6 s; frost1's 0.6/0.5 s host windows were that threshold seen at 0.1 s resolution. See the ground-probe row ("Frostfire's lost control is the online snap-back").)* reCOM names the likely state: `mp_major_game_state`, `late_joiner` (`research/11`). Frostfire spawns are 692 units apart (Medley 1485) with a 42-unit height difference. **One run; §3.12's movement fix is proven on Medley only** *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- the logs it names; the cause is the ground-probe row's, fixed by `b625291` and proven on launch 3c. Its two leads -- the `+0xd0/+0xd4` and `+0x20c` state difference, and reCOM's round-state names -- were never examined and are superseded by that cause, not confirmed. The right-hand cell is the section 2 cell as it was written.)* | A zero-launch pass mapping reCOM's round-state names onto the round-start path, then one relaunch with `PS2X_CALL_TRACE_EVERY=1`, a trace on the **caller** of `0x553dc0`, the snap-back timestamp pair, and the `0x200` idle counter *No issue: the same defect as the ground-probe row, fixed and proven in section 1; kept for the mechanism.* |
| **Valve name pointers are registry-allocated and platform-specific**: ours are identical in all 15 of our RDRAM images (e.g. `mp_round_count` 0x006b7f30), the console's differ non-uniformly (+0x20/+0x30/+0x40). Identify a valve by its name bytes (`**:3` peek), never by a pointer copied across platforms. Proven for single-player/menu images; **online too** — launches 1c, 3c and 8c identified all nine valves by name bytes on both instances *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- launches 1c, 3c and 8c identified all nine valves by name bytes on both instances. The right-hand cell is the section 2 cell as it was written.)* | First online peek of `*0x437ce8+0x0c**:3` on ours *No issue: a harness rule, not a defect -- identify a valve by its name bytes, never by a copied pointer.* |
| ~~Which of the two skeleton candidates is real — a lerp dropping its `a·w` term, or a second writer~~ **SETTLED 2026-09-15 — the root node no longer decays**: the gate's guest-value probe read `actor+0x2e8→+0x04` at **5.5039** (console 5.50391) as the settled value over 309 rows of `s6_probe` on the block-pointer exe. The lerp `FUN_001c0768` is a vf0 w-term site, so `b625291` (vf0.w = 1) is the likely fix; which of the two candidates it was is not separated and no longer matters. Promoted to §1 by the probe being scored (R80) *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- `logs/parity/gate/s6_probe/summary.txt` (now `D:/socom_archive/parity/gate/s6_probe/summary.txt`), `PROBE root_node_y PASS`. The right-hand cell is the section 2 cell as it was written.)* | `logs/parity/gate/s6_probe/summary.txt`, `PROBE root_node_y PASS ours=5.5039` |
| **The login screen runs at 12-30 fps under GL back-pressure in 4 of 10 launches** (`bp_pending=4 bp_waiters=1`, the guest clock seconds behind wall); in the others 54-60 fps. The driven lobby misses were its symptom (latched since 6b7a2b3, 10/10), and the menu music's late fill its other one (R97). **Measured 2026-09-19 (Sprint 8 Goal 2 Task 1, `PS2X_GS_UPLOAD_TRACE`)**: on a fast login screen the upload column is 59 ms/s, of which the CPU-side shadow swizzle is 42 (7.1 us x 5945 uploads/s) and the GL side only 13 (31 whole-band calls a second, already batched; rects 0/s); `transfer=` (executeTransfer) is 157-203 ms/s beside it, three times the uploads. The game re-uploads one menu atlas every frame (one destination texture). Tile batching would buy nothing (R108). **FIXED at the consumer, 2026-09-19 (`759e218`, R123):** the cost was `resolveTexture` re-decoding the whole texture cache every frame because every upload, identical or not, moves a page generation; cached textures now revalidate by a hash of the bytes their decode read. Same binary, knob off/on: login `transfer=` 121.9 -> 46.1 ms/s and decodes 537 -> 40 /s; lobby 410.8 -> 91.9 ms/s, decodes 1331 -> 0 /s, fps 52.2 -> 57.0. **Under the four-core load (`s8_menu_bar2`, `logs/run_A_20260919_084912.log`, the last 60 s of the login hold): 58-60 presents every second (57 of 60 rows at 60), `bp_pending` 0-2 with `bp_waiters` 0 throughout (the failure's signature was 4 with a waiter), `pcm_underruns` 0.** The plan's 60 ms/s proxy is not met (upload 142 ms/s, the CPU-side swizzle of ~8,200 uploads/s) and is let go (R125): the symptom it stood for is gone. One loaded launch, not ten; a recurrence reopens this row *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- `759e218` (R123), `s8_menu_bar2` (`D:/socom_archive/parity/s8_menu_bar2`). The right-hand cell is the section 2 cell as it was written.)* | `logs/run_A_20260919_003832.log` t=88-96 s, the `[gs-upload]` lines. Experiment: the same per-term split inside executeTransfer, then skip the swizzle for tiles whose bytes did not change (a per-tile hash or compare against the shadow), measured on a slow launch (four spinning cores) where the 12-30 fps shows. Sprint 8 Goal 2b *No issue: closed by measurement 2026-09-19; a recurrence reopens it.* |
| **Once the replay latches stalled, GsPendingCap::admit keeps every state-carrying command unbounded** (`gs_gl_backend.cpp:771,789`, codified in `gs_frame_backpressure_tests.cpp:422`): the header's own "~15 GB working set" note; on the 8 GB VM with no swap the run died with std::bad_alloc on a GS-register store (`s8_vm_title_audio3`). A stranger's small machine under a long stall hits the same wall **Superseded 2026-09-21 (Sprint 10 Q6, `ac80085`, gate `s10_q6_gate` 3/3):** past the cap on a latched stall the state-carrying commands (uploads, transfers, CLUT loads, VRAM writes) are refused and absorbed into the game's own VRAM -- only WHERE they wrote is remembered (merged rectangles per destination, at most 512, 256 palettes, 4 MB) -- and re-anchored when the latch clears as one upload per rectangle from a VRAM snapshot; draws and presents are dropped and the next frame re-records them. A 600-frame latched stall now peaks at the cap + one command (67 MB) instead of 1.45 GB. `[gs-gl] stall bound engaged/released` lines and `stall_engaged= stall_absorbed_bytes= stall_reanchor_bytes=` on the stats line. Not yet seen on a real stall (none latches on this host); the CPU-backend round trip is the proof so far. *(Seen since: `s10_q7_gate` (`D:/socom_archive/parity/gate/s10_q7_gate`), 2026-09-21, latched at t=200 s with pending commands at 18,852, and the bound absorbed for the remaining 280 s while the guest ran on -- `[gs-gl] stall bound engaged` once; the section 1 row "A header that defines its state in an anonymous namespace".)* *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- `ac80085`, `s10_q6_gate` 3/3, and the real stall in `s10_q7_gate` (both under `D:/socom_archive/parity/gate/`). The right-hand cell is the section 2 cell as it was written.)* | The stalled run's log; Sprint 8 Goal 5 (bare-run robustness): cap the state-carrying bytes too, or stop admitting and re-anchor, under a test that drives a stall with a bounded working set **Settled 2026-09-21** (Sprint 10 Q6, `ac80085`, gate `s10_q6_gate` 3/3): the state-carrying bytes are capped too, under a test that drives a stall with a bounded working set. *No issue: settled; this row is the record.* |
| **The launcher reports bugs and shows the server's status (Sprint 9 Goal 8, 2026-09-20).** REPORT A BUG on the rail mirrors the site's form and contract (`../scotho/apps/s2u/src/report.ts`): nothing is sent until SEND, a preview says what will go, the context is an allowlist, the optional log (off by default) is clipped, scrubbed of the home and the disc's folders and cut to 65,536 bytes, the honeypot is always emitted and always EMPTY (`bug_report.cpp:407-408`; this read "the honeypot is never emitted" until 2026-09-19 -- what is true is that no input can reach it), any failure (including 429) saves `logs/bugreport_<stamp>.json`. HTTPS with certificate checks on: WinHTTP on Windows, a `curl` subprocess on Linux; plain http is refused except to loopback, and the test seam `PS2X_LAUNCHER_API_BASE` accepts loopback only (it rejects `http://127.0.0.1:80@evil.example`). ONLINE shows `/api/stats` as one line, fetched off the UI thread *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- `BR-20260919-c6d666` on the box, suites `BugReport` and `Launcher`, `test_bug_report`; its Linux half is section 2's row "The Linux client has never run on a real GPU, and no bug report has been sent from Linux", its owner's-hands items section 2's row "Believed, not proven (Goal 1)". The right-hand cell is the section 2 cell as it was written.)* | Suites `BugReport`, `Launcher`; `test_bug_report` (a loopback server driving 201/400/413/429/500/refused, 8 cases, green on Windows and on Linux CI run 35470376311); one live proof `BR-20260919-c6d666` **from Windows**, and a live status line. **Confirmed on the box 2026-09-20** by the hosted-server session, after this row briefly said it was unevidenced: the report is `bugs-test/BR-20260919-c6d666.json`, stored `2026-09-19T21:08:16.959Z`, `source=launcher`, `test=true`, 606 bytes, `platform: windows`, context exactly the allowlist with no ISO path, empty log, no contact. It could not be found from this machine for two reasons worth remembering: **the id is stored lower-case and displayed upper-case**, so a grep for the upper-case form in a case-sensitive tree misses the file, and the local mirror had last synced 35 minutes before the report arrived. **Still owed:** the Linux half of the spec's bar ("from each platform") -- the VM is off. `778ebcb`..`c3faf9a`. Believed, not proven: the page's feel with a pad, and the wording (HUMAN_TASKS) *No issue: a landed result, nothing left to fix.* |
| **A smaller, checkable download (Sprint 9 Goal 2, 2026-09-20).** Both portable archives now carry exactly what their executables import (a pure-Python PE/ELF import-closure audit refuses a missing or an orphan library and picks its reader from the file's magic, not the host), a `SHA256SUMS` beside each archive, and a stripped release executable from its own build tree (`./build.sh release`, `build-release/`, `dist-release/`; the developer build's ninja files are byte-identical before and after) with its symbols kept in `symbols/`. **Windows zip 65,494,194 -> 55,682,325 (-15.0%), 31 DLLs -> 16; Linux tarball 114,321,112 -> 99,352,944 (-13.1%), lib/ 116 -> 76** (`--as-needed` dropped libavformat/libswresample and the ~40 libraries behind them). Measured, not assumed: `-O2` for the generated code made the exe 9.9% smaller and the ZIP 4.6 MB LARGER, ICF bought 0.2 MB, so the release keeps `-O1` (R151); `-Os` and LTO were not built. **Correction, 2026-09-20 (Sprint 9 P7):** "the release keeps `-O1`" was true as a DECISION and false as a FACT -- `build.sh`'s release default was `${REL_GENOPT:--O2}` from `285382e` and was never changed, so every `./build.sh release` run plainly since has built the configuration this measurement rejected. The 55.7 MB figure was measured with the value passed explicitly. Found because the playtest candidate came out 62.8 MB; the default is now `-O1` and a test pins it to this ruling *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- `s9_g2_release_gate` 3/3 on the release exe (`D:/socom_archive/parity/gate/`), CI run 35466213683. The right-hand cell is the section 2 cell as it was written.)* | Gate 3/3 ON the release exe, `s9_g2_release_gate` (`SOCOM_EXE`; the summary's last line carries its sha256 d6e30021...); both folders load with PATH cut to System32; in the VM `sha256sum -c` OK, audit 76/0/0, the 8 exit-code cases on the release runner; CI green (run 35466213683). `176e434`..`105f81d`; R140-R151 *No issue: a landed result, nothing left to fix.* |
| **HAZARD, fixed: the VM sync carried the host's mtimes, so ninja in the guest could link a stale object** (a file edited before the guest's last build but synced after it looked up to date: `undefined reference to ui::drawPad`). `vm_sync.sh tree` now re-stamps, with the guest's clock, every file whose content changed, prunes what the host deleted, and no longer pushes `dist-release/` (570 MB a sync) *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- `9e70730`, `018bbf9`, `test_vm_restamp`, `test_vm_prune`. The right-hand cell is the section 2 cell as it was written.)* | `9e70730`, `018bbf9`; `test_vm_restamp`, `test_vm_prune`. Left in the guest: one stale `dist-release/` copy; VM tarballs read the guest's lagging `git describe` in `version.txt` (VM-only) *(Moved to section 2 2026-09-25, Sprint 13 R5: the row "The VM guest may still hold a stale `dist-release/` and stamp its tarballs with a lagging `git describe`".)* |
| **Crouch was unreachable on any PC pad, and now has a shortcut (R139, 2026-09-20).** Stance is hard-coded on Triangle and reads PRESSURE: `PlayerUpd` tracks the press's peak, under 0.3 toggles stand/crouch at release, 0.3 or more goes prone at once (decomp ~453331-453425; the pad reader fills slot 6 from button 0xC with pressure id 0x18, scale 1/255). Our pad HLE reported 0xFF for every pressed button, so Y could only go prone. The launcher's CROUCH SHORTCUT (default L-stick click, the owner's choice; also touchpad, L2, off) sends a LIGHT Triangle (0x40) instead of that control's own button; a full Triangle from any source outranks it, so prone and the harness's injected pad are unchanged; off sends no variable. The disc's control dictionary, parsed structurally this time, has no stance entry at all (the earlier flat-dump pairs "Action/Square, Jump/Circle" were mispaired: X is Action, Square is Jump, Circle is TeamCommand) *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- gate `s9_crouch_gate` 3/3 (`D:/socom_archive/parity/gate/`), suites `PadInput`, `Launcher`, `BareRun`; the real-pad feel is section 2's row "Believed, not proven (Goal 1)"; the four-pad layout is a nit no one has hit, no issue. The right-hand cell is the section 2 cell as it was written.)* | Suites `PadInput`, `Launcher`, `BareRun` (RED watched); gate 3/3 `s9_crouch_gate`; screenshots `logs/parity/launcher_ui_crouch/`. Believed, not proven: the feel on a real pad, and the touchpad (GLFW raw button 13 on a Sony vendor id; not visible through Steam Input or DS4Windows; untested on Linux) -- HUMAN_TASKS. Open: with four pads listed the pad list would run into the new row *No issue: a landed result, nothing left to fix.* |
| **A failure explains itself (Sprint 9 Goal 1, 2026-09-20).** One table, `ps2x/exit_codes.h`, read by the runner, the launcher and Python: 0/1/3 named, 65 kept (no usable GL), 66 disc not found, 67 not r0001, 68 ELF missing, 69 config unreadable, 70 crash (NT statuses and 128+signal folded on), 71 out of memory, 72 card folder unwritable. Every start checks the ELF, the card folder, the disc and its revision BEFORE a window exists; `socom2` with no argument runs from the launcher's `config.json` and logs to `logs/run_<stamp>.log`; the launcher's LAST RUN shows the code's sentence; SAVE DIAGNOSTICS writes one STORE-only zip (clipped log, allowlisted config with the ISO cut to its file name, GL caps, crash record, versions) with the home directory scrubbed from every member *(Moved from section 2 to section 1 on 2026-09-25, Sprint 13 R5, the full read: settled -- gate `s9_g1_gate` 3/3 (`D:/socom_archive/parity/gate/`) with the preflight live, `test_runner_exit_codes` on Windows and in the VM. The right-hand cell is the section 2 cell as it was written.)* | Suites `ExitCodes`, `Preflight`, `BareRun`, `ZipStore`, `Diagnostics`; `test_runner_exit_codes` (8 cases on the real exe, RED 8 -> OK) and `test_diagnostics_zip`; gate 3/3 `s9_g1_gate` with the preflight live; **Linux, in the VM:** the 8 cases pass on `dist-linux/socom2` (70 from SIGSEGV, 71 from the new_handler), the zip test passes with no display, a `--home` bare run logs `[bare-run]` and `[preflight] ok:` and starts the game. `6f9996d`, `434bc3e`; R126-R138 *No issue: a landed result, nothing left to fix.* |

## 2. Believed, unconfirmed — with the experiment that would settle it

| What | What would settle it |
|---|---|
| **Two halves of the r0004 build are still open** (2026-09-25, split out of the proven row now in section 1). **(a)** The r0004 build has only ever run against the **r0001 disc's assets** -- the gate exercises nothing else -- so nothing yet says what r0004 expects from its own package's data. **(b)** The capsule's **r0004-layout stub table at `0x002CF330`** was never applied to our image, so whatever PSRewired disables there, our build runs. *No issue: Goal F, the owner's Discord answer and their own r0004 assets.* | (a) an r0004 disc tree of the owner's own, and one gate run against it; (b) the owner's Goal F answer on whether the capsule's r0004-layout stubs are applied at all -- neither is the machine's to settle |
| **The disc-to-ELF chain has run on one disc, on Windows** (2026-09-25, Sprint 13 R5, split out of section 1's row "A developer with their own r0001 disc can build and run the game from a fresh clone"). Proven there: the owner's r0001 image through `scripts/disc_to_elf.sh` on Windows, three times byte-identical; and, since Sprint 12, the recompile step on Linux (`ps2_recomp` in 52 s). Not proven: `disc_to_elf.sh` on Linux from an ISO (`CONTRIBUTING.md` says so in as many words), and any other r0001 dump -- a differently laid-out image is allowed by design (R231), so a second dump is exactly the input that could refuse. *No issue: the second-dump half needs a disc this machine does not have, and CONTRIBUTING asks the first stranger who has one to report the line it refused on, which opens the issue; the Linux half is one lock-bound VM run (the VM holds the ISO, `scripts/vm_sync.sh iso`), filler for the next VM session issue #25 already needs.* | `bash scripts/disc_to_elf.sh <iso>` on a Linux machine with Unicorn, ending in the digests recorded in `tools_py/disc_to_elf_expected.json`; the same command on a second r0001 dump |
| **The Linux client has never run on a real GPU, and no bug report has been sent from Linux** (2026-09-25, Sprint 13 R5, split out of section 1's row "The parity gate's title stage passes inside the Linux VM at the same 19/23 as Windows" and the launcher bug-report row). Every Linux result is the VirtualBox VM on VMSVGA software GL at ~3 fps: the boot frame, the title stage 19/23, the suites; nothing has measured a mission at speed, an online round or a frame rate on Linux hardware. The bug report's Linux path (a `curl` subprocess) is tested on Linux CI against a loopback server, but no live report has reached the box from Linux -- the spec's "from each platform". *No issue: the GPU half is the owner's hands (`docs/HUMAN_TASKS.md`, "Run the Linux tarball on a real Linux machine or a Steam Deck"); the report half needs only the VM and a network, one filler run.* | The tarball on a Linux machine with a real GPU through a mission and a control round, its log's GL line and fps; one REPORT A BUG from the VM's launcher, read back on the box as `source=launcher` with `platform: linux` |
| **`build_revision --out` re-decrypts from the disc tree and looks for the map in the out folder (2026-09-25, Sprint 13, the #48 closing run).** *(issue #56 (closed) 2026-09-26: the fix merged into `main` at `9b566459`, PR #64)* Here `game/disc_r0004/` reaches r0001's `APACHE00.ZDB` through junctions (the r0004 overlays come from the capsule), so the out layout decrypted the wrong package, merged an ELF of 4,835,072 bytes against the tree's 5,016,016 and `--check-against` said MISMATCH; the map refusal needs `--ghidra`. The in-tree layout skips both steps as current and never shows it. | `--out` reads the tracked map from the tree and reuses current `game/overlays_<rev>/` products instead of decrypting; a temp-tree test; one unseeded `--out` run whose check matches. Until then seed `<out>/overlays_r0004/` from the tree and pass `--ghidra`. **Fixed on `agent/s13x-56` (`b9a6aca0`, 2026-09-26; merged into `main` at `9b566459`, PR #64, fresh review PASS WITH FIXES; #56 closed with the artefact comment):** under `--out` the map defaults to the tracked `recomp/socom2_ghidra_<rev>.csv` (`--ghidra` still overrides), and when the tree's `game/overlays_<rev>/` holds both overlays, the merged ELF and an `<elf>.repair.json` that passes `overlay_repair --check` for the run's repair inputs, the four files are copied into `<out>/overlays_<rev>/` and nothing is decrypted (the sidecar records no disc input, so the disc tree given is not matched; `--force` decrypts regardless). `BuildRevisionOutReadsTheTreeTest` holds both halves over temp trees; the four older `--out` refusal cases that used `r0004` moved to throwaway revisions, because on a checkout holding r0004's products an r0004 case now skips the refusal it is about and runs on to the recomp and the runtime under the loop lock (seen once in this worktree's suite and stopped by hand). The run (`wt-s13x-56/logs/x56_out.txt`, `--stop-after recomp`, no seeding, no `--ghidra`, the worktree's game/ inputs copied from the main tree): `map: recomp/socom2_ghidra_r0004.csv`, `overlays: game/overlays_r0004/ is current (all 7 repair inputs match …) -- copied …; no decrypt`, `check-against: identical` (`08be8ce7…`), `Loaded 1736 display names`, 16,425 files, rc 0. Found on the way: the main tree's working copy of `tools_py/make_overlay_elf.py` is CRLF against `.gitattributes`' `*.py eol=lf`, so the module digest its sidecar records is one no fresh checkout reproduces -- reuse from another checkout is refused as stale (correctly) until that file is renormalised — done in the main tree 2026-09-26 (the working copy re-checked out from the index; `git ls-files --eol` reads `i/lf w/lf`). |
| **The mission frame rate has no bar (2026-09-25, Sprint 13 V4).** *(issue #59)* The gate's mission stage now prints `FRAME mean= worst1s= n=` — host ms per guest VBlank over the scripted walk (HUD step to s48), a lower bound on the time between presents — and records it as an informational pin (S13-R3: never compared). The three saved stamps read 23.15 / 32.41 / 21.86 ms mean (43–46 VBlanks/s, 31 on the middle one) on three different exes; the unscripted tail after s48 ran slower (36–39 ms) with half its wall time in back-pressure wait. **Three gates on ONE exe (`f90eeec0`, 2026-09-25 23:31Z–00:19Z, `s13_v4_gate1..3`): 24.24 / 30.09 / 22.72 ms mean, 31.25 / 35.71 / 28.86 worst second — a 30 % spread of the mean, past S13-R3's ~10 %; the host was not quiet (gate 2 overlapped five agents' dispatch and the owner's light work; gates 1 and 3 agree within 7 %). The pin stays informational (S13-R13).** | Three gates on ONE exe on a QUIET host (no agents, no owner at the machine, the quiet marker), back to back, agreeing within ~10 % of the median; then the ceiling as a refusal rule in `scripts/parity/pins.json` (S13-R3, S13-R13). |
| **Every generated file includes the all-functions header, so a one-name change recompiles the whole generated set (2026-09-25, Sprint 13 U3 → the backlog by S13-R11).** *(issue #57)* Upstream #253 forward-declares only what each file calls; the runtime build after a rename or a one-function edit is the slowest step of every proof here and on CI. **Fixed on `agent/x57` (`e5227c77` tests, `5d00f29d` emitter, `be5deebd` build, `5f914438` test fix; 2026-09-26), merged into `main` at `b3dae300` (PR #66); #57 stays OPEN for the r0004 leg (`build_revision.sh`'s delete-first and the r0004 gate) and the synthetic fixture's old preamble:** a generated file declares the functions it tail-calls by name (726 files, 804 declarations) and includes neither `ps2_recompiled_functions.h` nor `_stubs.h`; only `register_functions.cpp` does. Three more things rebuilt everything and are gone: `build.sh recomp`'s `rm -rf recomp/output` (ps2_recomp now rewrites a file only when its bytes change and prunes what it did not produce), the runner's PCH holding both generated headers, and GLOB's name order in the unity batches (now ordered by guest address, so a rename stays in its batch). The proof (`logs/x57_chain.txt` in the worktree, one lock hold): census `--diff` against the main tree's output renamed 0, extents_changed 0, only_in 0/0, unhandled 114399 → 114399, unmapped 0 → 0, S12-R11 OK; every file byte-identical to the baseline's but for the preamble; a re-recomp with nothing changed writes 0 files and the build rebuilds 0 objects; **Ascii2Sjis → Ascii2SjisX57 (no caller) rebuilds 2 objects** (its unity batch, the function table's) and relinks: 27 s, against 622 s for the full build (472 runner objects); heap_Free → heap_FreeX57 (17 files tail-call it, in 8 batches) rebuilds 10 in 44 s; the suites green (ps2x_tests CodeGenerator 72/72, PS2Recompiler 19/19); `x57_gate` **GATE PASS (3/3), PINS MATCH (13 compared)** on the worktree's exe `eced6f79…`. | The r0004 leg: `build_revision.sh` (it still deletes its own `recomp/output_<rev>/`) and its gate 3/3 with PINS MATCH; the merge. A rename is one batch of 32 files plus the table's, not one file: the unity build's price. A follow-up: `tests/fixtures/synthetic_recomp/`'s three function files still carry the old preamble (they compile; regenerate them with the new emitter). |
| **The BinDiff cross-check still runs on a second Ghidra 11.0.3 toolchain (2026-09-25, Sprint 13 N4 → the backlog by S13-R11).** *(issue #58)* BinExport `main` builds against Ghidra 12.x since 2026-09-01 with two builder fixes; research/49's numbers are not reproducible on the 12.1.3 the tree pins until it is redone. | BinExport `main` built on 12.1.3 with the EE extension, research/49's numbers reproduced within its tolerance, DEVELOPING's recipe on one Ghidra. |
| **The map's bounds lose to the JAL-target scan's carvings (2026-09-25, Sprint 13 U8, from research/63's #206 row).** *(issue #55)* A carving that shares a start with a map row counts as auto, survives the purge and wins the larger-end tie-break (`elf_parser.cpp:1069`, upstream's byte for byte): 6,640 of 14,657 generated functions run past their csv row, 1,112 swallow another whole, 6,750 files are `sub_`. Every gate is green on this shape; the cost is names and bounds-trusting analysis. S13-R10 sends it to the backlog: it changes the image's shape and moves every pin. | Upstream #206's rule applied, the census diff stated, both revisions recompiled and gated 3/3 with the pins re-accepted after a green run on the new shape (S13-R5). |
| **The tracked r0001 map carries 1090 nested forced-entry rows from before fix_ghidra_csv's fix (2026-09-25, Sprint 13 H7's review).** *(issue #54)* `recomp/socom2_ghidra.csv` has 1097 overlapping neighbour pairs, 1090 with a forced entry that nests inside another and ends at the same address (`FUN_001ac500`/`001ac600`/`001ac740` all end at `0x1ac860`): the footprint of the unsorted, duplicated forced entries `92540d21` fixed, placed by the earlier in-place rewrite. The fixed script leaves the map byte-identical because those addresses are now its own starts; the r0004 map (built through `recomp/build/`) has only Ghidra's 3. Every gate on the current map is green, so nothing the gate measures is wrong today. | Rebuild the map from Ghidra's raw export (or truncate each nested row at the next start) with the fixed script, then recomp, runtime and the r0001 gate 3/3 with PINS MATCH under the lock — the generated code changes. |
| **Two stub translators are not contained, and neither is bound (2026-09-25, Sprint 13 U2's review).** *(issue #53 (closed) 2026-09-26: the containment merged into `main` at `25828e1c`, PR #62)* `Kernel/Stubs/LibC.cpp` `ps2_stubs::fopen` handed the guest path to the host `fopen` untranslated; `Kernel/Stubs/Helpers/Support.h` `cdHostPath` joined onto the CD root with only a lexical normalisation. Neither symbol is in the tomls or the symbol CSVs, and the game's newlib `_fopen_r` reaches the host through the contained fio calls, so the shipped build has no exposure. **Contained on `agent/s13x-53` (2026-09-26, `8a06d413` RED tests, `1c1ada46` fix; merged into `main` at `25828e1c`):** both stubs took the bar's *contain* arm, not removal, because both stay reachable by name for any other image (`PS2_STUB_LIST` binds a function named `fopen` or `sceCdSearchFile`; the analyzer names both). `fopen` now goes through `translatePs2Path` (the fio calls' own root rule) and answers NULL on a refusal; `cdHostPath` goes through its `cdrom0:` branch and `registerCdFile` refuses before its fallbacks, so `sceCdSearchFile` answers 0. Two `ps2x_tests` cases: RED 953/955 (both failed on the climbs and drive letters, `fopen` also on `CON`/`NUL`) -> GREEN 955/955, Python 3198 OK. SOCOM II's own fio path is unchanged. | **Settled 2026-09-26: merged into `main` at `25828e1c` (PR #62, fresh review PASS); #53 closed with the artefact comment.** (`sceCdSearchFile`'s device-name assertion passed on the old tree too: `NUL` stats as a character device, not a file, so it was never found.) |
| **An `exact` match is a fingerprint match, not a proof of the same code (2026-09-25, Sprint 13 R6, from the codex audit's assignment 2).** *(issue #52)* `tools_py/fingerprint.py` zeroes the immediate of every `addiu`/`ori`, not only an address's low half, so two routines that differ only in an ordinary constant fingerprint equal and, when each is unique in its image, `address_matcher` places them as `exact`; `revision_toml` pairs repeated code windows by order when the counts agree. Nothing has been seen to go wrong from it (the r0004 gate `s11_r0004_probe2` 3/3 covers what the gate drives), and no test holds either reading against a counterexample. | The synthetic cases in issue #52's closing bar (changed constant, swapped duplicates, same-length control-flow change, interior patch site, adjacent tables, absent function), each RED first; then `game/r0004/match.json` regenerated and the count of placements that changed stated. |
| **Which group of the stub helpers' per-TU state broke the mission stage** (2026-09-25, Sprint 13 R5, split out of section 1's row "A header that defines its state in an anonymous namespace gives every translation unit its own copy"). The one-shot move of all 2041 lines (`50f8403`) latched the GL replay at t=200 s in `s10_q7_gate`; the revert (`955539c`) restored 3/3. Sprint 11 Task 8b then moved four groups into `PS2Runtime` under tests, and showed the CD group is referenced from `CD.cpp` alone, so no CD cursor was ever shared. Left: the CD group and `g_iopHeapNext`, untouched on purpose, and which group it was. *Issue #51 (closed) 2026-09-26 — merged into `main` at `123dd1c5`, PR #65 (opened 2026-09-25, Sprint 13 R7, under R265: the audit's D12 row leaves the ruled-out list for the stack). No player-visible defect -- the tree runs the reverted per-TU state and every gate passes; the move is hygiene.* **Both groups moved, one gate each, both 3/3 (2026-09-26, `agent/x51`, merged into `main` at `123dd1c5`, PR #65):** the CD group (the fourteen variables) is `ps2_stubs::CdRuntimeState` (`Helpers/CdRuntimeState.h`; RED `d9d8e2e6`, GREEN `1f358cb5`) and `g_iopHeapNext` is `ps2_stubs::IopHeapRuntimeState::next` (`Helpers/IopHeapRuntimeState.h`; RED `25e1053c`, GREEN `c69edf76`), each a `std::unique_ptr` member of `PS2Runtime` reset by `resetStubRuntimeState()`, Task 8b's shape. `ps2xTest/src/support_state_tests.cpp` includes `Stubs/Common.h` so its own copy of the Support.h helpers is the other translation unit: RED 956/958 (the two CD cases) and 958/959 (the IOP case), GREEN 958/958 and 959/959, Python 3207 OK at both. Gates from the main tree on the worktree's exe: `logs/parity/gate/x51_gate_cd/` PASS 3/3 (mission: HUD reached, 6/7 hold captures gameplay) and `logs/parity/gate/x51_gate_iop/` PASS 3/3 (7/7); chain log `logs/x51_chain.txt` in the worktree. So each group moved in its own step under its own 3/3 gate, and at `c69edf76` all six groups are runtime-owned together and the gate still passes 3/3: on today's tree, moving the state -- one group or all six -- does not lose the mission stage. That does not rule out the state move as the 2026-09-21 cause, since the tree has changed a great deal since `s10_q7_gate`. Not established, and not pursued (no defect today): which other part of `50f8403` did it -- that commit moved all six groups at once and, in the same step, gave every one of the header's ~70 helpers external linkage (one definition in `Support.cpp`, namespace `stub_support`); the two gates here keep the helpers per translation unit, as the tree always had them. | **Settled 2026-09-26: merged into `main` at `123dd1c5` (PR #65; gates `x51_gate_cd` and `x51_gate_iop` both 3/3, the per-TU copies gone); #51 closed with the artefact comment.** |
| **No round has been played between two different networks** (2026-09-25, Sprint 13 R5, split out of section 1's row "An online round plays end to end on the project's HOSTED Horizon server over the internet"). Every online result -- the hosted control round, the hosted kill, the mixed matches with PCSX2 -- is two instances behind one home NAT on one host. What a second network adds and nothing has measured: the peer UDP channel reached from outside that NAT, and a round-trip time the harness has never seen. *No issue: it needs a second machine on another network and a person at it (the two-machine match, carried since Sprint 7); the first such round's log is the evidence, and a failure there opens the issue.* | One control round between the owner's machine and a machine on another network, both logged in to the hosted server by name, `RESULT CONTROL-ROUND` on both sides |
| **The VM guest may still hold a stale `dist-release/` and stamp its tarballs with a lagging `git describe`** (2026-09-25, Sprint 13 R5, split out of section 1's row "HAZARD, fixed: the VM sync carried the host's mtimes"). Recorded 2026-09-20 as left in the guest: the `dist-release/` copy the sync stopped pushing, and `version.txt` in a VM-built tarball reading the guest's own `git describe`, which lags the host's because the sync carries files, not history. The 2026-09-23 VM rebuild wiped the build trees (`docs/DEVELOPING.md`'s Linux timings), not `dist-release/`, so neither half is known to be gone. *No issue: VM-only -- no archive a player receives is built in the guest from that copy, and the tarball's version string is a label, not an input; it matters the day a release tarball is cut in the VM.* | In the guest: `ls ~/socom_pc/dist-release` (and delete it if present); build a tarball and compare its `version.txt` with the host's `git describe` for the same tree |
| **VU0 macro-mode FTOI answers INT_MIN for a NaN, matching the Cucumber fork; the VU1 path saturates by sign (2026-09-23, Task 8a).** *(issue #33 (closed))* The emitter's `Ps2VuFtoi` clamps ±inf and out-of-range values to INT32_MIN/MAX and sends NaN (either sign) to INT_MIN — the fork's answer, which the game's own data has never been seen to exercise; PCSX2's interpreter likely saturates a positive NaN pattern to INT_MAX by its sign bit (not verified locally); the negative patterns agree. The rule is the fork's (`78ce1a21`), not hardware-verified. Since `fd08a106` (Sprint 13 C4) the three HLE sites in `Kernel/Stubs/VU.cpp` (`sceVu0FTOI0Vector`, `sceVu0FTOI4Vector`, `rotTransPersOne`) convert through `Ps2VuFtoiScalar`, one lane of the same helper, where they used a bare cast; the rule is held by ps2x_tests' "VU FTOI's NaN rule" and the three FTOI stub cases | A hardware trace of one FTOI on a NaN (or the console's behaviour cited from a tested source) decides the NaN rule; the gate on the re-recompiled exe (`s11_u_translators_gate`) shows whether any asset reaches the clamps at all **Fixed 2026-09-26 (Sprint 13 C4, `fd08a106`, merged `e9e7b4cc`): the three sites convert through `Ps2VuFtoiScalar`; gate `s13_merged_gate` 3/3 on `0633c484`. SETTLED.** |
| **The Linux VM's suites are not green on the merged tree (2026-09-23, chain 9b).** *(issue #25)* After the follow-up that fixed the five Linux-only causes (`5a2dc44`), the VM still reports Python 1953 with 3 failures and 1 error and C++ 788 with 4 failures (`logs/chain9b_test.log`; the runtime built, the knob-line cases 12/12). Linux CI is green, so these are the VM's environment or real Linux-only defects. Chain 10 read the names off the guest (`logs/chain10_details.log`): `test_gate` MissionSeeing still copies `game/disc/mc0_parity/…` (the follow-up's skip did not cover it); three cases of the new `test_python_resolution` fail on Linux because the PATH they build holds only the shim dir, so `bash` itself is not found, and the resolved path does not end in `python3` there; the C++ side lost four, one of them R40's slow big-batch consumer case | Fix or isolate each, re-run `scripts/build_linux.sh test` in the VM under the lock, and only then write the Linux row of DEVELOPING's timings as green |
| **A received chat line does not pass through the client's chat receive wrap (2026-09-26; was: "has not yet been seen traversing the client bound", 2026-09-23).** *(issue #26)* Sprint 13 O2 built the step #26 asked for -- R1 opens the chat box (the rooms' own button bars say so), the keyboard is `ChatSkb` with one more row than the login's, and `online_match_ours --chat` types a line after A hosts and B joins (`scripts/parity/control_round_chat.sh`) -- and in two rounds on two exes the lines crossed both ways (B's chat panel shows `socomc (All): hello`, A's shows B's reply) while **neither log ever printed the wrap's `[socom2] chat receive bound: seen=` line**, which the wrap prints on its first call: `chatFanoutRecv` 0x2f4ef0 is one of three callbacks the `MediusInit` UI action registers and posts types 2 and 1 to the HUD line, any type when the UI state byte is 0xb -- it is libmedius's legacy MediusChatFwdMessage callback (`FUN_0064c310` -> `DAT_00656228`); a game-lobby chat line reaches the panel another way. A call trace of the shell's chat method `FUN_001f4d20`, its two branches and the two sibling callbacks saw none of them run either. The receiving function is not identified; the briefing-room list the Task 2b list wrap walks is filled through the game's own terminating copy (`FUN_0039fc60`, name 0x20, message 0x40), whether the lobby panel uses it is not established (research/66) | `logs/parity/s13_o2_chat_20260925_232224` (round 2, exe `f90eeec0...`) and `..._20260926_003237` (round 3, merged exe `0633c484...`, the call trace) in the `wt-s13-o2` worktree: `B_chat_A_to_B.png` / `A_chat_B_to_A.png` show the lines; `logs/run_{A,B}_20260925_232239.log` and `..._20260926_003247.log` carry the wrap's install line and no `seen=` line; VERDICT `RESULT CHAT FAIL lobby=PASS keyboard-A=PASS receive-B=FAIL`. What would settle it: find the function that takes a game-lobby chat line off the network (trace libnetb's receive down to the message's handler, or `PS2X_WATCH` the panel's record while a line arrives), move or add the bound there with its tests and a gate, then `control_round_chat.sh` with B's log showing that wrap's first-call line; #26's closing bar (which names the old line) is the controller's to restate Also: read the Horizon MLS/DME log for the round's window to see which message carried the line (its server path was not logged). |
| **Something between `render()` and the audio dump's write loses about 50 ms of the mission music, on any endpoint** *(issue #42)* (2026-09-23, opened by the endpoint A/B in §1). What is known: the events are 50 ms holes 10-20 dB deep, they cluster in the minutes carrying the briefing score, they are present in the endpoint recording and absent from the dump at the aligned time, and they do not care whether the device is Bluetooth or wired (14 against 11; 10 DEVICE + 4 NODUMP on today's scorer, research/68 §3). What is not known is which of the three candidates it is: the render call itself running late under gameplay load; the dump being written from a different place in the chain than the device is fed from, so the two are not the same signal; or the scorer still mis-matching a dip against its dump twin on a local offset (the same class of mistake that made 31 out of 11 on 2026-09-22). **2026-09-23 audio-out: the first candidate is eliminated for one run, and the second is ruled out by reading.** The dump is written inside the device callback itself (`ps2_audio.cpp` `mixerRender`, after render and gain), so it IS what the callback handed miniaudio; and `PS2X_AUDIO_CB_TRACE` (`runtime/audio_cb_trace.h`, the branch `agent/audio-out`) recorded every callback of a briefing capture: 38,422 callbacks over 768 s, 0 late (over 60 ms), 0 dry (over the 80 ms buffer), max gap 21.7 ms, max render 3.12 ms (`logs/parity/audio_out_20260923_065836/cb_trace_report.txt`, and `cb_holes=0` on every `[audio-trace]` line). That capture's own DEVICE count is void (§4: a browser on the endpoint, the dump's cap, the endpoint's tail), so the A/B's 14 and 11 are still unattributed. What remains for them: the audio engine itself; the loopback recorder's silent overflow drops (`loopback_record.py`: `read(1024, exception_on_overflow=False)`); the scorer's alignment (its envelope correlation was 0.31 on that capture, 0.47 on the next); and, now demonstrated, another session on the endpoint | **Step 1 taken 2026-09-25 (Sprint 13 V5):** the quiet-endpoint capture `audio_out_20260925_074147`, `sessions_verdict.txt` = `clean`: DEVICE 6 over 16 min (11-14 before), 36,756 callbacks with 0 late / 0 dry, all six dips cleared -- not the device thread's (the plan's V5 row; its Log, 09:30Z). **What remains to settle it:** the loopback recorder's silent-overflow drops (count them) and the scorer's alignment, V5 Steps 2-5 (the DEVICE count as a pin in the audio gate, the route capture, research/36 items 5 and 7), not run. **Settles it, as written before Step 1:** the same ten-minute briefing capture on a quiet endpoint (`sessions_verdict.txt` = `clean`), then `python -m tools_py.parity.cb_trace <capture>/cb_trace.csv --dips <capture>/dips.txt`: a DEVICE count back near 14 with 0 late callbacks moves the search past the device thread (the recorder's overflow is the cheapest next instrument: count them); late or dry callbacks attribute each dip to the callback that rendered its dump time. Plus the per-minute DEVICE count pinned into the audio gate so the number cannot drift back silently. The owner's step was `docs/archive/HUMAN_TASKS-to-2026-09-25.md` (2026-09-23 morning), and the loop took it: Sprint 13 V5 Step 1, 2026-09-25 07:41Z |
| **The mission music degrades with time in the mission, and it is not a cut, a fade, or two cues at once** *(issue #28)* (the owner, 2026-09-22: "fine the first while, gets much worse as I proceed ... it is not playing linearly and I think that's noticeable to the human ear when entire ensembles etc are playing"). What is known: the first small stutters arrive in the briefing; the stall bound engaged 0 times in that run; the renderer reported "replay made no progress within 2000 ms" twice; music chunks re-fire from identical sector offsets (`1032ea+1920` seven times at 13.14 s, `11f275+0` six times with varying durations). Whether those re-fires are the audible defect or normal game behaviour is exactly what is not known | **Settles it:** the owner's own instruction -- a drive script that skips the cinematics and reaches gameplay fast, then a single long in-mission capture (10+ minutes, because the defect is time-dependent) scored against PCSX2 on the same mission. A capture short enough to sound fine proves nothing here |
| **21k texture uploads a second in an online round even with stable CLUT ids** *(issue #32)* (45 ms/s of render time; was 64k/s with the cache exploding). Textures whose pages take a shadow-generation bump every frame are re-decoded — the palette rewrites likely share a page with a texture.  **Re-measured 2026-09-17/18 (Sprint 7 Task 3): gameplay uploads ~10k/s at 20-28 ms/s in two online rounds' stats lines; the MENUS are the expensive screens at 7-11k/s and 80-133 ms/s; the boot loads reach 35-46k/s. The page-marking cause below (§2) was refuted by its trace.** | `PS2X_GS_STATS=1` plus the `[gs-pages]` trace on the HUD/skin texture pages; the cache key's page span. **A/B 2026-09-26 (Sprint 13 V2, `c2789ea7` + `84e48287`, merged `3804d10b`): on the login screen 41 % of the uploads are `same_free` and the skip removes them, but `upload=` moves ~4 % (111–115 -> 106–111 ms per 60-call window) while `present=` falls ~20 -> ~6 ms; the cost is in the 59 % that are `changed` (~18 %) or `same_rewritten` (~40 %). The knob stays off; the next step is batching or the rewrites. Issue #32 stays open on that bar (the comment carries the table).** |
| **The streamed full-screen image path puts every strip at its own DBP** *(issue #31 (closed) 2026-09-26: the capture and the gate done -- see the row above)* (`FUN_001c6570`, decomp 45100/45108): the guest sets the load packet once and, per 640x64 CT32 strip, rewrites only the DBP halfword at **packet offset 0x14** (+0x280); DBW, DPSM, TRXPOS and TRXREG never change. The row's old premise -- a 12-byte `GsImageMem` that ends before 0x14, so every strip lands at the set-time DBP -- was true until `545b85a1` (2026-09-16), since when `sceGsExecLoadImage` parses libgraph's six-quadword packet from guest memory on every call. `53c75981` pins it with the LOADING.RAW stream in `ps2_gs_tests.cpp` (seven strips, each sent to and read back from its own DBP) and stops the parse at TRXDIR, so the quadword past the packet (the caller's frame) can no longer replace its BITBLTBUF | Settle: `\RUN\RAW\LOADING.RAW` (640x448, the one full-screen caller: `FUN_001c6570(path,0x280,0x1c0,0,0)`, decomp 44718) captured and compared against the console image -- seven 64-line bands each in place, no band repeated or blank -- and the gate 3/3 | Citations the review added: `545b85a1`'s tests are `3d37abcc` and its gates `s6_lum7`/`s6_lum8`; the 2026-09-17 audit (`docs/audits/2026-09-17-audit-and-code-review.md:123`) had already said the fix was likely a side effect never verified against the loading screen; research/25 §10 (lines 704, 711) still lists the defect as outstanding. The settle bar's reference frame is `logs/parity/runs/transition_pcsx2/s00_CROSS.png` (the golden s00: the logo, the trident, LOADING… bottom-right). **Closed 2026-09-26: the merged exe `0633c484` (V3 `413439d3`, C4, V2) passed `s13_merged_gate` 3/3 and a 0.5 s boot burst (`logs/s13_31_boot_shots/host_000_1s.png`..`host_005_4s.png`) shows the loading screen whole from 1 s to 4 s, matching `transition_pcsx2/s00_CROSS.png`. SETTLED.**
| ~~Which of `I`/`K` (right stick up/down) raises the muzzle~~ *(believed settled 2026-09-13 by camera-orbit inference: `ry=0` (I) raises the muzzle — research/22 §3.2; damage is the ground truth)* *(Ruled 2026-09-25, Sprint 13 R5: still believed, not proven. The ladder's kills (section 1, "THE ACCEPTANCE TEST PASSED") were aimed with this reading and landed, which is use, not a measurement. No issue: the timed hold is owed only if a pitch question ever decides a result.)* | One timed pitch hold measured against the elevation of a target of known height difference. The engagement sweeps both ways precisely because this is unknown |
| ~~**`sceGsSetDefDBuff` reads its trailing args from the stack; the guest passes them in `$t0`–`$t2`**~~ **Fixed `07ffc0a`** (args via `decodeGsTrailingArgs3`, both clear packets seeded in context 1, byte-exact vs `title_pcsx2` in a unit test; gate 3/3, run-vs-run min 99.8, vram-diff 15/15). What remains believed: (both callers, 15/15 of our images vs PCSX2's 3/3), and never seeds the clear packet. The ZBUF/TEST/clear bytes look never sent, so the visible effect is believed nil; the consumed display environment (`disp[1].display`: MAGH 0 vs 3, DW 639 vs 2559, DY ~18 lines, PMODE) and zbp (0x118 vs 0x8c) also differ from the console (research/20 §4, review re-derived) *(Ruled 2026-09-25, Sprint 13 R5: the fixed half is `07ffc0a`; what is still believed is the display environment and zbp difference. No issue: its visible effect is believed nil -- every gate frame since has matched its reference and the console-spawn comparison passes (`s13_v1_gate`) -- so the console A/B is owed only if a display-geometry defect appears.)* | Task 4 Step 5 fix + unit test against `title_pcsx2` bytes + vram-diff; display env and zbp: a Sprint 6 A/B against a console image |
| **The `0x408F10` round clock is per client and starts when that client enters the round**, so a clock skew read between two machines measures entry-time difference plus drift, not server time (Sprint 7 Task 5's readout on `ours_control_foxhunt_guard`: +6.0 s skew, B entered ~5 s before A; the two spans agree to 0.05 s over 374 s). | The two-machine match (Task 5): anchor each side's clock on its own entry line; a residual over ~1 s is drift worth a row of its own. *No issue: a rule for reading the clock, not a defect.* |
| **Whether a parked opponent starving the mover matches console behaviour, or is an artefact of feeding the counter from RX bytes only (the game's own source may be richer)** *(No issue: the harness keeps both players moving (section 4, "A parked opponent starves a *stuck* mover"), so no measurement depends on the answer; the run is the Sprint 10 Goal 3 plan's task 5 (`docs/archive/sprints-7-12/2026-09-20-sprint-10-goal-3-mixed-match.md`), carried twice and never scheduled -- ruled 2026-09-25, Sprint 13 R5.)* | A PCSX2 pair with one player parked and the other walking, same `actor+0x1368` measurement |
| **The transition residual strip (~1 in 5 runs) is a `refreshDirtyRows`/`executeClear` ordering artefact** | No isolation test has been run; the *pre-existing on both binaries* half is measured. **Settles it:** one strip frame replayed with `refreshDirtyRows` and `executeClear` in each order. *No issue: held to R265's retire-or-test bar (`docs/audits/2026-09-25-project-audit.md` §4): three gates and three ladder runs on the renamed tree without a sighting retire it to section 3; a sighting opens the issue (ruled 2026-09-25, Sprint 13 R5).* |
| **The intro-cinematic freeze (seen once, Sprint 1) is a real defect** | Not reproduced since. **Settles it:** a recurrence with its log -- every gate's title stage and every launch plays the intro, and none has frozen since Sprint 1. *No issue: held to R265's retire-or-test bar (`docs/audits/2026-09-25-project-audit.md` §4): three gates and three ladder runs on the renamed tree without a sighting retire it to section 3; a sighting opens the issue (ruled 2026-09-25, Sprint 13 R5).* |
| **The DME aux-UDP channel carries nothing at round start** | Rate bound only; `PS2X_SOCOM2_NET_TRACE_ALL` now closes it. **Settles it:** one round start under `PS2X_SOCOM2_NET_TRACE=1` and `PS2X_SOCOM2_NET_TRACE_ALL=1`, the aux-UDP datagrams decoded -- never run. *No issue: the question was asked of the round-start freeze, whose cause was the GL texture cache (section 1, "FIXED 2026-09-16 (research/34): the online round that never started"); rounds and kills play, so nothing now waits on the aux channel's content (ruled 2026-09-25, Sprint 13 R5).* |
| **`sin`/`cos` have no live call sites** | Direct-`jal` count only; `0x001D55B8`/`0x001D55BC` hold their addresses in a probable dispatch table. **Settles it:** a `PS2X_CALL_TRACE` on both over a mission and an online round -- minding issue #40 (closed); the tail-call caveat lapsed with the 2026-09-26 recomp, which means zero lines is not proof by itself (a tail call to a stub is not counted) -- a caveat that lapses once the game is recompiled with Sprint 13 C4's emitter (`fd08a106` and its fix round), after which a tail call reaches the traced table entry. *No issue: it matters only if the functions' binding is wrong, and nothing points at that (ruled 2026-09-25, Sprint 13 R5; issue #40 (closed) was the tracer's defect (closed 2026-09-26), not this row's).* |
| **What is still Windows-only after the Linux port** (Sprint 8 Goal 1 close-out): ~~the recompile step (the owner's disc through ps2_recomp; the generated code travels to Linux as files),~~ *(corrected 2026-09-25, Sprint 13 R5: the recompile step runs on Linux -- Sprint 12 ran `ps2_recomp` there in 52 s (`docs/CURRENT_SPRINT.md`, Sprint 12's task table, row 3a); `disc_to_elf.sh` has not, section 2's row "The disc-to-ELF chain has run on one disc, on Windows")* `build.sh`'s vu1_replay verify and vram-diff (not mirrored in `build_linux.sh`; the tool itself builds on Linux now), the host CPU/working-set sampler in `run_detached.sh` (PowerShell counters; Linux starts none), `check_quiet_gate.sh` and the loop lock's busy list (tasklist/.exe names), the PCSX2 mixed match and its DNS stub on port 53, the PowerShell scripts (drag_window, archive_logs, the server's start-servers), the launcher's native file dialog (Linux uses zenity when present, else the typed path). Believed harmless for a Linux player; each is a harness or developer convenience, none is in the shipped client | The survey of 2026-09-18 and `build_linux.sh`'s own header. Experiment for the one that matters to measurement: a /proc-based sampler behind the same CSV columns, when a Linux frame-rate bar is wanted *No issue: a statement of scope, not a defect.* |
| **The 21k texture decodes a second: the page-marking hypothesis is falsified by its own trace, and the figure is stale** *(issue #32)* (Sprint 7 Task 3 Step 1, stop rule). `PS2X_GS_TRACE_PAGES=0x0c0:8` over an online login stage and an offline mission stage: the online log has zero decodes and zero `download gpu->cpu` on the HUD pages in 180 s; in the offline log 80% of decode frames have no preceding download and 92% of downloads FOLLOW a decode (a `gpu-dirty` source forcing a readback -- the arrow runs decode -> download, the reverse of the hypothesis). Re-measured from the stats line of two online rounds that reached gameplay (`run_A_20260917_215259`, `_220545`): gameplay uploads ~10k/s at 20-28 ms/s (not 21k at 45), submits 110-150k/s; the MENUS (login, universe, lobby, t=146-350 s) upload 7-11k/s at **80-133 ms/s**, and the boot loads 35-46k/s at 30 ms/s. | Uploads are 16x16 1 KB tiles (`upload dbp ... 16x16 psm=00 bytes=1024`, 100% of the traced uploads). Experiment for the menus' 80-133 ms/s: `PS2X_GS_TRACE_PAGES` over the login screen's own pages (find them from `resolveTexture`'s log for the menu atlas) and a per-upload cost breakdown (is it the 1 KB tile path's per-call overhead -- 10 us a tile -- rather than the byte count); the fix is then tile batching, not cache marking. Sprint 8. **Sprint 13 V2 (`c2789ea7`, 2026-09-25):** the instrument is in: under `PS2X_GS_STATS=1` a `[gs-gl stats] reasons` line counts each upload by branch (`chunked new changed same_rewritten same_gpu same_free skipped=`) and each texture resolve (`hit rt revalidated redecoded new`); `PS2X_GS_UPLOAD_SKIP=1` skips the `same_free` ones (same bytes, no block of the tile written since, no target's GPU-drawn rows over it), default off because R122 found 84% of the login screen's identical uploads under a GPU-drawn target. **Not new in kind:** R122's final version already validated per rectangle, not per page (`docs/archive/sprints-7-12/2026-09-19-sprint-8-menu-cost-2b.md:927`): it fired on 4.0% of 2.27 M login-screen uploads, its GPU-drawn-target guard refused 84% of the identical ones, and fps did not move (54.3 vs 54.2). Per-block tracking is only what R118's page-granular version lacked. V2's GPU guard is narrower (the drawn-row window since the last download, not the used extent), so it may fire more often -- unproven; the controller's A/B expects a SMALL `same_free`. Untested without a GL context: the backend wiring (`uploadUnderGpuRows`' page arithmetic, the stamp sites, `CompleteImageTransfer`'s early return); only the Gate header is unit-tested. The hash is a custom 64-bit multiply/xor-shift over 8-byte words: a single-word difference cannot collide, and a collision is bounded to one stale rectangle. Review fix round (`84e48287`): a target re-addressed at a narrower FBW now invalidates, and an unaligned-dbp local copy or Z-format upload stamps the page row it spills into. Still open: the measurement on login/universe/lobby decides it. If `same_free` is at least half of `uploads`, turn the skip on and gate it. If `same_gpu` dominates, batch the tiles. |
| **Believed, not proven (Goal 1):** the Explorer double-click lets go of its console window (R132) and the no-audio-device notice reaches LAST RUN -- both need a person (HUMAN_TASKS). A gate or harness flow that ever drives a non-r0001 image now exits 67 (R130); none exists. ~~`test_make_portable` (the `version.txt` of R138) is skipped on Linux by its own guard (it needs bash AND powershell), so the Linux packaging branch's `version.txt` has no test~~ *(superseded 2026-09-25, Sprint 13 R5: Sprint 10 Q7 gave the branch a test that runs on any host -- `MakePortableLinuxSyntheticTest` over a synthetic `dist-linux/` (`a4db0e1`) -- and `MakePortableLinuxTest` runs it for real on Linux CI; `docs/archive/sprints-7-12/2026-09-21-sprint-10-q7-residuals.md` item 2.)* Also the owner's hands, gathered here 2026-09-25, Sprint 13 R5 from the section 1 rows they sat in: the bug-report page's feel with a pad and its wording (the launcher bug-report row), and the crouch shortcut's feel on a real pad and the touchpad (the crouch row) -- HUMAN_TASKS. | Sprint 9 Goal 6 for the packaging test *No issue: the owner's hands.* |
| **HAZARD: three C++ cases are wall-clock flaky in the VirtualBox VM** -- `GsFrameBackpressure`'s two R41 VBlank re-anchor cases and `PS2RuntimeInterrupt`'s `accountCycles` fail 1-3 at a time there and never on Windows or the CI runner; the VM's Python ring also fails 18 cases for environment reasons (no `server/` in the sync, no bare `python`, the ISO really present). Read a VM suite result by suite name, not by its exit code | The VM run of 2026-09-20 (644 total, 641-642 passed); `018bbf9` fixed the sync that left `git mv`'d files behind *No issue: read a VM suite by suite name, not exit code -- a lesson; the VM's own suite run is issue #25.* |
| **Voice: the headset path, as far as it is proven (2026-09-19)**. The game talks to `lgaud` through one 0x840 block passed as both send and recv; it opens the headset when an online GAME SESSION starts (not in the lobby), `mode=3 rec=1ch/16bit/8000Hz play=2ch/16bit/8000Hz`, once our status word behaves: every client call merges the reply's +0x04 (`word & ~2 | reply`) and the voice tick reopens only on a merged word of exactly 1 -- we had answered 3 then 2, pinning it at 3. Recording is hold-to-talk with a floor request to the host (request type 1, grant type 3), 0x500 bytes per read, **SASE**-encoded — *superseded 2026-09-25: this row read "Nellymoser-encoded" from 2026-09-19. SOCOM II's voice codec is **SASE** (`SaseEncVad`/`SaseDec`), verified against all four images (`docs/research/44`'s addendum, `docs/research/56`); SOCOM 1's demo speaks LPC-10 and GSM appears nowhere. No image carries the string `Nellymoser` at all — only the assert macro `NellyNull` — so the earlier reading was always an inference from that macro, and it is withdrawn. The voice object opens at **8000 Hz and reads every 80 ms** (research/56 §3, §6)* — carried inside the existing DME game session by callbacks the game hands the network library (the game itself never sends). Live peeks in a round: tick state 1, +0x44 = 2 (idle), the master gate +0x4c = 1, DAT_0045a0c1 = 1 (zoom becomes talk), the talk flag +0x4a never moves with ANY of the sixteen pad bits held -- the talk action (0xb, shared with zoom/aim) looks unbound in the loaded control preset, which is a disc resource (`data/common/controller.rdr`, section ControllerConfigs), not an ELF table | `94937ec`, `79a9e26`; `s8_voice_round1` (Open on both instances), `s8_voice_round2`/`round3` (the sweep and the peeks). **Closed for Sprint 8 (`s8_voice_round4`, both instances in the match, headset open):** the second talk route, action 10 (decomp :83903-83920, no zoom gate, a 10 s cap), accumulates DAT_004130a8 while held, and it stayed 0 under all sixteen pad bits at 3 s each, as DAT_004130a0 did; the enclosing DAT_0044f108 is non-zero (0x848e20), so the block runs. +0xdcc is not a gate (it latches with the hold timer), the hold threshold is 0.3 s, and the disc's one control dictionary has no talk action. No pad button talks -- the game does not send your voice yet, which is the sentence the launcher's MICROPHONE page, README and INSTALL all use (Sprint 13 V8). What is left is the headset's own button as the module reports it: the status word's bits above bit 1, the device-info block's unread 0x00-0x61 span, and GetMixer's u16 at reply +0x2c -- a listing question, carried to Sprint 9. A lesson recorded with it: grepping the generated code for constant-address stores does not prove a global is never written (0x45a0c1 has one such store, of zero, and reads 1 live) *No issue: proven as far as it goes; what is left is Q5 and one peek in a live round (R221), both queued, and the codec correction above.* **R221's peek, read 2026-09-26 (Sprint 13 O2, research/66 section 2-3):** `DAT_004415a8` is a POINTER to the loaded controller preset (the decompilation assigns it, tests it for 0 and copies it), so the talk slots are `*(0x4415a8)+0x1c` / `+0x1d`, not the literal `0x4415c4/5` R221 was written up as. On r0001 it holds `0xd4f340` (the preset named `Default`) on both instances, in the lobby and in the round, in all three O2 rounds (1567-2053 resolved sampler rows per instance per round): the 13-byte action table reads `09 10 0a 0f 0b 08 0e 10 10 0c 0d 07 01`, **action 0x0a -> slot 0x0d, action 0x0b -> slot 0x07: both talk actions are BOUND**, so the sentence above that 0xb "looks unbound in the loaded control preset" is withdrawn. With every pad bit held in Sprint 8 and the flag unmoved, the gate is elsewhere (the routes' other conditions); which physical button slot 0x0d / 0x07 is, is not established (if a slot is the libpad bit index: CIRCLE and d-pad LEFT). `guest_addresses.talk_table_ptr` carries the pointer (r0004 `0x44dfc8`) |
| **Sprint 8 branch review, filed for later (2026-09-19, read-only, none blocking):** (a) a texture that really changed pays three walks of its source (hash, decode, hash again inside the decode) -- fold the hash into the decode loop; a 640x512 CT32 movie frame is the worst case. ~~(b) `micFramesNeeded` consumes one input frame too many on some reads at rates other than 8000 Hz (exact at the game's 8000).~~ *(b) fixed 2026-09-21 (Sprint 10 Q7, `a997238`; marked 2026-09-25, Sprint 13 R5): the real shape was wider -- under 8000 Hz a frame repeated, and 43,299 of 44,001 rates were affected -- and all 44,001 rates from 4000 to 48000 Hz are clean now (`MicResampleFeed`).* ~~(c) the launcher's profile name reaches `PS2X_MC_DIR` unsanitised and an in-game format runs `remove_all` on the card root; `PS2X_MC_DIR_SLOT1` takes any directory.~~ *(c) superseded 2026-09-25 (Sprint 13 R2, stranger audit S45): the profile is a name, not a path, since `f5809c84` (2026-09-19) -- `normalizeProfile` refuses a separator, a colon, `.` or `..` whole and the launcher falls back to `player`, so the card root an in-game format empties is `cards/<profile>` inside the game folder; outside developer mode a Path knob's value must lie under the game folder (R207, `knobs.cpp`), and `PS2X_MC_DIR_SLOT1` is a Dev knob, so a player's run cannot aim either at another directory.* ~~(d) the card's 8000-cluster size is not enforced on writes, an unwritable card directory reads as an empty formatted card, and GetInfo walks the directory recursively under the mutex on every poll.~~ *(d) fixed 2026-09-21 (Sprint 10 Q7, `dc5eeb1`; marked 2026-09-25, Sprint 13 R5), all three clauses: a write past 8000 clusters is refused whole (`sceMcResFullDevice`), an unwritable card root is "no card" with the exit-72 preflight line, and the free count is cached per port.* (e) `pcmStreamStart` no longer zeroes the ring, so a Stop then Start with no Open between would replay old blocks; the game is not seen to do that. *What is left, 2026-09-25, Sprint 13 R5: (a), written up with its measurement and not started (the Q7 record's section 3), and (e), which Sprint 9 P2 was to check in its trace and fix if it fired -- no record says it fired.* | The review's report in the session record; Sprint 9 Goal 6. The stale-mic-ring defect, the WAV chunk count, the trailing-dot path component and the trace's result names were fixed in Sprint 8 *No issue: a filed read-only review, none of it blocking; its items are taken as filler.* |

## 3. Retracted — believed, then killed by measurement

- **"The PCSX2 golden match is the same frozen state"** (`HANDOFF.md` "Open items" item 0). False. That golden was two
  stills of a match with **no input ever sent**. Weeks of server- and protocol-directed reasoning
  were aimed at the wrong body of code. *Retracted in the tree 2026-09-13 (Task 9a): `HANDOFF.md`
  item 0 and `STATUS.md` 2026-09-10 20:10.*
- **"The grey water shards in Seeding Chaos are caused by GL depth quantisation"** (research/26 condition sentence; KNOWN §2 2026-09-14). The **arithmetic is still true** — `gs_gl_backend.cpp:2630` `z/2^32` with the shader's `aPos.z*2-1` in float32 does quantise GS z to multiples of 128, and that is worth fixing on its own — but it is **not** this defect's mechanism: a mission run with `PS2X_GS_NO_ZTEST=1` shows the *identical* shards, and a disabled depth test cannot cause a depth-test artefact. The theory had "Against: none found" written against it, which is what a single disconfirming run is for. *Retracted 2026-09-14, same hour as the run; research/26 §4 candidate 1 and this list.*
- **"The actor rests 14.7 above the ground vs the console's 20.1"** (`HANDOFF.md` "Open items" item 2; `STATUS.md` 2026-09-09 01:30).
  Both numbers are camera-eye minus collision-hit. The player's feet are right to 0.008.
  *Retracted in the tree 2026-09-13 (Task 9a): `HANDOFF.md` item 2 and `STATUS.md` 2026-09-09 01:30,
  which prints the two camera y values it was differencing.*
- **The player actor at `*0x488de8+0xbc`.** Wrong; it is `*0x408c58`, verified across
  six images including the console's. Retracted in place 2026-09-13 (Task 9a): the correct chain is
  now in `HANDOFF.md` item 0 and the misreading is marked at `STATUS.md`'s 2026-09-09 01:30 entry.
  **Attribution correction:** this was never written in `HANDOFF.md`. `0x488de8` is the *camera*
  singleton; `STATUS.md` 2026-09-09 01:30 said `+0xbc` is "the camera's follow pointer … null in
  the spawn images", and later work read a warning as a route. HANDOFF's own peeks use
  `*0x488de8+0x320`, the camera position, which is correct and stays.
- **`actor+0x204`/`+0x208` are the player's health and max health.** Wrong — health is `+0x1044`
  with an alive byte at `+0xF7A` (`research/19`). The candidates were never confirmed (zero kills to
  confirm across), which is exactly why they were kept disarmed; the one row that leaned on them
  ("no damage was dealt") is withdrawn.
- **"Frozen at STARTING ROUND 1 OF 11, waiting for a go"** (spec §1 and several STATUS entries).
  The banner is transient on ours too and the round timer runs. The true sentence is *the round
  runs and the local player cannot move* — and that is fixed (`abf35bb`, `5ed29ca`).
  *Retracted in the tree 2026-09-13 (Task 9a): `HANDOFF.md` item 0, `STATUS.md` 2026-09-10 20:10,
  and the Sprint 4 spec's own §1 bullet.* The entry that recorded the symptom also recorded "the HUD
  timer runs regardless" and that RY worked while RX/LX/LY did not — a transport failure cannot
  deliver pitch and withhold yaw. Both refutations sat in the same paragraph as the claim.
- **The `cVar7 == 0` axis-clearing arm is the movement gate** (`4114ad4`'s commit message). Rejected:
  the routine is a generic axis setter, its guard cannot fire because our own IOP module hardcodes
  link-up, and the arm returns before all four axes are written — so pitch would have died too.
  Three calls matching three dead axes was a coincidence.
- **The mover's `4.0` vs `6.3338` is a capsule radius.** It was the 15-bit `rand`.
- **`PS2X_GS_SCALE=2` will sharpen the HUD** (Sprint 3 spec DoD). It cannot: HUD, menus and title
  are textured quads at native texel density. The design doc said so before anyone measured it.
- ~~**The owner's Bluetooth endpoint is dropping audio the mixer rendered correctly, again**~~ **SETTLED 2026-09-23, and the answer is NO: the endpoint is exonerated.** The A/B this row names as its own settling condition ran at 02:43Z and the dips SURVIVED the wired device (wired 14 against Bluetooth 11) -- they are ours, promoted to §1 as the row "The mission music's DEVICE dips are OURS, not the owner's Bluetooth endpoint". Everything measured below stands; only the attribution to Bluetooth is withdrawn. The premise as it was written, 2026-09-22 (`logs/parity/mission_music_ours_20260922_024457`). A ten-minute capture taking the mixer's dump and a WASAPI loopback together, whose own mix-open line reads `device Speakers (JBL Flip 6), period 20 ms x 4, engine 48000 Hz` with the app session held at 1.00: `audio_dips` classifies **11 events DEVICE** -- in the endpoint recording, absent from the dump at the aligned time -- every one 50 ms and 10-20 dB deep, ten of them in the ~4 minutes of the capture that carry the briefing score (per minute 1, 3, 2, 4, then zeros; `dips_rescored.txt`). The first reading was **31**, and 20 of those were the scorer's: a start-time match that let a bad local offset separate a 5 s hole from its own dump twin, and a greedy first-unused match that turned a cue's ending into a 365 s "DEVICE" event. Matching is by overlap now (2026-09-22 midday, `test_audio_dips.Matching`). This is the same class, on the same speaker, as the Proven row "The mission music through the owner's speaker drops out ~41 times a minute" (42 dropouts a mission minute, cut to 2 by the 20 ms x 4 mix device), and it is the shape of what the owner reported on 2026-09-22: "a short and ramping deviation from the note the song was currently playing ... it may be the game catching up". Every pre-device measurement of that fault is blind to it by construction. **Settles it:** the same capture on a WIRED endpoint. If the DEVICE events vanish, the Bluetooth path is the cause and the question becomes what regressed since the 20 ms x 4 fix; if they survive, they are ours. Also worth pinning a per-minute DEVICE count into the audio gate, so this cannot drift back silently a third time. **IT RAN 2026-09-23 (`logs/parity/endpoint_ab_20260922_232644`) and took the second branch: they survive.** The live question is section 2's row "Something between `render()` and the audio dump's write loses about 50 ms" (issue #42); the audio-gate pin is still worth doing and is a Sprint 11 audio item *(Moved from section 2 to section 3 on 2026-09-25, Sprint 13 R5, the full read: killed by the endpoint A/B, `logs/parity/endpoint_ab_20260922_232644` (wired 14 against Bluetooth 11).)*
- **The stray sound on the online screens is bank `0x00a00000`'s one-shots, not the music stream** (the owner, 2026-09-22: "a random sound is popping in that doesn't seem to belong ... a short and ramping deviation from the note the song was currently playing ... nothing should persist"; ruling R239). For it: the stream is exonerated on its own evidence -- one `989snd stream start` per screen, no re-seek, and all 2,560 `snd_SoundIsStillPlaying` polls answered "still playing", so the song never restarts under itself; and the one-shots correlate exactly with where the owner hears it -- bucketed over `run_20260922_005048.log`, **87 plays during boot and the intro movies, ZERO on the main menu (which the owner calls clean), 100+ steady across the online screens (which blop)**. The bank is 24 sounds in 60,928 bytes of ADPCM (about a tenth of a second each, so nothing persists); sounds 8 and 0xc fire in a fixed repeating cycle at three fixed volumes with pitchMod and pitchBend zero. Against it: nothing yet -- and a log cannot say whether the game is wrong to ask or we are wrong to render (wrong grain, wrong level, wrong pitch) *The experiment it named, as written:* **SETTLED 2026-09-22, and the answer is NO.** Three runs (`logs/parity/blop_a`, `_b`, `_c`) with `PS2X_AUDIO_DUMP` capturing and the play commands carrying the mixer's output-frame clock. Run C reproduced the owner's condition exactly -- the briefing-room menu navigated while the song played, music at -28 to -34 dBFS with 4-5 one-shots per ten seconds through it -- and there the 41 plays land at **-30.5 dBFS against a bed of -30.3 dBFS**: -0.2 dB, median step +0.64 dB, 9 of 41 above 3 dB. Inaudible against the song. The bank is cleared; the correlation that charged it was drawn by bucketing the owner's log BY LINE NUMBER, which is not time. See section 2's DEVICE row ("Something between `render()` and the audio dump's write loses about 50 ms") for where it went *No issue: settled 2026-09-22, the bank is cleared; the open half is the DEVICE row (issue #42).* *(Moved from section 2 to section 3 on 2026-09-25, Sprint 13 R5, the full read: killed by `logs/parity/blop_a`, `_b` and `_c`: the one-shots land -0.2 dB against the bed.)*
- ~~**The garbled glyph atlas in the HELP popup is R123's revalidate-by-hash**~~ **DROPPED 2026-09-23 by the owner: "could not repro, manually or via automated tests"** *(issue #29 (closed))* -- kept as written: (the owner, 2026-09-22, before Mallard at the church; Q6's latched-stall path is cleared, so the texture path is what is left). **NOT REPRODUCED 2026-09-23, and the A/B that was meant to settle it did not:** two walking captures (`logs/parity/mission_music_ours_20260922_224425` against `logs/parity/w6_norevalidate`), each **a twelve-minute walk inside a 21-minute capture**, produced **ten identical, clean popups in both walks**, the owner's "Headquarters has provided you with some HELP" among them, every glyph legible. Two reasons that is not an acquittal: the owner's sighting was **after the church load**, which an in-place walking leg never reaches; and the mission capture **records no environment dump**, so `PS2X_GS_NO_TEX_REVALIDATE=1`'s presence in run B is unproven from the artefact -- the "revalidate off" half of the experiment is not evidenced at all. The suspect is neither confirmed nor cleared *The experiment it named, as written:* **Settles it:** the same A/B over a route that actually reaches the church (the owner's route in stick directions, or one driven far enough to load it -- `docs/HUMAN_TASKS.md` asks for it), with the capture writing its `PS2X_*` environment into the run directory the way the gate's pins do, so each half can be proven to have run with what it claims **CLOSED 2026-09-23 on GitHub by the owner's account (issue #29 (closed)): "could not repro, manually or via automated tests". The row stands as the record; the church-route A/B above is no longer owed unless the owner sees the atlas again, which reopens the issue.** *(Moved from section 2 to section 3 on 2026-09-25, Sprint 13 R5, the full read: withdrawn by the owner's close of issue #29 (closed), not by a measurement -- the one entry here of that kind; the suspect was never confirmed.)*
- ~~**The title stage quietly lost four captures and the gate kept passing.**~~ **SETTLED 2026-09-25 (Sprint 13 V1, research/64; issue #30 (closed) with the bar amended):** `s19`..`s22` are not menu screens — they are the idle attract (the menu fading to black, then the flyovers), 114.4–114.7 s after `s00` in every archived gate since 2026-09-10; nothing was lost. The real defect was the scorer counting them: `s7_cpu_fallback2`, frozen on the menu from `s16`, passed. `score_title` now holds every `s00`..`s18` capture to 90 and names the ones that fail (`79aad10c`, `f3dd7f37`; gate `s13_v1_gate` 3/3, `window s00..s18: 19/19`). Open gap: a freeze that begins inside `s19`..`s22`. *The row as written:* `s8_audio_mc_gate2` scored 23/23 menu captures >= 90; `s8_close_gate` 20/23; every gate since `s9_g1_gate` (g2_release, p1, p7_playtest) 19/23, and it is the same four every time -- `s19`..`s22`, the last four menu screens, at 60-88. Whatever changed between those two gates (the Sprint 8 close-out, `09d9ec0`, or the pristine-card boot) moved the last menu screens' rendering or their timing, and the bar (`>= N of 23`) absorbed it without a word. Raised by the session writing Sprint 11 Goal 6 from the gate summaries, 2026-09-20; verified here *The experiment it named, as written:* The experiment: open `s19`..`s22` from `s8_audio_mc_gate2` and from `s9_p7_playtest_gate` side by side against `scripts/parity/refs/` and say what differs (a screen, a timing, a card state); then either fix it or re-reference deliberately, with the change recorded. This is exactly the drift Q1b's refuse-to-score is meant to make loud *(Moved from section 2 to section 3 on 2026-09-25, Sprint 13 R5, the full read: killed by research/64: `s19`..`s22` are the idle attract; the scorer's fix is `79aad10c`, `f3dd7f37`, gate `s13_v1_gate`.)*
- ~~**W10: on a virgin card the persona survives a restart and the saved password does not**~~ *(issue #27 (closed))* **SETTLED 2026-09-25 (Sprint 13 V6): the password survived. The driver misread the screen.** W10's relaunch (`logs/parity/w10_virgin_b/02_persona.png`) reached the form with the persona, `*****`, SAVE PASSWORD ticked YES and the cursor on CONNECT. The `--saved-password` step pressed CROSS first, which connected, then read PASSWORD off the CONNECTING screen (`05_password.png`): `login:saved-password:empty`. The card had kept the password through the kill. Fixed in the driver (`6ff8229f`: the form is read as it arrives, a non-form frame fails as `:no-form`, and `--clean-exit` was added; `tools_py/tests/test_saved_password.py`). Proven by the pair `logs/parity/v6_20260925_160634` on the hosted server. Launch a created the persona on a virgin card with SAVE PASSWORD = YES: `LOBBY class=ok`, `CLEAN-EXIT rc=0`. Launch b, on the same card with nothing typed: `PASSWORD reads 5 glyphs, SAVE PASSWORD reads yes, focus connect`, `LOBBY class=ok`, `CLEAN-EXIT rc=0`. The card trace's only failures are `-4 NoEntry` checks for things that are absent (the empty slot 2, the save folder before it was made, deletes before writes); every Open, Write, Close and Read succeeded. Launch c relaunched W10's own card against the hosted box and got `login:saved-password:no-persona`. That is the per-server persona rule (section 1), not a loss: W10 created its persona against loopback (`retail hostnames -> 127.0.0.1`, `logs/run_20260922_223849.log`), c ran against `the hosted server's address`, c's form is the fresh-server "Choose a different persona or create a new one.", and the card read without an error. **A name-content blind spot, recorded:** W10's persona is `w2-test` on the card, not the `w10test` asked for: the keyboard walk typed "10" as "2-", and `create_persona` checks only the name's glyph count, never its text. The row as written, 2026-09-23: launch 1 booted from an empty card directory, created the persona with SAVE PASSWORD answered LEFT, reached `LOBBY class=ok` and left `BASCUS-97275SOCOMII/SaveGame0-6` (written 22:41:52); launch 2 failed `LOBBY-FAIL login:saved-password:empty`; the two candidates were an exit-time write and the memory-card HLE. **Settled by:** the pair `v6_20260925_160634` (a and b, both clean exits) and W10's own `w10_virgin_b/02_persona.png`. **Still open (optional):** relaunching W10's card against loopback would show its persona there. That would tell whether the key is the resolved address or MUIS's `DNS` string (section 1's per-server row); this pair cannot, because both differ between loopback and the hosted box. Artefacts: `logs/parity/w10_virgin_a` (rc=0), `w10_virgin_b` (rc=4), `v6_20260925_160634/{a,b,c}` *(Moved from section 2 at the Sprint 13 close review, 2026-09-25.)*
- ~~**Our first mission is missing a scripted HUD cue the console shows: a blue arrow that flies over to mark the first enemies**~~ **CLOSED 2026-09-20 by the owner: "it was there"** -- the arrow shows on ours; it is tied to approaching the first enemies, which no standing-still script reached. Nothing to fix. *The experiment it named, as written:* the owner, at the desk, on the child-sound build *(Moved from section 2 to section 3 on 2026-09-25, Sprint 13 R5, the full read: killed by the owner's own look: the arrow shows on ours.)*
- ~~**The Seeding Chaos grey shards come from the `0x34` env-map pass' geometry or its texture/CLUT** (TBP0 0x38a8 PSMT8, CBP 0x3852 CT16) — what is left after the depth theory died. The footprint is right and the console shows continuous brown water at the same camera, so something inside the correct silhouette is drawing wrong~~ **SUPERSEDED 2026-09-16/17:** the shards were VU1 chunk truncation, fixed by the VIF wait plus the brighten/exposure HLEs (research/31 §15–17, commits `3d37abc`, `545b85a`, `c63729d`); what remains is the VU0 macro-mode flag latency (research/31 §17), which is a recompiler item, Sprint 8. *The experiment it named, as written:* Done (see research/31 §15–17). *(Moved from section 2 to section 3 on 2026-09-25, Sprint 13 R5, the full read: killed by research/31 §15-17: the shards were VU1 chunk truncation; the VU0 flag-latency remainder is section 4's issue #47 row.)*
- ~~**Frostfire's lost control may be UNINITIALISED MEMORY, not a movement defect.**~~ **CAUSAL HALF RETRACTED 2026-09-13 (launch 1, `logs/run_[AB]_20260913_073548.log` -- now `D:/socom_archive/run_[AB]_20260913_073548.log`, review re-derived):** `ng+0xd2` went 0xAF→0 around `FUN_001f6660` `#0` (A ln11143/11160, B ln10856/10867) 1.7 s **before** `FUN_002b7d60` read it; `FUN_00543d50` had 0 calls (all callers `jal`, hooks live); `+0x174` never 8. The chain is reachable and did **not** fire; the 0xAF-vs-0 byte difference stands but is not the cause. Kept as written: Bytes the game never initialises in the round-state object read `0xAF` on ours and `0x00` on the console — among them `+0xd2`, the flag behind "You are a ghost. You will play the next round as a real player", tested by five online routines. The object is allocated per map from our replacement heap, so the garbage can differ by map, which would explain Medley working and Frostfire not. The difference is verified; the causation is inference, and new-round code does clear the flag. **Reproduced by tool** (Sprint 5 Task 4 leg 0, `ad32088`, review re-read the bytes): `object_diff.py --static 0x437ce8:0x14c` over `title_ours`/`spawn_ours3` vs `title_pcsx2`/`spawn_pcsx2` finds the block at `0x869360`/`0xc496b0` and flags `+0xd2..+0xd4`. research/19 F3's range list is **wrong in three places**: `+0x6C..+0x7B` is real differing data (only `+0x7C..+0x83` is fill); `+0x100..+0x103` is zero on both sides; `+0x38`, `+0x3A..+0x3C`, `+0x3E..+0x3F`, `+0x14B` are flagged and unlisted. The flag rule needs one constant across *all* of ours' images, so its counts are a lower bound. **Mechanism now verified in the decomp and ELF bytes** (Sprint 5 Task 1 Step 1 review, 2026-09-13): once per player creation `FUN_001f5e70` → `FUN_002b7a90` → `FUN_002b7d60` reads `ng+0xd2`; when not JoinAsSpectator and `+0xd2 != 0` it calls `FUN_00543d50(local actor)`, which zeroes `+0x1044` and calls actor `vtbl[0x90](8)` = `FUN_0058a6b0` → **`actor+0x174 = 8`**, so the `FUN_00551ec0` guard dispatches `FUN_00592560` instead of `FUN_00594cf0` and MoveScale stops. The chain is verified; that it **fires on Frostfire** (and why not on Medley) is still inference. `CZNetGame` is allocated `FUN_002ad290` → `FUN_00180e10(0x14c)` → `FUN_00181b40` → **bound `malloc@0x00194C30`** → `guestMalloc` (no pool carving), so `PS2X_GUEST_MALLOC_ZERO` (`03d3aa6`, default off) does reach `+0xd2` *The experiment it named, as written:* Launch 1: peek `*0x408c58+0x174:1`, trace `0x543d50:SpawnDead` (its `ra` `0x2b7ea0`/`0x2b7eb0` = the ghost branch) and `*0x437ce8:64`; SpawnDead from the ghost branch + `+0x174 == 8` + `ng+0xd2 != 0` → the zero-fill A/B *(Moved from section 2 to section 3 on 2026-09-25, Sprint 13 R5, the full read: its causal half killed by launch 1 (`logs/run_[AB]_20260913_073548.log`, archived at `D:/socom_archive/`); the byte difference and the decomp chain stand.)*
- ~~**The mixed match (ours hosting, PCSX2 joining) can be driven end to end from the PCSX2 controller's macros**~~ **SUPERSEDED 2026-09-20 (Sprint 10 Goal 3):** the macros are blind timings and lost their place at boot; the console side now runs ours' screen-verified flow (`tools_py.parity.pcsx2_shell`) with console references where its narrower screen needs them, and both legs have reached a running round with both players moving -- section 1. *The experiment it named, as written:* the Goal 3 plan; `scripts/parity/mixed_match2.sh`, `mixed_match2_leg2.sh` *(Moved from section 2 to section 3 on 2026-09-25, Sprint 13 R5, the full read: killed by Sprint 10 Goal 3's runs: the macros are blind timings.)*
- **~~On Linux, the driven title stage freezes with PS2X_AUDIO_DUMP set~~ -- falsified the same evening**: the two black runs had booted with NO disc (`[socom2] WARNING: no .iso found`; the drive's `game/` symlink to the ISO was created between them), and with the disc present three of four dumped runs pass the title stage (`s8_frz_A` 19/23 with the dump, `s8_frz_C` 22/23 with it on tmpfs -- both exist only in the VM's own record, not on this machine); the dump's write costs 0.1 ms against a 21 ms callback. What the VM does show is the software rasteriser (llvmpipe on VMSVGA) at 0.8-2.9 fps starving the guest through GL back-pressure (bp_wait 204 s of 256, the render-stall shape), which makes the driven stage marginal there, not the port; the same starvation fills the music ring late nearly every block (`pcm_underruns=22998` over the passing run's 250 s), so the R97 policy silences the dump: the Linux audio bar waits for a real GPU (R107) *The experiment it named, as written:* The A/B table in the 2026-09-18 17:30 investigation; `s8_frz_D` (worker off) over-pressed into MISSION BRIEFING, the same flakiness as `s8_vm_title3`. Follow-ups: the gate resolves the disc through PS2X_CD_IMAGE instead of a directory scan; the untilref press budget is marginal at both ends against a 119 s boot *(Moved from section 2 to section 3 on 2026-09-25, Sprint 13 R5, the full read: killed the same evening: the two black runs had booted with no disc.)*

> Cite **item names and dated entries, never `file:NN`** — line numbers rot the moment anyone
> edits above them, and a dangling citation is how the actor-pointer mis-attribution happened.
>
> Retract **on discovery**, not at close-out. All of §3's first three were still false in the tree
> hours after being disproved — and in the end they were retracted **at close-out anyway**
> (2026-09-13, Task 9a), which is the outcome this note exists to prevent. The elapsed time between
> "a reviewer proved this sentence false" and "the sentence stopped being read by fresh sessions"
> was about a day for the first three and two weeks for the freeze description. The rule is in
> `docs/audits/2026-09-12-process-audit.md`: a review finding that a committed sentence is false produces a same-hour
> edit, and close-out *verifies* retractions rather than performing them.

## 4. Standing hazards — moved to `docs/HAZARDS.md`

On 2026-09-26 (Sprint 14 Task I5, ruling R270) this section's 104 standing hazards moved verbatim to
`docs/HAZARDS.md`, each under a heading naming the area it bites (renderer, audio, harness, lock, git, build, network,
documents, recompiler), so that this file is the claims — proven, believed, retracted — a session reads before a
hypothesis, without a hundred traps in the way. A new hazard goes there, not here; a citation of "section 4" or
"KNOWN §4" written before that day means a bullet there, under the same headline, and the section as it stood is
`docs/archive/KNOWN-section-4-to-2026-09-26.md`. KNOWN still wins on any disagreement: a hazard a §1 row contradicts
is retired there, in place.
