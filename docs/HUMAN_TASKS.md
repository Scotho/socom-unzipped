# Human tasks — the owner's sitting

last sitting: 2026-09-17 (the date `python -m tools_py.sitting` lists rulings since; docs/SITTING.md)

What only the owner can decide or do, in one table. Every row stands on a default: the loop proceeds on it and never
waits. Rewritten 2026-09-25 (Sprint 13 Task R4) from the project audit's owner sitting
(`docs/audits/2026-09-25-project-audit.md` §3, rows O1–O13), with two rows that audit missed (O14, O15).

**The rule.** The loop adds a row only when it reaches a step it cannot verify or a decision that is not its to make;
it writes the default it proceeds on beside it and moves on. Anything the loop can do itself (a window on this machine,
a capture, a VM run, a draft, a document) is a sprint task or a backlog row, never a row here.

**Numbers.** `O<n>` is a row of this table. A Sprint 13 task in milestone O is written "S13 O1". The old decision
numbers are named by their sprint — "Sprint 11 D2", "naming D1–D7" (Sprint 12), "r0004 D1" — because the list this
replaced used bare D-numbers for three different tables (documents audit D61).

**Before 2026-09-25.** The list this replaced — its "Start here" blocks, the "Sprint 11 close" and "Sprint 12 close"
sections, the numbered items and the `## Open` checkboxes — is `docs/archive/HUMAN_TASKS-to-2026-09-25.md`, verbatim,
under a table that gives each of its 87 items a disposition. Any citation of this file's sections, items or line
numbers written before that day means the archive.

| O | the decision or the hand | the default the loop is on | settles | first asked |
|---|---|---|---|---|
| O1 | **The legal position on shipping `socom2.exe` and the decrypted `socom2_game.elf`** (Sprint 11 D2) — to strangers, to a friend, in a release archive. It blocks every public download. | no public download; builds reach testers only by your hand | audit F3; carry C5; PLAYTEST's "who else gets this archive" | 2026-09-20 |
| O2 | **The release archives for `v0.10.0`, `v0.11.0` and `v0.12.0`**: `gh release upload` onto the three drafts, the release-draft workflow run by hand (its verify half's first real trial), the publish click; and a safe home for each release's `dist-release/symbols/`. | the drafts stay empty; the loop may build the archives short of the upload (a backlog row); publishing is always your click | carry C2, C3 | 2026-09-20 |
| O3 | **What the public tree holds and under which terms**: the disc-derived bytes (H7-A: the audio and VU1 fixtures; H7-C: ~240 pictures, and the story's frames and video); the licence split (Sprint 11 D3); what moves to `docs/dev/` (D4); signing (D5); the landing page's deploy and its community-server wording (D6). | nothing moves; GPL-3.0 for the whole tree; unsigned (the FAQ says what SmartScreen shows); the deploy is yours, its wording drafted in `docs/INSTALL.md` | carry C6–C10; stranger S48; `docs/audits/2026-09-21-disc-derived-bytes.md`; two 2026-09-25 notes for the same ruling — S13 H7 tracks one full-size (640x451) MISSION FAILURE results frame as a gate fixture, the most art-heavy picture in the set (the detector misses the banner at 320x224), and S13 C6's FFmpeg 7.1.5 (LGPL-3.0-or-later, its encoders built in) grows the Windows download by about 20% | 2026-09-20 |
| O4 | **The bug pipeline's words and the site**: I1, whether the launcher's post-SEND "open an issue" line says "unless it is a security report"; I2, drop "only if you want an answer" or change the reply policy (G7); the four relays to the site session — yours only because the site is another repository, outside this loop's scope: say whether its session takes them (drop its keyboard/mouse claim; show the launcher's GitHub sentence on its form; mark the deliberate prompt-injection canary in the inbox; move the AWS account id out of `secret-scan.mjs` into its ignored literals file), and the edits themselves are that session's; sending the drafted message to the PSRewired moderator (the draft: the archive, "Send this to the PSRewired moderator"); what the server keeps about players (a data page). | no reply is sent (G7: no); the triage routine never opens a public issue for a security report; the relays and the message unsent; no data page | carry C15, C16; stranger S16–S22 | 2026-09-20 |
| O5 | **PSRewired and the mixed revisions**: r0004 D1 as reopened by R251 — is a per-player pipeline (PCSX2, their login, our build on the player's machine, ~40 min) acceptable for v1; Goal F — their answer on a PC-native client, and whether their players carry `mc0:UPDATE.DAT`; two rooms or two servers, because r0001 and r0004 clients bounce silently from each other's games; the community preset's address; scheduling Horizon's unmodelled `MediusVersionServer` and three other messages; and the two r0004 halves only you can open — an r0004 disc tree of your own (the build has only run on the r0001 disc's assets) and, with Goal F's answer, whether the capsule's r0004-layout stubs apply to our image at all. | the community preset stays `COMMUNITY_SERVER_ADDRESS_TBC`; nothing connects to a server that is not ours; Task 11b stays withdrawn; the HDD maps are out of v1 (r0004 D4) | carry C17, C21–C24, C26 | 2026-09-17 |
| O6 | **The machine's windows and a second machine**: the scheduled ladder's window (the Task Scheduler entry `SOCOM Unzipped ladder` exists, DISABLED); the first two-machine match over the internet (PLAYTEST step 12; `scripts/parity/two_machine_readout.sh` reads the two logs). Not yours: the VM run that turns issue #25 green is the loop's, a night run under S13-R2 (a backlog row). | the scheduled entry stays DISABLED and the ladder runs by hand at night under the lock (S13 O1); every online result is two instances on one host | carry C87, C89 | 2026-09-17 |
| O7 | **Your ears and hands on the current build**: one listen to the music — mission, briefing, options — against the console (PLAYTEST step 6; it replaces the six listens owed since 2026-09-17, each on a build later fixes replaced); Q4's four tries (the Xbox guide switch, the PS button on Sony's driver, the game window's title and caption, the menu sounds at 0.45 with SLIDE as the focus cue); the CONTROLLER page's hold-to-remap on a real pad; the prefilled login with your real persona; whether a profile viewer is wanted. | the loop does not wait (the listen gate has been bypassed since 2026-09-20); no profile viewer | carry C60, C119–C122 | 2026-09-17 |
| O8 | **A PLAYTEST sitting on the current build** (`docs/PLAYTEST.md`). It carries the hands-on checks no row above names: the download's hash, the Explorer double-click with no console left behind (R132), the disc-not-found sentence and SAVE DIAGNOSTICS' zip, the launcher by pad, crouch, the save on a virgin card, free play, a hosted round, the mic meter, REPORT A BUG's wording — and one decision: drop the debugger and the probes from the download, once the size number exists. | the loop rewrites PLAYTEST for each build it can hand over; the report's log box stays OFF; the download keeps its debugger | carry C126; audit F10 | 2026-09-20 |
| O9 | ~~**The story's five missing days** (three of Sprint 11, two of Sprint 12), carried twice: keep them or drop them under a ruling.~~ **Answered by default (keep), 2026-09-25: S13 S4 wrote them (`d4a8f0ea`, merged `1dcaa57b`); the `../scotho` site copy is the controller's.** | ~~keep; S13 S4 writes them from the record~~ | carry C128; audit B12 | 2026-09-25 |
| O10 | **Public actions upstream**: file the three #244 patches, note our verdicts on the ten KEEP picks, report BinExport's two R5900 defects. | drafts ready in `docs/research/assets/63-upstream-drafts/` (S13 U4; its README orders them): the argv and printf issues with patches, the export-table one advised against, ten KEEP comments, two BinExport issues; nothing is filed | audit E5; external X9, X10, X20 | 2026-09-25 |
| O11 | **The naming programme's owner rows**: confirm or overturn the naming defaults D1–D7 (the Sprint 12 plan's "Owner decisions" table); a hand read of five engine routines — `CMission::Init` 0x002ad290, `CSealCtrl::ThrottlesPreTick` 0x005966a0, `CZSealBody::GetNodePos` 0x005df930, `CActionTxtrMachine::Open` 0x0021f850, `CNodeAction::Open` 0x002b4f40 — entered as a `Pass=hand` proposals file through `tools_py/apply_names.py`; whether to hold the 148 equal-count slot namings; whether to look for a named Aug 28 or Nov 25 2003 build; the two tail-truncated demo images. To know, not to decide: the SOCOM II browser client of 2026-09-21 (research/63). | the defaults stand; no hand names; the 148 stay applied; nothing is looked for | carry C32, C33, C48, C50, C51; audit E8, H5, H6 | 2026-09-25 |
| O12 | **The crouch default**: the launcher writes `l3` (you chose it 2026-09-20) while the knob's registered default is `off`. | the launcher's `l3` | audit A10 (F54) | 2026-09-25 |
| O13 | **Repository settings and history**: 0 approvals on `main` against CODEOWNERS; Dependabot security updates, and the three open Dependabot PRs (#8, #9, #10, each MERGEABLE on 2026-09-25; #7 you merged 2026-09-25 10:26Z) — merging a PR that touches a workflow file needs a token with the `workflow` scope, which the loop's `gh` lacks, so they are your click; homepage, topics, Discussions; SECURITY's one-week promise with no mechanism behind it; the money when the free-plan credit ends (around March 2027; the plan expires 2027-03-05) — the reminder is the loop's (a backlog row); the off-box backup pull is scripted (`server/ops/backup-pull.ps1`, S13 O3) and runs from your machine once `server/ops/ops.env` is filled in; the commit author e-mail; `server/config/simulated.db` and the chat path's mechanics (`f8cdbb4`, `0e3eeaf`) in public history. | as they are; no history rewrite | carry C11–C14, C20; stranger S39–S41, S46 | 2026-09-20 |
| O14 | **The merged-branch sweep**: lift the `sprint-*` ruleset (R182) with `gh api` under your credentials, let the loop delete the merged `sprint-*`, `fix/*` and `agent/*` branches on the written list (the Sprint 11 plan, Task 18 Step 3), then restore the ruleset. | the branches and the ruleset stay | carry C4 | 2026-09-20 |
| O15 | **Linux on real hardware** (a Linux PC or a Steam Deck): the tarball's launcher opens, the game boots with music and the pad, the first three `[gs-gl]` lines of the run log, and R107's title-loop correlation, which the VM's software renderer cannot give. | CI and the VM stand in; R107's number stays unmeasured; the VM half (the no-audio-device sentence in LAST RUN, the Linux bug-report send) is the loop's backlog | carry C123 | 2026-09-18 |
| O16 | **Three issues carried twice** (S13-R14, 2026-09-26): #25 the Linux VM suites (no sprint has owned it since Sprint 11), #26 the chat bound's path (Sprint 13 O2 restated the bar: find the receiver a game-lobby line uses, bound it, prove it with `control_round_chat.sh`), #42 the mission music (V5 stopped at Step 1; steps 2-5 need an unmuted client). Keep each on the backlog, or close it as not planned under a ruling. | keep all three on the backlog; the next plan that names one takes it | S13-R14 | 2026-09-26 |
| O17 | ~~**Merge PR #61 (Sprint 13 -> main)**: the ruleset's extra-approval rule and the token's missing `workflow` scope~~ **Done 2026-09-26: you granted the scope; merged `6a82caaa`, tagged `v0.13.0`. The full row is in the archive.** | ~~the PR stays open~~ | Sprint 13 Task 99 | 2026-09-26 |
| O18 | **The private-inputs location** (the cloud sessions' ELF source) has no consumer since 2026-09-26: retire it, or rotate its credential. | nothing changes; the fetch script stays; the loop touches nothing (R277) | Sprint 14 D9 | 2026-09-26 |

**How to answer.** One line per decision — "O5: acceptable for v1", "O12: off" — in the next session's prompt or as a
note in `docs/STATUS.md`. The loop strikes the row with the date and the ruling that records the answer, and moves
the work it opens into a task.
