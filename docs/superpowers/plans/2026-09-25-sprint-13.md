# Sprint 13 Implementation Plan — "nothing carried twice"

> Spec: `docs/superpowers/specs/2026-09-25-sprint-13-nothing-carried-twice-design.md`. The master list it came from:
> `docs/audits/2026-09-25-project-audit.md` (its six reports beside it). Opened 2026-09-25 night by the local
> controller (session socom-pc-6c) on the owner's instruction; branch `sprint-13` off `main` once Sprint 12's PR
> merges (until then the opening commits ride on a topic branch and rebase onto it). The loop runs this plan in
> `docs/LOOP_PROMPT.md`'s shape: one task at a time, a failing test first, review by a fresh agent, the lock for
> every build and run, the owner away.

## Global Constraints

- **Order:** milestones V, R, H, C, U, S, N, O (S13-R1) — visible defects first, then the record, then what costs
  every night. A task out of order needs a ruling that says what it jumped and why.
- **The lock:** one build or launch at a time (`scripts/loop_lock.sh run` / `run_detached.sh`); the owner is away on
  this sprint's nights (S13-R2), so lock-bound runs are allowed at night, never two at once, never a game window when
  the owner has said they are at the machine. At most two C++-building agents at once.
- **A failing test first**, unittest only; RED and GREEN pasted; the gate 3/3 with PINS MATCH on the rebuilt exe
  before a runtime change is called done; `./build.sh runtime` before any gate when the runtime changed.
- **Commit with an explicit pathspec**, subject `type(scope): what and why`, this session's trailer, the leak check
  in the hooks; never `--no-verify`; push `origin sprint-13`; check CI (a docs-only push starts no build run after
  H1 (only `secrets` and `docs` run); read a commit with `gh run list --commit`).
- **Every claim names its artefact:** an issue closes with the artefact in its closing comment; a document sentence
  about the tree cites a commit, a stamp or a test; a "merged as vX" names a tag that exists (R268).
- **The record's ceilings (R268)** hold from R1 onward: the sprint file, HANDOFF §2, STATUS's state block and
  HUMAN_TASKS stay under the sizes R1 sets; this file's Log is the sprint's live state, newest first.
- **Nothing that is the owner's** (§3 of the audit) is performed; a draft is prepared and an `O<n>` line updated.
- **Reviews:** every task by a fresh agent that did not write it, findings `file:line`, fix rounds to a clean
  re-review; the diffs under `.superpowers/sdd/2026-09-25-sprint-13/` (git-ignored).

## Owner decisions — the defaults this plan proceeds on (spec §3 and the audit's §3)

| # | decision | default | ruling |
|---|---|---|---|
| D1 | the sprint's order | V, R, H, C, U, S, N, O | S13-R1 |
| D2 | lock-bound runs on the owner's nights | run, one at a time, the owner away | S13-R2 |
| O1–O13 | the audit's owner sitting | each row's default as the audit states it | the audit §3 |

## The task table

Markers: **[A]** autonomous; **[L]** lock-bound; **[C]** cloud-able; **[O]** waits on the owner (never blocks).

| # | task | milestone | who | state |
|---|---|---|---|---|
| 0 | the open: the spec, this plan, the audit committed, the branch, the milestone, the sprint file's header, HANDOFF's state line | — | [A] | **DONE 2026-09-25 08:45Z**: `sprint-13` off `main` at `74fe2a9b`; milestone `Sprint 13` (#3) with the six carried issues (R266); the sprint file's header and OPEN block, HANDOFF's line, HUMAN_TASKS' pointer; the first agents V1, U1, U5, U6, U7, R1 dispatched |
| V1 | #30 the four low-scoring menu captures | V | [A] then [L] | **DONE (code) 2026-09-25 07:55Z** on `agent/s13-v1` (`79aad10c`): the issue's premise was wrong — s19..s22 are the idle attract (fade, flyovers), present in every gate since 2026-09-10; the real defect was a scorer that counted them, so `s7_cpu_fallback2` (frozen on the menu from s16) passed; `score_title` now holds s00..s18 each to 90 and prints `attract not reached by s22`; 7 tests; research/64 (165 stamps replayed, one verdict changes). **DONE 2026-09-25 08:52Z:** gate `s13_v1_gate` 3/3 (`window s00..s18: 19/19`, PINS MATCH) on the renamed exe; #30 retitled and closed with the bar stated as amended; KNOWN's two rows corrected; review one round, clean |
| V2 | #32 the menus' tile uploads | V | [L] | NOT STARTED |
| V3 | #31 streamed images read DBP | V | [L] | NOT STARTED |
| V4 | a tracked frame-time line in the gate; the mission-fps issue; README's cell dated | V | [L] | NOT STARTED |
| V5 | #42 and #28 the mission music: the quiet capture, the route capture, the count in the gate; research/36 items 5 and 7; the sound fixture | V | [L] | **Step 1 DONE 2026-09-25 08:23Z**: the first clean capture (`audio_out_20260925_074147`, sessions verdict `clean`): DEVICE 6 over 16 min (11-14 before), 36,756 callbacks with 0 late / 0 dry, all six dips cleared — not the device thread's; next: the recorder's silent-overflow drops and the scorer's alignment (Step 2's pin, then Step 3's route capture) |
| V6 | #27 the clean-exit relaunch | V | [L] | **FINDING 2026-09-25 (code on `agent/s13-v6`)**: the password was never lost; the driver misread it. W10's relaunch form (`w10_virgin_b/02_persona.png`) carried `*****`, YES and the cursor on CONNECT. The `--saved-password` CROSS connected, and PASSWORD was read off the CONNECTING screen. Fixed: the form is read as it arrives, with no CROSS first; `--clean-exit` added (RED/GREEN in `test_saved_password`). **Closes #27:** the pair re-run with the fixed driver (`logs/v6/v6_pair.sh`: a virgin card with `--save-password --clean-exit`, the same card with `--saved-password`, and W10's killed card relaunched), with the three verdict lines in the closing comment. The first queueing was stopped at 16:14Z by the host's memory reaper while it waited, before it got the lock. No game ran |
| V7 | #34 `waitReadable` bounded by the guest clock | V | [L] | NOT STARTED |
| V8 | `PS2X_SOCOM2_SERVER` refuses an unresolvable name; the launcher's wording defects with tests | V | [A] then [L] | **DONE 2026-09-25 14:50Z** (merged `5565f2ca`; the runtime half gated: `s13_v8_gate` 3/3 PINS MATCH on the relinked exe `d6deb35f…`; the layout walked at 1100x700 and 800x520) — on `agent/s13-v8` (`8510aeb4` the refusal, `74028b41` the wording, review round 1 on top): a name that does not resolve maps the seven retail names to nothing and writes the `server-unresolved` [notice] that LAST RUN appends (S13-R9: a notice, not exit 75); stranger rows 9-15 and F53 each held by a test (`test_launcher_wording.py`, `AboutAgreesWithTheNotices`, three C++ cases); row 15's premise did not hold (the sample is the shown, upper-cased form). The gate for the runtime half is the controller's combined proof |
| R1 | the archive split, HANDOFF §2 to one "now", STATUS's block, the overdue audits and closed plans moved; R268's ceilings and the tag check in `docmaint` | R | [A] | **DONE 2026-09-25 09:20Z** (`ec15ee20` the checks, `ae84b1c6` the split, `757164c2` the moves, `9207b34b` the fix round; review PASS WITH FINDINGS on four axes, clean after one round; merged): the sprint file 189,363 → 58,889 B (ceiling 72,000), HANDOFF §2 14,911 → 3,022 (3,800), STATUS's block 30,110 → 2,309 (2,900), HUMAN_TASKS untouched (104,000, R4 lowers it); ~229 citations re-pointed; 26 plans, 4 specs, 2 audits moved; §4/§9/§10 archived; the tag check and the CRLF-safe byte counts tested |
| R2 | DEVELOPING as current truth; README, GIT_STRATEGY, LOOP_PROMPT, PLAYTEST, ROADMAP §6, STORY, the archive README, SECURITY's hash — every D/S REVISION row | R | [A] | **DONE 2026-09-25 10:05Z** (`85941314`, `8336e3f2`, fix rounds `ad3f16be` `b40bf5a6` `e8bbc9eb`; review PASS WITH FINDINGS both axes, clean after two rounds; merged `bab6e5d0`): 30 audit rows applied, a `tools_py/` map of 172 modules with the 28 uninvoked marked by Appendix A's rule, ten claims no report listed fixed; S55/S56 (KNOWN, HUMAN_TASKS rows) left for R5/R4 |
| R3 | the ruling record: duplicates noted, vacancies recorded, research 43a/43b, 35; a duplicate-number check | R | [A] | **DONE 2026-09-25 10:30Z** (`117bf5f3` the checks, `ff8ea18e` the record, `c93504f4` the round; review PASS WITH FINDINGS, minors fixed; merged): two docmaint checks (a number defined twice; cited but undefined, with a vacancy-note mechanism built and unused); R107/R108/R109/R110's second issues cited as R<n>b (36 re-points); six rulings made in passing (R112–R116, R124, R139) given definition lines from their sentences or commit messages — the audit's "vacant" was not the truth; research 43a/43b named in 28 citations; 35 recorded. At the merge the archived HUMAN_TASKS read as a second definition of R246/R247 — the archive homes narrowed to the sprint file's and the plans' archives, with a test |
| R4 | HUMAN_TASKS reduced to the owner's sitting | R | [A] | **DONE 2026-09-25 10:35Z** (`4adbf2bc`, `1ed975a4`, round `8b698378`; review PASS WITH FINDINGS on three axes; merged): fifteen owner rows O1–O15 with the default the loop is on; the old file (702 lines) archived verbatim with all 87 items dispositioned (30 superseded, 51 folded, 3 taken by the loop, 3 kept); 62 citations re-pointed; the ceiling 104,000 → 12,230 |
| R5 | KNOWN read in full; §2 settled rows moved; §4 fixed states superseded; positional pointers replaced | R | [A] | **DONE 2026-09-25 11:15Z** (`7270367f` `1ef3f7de` `6f4e3225`, round `5fedf359`; review PASS WITH FINDINGS on four axes, every artefact checked held; merged `a7121138`): §1's twelve open tails settled or split out; §2's 50 rows → 18 to §1 with artefacts, 9 to §3, 7 newly ruled (the two believed render rows held to R265's retire-or-test bar), 3 corrected; §4's 12 fixed states superseded in place with struck headlines; positional pointers replaced; the stamp says the full read. #41 narrowed to the missing menu frame (its gameplay half was met by `99a059c` on 2026-09-21) |
| R6 | the codex task-allocation audit's six assignments dispositioned | R | [A] | **DONE 2026-09-25 11:15Z** on `agent/s13-r6` (`9639de5f`): the six-row table in the Log — 1 ALREADY DONE (Task 19's commits), 2 TAKEN as a new issue (the body under `logs/`, for the controller to open), 3 TAKEN by C4 (PMULTW named in its Step 1), 4 done in part / V5 / S13-R7, 5 U5+U6 and the `multiplayer-security` row, 6 H2 and done; the model allocation and its comparison declined by S13-R6 |
| R7 | `docs/BACKLOG.md` generated (R267); `issues.py backlog/carry/milestone/tally`; the skill's label list | R | [A] | **DONE 2026-09-25 10:50Z** (`9ebac7c1`..`cee65bed`, round `195e0de9` `43525bb2`; review PASS WITH FINDINGS, every finding fixed; merged `33fa5940`): `issues backlog/carry/milestone/tally/labels`; `docs/BACKLOG.md` generated from the open stack (18 issues) plus the 38 ruled-out rows; the skill's rule 6 points at `issues labels`; the audit's D12 row left the ruled-out list for issue #51 and KNOWN §2 cites it; the teleport count stays paid, no issue |
| H1 | a docs-only push reads `skipped`; **and the pull-request run's `changes` filter compares base..head, not the head commit's own diff** (PR #49 and PR #50 both showed `build: skipping` on a merge-commit head while their branches carried code — the required checks passed on skips, so a PR to `main` has never been built as a PR) | H | [A] | DONE 2026-09-25 11:33Z (`9c3f96c4`, fix round 1 `ci(h1 fix round 1)`): a docs-only push starts no `linux`/`windows` run (`on.push.paths-ignore`), because GitHub reads a run `skipped` only when every job skipped and `changes` always runs; the PR filter already compared base..head -- PR #49/#50's pull_request runs built (36107390817, 36110478507), the `build skipping` rows were the branch's docs-only push runs on the same SHA -- now event-keyed and three-dot. |
| H2 | the lock's ticket queue (#36), `--wait` in minutes (#35), `run_detached --wait`, `ladder_job.sh` (#37), the quiet marker at the common dir, the rollout procedure | H | [A] then [L] | NOT STARTED |
| H3 | #45 accept-pins after the run; #38 captures record their environment; the 51 references pinned; the speed-freeze re-measure (R242) | H | [A] | **DONE 2026-09-25 10:55Z** (`d66dc442` `ace45bc3` `21b497d6`, rounds `5e79543b` `3b5bbadd` `38bbbb77` `2891bb3b`; review PASS WITH FINDINGS both axes; merged `74de313a`): `--accept-pins` writes only after a run whose every stage PASSed (S13-R5), nothing before the lock, nothing on an abort; every launcher under `scripts/parity/` and the two Python launchers write `env_ps2x.txt` + `env_pins.json` through `capture_env`; 50 references pinned in `pins_refs.json` (a suite tripwire, not a gate refusal) and one dead image removed; R242's re-measure as a KNOWN §1 row with its script `tools_py/research/ladder/r242_speed_freeze.py`; #45 and #38 closed. The pushed merge tripped gitleaks on a digest keyed by an image name containing "api" — allowed by path with the reason |
| H4 | the 28 uninvoked modules dispositioned; #46; `patch_*.py` archived; the tracked logs removed; absolute paths | H | [A] | **DONE 2026-09-25 12:00Z** (`3494e151` `1666c831` `62252fbc`, round `a0730d7f`; review PASS WITH FINDINGS on four axes; merged): nine archived under `docs/archive/tools/` (the four patchers stop on their first line; the four bring-up probes; blue_marker), nineteen documented entry points, the two tracked logs deleted, the research paths relative; an inventory test (22 RED → 0; research/ excluded and the limits stated); #46's premise was wrong — `movie_blocks` has run in the suite since `bd27443e` — closed with the KNOWN row as a lesson |
| H5 | #41 the console-replay case: settled, re-dumped or deleted | H | [L] | NOT STARTED |
| H6 | per-revision literals out of the five instruments; `env.sh` to the hosted box; the grep test | H | [A] | **DONE 2026-09-25 10:10Z** (`344c491c`, `441411d0`, round `b80f1b15`; review PASS WITH FINDINGS both axes, re-review CLEAN; merged): six statics into the table with r0004 values every vote agreed on (cue_route, cue_manager_ptr, music_globals, music_off, music_tables, camera_ptr), three UNPLACED (vagstore_base, the motion-pack pair) with `music_state_poll`, `motion_pack_check` and `cam_poll` refusing r0004 — offsets included; `verdict_replay`/`verdict_core` read the table; the bare-literal test (32 RED → 0) with an allow list keyed by file and value; the console lanes require an IPv4 and refuse a name; the r0001 side byte-identical (the reviewer diffed the rendered specs). Left for the controller: the H6 commands in the report (data_via_twin --control --column MATCHES; the specs unchanged) — run at the next lock window with a gate |
| H7 | the fast lock-free subset; the dependency manifest; `build.sh` through `python_env.sh`; `fix_ghidra_csv` under test; log-dependent skips to fixtures | H | [A] | **DONE 17:49Z** — the suites ran on the merged tree (build.sh through python_env.sh, CI installing from requirements.txt) and were gated by the second proof (`s13_proof2_gate2` r0001 3/3 PINS MATCH on `9aa2356b…`; `s13_proof2_gate2_r0004` 3/3 PINS MATCH on `9a66a95d…`). Was: DONE (branch) 2026-09-25 14:18Z on `agent/s13-h7` (`92540d21` `012a9945` `96bf7138` `e60d19c8` `660919a2`): `python -m tools_py.tests.fast` 2360 tests OK in 84 s (25 slow modules and the product/disc cases excluded, off the lock's busy list); `requirements.txt` pinned from the tree's imports, CI installs it; `build.sh` through `python_env.sh`; `fix_ghidra_csv` 14 cases (RED: forced entries overlapped out of order and doubled -- fixed, products byte-identical); 8 log skips in test_gate served (13 -> 5); review pending |
| C1 | CI compiles the overrides, the crypto file and the runner's link | C | [A] | **DONE 2026-09-25 15:05Z** (merged `0cf4bfa0`; CI observed: runs 36148051587 linux and 36148051582 windows, the step "Compile game_overrides_socom2.cpp and socom2_crypto.cpp and link the runner" success on both, each log ending `build_synthetic_runner: ok (the runner starts and refuses a missing ELF with 68)` — the first Linux build of the runner) — was **DONE (local) 2026-09-25 14:25Z** on `agent/s13-c1` (`e13b78b5`): `tests/fixtures/synthetic_recomp/` (three hand-written functions at 0x100000..0x100040, the table, the two headers) and `scripts/build_synthetic_runner.sh` (links `ps2EntryRunner`, requires 68 elf-missing); a step in `build` and `build-windows` (names unchanged); RED x3 (overrides syntax, crypto syntax, undefined symbol) and GREEN in one lock run; `test_workflows` 14/14. **CI observation owed** (push disabled for the agent) |
| C2 | the throwing-stub run and HLE leg three (R265) | C | [L] | NOT STARTED |
| C3 | the after-return trap; #39; R256's finding 9; the stale comments | C | [A] then [L] | **[A] MERGED `e7a8af3c`; CI green on both runners (the new suites compiled and passed there — GREEN; RED was the compile failure against the parent)**; owed: the r0001 gate (the third proof) and the control round with `PS2X_SOCOM2_UDP_SHIFT=2` on one of two instances (the install line `getter global 0x656340`, `port -> 3660`, B's DME record :3660), the `PS2X_CULL_TRACE` row-count comparison, the NetIdle `[ret]` count. Was: NOT STARTED |
| C4 | #33 FTOI; #40 tail calls and the nine stubs | C | [L] | NOT STARTED |
| C5 | build products out of the source tree | C | [A] then [L] | **DONE 17:49Z** — recomp left git status unchanged, the fixed map in recomp/build/, the census renamed 31 / extents 0 with N1 the second proof (`s13_proof2_gate2` r0001 3/3 PINS MATCH on `9aa2356b…`; `s13_proof2_gate2_r0004` 3/3 PINS MATCH on `9a66a95d…`). Was: [A] done 2026-09-25, recomp and gate owed (`ce61a5e6` on `agent/s13-c5`): `./build.sh recomp` writes the fixed map to `recomp/build/socom2_ghidra.fixed.csv` (byte-identical to the tracked map, which it no longer writes), `fix_ghidra_csv.py` requires `--out`; `socom2_r0004.toml` kept tracked and held to step 3's bytes by `test_build_products` (ruling in the Log); `socom2_ghidra.toml` removed; `socom2.toml` headed hand-maintained. Owed [L]: `git status` empty after `./build.sh recomp` and after an r0004 `build_revision`, `recomp/output` diff-identical, the gate 3/3 |
| C6 | FFmpeg 7.1.5 with `URL_HASH`; commit pins; CI pins; the notices | C | [A] then [L] | **DONE 17:49Z** — the runtime refetched FFmpeg 7.1.5 under URL_HASH (avcodec 53,623,808 bytes), the release closure is the four FFmpeg DLLs plus libc++/libunwind, the movies decoded in the second proof (`s13_proof2_gate2` r0001 3/3 PINS MATCH on `9aa2356b…`; `s13_proof2_gate2_r0004` 3/3 PINS MATCH on `9a66a95d…`). Was: DONE ([A]) 2026-09-25 14:18Z on `agent/s13-c6` (`af85f907`): FFmpeg `n7.1-241205` → `ffmpeg-7.1.5` (same channel, SHA-256 = GitHub's asset digest; vcpkg-built now: import libs in `lib/`, codecs static, LGPL-3.0-or-later); ten `GIT_TAG`s at commits (each the one `build-clang/_deps` already had); notices: 25 static-inside rows, a Linux table. Review round 1 (FFmpeg and the git pins PASS; CI FAIL; notices PASS WITH FINDINGS) fixed in the round commit: apt unpinned with the green run's versions as a comment and `dpkg-query -W` in the log; pip left to H7's `requirements.txt` (the test reads a `-r` file's rows); the Linux list is the 76-library `--as-needed` ELF closure (58 rows, checked both ways), not the 130-name package superset; the `SHA256=` leak exception. The Windows zip grows about 20% (+11.4 MB deflated: the 7.1.5 channel builds the encoders in; no slimmer variant), kept for the decoder's security fixes. Build and gate owed: `./build.sh runtime`, then `./build.sh release` (so `dist-release/` loses the jxl/brotli/webp/zlib1 DLLs, else the notices test is red), then the gate 3/3, the movies playing |
| C7 | the dead configuration removed | C | [A] then [L] | **DONE 17:49Z** — ps2x_tests and the launcher built without the removed modules and the bold face, gated by the second proof (`s13_proof2_gate2` r0001 3/3 PINS MATCH on `9aa2356b…`; `s13_proof2_gate2_r0004` 3/3 PINS MATCH on `9a66a95d…`). Was: [A] DONE 2026-09-25 14:05Z, build and gate owed on `agent/s13-c7` (`7ab2ceb5` F20 Vita/Android CMake + 2 sources, `7971b425` F21 Rajdhani Bold 400,680 B, `e9e6c481` F24 the five other-game IOP modules (3,133 lines) + three profiles + 19 tests, one rewritten on socom2-us, a no-dead-rows test and (review round 1) the isolation and reset-closes-files checks restored on DBCMAN and 989snd, `610d28e4` F26 the two nested workflows, `98181c0d` F27 the vendored ExportPS2Functions.java): not built (syntax-only compile of the touched IOP/test TUs clean); targets for the controller's build: ps2_iop, ps2_runtime, ps2EntryRunner, ps2x_tests (16 fewer cases: 19 removed, 3 added), vu1_replay, socom_unzipped_launcher; then the gate 3/3 |
| C8 | `ps2HostProfStart` out of the game file; libpad2/msifrpc/crypto tests | C | [A] | NOT STARTED |
| C9 | the knob registry's text against the code: `PS2X_SOCOM2_NET_STATS` is described as the periodic net-stats line but the code (`socom2_libnetb.cpp`, case 0x200) makes it the RX-counter fix behind the online movement defect (found by R2); every Shipping and Switch knob's description read against its code path, a test that the registry's one-line purpose names the code's effect | C | [A] then [L] | **MERGED 2026-09-25 12:38Z, build pending** (`5ece5e3e`, round `bce47d19`; review PASS on the meanings and the header, findings fixed; merged `b59daa0d`): ten Shipping/Switch lines corrected (NET_STATS names the 0x200 RX counter), every Shipping/Switch row cites its read site and `test_knob_read_sites` ties each to the code; `docs/KNOBS.md` has a Read-in column. The header's comment-and-string change compiles in the combined proof |
| U1 | research/63: the 27 upstream PRs; the watch table; picks by ruling | U | [C]/[A] then [L] | **DONE 2026-09-25 09:00Z** (`fec75d37`, two fix rounds `fbe5d5f5` `a3a21799`, re-review clean; merged): 24 open of the 27 (two merged, one closed); TAKE 6 (#206, #221's LWU hunk, #223, #224, #239 → U2, #253 → U3), ALREADY OURS 7, NOT OURS 9, LATER 5 (#217 latent until the pools move; #222's premise differs: nothing reads the DI/EI bit); the watch table with a row per source; corrections to the external audit (#216 must not be taken: our pools live above the cap). The picks are U7 and U8 |
| U7 | three small correctness picks with a test each and one gate: #223 (a data-less END tag never completes the chain), #224 (DMA bit 31 = scratchpad; needs `--vram-diff`), #221's LWU hunk (138 sites sign-extend where they should zero-extend) | U | [A] then [L] | **DONE 2026-09-25 13:35Z** (`f5028386` `c7414c2b` `53d0d50b`, round `d4a1eda7`; review PASS WITH FINDINGS, four Minor, all taken; the review reads #224 as latent here — no bit-31 MADR/TADR in research/63's fifo log — so `--vram-diff` is for LWU's GS-register site at 0x3b091c; the recompile must turn the 130 `SET_GPR_U32(ctx, N, READ32` lines to 0). Proof `s13_proof_gate` 3/3 PINS MATCH: the recompile turned the 130 `SET_GPR_U32 READ32` lines to 0 and 130 U64 lines, suites green, vram-diff 15/15, on the main tree's exe `5563d55b…`; the r0004 leg `s13_proof_gate_r0004` 3/3 PINS MATCH) |
| U8 | #206, oversized function bounds (6,640 of 14,657 functions run past their map row; 1,112 swallow others; 6,750 `sub_` would become `FUN_` and the pins move) — a ruling first: it is the csv row-extent work the audit's C36 names, and it changes the generated image's shape | U | [C] then [L] | **RULED TO THE BACKLOG 17:06Z (S13-R10; issue #55 with its KNOWN §2 row)** |
| U2 | path containment (#239's class): the test, the fix | U | [A] | **DONE 2026-09-25 13:35Z** (`4eb07b45`, round `feb29971`; RED 894/899 → GREEN 899/899 under the lock; review PASS WITH FINDINGS, S13-R8 followed; the two unbound stubs are issue #53). `s13_proof_gate` 3/3 PINS MATCH on the main tree's exe `5563d55b…` |
| U3 | #253's emitter change | U | [C] then [L] | NOT STARTED |
| U4 | the upstream drafts for O10 | U | [A] | **DONE 2026-09-25** (`3dc9cb5e`): `docs/research/assets/63-upstream-drafts/` -- the argv-ABI and printf issues with `git format-patch` output against upstream `75d729c` (code identical to research/40's spike, `git apply --check` clean); the export-table walk drafted and advised against (upstream's first-zero rule is the IOP's; our guard over-reads); ten KEEP comments (each pick's lines identical to upstream's commit, heads unchanged); the two BinExport issues (both still on `main` `087a035`); HUMAN_TASKS O10 points there. Nothing filed |
| U5 | Memdusa: the client's handling of a server-sent payload, private note first | U | [A] | **DONE (read) 2026-09-25 07:40Z**: the private note is under the main tree's ignored `vm/security/`; the class is live as a write primitive (S13-R4 below); nothing tracked until U6 lands |
| U6 | the server memory-poke message refused on the client (the wrap, its test, the gate, a control round), then the KNOWN row and the SECURITY bullet | U | [A] then [L] | **DONE 2026-09-25 13:35Z** (`6b30e42e` `8df7c036`; 899/899; review CLEAN; gate `s13_u6_gate` 3/3 PINS MATCH on the relinked exe; merged; the KNOWN §1 row and the SECURITY bullet written without mechanics). The combined proof `s13_proof_gate` 3/3 PINS MATCH on the main tree's exe `5563d55b…`; the r0004 leg `s13_proof_gate_r0004` 3/3 PINS MATCH on `dist-r0004/socom2.exe` `b1dd4178…` (1,705-name sidecar); the control round is O1's |
| S1 | README, INSTALL, FAQ, PLAYTEST true to the build | S | [A] | **DONE 2026-09-25** with R2 (`8336e3f2`, `b40bf5a6`; merged) |
| S2 | the launcher's wording defects (with V8) | S | [A] | NOT STARTED |
| S3 | `help wanted`; the twelve evidences; the Dependabot PRs; the lock issues marked internal | S | [A] | **DONE 2026-09-25 11:45Z** (`150ce83c`; merged): `help wanted` on #33, #39, #40, #46, #48 (#48 also `good first issue`), a comment each; twelve Evidence sections say what lives only on the owner's machine and what a clone holds; #26, #35, #36, #37 marked Internal; CONTRIBUTING true; the two GitHub labels added to `scripts/github_labels.sh`. **The four Dependabot PRs are the owner's click** (#7, #8 green and clean; #9, #10 rebased on request): merging a PR that touches a workflow file needs a token with the `workflow` scope, which this session's `gh` lacks — added to O13 |
| S4 | the story's five days | S | [A] | **DONE 2026-09-25** (`d4a8f0ea` on `agent/s13-s4`): five entries under a new era in `docs/STORY.md` — 2026-09-23 (Sprint 10 merged, the r0004 capsule and package, the build to its credits), 2026-09-24 twice (the r0004 gate 3/3 and its round on our server; Sprint 12's cloud naming), 2026-09-25 twice (Sprint 11's close; Sprint 12's local proof on both revisions); four frames from the day's own runs, the cloud day without one and saying why; seven witnesses; `tools_py.story.cite` 57 entries clean, `test_story_cite` 57 OK; the site copy to `../scotho` is the controller's |
| S5 | the Linux notices | S | [A] | **DONE by C6 2026-09-25** (`af85f907`, `f3aa0722`: the notices test walks a Linux tarball's `lib/` — the fixture from the `--as-needed` closure, a real tarball when present, both directions — and the Linux rows are the 76 libraries that ship) |
| S6 | private addresses as tracked defaults, found by H6's round: `server/config/{dme,medius,muis}.json` (`PublicIpOverride`, `NATIp`, `Endpoint`, rewritten by `start-servers.ps1 -PublicIp`), a second private address hard-coded as `SERVER_IP` in `server/horizon-server/Server.{Dme,Medius}/Program.cs`, the launcher's C++ tests and the saved-config default at `ps2xLauncher/src/main.cpp:2082` — each becomes a required value or a documented example address (RFC 5737), never the owner's LAN | S | [A] | **DONE 2026-09-25** on `agent/s13-s6` (the commit subject `fix(server,launcher): no private address as a tracked default`): the three configs' six advertised fields hold the RFC 5737 placeholder `192.0.2.1` and `start-servers.ps1` refuses to start on it (exit 2, one sentence naming the fields and `-PublicIp`), as does `linux/horizon-ctl.sh start/restart` (fix round 1); the two vendored `SERVER_IP` initialisers the same placeholder (one line each); the launcher's `_community_healed` shot and the C++ tests RFC 5737 addresses; leakcheck's `tree` and `staged` report any private address in a tracked file as `tracked-private-ip` (not `history`), the dated record and two planted/quoted lines allowed by reasoned rows in `leak_allow.txt`; `test_no_private_address` RED (31 sites) then GREEN; server/README, INSTALL §8, SECURITY updated; the C++ build NOT RUN (lock held by U6's gate), left to the combined proof |
| N1 | the runtime's names into the sidecar; research/50's four offsets; `hostprof_diff` | N | [A] then [L] | **DONE 17:49Z** — the census diff renamed 31 / extents_changed 0 against the pre-N1 output; its runtime and gates in the second proof (`s13_proof2_gate2` r0001 3/3 PINS MATCH on `9aa2356b…`; `s13_proof2_gate2_r0004` 3/3 PINS MATCH on `9a66a95d…`). Was: [A] done, recomp and gate owed 2026-09-25 on `agent/s13-n1` (`199fbce2`): 31 `Pass=hand` rows (`recomp/names_proposals_hand_2026-09-25.csv`, evidence `file:line`, no score) applied, `socom2_names.csv` 1,840 → 1,871, `socom2_names_r0004.csv` 1,705 → 1,722 through `match.json` (`exact` only; 14 not carried, printed); the applier takes hand rows and `--through`; research/50's four offsets as comments at their six sites; `hostprof_diff` pairs by the `_0x<start>` suffix (5 tests). Owed: the recomp with `extents_changed: 0` and the gate 3/3 |
| N2 | #48; the header wording; the r0004 build with its sidecar and its gate | N | [A] then [L] | **[A] done; the r0004 build with the sidecar and `s13_names_r0004_gate` owed in the owner's window** 2026-09-25 on `agent/s13-n2` (`9cafac47` #48: `--out` copies the sidecar beside the toml, the unresolved-names line a WARNING build.sh and step 4 print; the names commit after it: 14 r0004 `Pass=hand` rows from the `kR0004` column, `socom2_names_r0004.csv` 1,722 → 1,736, and the generated header's `// Name source: <sidecar> row 0x… (map name Y)`). Owed: ps2xTest and the r0004 recomp (`Loaded 1736 display names`), an `--out` build whose toml resolves (closes #48), the gate 3/3 |
| N3 | the cloud's list, if a session is opened; else `docs/BACKLOG.md` | N | [C] | NOT STARTED | <!-- docmaint: future -->
| N4 | BinExport `main` on Ghidra 12.1.3 | N | [C] | NOT STARTED |
| O1 | one ladder run and one mixed-match leg on the renamed tree | O | [L] | NOT STARTED |
| O2 | #26 the chat step and R221's peek in one round | O | [L] | NOT STARTED |
| O3 | the box in the repository; `/api/stats` build id; `server/README`; Horizon's base and PR #35 | O | [A], deploy [O] | **[A] done, deploy is O5's — 2026-09-25** on `agent/s13-o3` (`4167ea29`): `server/ops/` backup.sh, health.sh (four units, five ports, the build against `OPS_DEPLOYED_COMMIT`), backup.cron, backup-pull.ps1, every secret, address and key path from the ignored `ops.env` (`ops.env.example` tracked); `BUILD_ID` in the package, served as the stats JSON's `build` (so `/api/stats`, which nginx proxies verbatim); `server/README` true (running `4d7e4816`, base `1a7b9cd`, PR #35 not carried, fifteen local changes by file, `ChatClamp.cs` marked the project's); `test_server_ops.py` (13), `test_make_server_zip.py` (+3), `BuildIdTests.cs` (8) |
| 99 | the close: DOC_MAINTENANCE §5 and §7 (with `issues.py carry` and the tally), the Outcome, the PR, the tag | — | [A] | NOT STARTED |

## Task 0: The open (2026-09-25 night)

**Files:** this plan; the spec; `docs/audits/2026-09-25-project-audit.md` and `docs/audits/2026-09-25-project-audit/`
(the six reports, the LAN address masked); `docs/CURRENT_SPRINT.md`'s header; `docs/HANDOFF.md`'s state line;
`docs/HUMAN_TASKS.md`'s top (a pointer to the audit's §3 until R4); the `Sprint 13` milestone on GitHub.

- [ ] **Step 1:** the audit and its reports committed on the Sprint 12 branch as the close's last docs commit (they
  are the close's reading, and a docs-only commit): `docs(audit): the 2026-09-25 project audit …`.
- [ ] **Step 2:** once Sprint 12's PR has merged, `git checkout -b sprint-13 origin/main`; this plan and the spec
  committed there; `gh api` creates the `Sprint 13` milestone and moves the six carried issues into it (R266).
- [ ] **Step 3:** the sprint file's header (`branch: sprint-13 -- OPEN …`, the spec and plan lines, the next-ruling
  line), HANDOFF's "Where the loop is now" bullet, HUMAN_TASKS' top pointer; `docmaint` green; pushed.
- [ ] **Step 4:** the first tasks dispatched: V1 (lock-free reading of the archived captures), R1 (the split), U1 (the
  upstream triage, an agent with web access), U5 (the Memdusa read, a private note) — four agents in their own
  worktrees, none needing the lock; the lock-bound queue for the night: V5's quiet capture, O1's ladder run, N2's
  r0004 leg if Sprint 12's own run did not finish it.

## Task V1: #30 — the four low-scoring menu captures

**Files:** `logs/parity/gate/*/title/` (archived stamps; git-ignored, on this machine); `tools_py/parity/gate.py`
(the title scorer, `BRIEFING_THRESH`, the menu reference set); `scripts/parity/ref_*.png` (the references); a
research note `docs/research/64-the-four-menu-captures.md`. <!-- docmaint: future -->

**Interfaces:** the title stage's score per capture (`s00`…`s22`), the reference each is scored against, and the
capture's own PNG. Consumes the archived stamps since Sprint 9; produces the side-by-side and the verdict.

- [ ] **Step 1 (read):** for every archived stamp, the four scores and the four PNGs beside their references; the
  first stamp where they fell; what changed in the tree at that commit (the menu, the reference, the scorer).
- [ ] **Step 2 (RED):** a test that the title stage refuses a capture under 90 when the reference matches its own
  previous capture within tolerance (the "kept passing" half of the issue).
- [ ] **Step 3:** the fix — the render defect if the captures are wrong, a re-reference with the change written down
  if the menus changed, or the scorer if the threshold hides a real loss; GREEN; the gate 3/3.
- [ ] **Step 4:** review; commit; `python -m tools_py.issues close 30 --artefact …`.

## Task V2: #32 — the menus' tile uploads

**Files:** `third_party/ps2recomp/ps2xRuntime/src/gs/*` (the texture upload path and the `[gs-gl stats]` line);
`docs/research/34-*.md` §6 (the measurement); `docs/research/41-cucumber-fork.md` (the reason counters, ADAPT item 2).

- [ ] **Step 1 (measure):** the per-upload breakdown by reason under `PS2X_GS_STATS=1` on the login, universe and
  lobby screens (research/41's counters, ~40 lines, land here as the instrument).
- [ ] **Step 2 (RED):** a `ps2_gs_tests.cpp` case that uploads the same tile twice and expects one upload.
- [ ] **Step 3:** tile batching or skip-unchanged, whichever the breakdown says; GREEN; the stats line before and
  after in the gate's record; the gate 3/3; a control round (the menus are online's front door).
- [ ] **Step 4:** review; commit; close #32 with the two stats lines.

## Task V3: #31 — streamed images read DBP at packet offset 0x14

**Files:** the load-image stubs (`Kernel/Stubs/*`, `sceGsSetDefLoadImage` and the streamed path research/25 §9 names);
`ps2_gs_tests.cpp`.

- [ ] **Step 1 (RED):** a multi-strip stream whose strips carry different DBP halfwords; the test expects each strip at
  its own DBP.
- [ ] **Step 2:** the stub reads DBP at exec time; GREEN; a loading screen captured and compared against the console
  image; the gate 3/3.
- [ ] **Step 3:** review; commit; close #31.

## Task V4: a tracked frame-time number

**Files:** `tools_py/parity/gate.py` (the mission stage's record), `tools_py/parity/pins.py` (an informational pin),
the sampler's FreezeFields line (`PS2X_PC_SAMPLER`), `docs/KNOWN.md` §1, `README.md`'s status cell, a new issue.

- [ ] **Step 1 (RED):** the mission stage's summary carries `FRAME mean=<ms> worst=<ms> n=<frames>` read from the
  sampler; a test on a saved stamp.
- [ ] **Step 2:** GREEN; three gates; the spread recorded; the pin informational (no refusal yet, S13-R3 sets the
  refusal rule when three runs agree); the issue "the mission frame rate has no bar" opened with the three numbers
  as its evidence and the bar "a refusal rule in `pins.json`".
- [ ] **Step 3:** README's cell rewritten from the gate's line, dated; review; commit.

## Task V5: the mission music — #42 and #28

**Files:** `scripts/parity/music_only_mission.txt` and the music-only capture pair; `tools_py/parity/audio_dips.py`;
`tools_py/parity/audio_gate.py` (the count pin); the W7 route script; `docs/research/36-*.md` items 5 and 7; the
per-stage sound fixture.

- [ ] **Step 1 [L, night]:** the quiet-window capture — the script refuses a contaminated run by itself; the DEVICE
  count per minute; the stamped capture named in #42.
- [ ] **Step 2 (RED):** the audio gate's DEVICE-dip count as an informational pin; a test on the saved capture.
- [ ] **Step 3 [L, night]:** the route drive (cinematics skipped, a walk of ≥ 10 minutes) captured and scored against
  PCSX2 (#28's bar); the verdict recorded either way.
- [ ] **Step 4:** research/36 items 5 (the `playVagStream` queue semantics, with its test) and 7 (the doc-only
  corrections); the per-stage sound fixture (a WAV per gate stage, compared by `audio_dips`).
- [ ] **Step 5:** review; commit; #42 and #28 closed by their bars or carried with the measured reason.

## Task V6: #27 — the clean-exit relaunch

- [ ] **Step 1 [L]:** one launch that creates the persona and quits through the driver's clean-exit path; the same
  relaunch; the card read back. Bar as written in #27.
- [ ] **Step 2:** the fix if the password is still lost (the save path on clean exit); the test; close #27.

## Task V7: #34 — `waitReadable` bounded by the guest clock

**Files:** the network path's `waitReadable` (`ps2xRuntime`), the scheduler's clock exclusion, research/29 §shape 2.

- [ ] **Step 1 (RED):** a unit test that a blocked read cannot hold the EE executor past one guest tick.
- [ ] **Step 2:** the bound; GREEN; a control round with the FreezeFields line; the gate 3/3.
- [ ] **Step 3:** review; commit; close #34 or carry with the round's numbers.

## Task V8: the server name that does not resolve; the launcher's wording

**Files:** `ps2xLauncher/src/*` (`focus.cpp`'s blocked reason, `page_about.cpp`, `page_microphone.cpp`,
`page_controller.cpp`, `main.cpp`'s sample ids, `launcher_config.h`'s comments); the runtime's server lookup; `docs/FAQ.md`.

- [ ] **Step 1 (RED):** launcher tests for each wording defect (S9–S15, F53) and a runtime test that a name that
  resolves to 0 refuses with the sentence LAST RUN shows.
- [ ] **Step 2:** GREEN; the FAQ's exit table says where each code is reachable from; review; commit; the gate 3/3
  for the runtime half.

## Task R1: the archive split and the ceilings

**Files:** `docs/CURRENT_SPRINT.md`, `docs/archive/CURRENT_SPRINT-sprints-9-to-11.md` (new), `docs/archive/README.md`, <!-- docmaint: future -->
`docs/HANDOFF.md` (§2, §4, §9, §10), `docs/STATUS.md`, `docs/audits/` (the two moves), `docs/archive/sprints-7-12/`
(the closed specs and plans), `docs/DOC_MAINTENANCE.md` (registry rows, the ceilings), `tools_py/docmaint.py`,
`tools_py/tests/test_doc_maintenance.py`.

- [ ] **Step 1 (RED):** `docmaint` fails on a sprint file over N bytes, a HANDOFF §2 over M, a STATUS state block over
  K, a HUMAN_TASKS over J (the numbers set from the live content after the split, with headroom); fails on "merged to
  `main` as vX" where `git ls-remote --tags origin` lacks vX (skipped offline); the counter scan includes the new
  archive file.
- [ ] **Step 2:** the moves, verbatim, with banners (check 5) and registry rows; every citation of a moved block
  re-pointed (check 6: `git grep` each block name); the ledgers kept in the live file with a pointer from the archive.
- [ ] **Step 3:** GREEN; review (a fresh agent reads the live file as a new controller would); commit.

## Task R2: the live documents true

- [ ] **Step 1:** DEVELOPING rewritten from its dated history into current truth (the audit's D3–D8, D67, F36, H50; the
  `tools_py/` map from the harness report's Appendix A); README, GIT_STRATEGY, LOOP_PROMPT, PLAYTEST, ROADMAP §6,
  STORY, the archive README, SECURITY's hash (D35–D48, D65, S24, S42–S44, S50); each REVISION row's number in the
  commit message.
- [ ] **Step 2:** review by an agent that reads only the documents (not the reports) and checks each claim against
  the tree; commit.

## Task R3: the ruling record

- [ ] **Step 1 (RED):** `docmaint` reports a ruling number defined twice; a `R<n>` cited with no definition.
- [ ] **Step 2:** the notes beside R107/R109/R110's second definitions ("issued twice; the second is R107b in
  citations"), R114/R116/R124 recorded as vacant in the Sprint 8 plans' ruling sections with the reason, research
  43a/43b named in every citation, 35 recorded in ROADMAP as never written; GREEN; commit.

## Task R4: HUMAN_TASKS reduced to the owner's sitting

- [ ] **Step 1:** every open checkbox item struck with its reason (superseded by which fix or round) or moved to this
  sprint's task; the audit's §3 table becomes the file's one list, the defaults beside each; the 2026-09-22 banner
  and the seven "Start here" generations archived to `docs/archive/HUMAN_TASKS-to-2026-09-25.md` with a banner.
- [ ] **Step 2:** under R268's ceiling; review; commit.

## Task R5: KNOWN in full

- [ ] **Step 1:** every §2 row: settled → §3 or §1 with the artefact; still believed → the experiment named; §4:
  every bullet describing a fixed state superseded in place (D31, D32); positional pointers replaced (D34); the
  Windows-only row (D29); the review items (b), (c), (d) (D30, S45); the audit stamp.
- [ ] **Step 2:** `python -m tools_py.issues audit` OK; review; commit.

## Task R6: the codex audit's six assignments

- [ ] **Step 1:** each of `docs/audits/2026-09-23-codex-task-allocation.md`'s six recommended assignments against the
  master list: taken (which task), declined (a ruling), or already done (the commit); a table in this Log.

## Task R7: one home for the carry (R267)

**Files:** `tools_py/issues.py` (`backlog`, `carry`, `milestone`, `tally`), `docs/BACKLOG.md` (generated), <!-- docmaint: future -->
`docs/backlog_ruled_out.txt` (the tracked list of ruled-out rows with their rulings), the registry row, the <!-- docmaint: future -->
bug-report skill's label list, `tools_py/tests/test_issues.py`.

- [ ] **Step 1 (RED):** `issues backlog` writes the file from a saved listing plus the ruled-out list; `carry N
  --comment` labels, comments and moves the milestone; `milestone close <name> --next <name>`; `tally --since` prints
  opened/closed/carried; the generated file's registry row; the doc check fails when the file is stale (a `--check`).
- [ ] **Step 2:** GREEN; every `backlog` row of the audit written into the ruled-out list with its bar; commit.

## Task H1: a docs-only push starts no build run (planned as "reads `skipped`")

- [x] **Step 1:** as landed (`9c3f96c4` + fix round 1): `linux.yml` and `windows.yml` take `on.push.paths-ignore:
  docs/**` (a docs-only push starts no build run; a status job cannot make a run `skipped` while `changes` passed);
  the `changes` step's base/head are keyed on `github.event_name` and compared three-dot from the merge base (no
  merge base builds); a new `docs.yml` (job `docs`, not required) runs `docmaint` and the doc tests on docs pushes and
  every PR; `tools_py/tests/test_workflows.py` pins it all; HANDOFF rule 4 and DEVELOPING's "Reading a CI run" say
  how to read a run. The docs-push and code-push observations are the controller's after the push.

## Task H2: the lock's queue

**Files:** `scripts/loop_lock.sh`, `scripts/run_detached.sh`, `scripts/ladder_job.sh`, `scripts/check_quiet_gate.sh`,
`tools_py/tests/test_loop_lock.py`, `docs/KNOWN.md` §4 (the rollout hazard), `docs/LOOP_PROMPT.md` (the protocol).

- [ ] **Step 1 (RED):** two waiters (5 s and 60 s polls) served in arrival order; `--wait 3` waits three minutes at
  any poll interval; `run_detached --wait 10` waits; the quiet marker under the git common dir is seen from a
  worktree; `ladder_job.sh` no longer checks-then-takes (a planted competing taker in the test).
- [ ] **Step 2:** the ticket queue (the harness report's H9 design: a `$LOCK.q/` of tickets under the existing
  mutex; `do_take` grants to the oldest live ticket; a waiter's ticket carries its heartbeat); GREEN
  (`LOOP_LOCK_SLOW_TESTS=1` too).
- [ ] **Step 3:** the rollout procedure written (KNOWN §4: a lock script waiters run by offset — land it when
  `check` says FREE and `busy` is empty, then restart every waiter); one real hand-off at night between two chains;
  review; commit; close #35, #36, #37.

## Task H3: pins, environments, references

- [ ] **Step 1 (RED):** #45's test (a gate queued, cancelled before the lock, the standard byte-identical); #38's
  (one capture entry point dry-run writes the `PS2X_*` environment); a pins test that every `scripts/parity/*.png`
  the harness reads is in a standard.
- [ ] **Step 2:** GREEN; `ref_lobby_news_title.png` deleted; the speed-freeze re-measure from the ladder logs written
  as a KNOWN row (R242); commit; close #45, #38.

## Task H4: the uninvoked modules

- [ ] **Step 1:** the harness report's Appendix A "invoked by nothing" list dispositioned: an entry point documented
  in DEVELOPING's `tools_py/` map, or moved to `docs/archive/tools/` (the `patch_*.py` set), or deleted; the two
  logs removed; the absolute paths made relative; #46 closed either way; a test that every `tools_py/*.py` is
  invoked, tested or listed in the map.

## Task H5: #41

- [ ] **Step 1:** the record settled (Q7's row vs the issue); a fresh PCSX2 dump if the case is kept, the case RED on
  a planted pixel change and GREEN on the tree; or the case deleted and KNOWN rewritten; close #41.

## Task H6: per-revision literals

- [ ] **Step 1 (RED):** a grep test refuses a bare `0x[0-9a-f]{6,8}` guest address in `tools_py/parity/*.py` and
  `scripts/parity/*` outside `guest_addresses.py` and an allow list that names each survivor's reason.
- [ ] **Step 2:** `music_state_poll` (eight statics, r0004 column via `data_via_twin`), `motion_pack_check`,
  `cam_poll`'s default spec, `verdict_replay`'s copy, `verdict_core`'s pointer mode through the table; `env.sh`'s
  default the hosted box; GREEN; commit.

## Task H7: the suite's shape

- [ ] **Step 1:** the fast subset (cases that need no build product, marked or discovered by directory) runnable
  outside the lock in under two minutes; `requirements.txt` true (no pytest; unicorn, capstone, zstandard, pycaw,
  comtypes, pyaudiowpatch); CI installs from it; `build.sh` through `python_env.sh`; `fix_ghidra_csv` under test;
  `make_gate_fixtures.py` applied to the log-dependent skips it can serve; commit.

## Task C1: CI compiles the game's files

- [x] **Step 1 (RED):** a CI job that compiles `game_overrides_socom2.cpp`, `socom2_crypto.cpp` and links the runner
  against a synthetic generated set (three functions), red on a planted syntax error.
- [x] **Step 2:** GREEN on Linux and Windows (CI runs 36148051587, 36148051582); commit.

## Task C2: the throwing stubs and HLE leg three (R265)

- [ ] **Step 1 [L]:** one instrumented gate run with `PS2X_HLE_STATS=1` and the stub census; which of the 20 throwing
  stubs are reached; the consumers of research/20's flagged rows listed.
- [ ] **Step 2:** each reached stub a real body with a test or a KNOWN row with an issue; research/65 records the run;
  commit.

## Task C3: the after-return trap and the stale comments

- [ ] **Step 1 (RED):** a test that `socom2_RtNetConfigInit`'s write happens before the original returns (the
  pattern the OSK wrap uses); the traces' "after" reads moved to the checkpoint; `PS2X_PEEK` over 64 words warns once
  (#39's test in `ps2x_tests`); `GetEntryAddress` refuses the unrunnable handler (R256's finding 9).
- [ ] **Step 2:** GREEN; the comments (F15, F17, F19, C52, X28, C30); the gate 3/3 and a control round; commit;
  close #39.

## Task C4: #33 and #40

- [ ] **Step 1 (RED):** the three FTOI sites through the clamping helper under a unit test with the NaN rule; the
  stats' counting entry reached by tail calls (a synthetic tail-call case); the master list's "PMULT row" (D5,
  research/41's PMULT{W,UW,H} line, the codex audit's assignment 3 via R6): `translatePMULTW`
  (`mmi_translation_helpers.cpp:334`) multiplies unsigned and sums both lanes into one 64-bit result where PCSX2
  multiplies lanes 0 and 2 signed into their own lanes and HI/LO — a case per lane, sign and HI/LO effect, RED first.
- [ ] **Step 2:** GREEN; the nine unregistered stubs registered or removed with the reason; the gate 3/3; close #33, #40.

## Task C5: build products out of the source tree

- [x] **Step 1 (RED)** (`ce61a5e6`, 10 RED then 33/33 GREEN)**:** `./build.sh recomp` leaves `git status` clean (the fixed map to `recomp/build/`);
  `socom2_r0004.toml` regenerated by a test from `socom2.toml` + `match.json` and untracked, or its tracked copy
  held equal by a test; `socom2_ghidra.toml` removed; `socom2.toml`'s header says hand-maintained.
- [ ] **Step 2:** GREEN; a recomp; the gate 3/3; commit.

## Task C6: the supply chain

- [ ] **Step 1 (RED):** the notices test extended to the Linux tarball's `lib/`; a CMake configure test that every
  `ExternalProject`/`FetchContent` has a hash or a commit.
- [ ] **Step 2:** FFmpeg 7.1.5 (the same prebuilt channel) with `URL_HASH`; nine pins to commits; CI's pip and apt
  pinned; `THIRD_PARTY_NOTICES` updated; a runtime build; the gate 3/3 (the movies play); commit.

## Task C7: the dead configuration

- [ ] **Step 1:** the Vita/Android CMake and sources, `rajdhani_bold.h` and the font, the five other-game IOP modules
  behind a profile flag off for SOCOM, the nested workflow, the duplicated Ghidra script — each removed with the
  report's finding number in the commit; a runtime build; the gate 3/3; commit.

## Task C8: the runtime's seams

- [ ] **Step 1 (RED):** `ps2HostProfStart` declared in the runtime with a weak default; unit tests for the libpad2
  HLE (state, button info, profile), the msifrpc HLE and `socom2_crypto` (RC4 and SHA-1 against published vectors).
- [ ] **Step 2:** GREEN without stand-ins; commit.

## Task U1: research/63 — the upstream triage and the watch table

- [ ] **Step 1 [C or A with web]:** the 27 PRs read; a row each (verdict, reason, the files it touches against ours);
  #218/#215/#208 recorded as covered; #217, #222, #223, #224, #216 diffed with a test written for each defect they
  claim; the watch table (source, last read, what changed).
- [ ] **Step 2:** picks by ruling (S13-R4…); each pick gated 3/3 in an agent worktree; commit.

## Task U2: path containment

- [ ] **Step 1 (RED):** a translated path with `..` and a symlink inside `mcRoot` cannot resolve outside it (three
  roots, three cases).
- [ ] **Step 2:** the fix if red; a SECURITY.md sentence; commit.

## Task U3: #253's emitter change

- [ ] **Step 1 [C]:** the emitter stops including `ps2_recompiled_functions.h` per file (forward declarations instead);
  a recomp census identical but for the includes; **[L]** a runtime build after a one-name change rebuilds one object;
  the gate 3/3; commit.

## Task U4: the drafts for O10

- [x] **Step 1** (`3dc9cb5e`; the export-table walk judged not an upstream bug, its draft says so)**:** three upstream issue drafts (the modload argv ABI, the IOP printf, the export-table walk), one note
  per KEEP pick ("gated 3/3 on SOCOM II at …"), the two BinExport defects as issue drafts — under
  `docs/research/assets/63-upstream-drafts/`; the O10 line updated.

## Task U6: the server memory-poke message refused on the client

**Files:** `third_party/ps2recomp/ps2xRuntime/src/game_overrides_socom2.cpp` (the chat receive bound at the lines the
private note cites is the shape: `replaceFunction` on the handler, every time, no knob, one log line), the address
table (`socom2_addresses.h`, the r0004 column through `game/r0004/match.json`), `ps2xTest` (a case that a poke of N
bytes at a chosen address leaves the bytes unchanged and the handler's return the success the library expects),
`docs/KNOWN.md` §1 and `SECURITY.md` (after the merge only).

- [x] **Step 1 (RED):** the `ps2x_tests` case against a synthetic message buffer: before the wrap the bytes land;
  with it they do not and the handler returns as if they had.
- [x] **Step 2:** the wrap for both revisions; the neighbouring cheat-query arm read and bounded the same way if it
  reads server-chosen memory back; GREEN.
- [ ] **Step 3 [L]:** `./build.sh runtime`, `./build.sh test`, the r0001 gate 3/3, a control round on our server (the
  message never arrives there, so the round proves only that nothing else changed); the r0004 gate.
- [x] **Step 4:** review; commit; then the KNOWN row and the SECURITY bullet in a docs commit; the private note stays.

## Task U5: Memdusa

- [ ] **Step 1:** the client's handling of a server-sent payload of the exploit's shape, read from the recompiled
  handler and the runtime's network path; a private note (git-ignored under `vm/`) first; the bounded finding into
  KNOWN and, if the class is live in our client, an issue under SECURITY.md's process and a fix; commit what is public.

## Task S1: the stranger's pages

- [ ] **Step 1:** README (S6, S7, S36–S38, S42, S43, S52), INSTALL and FAQ (S9, S26, S54), PLAYTEST rewritten for the
  current build with the steps that have answers struck; commit.

## Task S2: the launcher's wording — with V8.

## Task S3: the tracker for a contributor

- [ ] **Step 1:** `help wanted` on #39, #40, #46, #48 and the new tooling issues; the twelve log-based evidences
  replaced by fixtures or by one sentence naming what the owner's machine holds; the four Dependabot PRs rebased and
  merged (the controller merges outside PRs, GIT_STRATEGY §2) or closed with a reason; the lock issues labelled
  `harness` and marked internal in their bodies; commit.

## Task S4: the story's five days

- [ ] **Step 1:** the three Sprint 11 days and the two Sprint 12 days written from the record (the ledgers, STATUS,
  the plans), each frame cited; `timeline.json` regenerated; the citation checker green; commit.

## Task S5: the Linux notices

- [ ] **Step 1 (RED):** the notices test walks a Linux tarball's `lib/`; **Step 2:** the rows; commit.

## Task N1: the runtime's names into the sidecar

- [ ] **Step 1:** a `Pass=hand` proposals file from the overrides' and the address table's own names (~30), applied
  through `tools_py/apply_names.py`; research/50's four offsets beside their numbers; `hostprof_diff.py` by suffix;
  **[L]** a recomp with 0 extents moved; the gate 3/3; commit.

## Task N2: #48, the header, the r0004 leg

- [ ] **Step 1 (RED):** `build_revision --out` copies the sidecar (or writes the path absolute) under a test; the
  unresolved-names line a warning that `build.sh recomp` surfaces.
- [ ] **Step 2:** GREEN; the header wording (`Name source: …`); **[L]** the r0004 build with `socom2_names_r0004.csv`
  and `s13_names_r0004_gate` (or Sprint 12's `s12_names_r0004_gate` if its chain finished it — the Sprint 12 plan's
  row says); close #48; commit.

## Task N3: the cloud's list — dispatched only if a cloud session is opened; else each row into `docs/BACKLOG.md` by R7. <!-- docmaint: future -->

## Task N4: BinExport on 12.1.3 — [C]; research/49's numbers reproduced; DEVELOPING pins 12.1.3.

## Task O1: the ladder and the mixed leg

> The tasks O1-O3 of milestone O are not the owner's rows O1-O15 of `docs/HUMAN_TASKS.md`; that file's head says how the two are written apart.

- [ ] **Step 1 [L, night]:** `scripts/ladder_job.sh` once on the renamed tree (after H2 lands, through
  `run_detached --wait`); one mixed-match leg (`online_match_ours.py --prefilled` against PCSX2); the rows in
  `docs/LADDER.md` and the site; commit.

## Task O2: the chat step and R221's peek

- [ ] **Step 1 [L, night]:** a harness step that opens the briefing-room chat box and types a line, A hosting before
  B joins; B's log shows the receive bound; the talk-slot bytes peeked in the same round; close #26; the KNOWN rows.

## Task O3: the box in the repository

- [x] **Step 1:** the backup/health/off-box-pull scripts tracked under `server/ops/` with every secret and address
  read from an ignored file; `/api/stats` carries the build id; `server/README` true (the running commit, the
  layout, the local fixes, `ChatClamp.cs` marked); Horizon's base commit and PR #35 recorded; tests where they can
  be; commit. The deploy is O5's.

## Task 99: the close

- [ ] DOC_MAINTENANCE §5 and §7 (with `issues.py carry` and `tally`); the Outcome below; `docs/BACKLOG.md` <!-- docmaint: future -->
  regenerated; the sprint file's CLOSED block; the PR `sprint-13 -> main`; the tag `v0.13.0`.

## Rulings made on the owner's behalf

- **S13-R1 (2026-09-25) — the order is V, R, H, C, U, S, N, O.** Visible defects first (the owner's standing rule),
  then the record (every session reads it first), then what costs every night (the lock), then the code's own
  hygiene, then outside, then the stranger, then the naming follow-ups, then online. Cost: the record waits behind
  four visible defects; the owner can move R first.
- **S13-R2 (2026-09-25) — lock-bound runs on the owner's nights are allowed**, one at a time, never a game window
  when the owner has said they are at the machine, nothing that needs their hands. Cost: the owner's machine works
  nights; what it buys is #42, #27, O1 and O2 without a named window. The owner can withdraw it with a word.
- **S13-R4 (2026-09-25 07:40Z) — a live write primitive jumps the order.** Task U5's read found that the client's
  handler for the RT server memory-poke message copies server-chosen bytes to a server-chosen guest address with no
  check but the message's own length, on any server the player connects to (our Horizon never sends it); the payload
  cannot run as code in our runtime, but the write can redirect the recompiled game through its own function table.
  Task U6 (a wrap that refuses the message every time, no knob, logged once; its unit test; the gate 3/3; a control
  round; the r0004 column) runs before V1, and the finding stays in the private note until the wrap is merged and
  gated, after which one KNOWN §1 row and one SECURITY.md bullet say it without the mechanics. Cost: V1 waits a day;
  what it protects is every player who points the launcher at a server that is not ours. The owner can overturn it.
- **S13-R5 (2026-09-25 10:40Z) — a run whose stages FAIL sets no standard.** `--accept-pins` writes the standard
  only when every wanted stage reached PASS: a standard is the measured input set of a run that passed, and a FAILed
  run says nothing about whether its inputs are right (the second #45 instance was a good run with a stray knob; a
  bad run with the right knobs is the mirror case). On a FAIL the gate prints `PINS NOT ACCEPTED` and refuses the drift
  (exit 7). Cost: an operator whose run fails must fix the run before the standard moves; what it prevents is a broken
  input baked into the standard. The owner can overturn it.
- **S13-R3 (2026-09-25) — the frame-time pin is informational until three gates agree on its spread**; the refusal
  rule is set then, from the numbers. Cost: no refusal for three gates; what it avoids is a bar set from one reading.
- **S13-R6 (2026-09-25, Task R6) — the codex audit's work is dispositioned on its merits; its allocation to a
  second model, and the head-to-head that would measure it, are declined.** `docs/audits/2026-09-23-codex-task-allocation.md`
  recommends work *and* a tool for it; the work is in the R6 table in the Log (each assignment taken, done or
  declined), the tool is not the loop's to adopt. The audit itself says a Codex advantage is unmeasured and that
  independent review is its one rationale; the loop already reviews every task by a fresh agent that did not write
  it, and a cross-model review needs a session and a subscription only the owner can open. Its "How to establish
  whether the allocation actually helps" check (accepted findings, missed defects, false positives, repair rounds,
  per model) is therefore not a backlog row: it measures a tooling choice nobody has made. Cost: writer and reviewer
  share a model, so a blind spot they share stays possible. The owner can overturn it by opening a Codex session
  with the audit's first brief at a fixed commit; assignments 1 and 6 are then fixed commits to review, and 2 is the
  new issue R6 wrote.
- **S13-R7 (2026-09-25, Task R6) — process-specific loopback capture is not added as an audio instrument now.** The
  codex audit's assignment 4 proposes it (Windows build 20348+, the owner's machine qualifies). The mixer dump is
  written inside the device callback, so it is what miniaudio is handed (KNOWN §2, the #42 row); the endpoint
  recording is what the listener hears; the session monitor refuses a contaminated capture (`sessions_verdict.txt`,
  `9d9da69e`), and V5's first clean capture was taken under it. A third capture point needs its own alignment and
  answers nothing until the recorder's overflow drops are counted (V5, next) and a clean capture still leaves a
  DEVICE dip unexplained between the callback and the endpoint — the condition on which it is taken. Cost: a capture
  on a busy machine is thrown away rather than salvaged. The owner can overturn it.
- **S13-R8 (2026-09-25 12:25Z, Task U2) — the link-resolving containment applies to the memory-card root only; the disc
  and host roots keep the lexical walk.** U2's review found `game/disc_r0004/{RUN,OVERLAY,NETGUI,CNF,...}` are junctions
  into `game/disc` on this machine, and any operator may lay a disc tree out with links: a canonical check on `cdRoot`
  and `hostRoot` would refuse every file under them. The memory-card folder is the one a player might be *sent*, so
  it is resolved through links; the disc and host roots are the operator's own layout and get the lexical rule
  (`..` above the root, `:` parts, dots-and-spaces and Windows device names refused). Cost: a planted link inside the
  disc folder is not caught; a stranger cannot plant one there. The owner can overturn it.
- **S13-R9 (2026-09-25 13:15Z, Task V8) — a server name that does not resolve is a LAST RUN notice, not a process exit
  code.** V8's first round exited 75 for it; the review showed the code overwrote 65 (the slow renderer) and 72, and its
  `[preflight]` label filed a DNS refusal under the diagnostics zip's crash record. The existing `[notice]` line reaches
  LAST RUN through `lastRunLine` beside whatever the run's real exit was, so the player reads both. Cost: the exit
  table has no row for it; the FAQ carries the sentence under LAST RUN's notices. The owner can overturn it.
- **S13-R10 (2026-09-25 17:06Z, Task U8) — the oversized function bounds go to the backlog as issue #55, not this sprint.**
  Applying upstream #206's rule changes the generated image's shape (6,640 extents, 6,750 names) and moves every pin of
  both revisions, so it is two recompiles, two runtime builds, two gates and a pins re-accept under its own ruling — a
  sprint task of its own, and a Sprint 13 that carries it would not close. Every gate is green on the current shape.
  Cost: the names programme keeps riding on inflated bounds until then. The owner can overturn it.
- *(R264–R268 are in the audit and the Sprint 12 close; the global counter is HANDOFF's.)*

## Outcome

*(written at the close)*

## Log (newest first)

- **2026-09-25 18:57Z — the third proof, steps 0–1 green; its suite run tripped a lock smoke test.** The r0001 recomp
  after N2 and C3: `Loaded 1871 display names`, the census diff renamed 0 / extents_changed 0 / r11_ok (only the
  `// Name source:` header line changed), the runtime built. The suite run (3,107 Python tests) failed one case,
  `test_smoke_holder_renew_refreshes_the_heartbeat`, which allowed 5 s for a renew while C8's build ran beside it;
  widened to 30 s (`32b2a517`; still proof of a refresh). Resumed at the test step as the fourth ticket: C8's build
  holds the lock, then V6's pair (with the server set), V7's rerun, the resumed proof (test, gate, the r0004 leg with
  1,736 names and `s13_names_r0004_gate`, #48's --out build).
- **2026-09-25 18:24Z — two queue results read.** V6's pair (`logs/parity/v6_20260925_145622`) failed all three launches at
  `login:persona:list`: the pair script never sourced `scripts/parity/env.sh`, so `PS2X_SOCOM2_SERVER` was unset and the
  game's hostnet line read `retail hostnames -> 127.0.0.1` — no server, not the card. The script now sources env.sh; the
  pair is re-queued behind the third proof and C8. V7's RED build stopped on a compile error in the RED variant (a stale
  `ctx->` at socom2_libnetb.cpp:773/776), so GREEN never ran; the agent fixes the variant and re-queues. C8 committed
  (`db9e8a17`: ps2HostProfStart weak in the runtime, the pad2/msifrpc/crypto HLEs under test with Horizon's RC4 vectors;
  its build queued). Rule for the record: a launch script sets the server through env.sh or the game talks to loopback.
- **2026-09-25 17:49Z — the second proof ALL GREEN.** `s13_proof2_gate2` (r0001, 3/3, PINS MATCH 13, exe `9aa2356b…`) and
  `s13_proof2_gate2_r0004` (r0004, 3/3, PINS MATCH 13, `dist-r0004/socom2.exe` `9a66a95d…`) on the tree with C5, N1, C6, C7 and
  H7 — all five DONE. C3's lock-free half merged (`e7a8af3c`) after its round; the third proof (N2 + C3) is a ticket
  behind V7's build and V6's pair.
- **2026-09-25 17:44Z — the second proof's r0001 gate rerun 3/3 (`s13_proof2_gate2`), the r0004 leg's gate running; N2's
  lock-free half merged (`a517291a`, review PASS: #48's fix on both layouts, the header's `// Name source:` line, the 14
  r0004 rows verified against the kR0004 column); C3's half in its fix round (the review keeps the host reimplementation
  of RtNetConfigInit — the OSK pattern cannot work there — and wants the ps2_runtime.h change dropped for a static set).
  The third proof is written (`logs/s13_proof3_chain.sh`: r0001 recomp with the census renamed 0 / extents 0 against the
  post-N1 census, runtime, test, gate; the r0004 leg fresh with renamed 14 against `logs/census_r0004_pre_n2.json`, the
  names gate; one `--out` build as #48's closing evidence). After N2, file-hash checks of the output are void — the
  header line changes every renamed file; the census is the instrument.
- **2026-09-25 17:25Z — C3 (`agent/s13-c3`): code and tests written, RED/GREEN and the gate owed in the window.**

- **2026-09-25 17:20Z — N2 [A] half** (`agent/s13-n2`, `9cafac47` + the names commit; no build, no lock — the owner at
  the machine). #48: step 0 places `recomp/socom2_names_<rev>.csv` beside the toml (copied under `--out`; an r0001*
  check build reads `socom2_names.csv`, whose toml used to name a file that exists nowhere); a configured names path
  that does not open is a `[warning] names` event, printed by `build.sh recomp` and step 4 (RED 8/8, GREEN 70 OK).
  The header ruling: the plan's `Name source: …` is research/59 §3's generated-code header, not a comment in the
  sidecar csv (every reader, the recompiler's included, takes line 1 as the header), so `function_emitter.cpp` emits
  `// Name source: <names path> row 0x%08x (map name Y)` in place of `(identity Y)`, the ps2xTest case updated
  (compiled in the window); what a hand row is stays in the proposals files' heads and apply_names' docstring.
  The 14: `recomp/names_proposals_hand_r0004_2026-09-25.csv`, applied without `--through` (applied 14, 1,722 →
  1,736). Owed in a window: ps2xTest, the r0004 recomp showing `Loaded 1736 display names`, an `--out` build whose
  toml resolves (#48's closing evidence), `s13_names_r0004_gate` 3/3.
- **2026-09-25 17:13Z — the queue's first evening: V7's build and V6's pair are tickets 1 and 2 behind the gate rerun.**
  The orphan guard bit the controller first: a waiter launched with `nohup … &` from a tool shell left the queue when the
  shell exited, a `Start-Process bash` had no coreutils, and a relaunch beside a live waiter made duplicate tickets
  (killed by their pids). The escape hatch is `LOOP_LOCK_WAIT_PARENT=""`; the KNOWN HAZARD row carries it.
- **2026-09-25 17:06Z — U8 ruled to the backlog (S13-R10, issue #55); S5 marked done by C6.** The owner allows lock-bound
  work while muted (no run measures audio): the r0001 gate rerun with the r0004 leg holds the lock, V6's pair is the
  first ticket; V7's build queues after its fix round. N2's and C3's lock-free halves dispatched.- **2026-09-25 17:02Z — the second proof's r0001 gate 2/3 (a capture shortfall), the owner at the machine.** The release- **2026-09-25 17:02Z — the second proof's r0001 gate 2/3 (a capture shortfall), the owner at the machine.** The release
  closure is the four FFmpeg DLLs plus libc++/libunwind (the launcher 2.7 MB, the release exe 227 MB); the notices test
  green against it. Gate `s13_proof2_gate`: transition and mission PASS, PINS MATCH (13), title FAIL on count alone —
  11 of the 16 menu-window captures were taken and all 11 scored ≥ 93.8 (s00..s10), so the frames were right and the
  harness missed capture windows under host load (the owner began light work on the machine as the gate ran; the release
  build had just ended). S13-R5: no standard set; the r0001 gate and the r0004 leg rerun in the window the owner names.
  The chain stopped itself before the r0004 step. V6's driver fix merged (`6ff8229f`; its three launches owed in the
  window); V7 in a fix round (park only past one tick; a parked counter on the sampler line; the commit's frames claim
  corrected — thread 1's recv still stops frames for the game's own timeout). No build or launch until the window.
- **2026-09-25 16:47Z — the second proof, steps 0–2 green; two traps at the release step.** The suites passed on the
  merged tree (3,087 Python tests; ps2x_tests and the launcher; vram-diff 15/15) once the stale 7.1.0 DLLs were moved
  out of `dist-release/` (the notices test reads that folder; the release step that clears it ran after the tests in
  the chain — order the release before the tests next time, or clear the folder first). The release closure then
  called `cfgmgr32.dll` and `crypt32.dll` missing: FFmpeg 7.1.5's avutil imports them and they are Windows-provided,
  so `tools_py/portable_audit.py`'s system list carries them (`d3c066d3`). Resumed at the release step. The host ran
  low on memory during the test step and the reaper removed V6's queued launches and V7's queued build and this
  session's CI poll; they re-queue when the chain releases the lock.
- **2026-09-25 16:40Z — V6 review round 1 fixed (no game, no build; the owner is at the machine).** The relaunch requires the form before reading PASSWORD: up to 3 reads 1 s apart, then `login:saved-password:no-form`, never `:empty`. `:no-persona` is read off the form too, not off `--existing`'s guess. `CLEAN-EXIT` names run.sh's timeout (rc 124) as not clean, and the kill logs `[exit] kill`. The blop_c capture test reads the data root. KNOWN's W10 row: the persona is `w2-test` on the card (the keyboard typed "10" as "2-"; `create_persona` checks the count, not the text: the blind spot recorded). **Recorded, not fixed:** `x11shot.close_window` has no test, and `wmctrl` is not a declared dependency of the Linux ring. The pair still runs in the next window with the V6 command.
- **2026-09-25 16:15Z — V6: #27 was the driver's read, not the card.** W10's launch b arrived on the form with the persona, `*****`, SAVE PASSWORD YES and the cursor on CONNECT (`w10_virgin_b/02_persona.png`; the driver's readers: 5 glyphs, `yes`, focus `connect`). The `--saved-password` path's persona-list CROSS connected, and PASSWORD was read off the CONNECTING screen (`05_password.png`), so it reported `login:saved-password:empty`. The card kept the password through the kill. The fix is on `agent/s13-v6`: read the form as it arrives, and add `--clean-exit`. The confirming pair is owed under the lock (its first queueing was stopped by the memory reaper before it got the lock; no game ran).
- **2026-09-25 16:05Z — H2 rolled out; O3 merged.** The slow suite on the final blob `36236f94…`: 88/90 on the full run
- **2026-09-25 15:56Z — the second proof's recomp step green; V6 and V7 dispatched.** Under the new lock: `git status`
  unchanged by the recomp, `Loaded 1871 display names`, the census diff renamed 31 / extents_changed 0 — C5 and N1's
  [L] halves proven in one step. The first launch stopped on a false dirty-tree read (the controller's own notices-test
  edit landed mid-step; the check now snapshots status before and after). CI red at `b7e13676` was the notices test
  reading `server/ops` as vendored (`b001dc64`). V6 (#27, the clean-exit relaunch) and V7 (#34, waitReadable bounded by
  the guest tick) are dispatched; their builds and launches queue behind the chain with `--wait` in minutes.
- **2026-09-25 15:50Z — H2 rolled out; O3 merged.** The slow suite on the final blob `36236f94…`: 88/90 on the full run  (14:41–15:45Z), the two renewal cases widened to poll for the heartbeat (a renew costs 0.7–1.6 s on this host) and green
  after; the stamp recorded (`2654f717`). Rollout per the KNOWN row: `check` exactly FREE, `busy` empty, no
  loop_lock/run_detached process alive, the merge (`49d6fba2`) and a second `check` in one call, `version` = the git blob.
  From here `--wait` is MINUTES, a free lock goes to the oldest ticket, and a bare `take`/`run` is refused while anyone
  is queued. Owed to close #35/#36/#37: one real night hand-off between two chains and one scheduled ladder run that
  launches first time. O3 merged (`b7e13676`): server/ops with the ignored env file, the build id on /stats, README true;
  the deploy is the owner's (server/README's ops section). The second proof launches now under the new lock.
- **2026-09-25 15:35Z — N1 merged** (`199fbce2`; review PASS WITH FINDINGS, merged as is). Deferred to N2's r0004 leg: the
  14 r0004 addresses the `exact`-only carry left out get their own `Pass=hand` rows from the `kR0004` column (the runtime's
  own evidence beats a matcher placement). Recorded: N1's agent ran one full `unittest discover` that sat on the lock's
  busy list for ~10 min (it used the fast subset after). The second proof's recomp step now diffs a fresh census
  (`logs/census_pre_n1.json`, taken on the pre-N1 output at 1,840 names): expected renamed 31, extents_changed 0,
  counts unchanged — which proves C5 (nothing but identifiers moved) and N1 together.
- **2026-09-25 15:26Z — C5 merged; the notices test scoped; O3 and N1 in review.** C5 (`7c24ac44`, review PASS WITH
  FINDINGS: the r0001check map's `.gitignore` line and the case-folded same-file check folded into the merge) — build.sh's
  recomp had rewritten the tracked map in place (the origin of #54); the fixed map now lives in `recomp/build/`. C6 meeting
  V8 turned CI red at `b1f087b6`: ABOUT credited FFmpeg under LGPL-2.1 and the widened notices made the ABOUT test demand
  every codec inside the DLLs and every Linux row; the test's scope is now the Windows closure (rows inside a DLL
  creditable, Linux rows out of scope) and ABOUT says LGPL-3.0-or-later (`20a43d70`, Windows green). N1's [A] half
  (`199fbce2`: 31 hand rows, 17 carried to r0004 at exact placements; hostprof_diff pairs by address) and O3's
  (`4167ea29`: server/ops with an ignored env file, the build id on /stats, server/README true) are in review. The second
  proof gained a recomp step (git status clean, the fixed map's sha, recomp/output hash unchanged from `31dde8853aa89029`).
- **2026-09-25 15:05Z — C1 observed on CI; H7 and C6 merged.** The synthetic link step ran to `ok` on both runners
  at `0cf4bfa0` (the runner's first Linux build). H7 merged (`be4654bb`; issue #54 for the r0001 map's nested forced
  entries). C6 merged (`69619550`) with three conflicts; the CMake one was resolved from C7's structure and lost C6's
  FFmpeg block for one push (test_supply_chain_pins red), restored at `b1f087b6`. O3's two owner notes (the full-size
  MISSION FAILURE fixture, FFmpeg 7.1.5's licence and size) are in HUMAN_TASKS. Dispatched C5, N1, O3. H2's slow suite
  is running on a quiet host; the second proof (`logs/s13_proof2_chain.sh`: C6, C7, H7) launches when it reports.- **2026-09-25 — C5 [A] done (`ce61a5e6`, `agent/s13-c5`), recomp and gate owed.** `./build.sh recomp` rewrote the
  tracked `recomp/socom2_ghidra.csv` in place; it now writes `recomp/build/socom2_ghidra.fixed.csv` (sha `6ef89a65…`,
  byte-identical to the map, so the generated code cannot change), `socom2.toml`'s `ghidra_output` names it, and
  `fix_ghidra_csv.py` refuses a run without `--out`. **Ruling (C5):** `recomp/socom2_r0004.toml` stays TRACKED -- its
  per-line provenance and `[revision.unresolved]` are readable without a disc -- and `test_build_products` regenerates
  it with step 3's arguments and fails on a byte of drift (LF-normalised; `game/r0004/match.json` is git-ignored, not
  tracked, so the case skips without it and the two images; `SOCOM_DATA_ROOT` names a checkout that has them); every
  other revision's derived toml is git-ignored. `socom2_ghidra.toml` removed (no build read it; `toml_names.py` reads
  it from `c59ad14c`). RED 10 failures, GREEN 33/33 (with the main tree's data), the affected modules 308 OK.
  Owed to the controller under the lock: `./build.sh recomp` then `git status` empty and `recomp/output` identical,
  an r0004 `build_revision` (at least `--stop-after toml`) then `git status` empty, a runtime build and the gate 3/3.- **2026-09-25 — N1 [A] half** (`199fbce2` on `agent/s13-n1`): the 31 addresses the runtime binds with a placeholder
  csv name and no sidecar row get the runtime's own identifier as `Pass=hand` (r0001 +31; r0004 +17 through
  `match.json` `exact`, 14 not carried: `netbExStartAsync`/`2` seed+delta, ten relinked-body, `defer` and
  `serverMemRead` unresolved); the 16 bound addresses already named keep their names (D2). research/50's `m_RangeScale`
  0x2c8, flags 0x5c/0x5d, `m_visual` count 0x88 and `m_Opacity` 0x9c named in comments beside the numbers.
  `hostprof_diff` pairs by guest address. Owed [L]: `./build.sh recomp` (`Loaded 1871 display names`), the census
  diff against the pre-N1 census (`renamed 31`, `extents_changed: 0`), `./build.sh runtime`, the gate 3/3.- **2026-09-25 — O3 done on `agent/s13-o3` (`4167ea29`); the deploy is O5's.** The box's backup, health and off-box
  pull are tracked under `server/ops/` with their secrets in the ignored `ops.env`; the package's `BUILD_ID` is the
  stats JSON's `build`, so `/api/stats` carries it with no site change (the page showing it is the site's own
  session's); `server/README` records the running commit, Horizon's base `1a7b9cd` and PR #35 (not carried).
  `dotnet test` of `Server.Test` ran under the lock (15 passed); nothing deployed, no server touched.

- **2026-09-25 14:50Z — V8 gated, C1 and C7 merged, H2 round 2 in.** `s13_v8_gate` 3/3 PINS MATCH on the relinked exe
  (`d6deb35f…`) closes V8 (S13-R9's notice form). C1 merged (`0cf4bfa0`, review PASS, Minor only; its first CI run is
  the first Linux build of the runner). C7 merged (`3f043ff5`) after one round that restored the two checks on
  DBCMAN and 989snd; its build, test count (16 fewer) and gate ride the second proof tonight with C6 and H7 (both in
  their fix rounds). H2's fix round 2 (`dd372fef`, final blob `36236f94…`): the slow suite and the rollout wait for a
  quiet host tonight (process starts were 0.5–0.9 s under the builds).
- **2026-09-25 14:43Z — C6 review round 1 fixed on `agent/s13-c6`.** apt unpinned (a -dev pin holds back what the
  image upgrades); pip to H7's `requirements.txt` (C6's pip test skips until it exists, so merge H7 first or with
  it); the Linux notices now list the 76 libraries of the `--as-needed` closure. No real listing could be had (VM
  off, no log keeps one), so the fixture is the ELF-level walk over the downloaded Ubuntu .debs from libavcodec,
  libavutil and libswscale -- 76, KNOWN's measured count. **Controller, on the VM's next release build:**
  `bash scripts/vm_sync.sh ssh 'ls ~/socom_pc/dist-linux-release/portable/socom2-linux/lib'`, replace
  `tools_py/tests/fixtures/linux_tarball_lib.txt` with that listing and run `test_third_party_notices` (both
  directions).
- **2026-09-25 14:40Z — C7 review round 1** (F20, F21, F26/F27 PASS; F24 PASS WITH FINDINGS), one fix commit: the
  two checks the LotR services carried are restored on SOCOM II's own — instance isolation on DBCMAN's CreateSocket
  counter (the `sockets` metric), reset-closes-host-files on 989snd's stream-safe CD read (the CD image handle) — so
  ps2x_tests ends 16 fewer, not 18; PS2X_DEFAULT_BOOT_ELF's help no longer describes the Vita case. The plan's
  "behind a profile flag" had no flag to remove: the ELF-name matcher was the only selector, so the modules and their
  rows were removed instead (ruled by the controller). Recorded, not fixed: **F20 residue (filler)** — 25
  `PLATFORM_VITA`/`__ANDROID__` guard lines stay in six runtime files (`main.cpp:28-60,163,195,217,257,288`,
  `ps2_runtime.cpp:84,348,587,769`, and the gs/audio/input/debug-panel guards); **coverage** — the runtime's
  `Kernel/Syscalls/RPC.cpp` branches whose only default-build tests were the removed LotR/RECVX/DTX cases (the
  stack-ABI preference :448, `signalNowaitCompletion` :560, the service-set `guestFunction` dispatch :571/:701,
  `CallbackPolicy::Suppress` :663) are reachable now only through plugins; a test-profile seam in ps2xTest or a
  plugins-ON test build would cover them again. research/63's #228 row notes the removal.
- **2026-09-25 14:25Z — the combined proof, r0004 half GREEN.** `logs/s13_r0004_chain.sh`: the stale products removed,
  build_revision rebuilt recomp/output_r0004 and the r0004 runtime with `socom2_names_r0004.csv` (1,705 names), the exe
  copied into `dist-r0004/` as `socom2.exe` beside the DLLs (the gate refuses any other name), gate `s13_proof_gate_r0004`
  3/3 PINS MATCH (13) on `b1dd4178…`. Both revisions pass on the merged tree with U6, U7, U2, S6 and C9. Two traps for the
  record: build_revision skips a step whose product exists (a stale exe proves nothing — remove `recomp/output_<rev>`
  and `dist/socom2_<rev>.exe` first), and its toml step rewrites the tracked `recomp/socom2_r0004.toml` with LF
  endings (content unchanged; `git checkout --` it after the run). V8 merged (`5565f2ca`); its relink + gate queued.
- **2026-09-25 14:25Z — C1 done locally, CI observation owed.** `e13b78b5` on `agent/s13-c1`: the runner links against a synthetic generated set on every code push. One lock run (`agent-s13-c1`) on `build-synthetic`: an overrides syntax error exit 1 (570 s, cold tree), a crypto syntax error exit 1 (98 s), an undefined symbol exit 1 at `ld.lld` (122 s), clean exit 0 (93 s; `ps2EntryRunner.exe` 9,088,512 B, `[preflight] exit 68 elf-missing`). Owed after the push: `gh run list --commit <sha>` shows `linux` and `windows`, and each build job's log has the step `Compile game_overrides_socom2.cpp and socom2_crypto.cpp and link the runner (synthetic generated set)` ending `build_synthetic_runner: ok`.
- **2026-09-25 14:18Z — H7 done on its branch** (`agent/s13-h7`, five commits `92540d21`..`660919a2`, for review and merge). The fast subset `python -m tools_py.tests.fast`: 2360 tests OK, 84.0 s wall. `requirements.txt` at the root (numpy, pillow, zstandard, unicorn, capstone, PyYAML; pycaw, comtypes, psutil, PyAudioWPatch on win32), `tools_py/parity/requirements.txt` removed, both build workflows `pip install -r requirements.txt` (first CI run on it is the check). `fix_ghidra_csv` had a real defect (unsorted forced entries in one gap overlapped; duplicates doubled), fixed with no change to either lane's product. Skips still needing a real run: test_gate s6_fade (mtime-ordered captures), the two full-size dbuff cases, vr_title full-size, the pristine card; test_gate_pins:669 archived stamps; test_audio_dips:287; test_saved_password:81; test_motion_pack_check:50.
- **2026-09-25 14:18Z — C6's [A] half done on `agent/s13-c6` (`af85f907`); build and gate owed.** FFmpeg 7.1.5 from
  System233/ffmpeg-msvc-prebuilt with `URL_HASH` (the channel's vcpkg builds moved the import libs to `lib/` and link
  the codecs into the DLLs, so the release closure is the four FFmpeg DLLs and the licence is LGPL-3.0-or-later); the
  ten fetches at commits; CI's pip and apt at the green runs' versions (three apt pins are -updates builds that will
  leave the mirror; the step says how to bump). The Linux tarball has no tracked listing and the VM was off, so the
  notices' Linux walk runs on a fixture read from noble's package indexes (a superset). For the controller: `./build.sh
  runtime`, then the gate 3/3 with the movie stage; check `dist/avcodec-61.dll` is 53,623,808 bytes (7.1.5).
- **2026-09-25 14:05Z — C7's [A] half done** on `agent/s13-c7`, five commits, one per audit row (F20, F21, F24, F26,
  F27; the server's nested workflow taken with F26 as the same defect). Nothing built and no lock taken; the IOP and
  test changes compile under `clang++ -fsyntax-only`. The PLATFORM_VITA/`__ANDROID__` guards inside six runtime
  sources are left (inert without the CMake). Owed after the merge: `./build.sh runtime`, `./build.sh test`
  (ps2x_tests 16 fewer cases: 19 removed, 3 added), the launcher build, the gate 3/3.
- **2026-09-25 13:35Z — the combined proof, r0001 half GREEN.** On the main tree at `9ba9ceee`: recomp with the sidecar
  (1,840 names; the 130 LWU sites now emit the zero-extending form, 0 left), runtime `5563d55b…`, the suites and the
  vram-diff 15/15, gate `s13_proof_gate` 3/3 PINS MATCH (13). U6, U7 and U2 are done; S6's and C9's C++ compiled in it.
  The r0004 leg proved nothing — build_revision skipped its stale products and the gate refuses an exe not named
  `socom2.exe` — and is rerun as `logs/s13_r0004_chain.sh` (fresh recomp + runtime, the exe copied into `dist-r0004/`),
  queued behind V8's build. S13-R9 ruled for V8 (a notice, not exit 75); its fix round is running.
- **2026-09-25 13:05Z.** V8 review round 1 (runtime and wording PASS WITH FINDINGS, layout FAIL): ruling S13-R9 -- an
  unresolved `PS2X_SOCOM2_SERVER` is a LAST RUN notice (`ExitCodes::kServerUnresolved`, the `[notice]` mechanism),
  not exit 75, which overwrote 65 and 72; row 75 removed from the table and the FAQ. ABOUT's flow tightened to end
  above OPEN LOGS, winpthreads out of BUILT FROM (a "when" row is not shipping), the keyboard line names Esc and
  Space, the issue template's example id upper-case.
- **2026-09-25 12:41Z — U2 merged**; issue #53 for the two unbound stub translators, its KNOWN §2 row placed. The
  combined proof (U6, U7, U2, S6's and C9's C++) launches on the main tree now.
- **2026-09-25 12:38Z — C9 merged** (`b59daa0d`) after one fix round; the header's text change rides the combined proof.
- **2026-09-25 12:33Z.** CI green at `5f33f390` on both runners: the first CI build of the tree with U6, U7 and H1
  (the red at `936c9231` was `write_env.sh` invoking `$PYTHON` without `socom_require_python`, fixed in a subshell so the
  sourced capture goes on). H1's observations: a docs-only push starts `secrets` and `docs` only; a code push builds.
  S6 merged after its fix round (`cf444ee7`). The host carried 213 orphaned `tail`/`grep` watchers from dead sessions'
  monitors (the oldest from 09-17), which made every bash start take seconds and failed H2's stale-mutex tests; killed,
  bash starts at ~56 ms. Reviews out for U2 (S13-R8 followed: the link-resolving check is the memory-card root's),
  H2 (four Important, fix round sent) and C9. V8 holds the lock for its GREEN build. Next: U2's fix round, then the
  combined proof.
- **2026-09-25 12:07Z — S4 done** (`d4a8f0ea`, lock-free, no game run): the story's five missing days written from the
  record (O9's default, keep); the citation checker and its test green. `tools_py/story/site.py`'s `--ui-css` default
  still points at `../scotho/sites/s2u` (the site moved to `apps/s2u`); rendered here with the path given, a filler fix.
  The site copy to `../scotho` is the controller's step.
- **2026-09-25 11:54Z — U6 merged and gated.** `s13_u6_gate` 3/3, PINS MATCH on the relinked exe; the KNOWN §1 row and
  the SECURITY bullet say what a hostile server could do and that the client refuses it, not how. Step 3's r0004 gate
  and control round ride the combined proof. U4 and H1 merged before it (H1 after one fix round: the `docs` workflow,
  three-dot compare, HANDOFF rule 4). S6 committed, in review.
- **2026-09-25 11:40Z — U4 done** (`3dc9cb5e`). The O10 drafts are under `docs/research/assets/63-upstream-drafts/`, with a README
  that orders them. The owner files; nothing is posted. The third #244 patch (the export-table walk) was re-read: upstream
  is right, so its draft recommends not filing. The owner's O10 now has two PS2Recomp issues, ten PR comments and two
  BinExport issues.
- **2026-09-25 11:33Z.** H1 done on `agent/s13-h1` (`9c3f96c4` + fix round `ci(h1 fix round 1)`, review round 1 PASS WITH
  FINDINGS, fixed): a docs-only push now starts no `linux`/`windows` run, only `secrets` and the new `docs` checks;
  the PR `changes` filter is event-keyed and three-dot. Observations after the push: a docs commit and a code commit
  by `gh run list --commit`.
- **2026-09-25 11:15Z — R6: the codex task-allocation audit's six assignments, dispositioned.** Read against the
  master list, `docs/BACKLOG.md`, KNOWN and the tree at `ab6d7ff3`; every hash below checked with `git log`/`git show`.
  The audit's "Work to retain with the existing owner" table allocates no new work — its rows already live where
  they are carried (RuntimeState's leftovers #51, the Linux VM #25, the upstream picks U1/U7, the launcher wording
  V8/S2, the owner's judgments O1–O15) — and its allocation of the work to a second model is S13-R6's.

  | # | assignment | disposition | carried by / done by | the check it names |
  |---|---|---|---|---|
  | 1 | r0004 revision identity across the execution path | **ALREADY DONE** (Sprint 11 Task 19, 2026-09-23/24, after the audit's snapshot `5ab2ad6`) | `80e8501b` `SOCOM_GAME_ELF` names the image `run.sh` and `drive.py` boot; `65cff6db` the address table's r0004 column, chosen from the image's own build banner; `5cda6a51` an executable and an image that name different revisions refuse (exit 73, `socom2_revision_guard_tests.cpp`); `42f504f7` + `91f245fa` the gate's probes per revision, its record's `ELF <path> sha256 <rev>` line, an unknown revision refused (exit 8); `4ab6e9f6` the online instruments per revision; the literal inventory is KNOWN §4's struct-offset row (three trace-only survivors ruled at the Sprint 11 close); the launcher's GAME VERSION row greys r0004 (O5). Proven by `s11_r0004_probe2` 3/3 | exists (the guard's unit cases; the gate's ELF line) — no row |
  | 2 | matcher confidence; unresolved configuration refused | **TAKEN — a new issue** for the counterexample half; the refusal half met by the numbers | the body `logs/r6_issue_matcher_counterexamples.md` (`check-body` OK; the controller opens it, area `recomp`, no milestone, and adds the KNOWN §2 row drafted in `logs/r6_known_row.md`): `fingerprint.py:17` zeroes every `addiu`/`ori` immediate, a unique hash is labelled `exact`, repeated windows pair by order — no test holds any against a counterexample. Unresolved: `67e311e6` lists what the translator cannot place; `recomp/socom2_r0004.toml` now carries `unresolved_count = 2`, both `[mmio]` keys, no patch or stub selector | **becomes a backlog row** (the issue above) |
  | 3 | R5900/VU semantics: PMULTW, FTOI, a behaviour matrix | **TAKEN** by S13 C4 (master list D5; FTOI is #33) | `translatePMULTW` is still the open-coded unsigned sum (`mmi_translation_helpers.cpp:334`); C4's Step 1 now names it (this commit). The soft-double chain is R265's decline; the HLE consumer audit is S13 C2 (R265) | C4's RED-first cases are the check — no row |
  | 4 | audio measurement validity before mixer changes | **ALREADY DONE** in part, **TAKEN** for the rest, one sub-item **DECLINED** | done: `8357b55a` + `9d9da69e` + `71eec254` (a dip outside the trace is unattributable, alignment confidence carried and `LOW CONFIDENCE` under 0.50, contamination sampled into `sessions.csv` with a verdict, the trace's drop count in its file); taken: the recorder's overflow (`loopback_record.py:62`, `exception_on_overflow=False`, uncounted) is V5's next step (its row) and #42's KNOWN row; declined: process-specific loopback capture, S13-R7 | V5 Step 2's pin and #42's bar — no row |
  | 5 | guest-memory and network boundary review, private | **TAKEN** for the first subsystem; the rest stays the ruled-out row | U5 (the server memory-poke read, private note, done) and U6 (the client refuses the message); U2 is the filesystem side (#239's class); the whole network path's inventory is `docs/BACKLOG.md`'s `multiplayer-security` row (audit I5), private under SECURITY.md's process, so no public issue by design | U6's `ps2x_tests` case — no row |
  | 6 | Windows orchestration and verification contracts | **TAKEN** (the lock) and **ALREADY DONE** (provenance, claims) | waits in minutes and authoritative acquisition: S13 H2 (#35, #36, #37); revision provenance: `91f245fa` (the gate's ELF line) and `5cda6a51` (a mismatched run refuses), captures' environment `ace45bc3` (H3, #38 closed); claims against their records: R1's tag check `ec15ee20` (R268) and this plan's "every claim names its artefact" | H2's Step 1 cases (`--wait 3` at any poll interval; a planted competing taker) — no row |

  The audit's comparison measurement ("How to establish whether the allocation actually helps") is declined by
  S13-R6, not written as a backlog row. One backlog row results: the issue in row 2, which the controller opens.
- **2026-09-25 11:09Z.** The secrets workflow was red from `d94c55f1` (R4's merge) to `d162a228`: R4 archived the
  cloudflared certificate sentence and its allow row moved to the archive path alone, so the history leg found the old
  commits' copy at `docs/HUMAN_TASKS.md` — the second time a move lost its old-path row (after `05f35232`). Green again
  at `048c9772`. Rule for the record: a moved file keeps its allow rows at BOTH paths (DOC_MAINTENANCE §7 should say so;
  Task 99 checks).
- **2026-09-25 10:55Z.** R7 merged (`33fa5940`; issue #51 opened for the per-TU copies, KNOWN §2 cites it; BACKLOG 18
  open). U7 merged after one fix round (`d4a1eda7`), its gate folded into the combined proof. H4 merged and #46 closed.
  Fourteen merged worktrees removed. Dispatched lock-free: V8+S2, R6, H1, U4, C9. On the lock: U2's RED run (holder),
  U6's relink + gate queued behind it; H2's suite runs in its own temp dir. Next: the combined proof on the main tree
  once U6 and U2 are merged.
- **2026-09-25 09:30Z.** CI green on `sprint-13` at `5fadad9a` (Linux and Windows: the first build of the merged
  Sprint 11 + 12 tree plus V1's scorer) and skipped-as-docs at `ff97cdd1`. V5 Step 1: the first clean music capture
  (DEVICE 6 / 16 min, 0 late callbacks — not the device thread's; #42 commented). U1 merged after two rounds. R2+S1
  done on `agent/s13-r2`, in review. V1's gate on the lock; U7's three picks built and tested; U6 and R1 still working.
  C9 added (a knob's registry text describes the wrong effect, found by R2).
- **2026-09-25 08:45Z — the branch is open.** Sprint 12 merged as `v0.12.0` (`74fe2a9b`) behind Sprint 11's `v0.11.0`;
  `sprint-13` off it; the milestone and the six carried issues; the header. Running: U6 (the refusal, its tests under the
  lock), U7 (three picks, queued on the lock), R1 (the split), the U1 re-review; V1's scorer (`agent/s13-v1`,
  `79aad10c`+`f3dd7f37`, review clean after one round) merged here and its gate `s13_v1_gate` queued after the music
  capture (#42's quiet-window run, started 07:41Z, queued on the lock behind U6). The PR filter defect (H1) widened.
- **2026-09-25 (S6 fix round 1).** Review PASS WITH FINDINGS (two Important, two Minor), all fixed: per-file allow rows;
  `horizon-ctl.sh start/restart/check` refuse the RFC 5737 placeholder (test RED then GREEN); the runtime comment
  names no address; `start-servers.ps1 -CheckOnly` makes the real-address test prove something.
- **2026-09-25 (S6).** S6 done on `agent/s13-s6`: no private address as a tracked default -- the Horizon configs carry the
  RFC 5737 placeholder and `start-servers.ps1` refuses to start until `-PublicIp`; the launcher and its tests use
  documentation addresses; leakcheck `tree`/`staged` now report `tracked-private-ip` (RED 31 sites, GREEN 0). The
  launcher-tests build NOT RUN: the lock was U6's gate for the whole 6-minute wait (exit 75), so the combined proof
  builds it; merge is the controller's.
- **2026-09-25 night — the open.** The audit written (`docs/audits/2026-09-25-project-audit.md`, six reports, 403
  findings across them, 133 carry rows deduplicated); the spec and this plan written; R265–R268 ruled in the audit.
  Waiting only on Sprint 12's PR to merge to `main` (behind Sprint 11's #49, whose red suite this controller
  diagnosed and the Sprint 11 controller fixed) for the branch to open off `main`. Next: Task 0 Steps 2–4.
