# Sprint 15 Implementation Plan — "borrowed confidence" (OPEN 2026-09-26 16:57Z)

> **Re-cut 2026-09-26 (the owner's word): value first** — audio registered and tried, the drag freeze (#67) fixed,
> the frame-rate bar (#59) and the atlas re-upload (#32) if time allows, about three loop days; the walk list's other
> subsystems deferred to `docs/LATER.md` with a trigger each. The first proposal's shape is summarised at the end.

> Spec: `docs/superpowers/specs/2026-09-26-sprint-15-borrowed-confidence-design.md`. The origin:
> `docs/superpowers/plans/2026-09-25-borrowed-confidence-cloud-handoff.md`, the cloud research handoff that never
> ran, rewritten here for the owner's machine (no cloud session from 2026-09-26). Proposed 2026-09-26 while Sprint 14
> is open; **opens when Sprint 14's Task 99 closes** (Sprint 14 D8) — branch `sprint-15` off `main` at the Sprint 14
> merge. The loop runs this plan with the `loop-iteration` skill: one task at a time, a failing test first, review by
> a fresh agent, the lock for every build and run, the merged chain as the gate unit.

**Goal:** the audio subsystems we replaced in PS2Recomp have a row each with its confidence, today's number and the
experiment that would settle it; #254's LLE IOP is answered as a candidate; one audio trial runs to a recorded
outcome; the drag freeze (#67) meets its bar; then, if time allows, #59 gets a bar and #32 a measured trial. Every
other subsystem is a row in LATER with a trigger.

**Architecture:** two research notes (class S, dated) — the audio register and the audio survey — written by agents
from the tree, `logs/` and `gh`, reviewed by a fresh agent against their own commands; T1 written into this plan at a
checkpoint from the survey's shortlist; T2–T4 defect tasks with their bars from the backlog; each in its own agent
worktree with a RED test, a named measurement and a stop rule; every lock-bound step through the merged chain at a
quiet window; KNOWN moved with the evidence.

**Tech Stack:** Python 3 unittest, Git Bash via `tools_py/tests/shell.BASH`, `gh` against `ran-j/PS2Recomp` and the
DQ8 fork (read-only), `ps2xTest`, `tools_py/parity/gate.py`, `audio_dips.py`, `audio_parity.py`, `frame_time.py`,
`scripts/loop_lock.sh`, `scripts/agent_worktree.sh`, the Sprint 14 guards and skills.

## Global Constraints

- **Order:** R1, X1, T0, T1 (audio first); T2 (#67) beside it, from day one; T3 (#59) and T4 (#32) only after T1
  and T2 have a recorded outcome; L1 before the close (D5's boxes, D7). A task out of order needs a ruling that says
  what it jumped and why.
- **The register decides the scope:** no survey entry without a register row; no trial without a shortlist entry;
  no shortlist entry without a validation in our terms and the number today.
- **A failing test first**, unittest only; RED and GREEN pasted into the Log; a trial's measurement named, with
  today's value, before the trial's branch exists.
- **The lock:** one build or launch at a time through `scripts/loop_lock.sh run` / `run_detached.sh`; at most two
  building agents (Sprint 14 W1 enforces it); the lock-bound steps of a trial run at night through the merged chain
  (`scripts/parity/merged_chain.sh`, Sprint 14 W2), never a gate per trial; a new capture at a quiet window only.
- **Nothing derived from the disc enters the tree:** not a byte of `game/`, `recomp/output*/`, `logs/` or a
  recording; a count from the generated code is cited with its command. The private inputs are on this machine;
  nothing fetches them.
- **Commit with an explicit pathspec**, subject `type(scope): what and why` under 120 characters, the trailer, the
  leak check in the hooks; never `--no-verify`; push `origin sprint-15`; `gh run list --commit` after a code push.
- **Every claim names its artefact;** a research note's numbers name their commands; a disagreement with a prior
  verdict (research/42, 63) is stated with the evidence, never by editing the note.
- **KNOWN wins;** a finding that moves a row moves it in KNOWN in the commit that carries the evidence.
- **Rulings from the global counter only** (Sprint 14 D5); each moved default gets one at the open; no `S15-R` names.
- **Reviews:** every task by a fresh agent that did not write it (the `reviewer` definition, Sprint 14 G4), findings
  `file:line`, fix rounds to a clean re-review; diffs under `.superpowers/sdd/2026-09-26-sprint-15/` (git-ignored).
  A research note's review checks the claims against the commands it names.
- **Nothing that is the owner's** is performed; O rows gain a line, never an act; no upstream PR is filed (D3).
- **No cloud session.** Every task runs on this machine in an agent worktree; `gh` reads are host work too.

## Owner decisions — the defaults this plan proceeds on (spec §3)

Each default gets a ruling from the global counter at the open; none is numbered here.

| # | decision | default | ruling |
|---|---|---|---|
| D1 | the LLE IOP as a candidate for audio | yes as a candidate; the bar argued both ways; the owner decides the trial | R282 |
| D2 | a candidate that changes the generated image's shape | only with a full recomp, the re-derivation green, `--accept-pins` after a green run, a ruling | R283 |
| D3 | upstreaming | none from this sprint; what we could give back noted for O10 | R284 |
| D4 | Proven rows in the survey | excluded unless contradicted, the artefact named | R285 |
| D5 | time boxes | R half a day, X one day, T1 and T2 the second and third days, the close by the fourth; T3/T4 if time allows | R286 |
| D6 | the "overwhelming case" bar for a large take | stands; a TAKE over about five hundred lines needs a ruling | R287 |
| D7 | the order of the work | audio first, then the drag freeze, then the measurements (#59, #32) | R288 |
| D8 | the walk list's non-audio subsystems | deferred to LATER, not registered this sprint; VU1 deferred with its experiment — the owner's word of 2026-09-26 | R289 |

## The task table

Markers: **[A]** autonomous, lock-free; **[L]** lock-bound; **[O]** waits on the owner (never blocks).

| # | task | milestone | who | box | state |
|---|---|---|---|---|---|
| 0 | the open: branch, milestone, the sprint file's header, HANDOFF's line, the eight rulings, the Sprint 14 assumptions and LATER checked | — | [A] | the open | **DONE 2026-09-26 16:57Z**: `sprint-15` off `main` at `200f3287` (the Sprint 14 merge, `v0.14.0`, PR #68); the milestone #5 open; R282-R289 for D1-D8; the Sprint 14 assumptions checked (the Log); LATER present, the markers off |
| R1 | the audio register: the host mixer, snd989, lgaud, the IOP host's audio path, the SPU2 model — the dip counter over the existing recordings, the rows with today's numbers | R | [A] | half a day, day one | **DONE 2026-09-26 17:25Z** on `agent/s15-r1` (`8a41f6dd`: `docs/research/68-confidence-register.md`, five rows -- host mixer Believed, snd989 Believed, lgaud Believed, the IOP host's audio path Untested, the SPU2 model Believed -- each with its gap and experiment; the bar: 6 DEVICE dips over 16 minutes, max 2 a minute, on `audio_out_20260925_074147`; `cb_trace` 0 late 0 dry; 31/48 re-scored from the archive; §3: KNOWN's wired 14 reads 10 and the walking 47 reads 5 on today's scorer, the dips' true length unknown at one 50 ms step; 5 tests). Review PASS WITH FINDINGS (every number reproduced; five wording fixes in `496d78c1`); the three KNOWN rows carrying the old 14 corrected at `e62851ac`. Merged |
| X1 | the audio survey: #254's LLE IOP as a candidate, what the DQ8 fork does for sound, the licence, the MSVC state; the LLE question answered; a shortlist of at most three | X | [A] | one day, days one–two | **DONE 2026-09-26 18:02Z** on `agent/s15-x1` (`85fb4a75`: `docs/research/69-audio-survey.md`; #254's LLE kernel binds 42 of 989SND.IRX's 70 imports, 16 via libsd, and returns zero for the 12 `cdvdman`/`ioman`/`sifman` ones -- so the game's sound drivers cannot load a bank from the disc on it as it stands; its CI red on every toolchain; the shortlist: the LLE as an ORACLE out of the tree (a take needing R287's ruling and R282's owner word), AutoVol's integer step schedule reimplemented (254 vs 360 ticks), reverb and Gaussian interpolation borrowed from PCSX2; D1 argued both ways: as an oracle it contradicts nothing, as the product a detour). Review PASS WITH FINDINGS (every count re-run; three wording fixes in `e7960060`: the driver's behaviour after a refused read left to the spike, 254 ticks as a decomp reading, entry 1's R287 question for T0). Merged 18:10Z with the R284 note for #254's author in the drafts' NOT-UPSTREAM |
| T0 | the checkpoint: T1 written from X1's shortlist (a V1 first if its claim has no measurement) | T | [A] | at X1's end | **DONE 2026-09-26 18:13Z** (the controller): T1 written as the spike on the shortlist's first entry (the LLE IOP as an oracle; the owner has not decided D1, R282), T1b added from the second entry (the AutoVol integer step schedule, a REIMPLEMENT with a unit number and no owner word needed); no V1 (T1b's measurement exists) |
| T1 | the LLE IOP as an oracle: a spike (one lock-free day of reading, one build of the slice under the lock, a recommendation; the trial waits for the owner's D1 word) | T | [A] then [L] | days two–three | IN PROGRESS: the spike's one build (granted 19:27Z after ten exit-4 refusals from 18:29Z, 8 s) stopped on two missing `<cstdlib>` includes in the fork's slice (`kernel.cpp:328`, `spu2.cpp:73`), everything else compiling; the reading done: after a refused read 989SND.IRX fails cleanly (cause 61/62, the bank load answers 0, no retry loop -- Believed, from the disassembly); the "cdvdman callback form" of research/40 §9 is `sifman`'s `sceSifSetDmaIntr`; the glue ~660 lines in scratch. The controller amended the stop rule by one build at 19:34Z for N |
| T1b | the AutoVol integer step schedule: the trial (RED in `socom2_audio_tests.cpp`, 254 ticks not 360; the dip count and parity as the no-regression bars in the chain) | T | [A] then [L] | days two–three | IN PROGRESS 2026-09-26 18:17Z on `agent/s15-t1b` (the `implementer` definition; Steps 1-2; its build queues behind T2 and the owner's session -- the cap's rule) |
| T2 | #67 the drag freeze: presents flow through the move loop, or the back-pressure is decoupled from it; issue #67's bar | T | [A] then [L] | days one–three, beside T1 | **DONE (code) 2026-09-26 19:59Z** on `agent/s15-t2` (`c42ca7b8`: option (b) -- a flag set only between `WM_ENTERSIZEMOVE` and `WM_EXITSIZEMOVE` suspends the frame back-pressure and the GL token waits, byte-identical otherwise; a Win32 window-procedure subclass reports the loop's edges; two C++ cases RED then GREEN, the full suite green under the lock: 964/964 ps2x_tests, 3547 Python; `tools_py/parity/window_drag.py` the readout FROZEN/SLOWED/ADVANCING and the drag driver, 14 tests). Review (code-read) PASS WITH FINDINGS 20:05Z: the readout read an absent cb-trace field as +0 (a false clean unless `PS2X_AUDIO_CB_TRACE` is set), the driver's moved check fails a drag with contents-while-dragging off, DEVELOPING's count -- fix round sent, no build; recorded for Step 3: a readback that returns early during a drag leaves stale VRAM for a frame or two after `WM_EXITSIZEMOVE` (look at those frames), and the token-wait release has no unit case (Step 3 shows it). Step 3 (the drag in a mission, the control round) waits for a window the owner names |
| T3 | #59 the frame-rate bar: measure first (three quiet gates on one exe), then a bar as a KNOWN row or a test | T | [L] then [A] | if time allows | NOT STARTED |
| T4 | #32 the atlas re-upload: tile batching, the 80–133 ms/s as the measurement | T | [A] then [L] | if time allows | NOT STARTED |
| L1 | the lessons: the register from the Log, KNOWN moved, the next candidate or its LATER row, T3/T4 to LATER if not reached | L | [A] | day four | NOT STARTED |
| 99 | the close: §5 and §7, the register's final state, the PR, the tag | — | [A] then [L] | day four | NOT STARTED |

---

## Task 0: the open

**Files:** `docs/CURRENT_SPRINT.md` (header and an OPEN block), `docs/HANDOFF.md` §2 (the one "now" bullet), this
file's Log and Rulings, and this pair's LATER lines (the `docmaint: future` markers come off once LATER exists).

- [ ] **Step 1:** after Sprint 14's PR merges and its tag exists: `git checkout -b sprint-15 main`; `gh api` the
  milestone `Sprint 15`; the issues at carry 2 stay the owner's question.
- [ ] **Step 2:** eight rulings from the global counter (HANDOFF's line is the one home), one per D1–D8, each with
  the decision, the cost and the overturn.
- [ ] **Step 3:** the Sprint 14 assumptions checked and recorded in the Log, one line each: the fourth leg (E2), gate
  freshness (E4), the re-derivation job (E3), the merged chain template (W2), the WIP cap (W1), the skills (I2), the
  `reviewer` definition (G4). Any that did not land: the trials' bars fall back to the three-stage gate alone and the
  Log says so; nothing here waits on Sprint 14's leftovers.
- [ ] **Step 4:** LATER checked (drafted on `agent/s14-c3`): it exists on `sprint-15` and carries every non-audio
  row of the spec's §1.1 walk list with a confidence and a trigger or a dismissal with its reason, and VU1 with its
  experiment (spec §1.4). RED → GREEN: with the markers removed from this pair's LATER lines, `python -m
  tools_py.docmaint` is RED while LATER is absent and GREEN once it is merged; if LATER did not merge, the markers
  stay, the Log says so and L1 owes it.
- [ ] **Step 5:** the sprint file's header and OPEN block; HANDOFF §2's bullet; the next free research number read
  (`ls docs/research | sort -n | tail -3`); commit with the pathspec; push; `gh run list --commit` shows only
  `secrets` and `docs`.

**Verification:** `python -m tools_py.docmaint; echo $?` (OK and 0; the rulings line names the new highest).

## Task R1: the audio register

**Files:** `docs/research/<n>-confidence-register.md` (new; class S by the glob; the date in its first fifteen lines
and a "state at last commit" block at its top that every later task updates); `tools_py/tests/test_confidence_register.py`
(new).

- [ ] **Step 1 (RED):** `test_confidence_register.py`: the register's audio table has the five rows (the host mixer,
  snd989, lgaud, the IOP host's audio path, the SPU2 model); each confidence is one of Proven, Believed, Untested,
  Hazard; each confidence cell cites a KNOWN row or a test file; each number cell carries a backticked command; a
  header line names LATER for the non-audio rows. Run it while the note is absent: it fails; paste the run.
- [ ] **Step 2:** the instrument before its number: `python -m unittest tools_py.tests.test_audio_dips` green (the
  counter reads its planted cases). Then the dip counter over the existing mission recordings under `logs/`
  (lock-free): `python -m tools_py.parity.audio_dips <endpoint.wav> --dump <mix.wav> --log <game.log>` per capture,
  the recordings named by path, the DEVICE count per minute pasted with the command. Today's number to confirm or
  replace: 6 DEVICE dips over 16 minutes on `audio_out_20260925_074147` (KNOWN §2, #42's row). `audio_parity.py`'s
  31/48 cited with its run.
- [ ] **Step 3:** per row: the files and `git log --oneline 8736759..HEAD -- <file> | wc -l`; what we replaced and
  why; the validation (the tests under `third_party/ps2recomp/ps2xTest/src/` by file — `socom2_audio_tests.cpp`,
  `socom2_lgaud_tests.cpp`, `audio_cb_trace_tests.cpp` — whether the gate reaches it, the KNOWN rows by line); the
  confidence; the gap; the experiment; the issue (#42, #28). The RPC model read against
  `docs/research/36-989snd-decomp-audit.md`; the streamer's inference and what it rests on. The dip count the
  sprint's no-regression bar uses is written in the state block.
- [ ] **Step 4 (GREEN):** the test passes; paste the run. Review by a fresh agent: every number re-run from its
  command; every confidence checked against the KNOWN row it cites. Commit `docs(research): the audio confidence
  register -- five rows, today's dip count, the experiment per row (Sprint 15 R1)` with the note and the test.

**Verification:** `python -m unittest tools_py.tests.test_confidence_register tools_py.tests.test_audio_dips 2>&1 | tail -3`;
`python -m tools_py.docmaint; echo $?`.

## Task X1: the audio survey

**Files:** `docs/research/<n+1>-audio-survey.md` (new; class S; the "state at last commit" block);
`tools_py/tests/test_confidence_register.py` (a second case).

- [ ] **Step 1 (RED):** the second case: every shortlist entry of the survey names a register row that exists, a
  path (TAKE, REIMPLEMENT, BORROW THE IDEA, LEAVE), a licence file and a validation with a number; at most three
  entries. It fails while the survey is absent; paste the run.
- [ ] **Step 2:** start from `docs/research/67-external-sweep.md` §3 and do not repeat it; the forks table, the peers
  and the ten KEEP notes are out. Re-read #254 as it stands (`gh api repos/ran-j/PS2Recomp/pulls/254`, the files
  under `ps2xIOP/src/lle/`): whether the LLE kernel's imports cover what `989SND.IRX` and `989DSTRM.IRX` import (the
  import tables read from our own IRX copies, cited by command, nothing pasted); the SPU2 cores against our mixer
  (ADPCM, ADSR, the 240 Hz tick); the device-driven IOP clock against #42's holes and #28's degradation; what the
  DQ8 fork does for sound (LIBSD, SDRDRV, the Standard Kit, PCMPLAY; `PS2X_AUDIO_DUMP`); the licence file; the MSVC
  and GCC state re-read (`gh api 'repos/Sinan-Karakaya/PS2Recomp/actions/runs?per_page=5'`) and whether llvm-mingw
  could build it (no `ucontext.h`, research/67 C24).
- [ ] **Step 3:** the question under D1 argued for and against: does an LLE IOP running the disc's own IRX fit
  "audio native, the N64-recomp shape" or contradict it; the cost against our HLE at the RPC boundary; what it would
  settle (#42, #28, the streamer's inference) and what it would not. PCSX2's SPU2 or OpenGOAL's 989snd only where a
  register row's gap needs a reference, each with its licence file.
- [ ] **Step 4 (GREEN):** the shortlist of at most three, ranked; the case passes; paste the run. Review (the
  claims against the commands); docmaint green; commit `docs(research): the audio survey -- #254's LLE IOP answered,
  the DQ8 fork's sound, the shortlist (Sprint 15 X1)`.

**Verification:** `python -m unittest tools_py.tests.test_confidence_register 2>&1 | tail -3`;
`python -m tools_py.docmaint; echo $?`.

## Task T0: the checkpoint — T1 written from the shortlist

**Files:** this file (T1's section filled in the shape below; a `## Task V1` if T1's claim has no measurement).

- [ ] **Step 1:** T1 from the shortlist's first entry, in the shape below; a spike where X1 could not tell whether
  the design fits (a question, one lock-free day, a recommendation as its output, a second task conditional on it —
  `docs/research/14-gs-render-target-scale-spike.md`'s shape). If the owner has not decided D1's trial and the first
  entry is the LLE IOP, T1 is that spike and the trial waits for the owner's word.
- [ ] **Step 2:** where the claim has no measurement in our tools (for #28: the long in-mission capture scored
  against PCSX2), V1 first: the measurement under a planted case, reading a known number on a known input.
- [ ] **Step 3:** the table updated; commit `docs(sprint-15): T1 written from the audio shortlist (T0)`.

**Verification:** `python -m tools_py.docmaint; echo $?`.

## Task T1: the LLE IOP as an oracle -- a spike, not a trial  — settles register row "the IOP host's audio path"
Adoption path: TAKE, out of the tree (an oracle in research/40's comparison harness; nothing of the 3,311 lines enters the
tree; the harness glue and a `cdvdman` provider would, as research assets). Source: `Sinan-Karakaya/PS2Recomp@e42efbe`
`ps2xIOP/src/lle/`, `LICENSE` (GPL-3.0). **Why a spike (T0, 2026-09-26):** the shortlist's first entry is the LLE IOP and the
owner has not decided D1's trial (R282), so T0's rule makes T1 the spike and the trial waits for the owner's word; the
spike also asks the R287 question (the glue that enters the tree).
Measurement: none changes here -- the runtime is untouched, so the dip count stays at 6 (max 2 a minute) and audio parity at
31/48 by construction. The spike's number: of the mission replay's 13,044 answers the LLE oracle disagrees on N (today our
HLE disagrees on 12,643; the menus' order is 19 in 1,794). Stop rule: one lock-free day of reading plus one build of the
`lle/` slice with llvm-mingw under the lock; if the slice does not build with llvm-mingw, or 989SND.IRX does not reach its
first bank load on the oracle with a minimal `cdvdman` provider, the spike ends TRIED, NOT ADOPTED with the reason.
Files: `docs/research/70-lle-oracle-spike.md` (class S, the shape of `docs/research/14-gs-render-target-scale-spike.md`: a <!-- docmaint: future -->
question, a time box, a recommendation); out-of-tree: a clone of the PR's head under the scratch area, never under the
repository. Lock-bound steps: the one build of the slice, through `loop_lock.sh run --class build`.
- [ ] Step 1 (RED): the spike's test is its question written before the reading: `tools_py/tests/test_confidence_register.py`
      gains a case that the spike note, when present, carries a recommendation line (ADOPT AS ORACLE / NOT ADOPTED) and
      the N with its command; it fails while the note is absent (the run pasted).
- [ ] Step 2: the reading: what 989SND.IRX does after a refused disc read (research/69 §2 left it: `sceCdSync` 0 and
      `sceCdGetError` 0 read as success; a retry loop would spin) -- read from the decomp (research/36) and the IRX's
      import use by command, no bytes; the minimal `cdvdman` provider the oracle needs (read, sync, the callback form);
      the build of the slice with llvm-mingw (one lock run); the harness wiring in a scratch clone; the replay's N.
- [ ] Step 3: the recommendation and the R287 question answered (what would enter the tree, how many lines); the register
      row's confidence moved only if the oracle ran (Untested -> Believed with the artefact); the owner's D1 decision
      requested in one line of `docs/HUMAN_TASKS.md` O5's neighbourhood (a new row O20 only if the spike recommends the trial).
- [ ] Step 4: the outcome line in this Log; the note committed `docs(research): the LLE IOP as an oracle -- the spike (Sprint 15 T1)`.
Verification: `python -m unittest tools_py.tests.test_confidence_register 2>&1 | tail -3`; the build's last line; the replay's N line.

## Task T1b: the AutoVol integer step schedule  — settles register row "snd989"
Adoption path: REIMPLEMENT (research/36 item 4: the real IRX fades in 127 integer 7-bit steps; ours interpolates in doubles
over `ticksTotal`). Source: the decomp reading `docs/research/36-989snd-decomp-audit.md` Q5 (research/68 row 2; `Ziemas/989snd`
has no licence file, so nothing is copied -- the code is ours under `LICENSE`). **Why beside the spike (T0, 2026-09-26):**
the shortlist's second entry needs no owner word, changes no image shape (D2), has a unit-level number before any capture,
and is the register's named gap on the #28 row.
Measurement: the unit number first -- a 1.5 s fade lands in 254 ticks on the IRX's schedule and 360 on ours (research/69
entry 2, a reading of the decomp); adopted if `socom2_audio_tests.cpp`'s new case reads 254 on ours AND the no-regression bars
hold in the chain: the dip count at or below 6 (max 2 a minute, `audio_dips.py`, the command in research/68's state block)
and audio parity not below 31/48. Stop rule: if the step schedule changes the mission's parity below 31/48 or the dip count
above 6, TRIED, NOT ADOPTED with the numbers; the branch left unmerged with "throwaway" in its last subject.
Files: `third_party/ps2recomp/ps2xRuntime/src/lib/snd989_mixer.cpp` (`tickVolRamps`), `third_party/ps2recomp/ps2xTest/src/socom2_audio_tests.cpp`.
Lock-bound steps: `ps2x_tests` through `loop_lock.sh run --class build`; then the merged chain with a mission capture and the dump.
- [ ] Step 1 (RED): the case in `socom2_audio_tests.cpp`: a 1.5 s fade from full to silence steps the volume 127 times in
      integer 7-bit values and settles at tick 254 -- today it moves every tick and settles at 360; the failing run pasted.
- [ ] Step 2: the schedule in `tickVolRamps`; the suites; review by a fresh agent; fix rounds; GREEN pasted.
- [ ] Step 3 [L]: the merged chain at a quiet window: runtime, suites, the gate 3/3, the fourth leg, a mission capture with
      the dump, the dip count, audio_parity.
- [ ] Step 4: the outcome -- ADOPTED (research/68 row 2 and KNOWN's #28 row moved with the artefact) or TRIED, NOT ADOPTED
      (the numbers and the reason in the register and this Log). One line in the Log either way.
Verification: `ps2x_tests` through `./build.sh test` under the lock; the chain's gate summary line; the dip count's command.

## Task T2: #67 the drag freeze

**Mechanism** (KNOWN §1, #67's row): the Win32 modal move loop (`WM_ENTERSIZEMOVE` to `WM_EXITSIZEMOVE`) runs on the
presenting thread; the guest's frame back-pressure waits on those presents, so the guest clock stops while the title
bar is held — the render-thread stall class (`docs/HAZARDS.md`). **The fix**, one of: presents keep flowing through
the move loop (a timer the modal loop dispatches, presenting from inside it), or the back-pressure treats "the host
is in a move loop" as a released wait so the guest runs unpresented and catches up at `WM_EXITSIZEMOVE`. The agent
picks with a paragraph of reasoning in the Log.

**Files:** `third_party/ps2recomp/ps2xRuntime/src/lib/ps2_runtime.cpp` (the window loop),
`third_party/ps2recomp/ps2xRuntime/include/runtime/gs/gs_frame_backpressure.h`,
`third_party/ps2recomp/ps2xTest/src/gs_frame_backpressure_tests.cpp` (the case); a scripted drag and its readout,
`tools_py/parity/window_drag.py` and `tools_py/tests/test_window_drag.py` (new; the names are this plan's proposal).

- [ ] **Step 1 (RED):** two tests that fail today. (a) `gs_frame_backpressure_tests.cpp`: with the presenting side
  held in a simulated move loop for longer than the back-pressure's bound, the guest's wait returns within the bound
  (or presents are counted through the hold) — today it blocks. (b) `test_window_drag.py`: the readout over a
  pc-sampler log (planted lines, not a real log) says FROZEN when `t=`/`vsync=` stand still across the drag's window
  and ADVANCING when they move — the readout does not exist today. Paste both runs (the C++ one through
  `scripts/loop_lock.sh run <owner> --class build -- ./build.sh test`).
- [ ] **Step 2 (GREEN):** the fix and the readout; the drag driver (a title-bar drag of N seconds on the game's
  window by `SetCursorPos` and mouse events, stamped into the log); both tests pass; the suites; review by a fresh
  agent; fix rounds.
- [ ] **Step 3 [L]:** in the merged chain at a quiet window: a run with `PS2X_PC_SAMPLER` on and a scripted drag of
  ten seconds in a mission — `t=`/`vsync=` advance through it, the `[audio-trace]` counters unchanged (`cb_holes=0`,
  0 late, 0 dry); the gate 3/3 and the fourth leg; a control round with one instance dragged mid-round.
- [ ] **Step 4:** KNOWN §1's #67 row moved to fixed with the artefacts (the sampler trace, the gate summary, the
  control round's readout); issue #67 closed on its bar; commit.

**Verification:** `python -m unittest tools_py.tests.test_window_drag 2>&1 | tail -3`; `ps2x_tests` through
`./build.sh test` under the lock; the chain's gate summary line.

## Task T3: #59 the frame-rate bar — if time allows

**Files:** `scripts/parity/pins.json`, `tools_py/parity/gate.py` (the refusal, if a bar holds),
`tools_py/tests/test_gate_frame_time.py`, `docs/KNOWN.md` (§2's #59 row).

- [ ] **Step 1 [L] (measure first):** three gates back to back on one exe on a quiet host (no agents, the owner away,
  the quiet marker), each `FRAME mean= worst1s= n=` line read; set against the three saved stamps of three exes
  (23.15 / 32.41 / 21.86 ms mean) and the one-exe spread of 2026-09-25 (30 %, KNOWN §2).
- [ ] **Step 2 (RED):** only if the three agree within about 10 % of their median: a case in
  `test_gate_frame_time.py` where a mission stage over the ceiling fails the gate — today the pin is informational
  (S13-R3), so it passes; paste the failing run.
- [ ] **Step 3 (GREEN):** the ceiling (the median plus the measured spread) as a refusal in `pins.json`; the case
  passes; KNOWN's row moved with the three stamps. If they do not agree: no rule; the three numbers and the spread
  in KNOWN's row, and #59 to LATER with the trigger "a quiet host where three gates agree".

**Verification:** `python -m unittest tools_py.tests.test_gate_frame_time 2>&1 | tail -3`; the three gates' summary
lines.

## Task T4: #32 the atlas re-upload — if time allows

**Measurement:** the stats line's `upload=` on the login screen under `PS2X_GS_STATS=1`; today 80–133 ms/s on the
menus (KNOWN §2) and 106–115 ms per 60-call window on the login screen (Sprint 13 V2's A/B). Adopted if it halves or
better with the gate 3/3 (BACKLOG's bar); stop rule: under a 25 % drop after one batching design, TRIED, NOT
ADOPTED with the number.

**Files:** `third_party/ps2recomp/ps2xRuntime/src/lib/gs/gs_gl_backend.cpp` (the tile upload path),
`third_party/ps2recomp/ps2xRuntime/include/runtime/gs/gs_gl_upload_reasons.h`,
`third_party/ps2recomp/ps2xTest/src/ps2_gs_tests.cpp` (the case).

- [ ] **Step 1 (RED):** a case in `ps2_gs_tests.cpp`: N adjacent 16x16 tiles of one atlas uploaded in one frame are
  coalesced into fewer uploads than tiles (counted by the reasons header's counters, no GL context needed) — today
  one per tile; paste the failing run.
- [ ] **Step 2 (GREEN):** the batching (adjacent `changed` and `same_rewritten` tiles of one texture merged into one
  rectangle per frame); the case passes; the suites; review.
- [ ] **Step 3 [L]:** in the merged chain: the login screen's `upload=` before and after on the same exe pair; the
  gate 3/3 and the fourth leg; the outcome recorded either way; KNOWN's #32 rows moved.

**Verification:** `ps2x_tests` through `./build.sh test` under the lock; the stats line's two readings; the gate
summary.

## Task L1: the lessons

**Files:** the register (its "state" block and the rows the trials touched), `docs/KNOWN.md` (the rows moved), LATER
(T3/T4 if not reached; the next candidate if it waits past Sprint 15), this file.

- [ ] **Step 1:** what T1 and T2 taught, one paragraph each; the register's confidence column against KNOWN, row by
  row; the audio shortlist re-ranked.
- [ ] **Step 2:** the next candidate named for Sprint 16, or its row in LATER with a trigger; T3/T4 not reached go
  to LATER with their measurement and a trigger; commit.

**Verification:** `python -m unittest tools_py.tests.test_confidence_register 2>&1 | tail -3`;
`python -m tools_py.docmaint; echo $?`.

## Task 99: the close

- [ ] **Step 1:** the `sprint-close` skill: DOC_MAINTENANCE §5 and §7; the generated pages regenerated and `--check`
  green; the register's final state block; T1 in one of the two outcomes; T2 at its bar or its remainder recorded.
- [ ] **Step 2:** the close chain at a quiet window: recomp, runtime, suites, the gate 3/3 PINS MATCH, the fourth
  leg, audio parity (not below 31/48), the dip count (not above the register's number), the release archive,
  PLAYTEST's block.
- [ ] **Step 3:** the Outcome section (the rows moved, the tasks by outcome, the numbers before and after); the PR
  `sprint-15 → main` built on its head; the tag `v0.15.0`; the CLOSED block; HANDOFF's line; the next sprint named
  from the register's remaining non-Proven rows, LATER's triggers that fired and the backlog's visible defects.

**Verification:** the gate's summary line; `python -m tools_py.docmaint; echo $?`; `gh run list --commit <head>`.

## Rulings made on the owner's behalf

- **R282 (2026-09-26 16:57Z, Task 0, D1) — the LLE IOP running the disc's own IRX is a candidate for the audio trial, with its bar argued both ways in X1; the owner decides whether it is tried.** Cost: a candidate that could replace the 989snd HLE outright is judged before it is built; what it buys is the one credible outside answer to #42/#28 on the table. The owner can overturn it.
- **R283 (2026-09-26 16:57Z, Task 0, D2) — a candidate that changes the generated image's shape runs only with a full recomp, the re-derivation job green, `--accept-pins` after a green run, and a ruling.** Cost: a shape change costs a chain and a pin update; what it buys is the gate's pins never drifting silently under a trial. The owner can overturn it.
- **R284 (2026-09-26 16:57Z, Task 0, D3) — no upstream PR or issue is filed from this sprint; what we could give back is written into `docs/UPSTREAM.md` for row O10.** Cost: upstream waits; what it buys is the owner's control over every public act, in their words of 2026-09-26. The owner can overturn it.
- **R285 (2026-09-26 16:57Z, Task 0, D4) — register rows that are Proven today are excluded from the survey unless a source contradicts the proof, and then the artefact it contradicts is named.** Cost: a contradicted proof is missed if nobody names it; what it buys is a survey of the unsure rows only. The owner can overturn it.
- **R286 (2026-09-26 16:57Z, Task 0, D5) — the time boxes are R half a day, X one day, T1 and T2 the second and third days, the close by the fourth; T3 and T4 only if time allows; a phase past its box hands its rows to LATER.** Cost: a phase that runs over drops work to LATER rather than the sprint; what it buys is a sprint that ends when the owner expects. The owner can overturn it.
- **R287 (2026-09-26 16:57Z, Task 0, D6) — the "overwhelming case" bar for a large take stands: a TAKE over about five hundred lines needs a ruling of its own.** Cost: a good large take waits for a ruling; what it buys is no wholesale merge under a trial's name. The owner can overturn it.
- **R288 (2026-09-26 16:57Z, Task 0, D7) — the order of the work is audio first, then #67 the drag freeze beside it, then the measurements #59 and #32.** Cost: VU1 and the rest wait in LATER; what it buys is the visible defects first, the standing order resumed (R276). The owner can overturn it.
- **R289 (2026-09-26 16:57Z, Task 0, D8) — the walk list's non-audio subsystems are deferred to `docs/LATER.md` and not registered this sprint; VU1 is deferred there with its experiment written.** Cost: a latent defect in a deferred subsystem is found later, not now; what it buys is the week the first proposal cost, on the owner's word of 2026-09-26. The owner can overturn it.


## Outcome

*(written at the close)*

## What the first proposal had and why it was cut

The first proposal (this file until the re-cut, the same day) ran R, X, T, V and L over the whole walk list in
about seven loop days: R1 a register row for every replaced subsystem, R2 a VU1 paragraph with the replay run here,
R3 the audio paragraph with the dip counter; X1 a fresh sweep of upstream, every fork and the peers, X2 #254 in depth
for both VU1 and audio, X3 emulators as references for every gap, X4 an adoption framework with an agentic glance and
a shortlist of ten; T0 writing a trial per shortlist entry, at least three trials, the first two on VU1 and audio; V
tasks where a measurement was missing; L1 after the first three trials; seven rulings (D1–D7), D7 "the two named
subsystems first". It was cut on the owner's word of 2026-09-26 — "a week of planned work ... order it by perceived
value and time ... we don't need to blindly test everything ... if confident, some can be dismissed outright":
registering every subsystem is the blind test; VU1 has no visible defect but #47; Sprint 14 X1 (research/67) had
already swept the forks and the peers; and #67, #59 and #32 are what a player meets now. R2, R3 and X2 live on as
R1 and X1's audio halves; X3 and X4 shrink to X1's references and its shortlist of three; nothing the first proposal
would have registered is dropped — every non-audio row and VU1's experiment are in LATER with a trigger, or dismissed
there with the reason.

## Log (newest first)

- **2026-09-26 20:00Z — the owner's sitting (the HUMAN_TASKS session, the owner at the desk) takes the ruling numbers 290 to 297 on a docs branch
  off `main`: O1 (#70), O3, O4, O5 (#71, #72), O6/O13 (no nightly ladder; agent code reviewed by a Fable-class reviewer with
  high confidence or confirmed by tests may merge to `main` without human review until the owner calls stability), O8,
  O11/O16/O18 (#75), and the last of them: heavy lock-bound work is announced as a window by the session that runs it, the owner names
  game-run windows. This sprint allocates from 298; HANDOFF's counter line says so and moves when that branch merges here.**
- **2026-09-26 19:47Z — the owner's word (through the HUMAN_TASKS session, the owner at the desk): the machine is at its
  memory limit (0.5 GB free of 32 with three builders queued); lock-bound work is communicated as a window from now on,
  and the owner's `owner-psrewired-fetch` run (issue #71) goes before any build of the loop's. Applied: T2 keeps the lock
  to its build's end; T1b's and T1's queued tickets dropped (their waiters killed, the agents told not to re-queue); the
  window offered: after the fetch releases, T1b's RED then GREEN builds and T1's 8-second build, one at a time, `-j 4`,
  only above 3 GB free, about 20:15Z-21:00Z; no game run of the loop's tonight (T2's chain waits for a named window).**
- **2026-09-26 19:34Z — T1's stop rule fired on the one build: not on the question but on two `<cstdlib>` includes the fork's
  slice lacks under llvm-mingw; the controller grants one more 8-second build for N (the spike's value). The WIP cap's first
  refusals: T1's build ticket got exit 4 ten times from 18:29Z while T2's and T1b's build tickets were live -- W1's watch
  has its first data (the cap refuses a third builder as designed). The queue log holds 11 stamped ticket waits.**
- **2026-09-26 18:13Z — X1 merged (`6d5f53cc`); T0 done by the controller: T1 is the spike (the LLE IOP as an oracle -- the
  owner has not decided D1, and the entry is a 3,311-line take with an R287 question), T1b the AutoVol integer step schedule
  (a REIMPLEMENT with a unit number, no owner word needed). The queue log's first stamped ticket waits landed today: the
  owner's other session's `agent-launch-rev` 136 s and `owner-psrewired-probe` 496 s (class run), T2's build 797 s (class
  build) -- W2 step 3's line is met on the branch's first night; no exit 4 yet (W1's watch).**
- **2026-09-26 17:39Z — R1 merged (`39d5c0ab`): the five audio rows, the bar 6 DEVICE dips / 2 a minute; KNOWN's three rows with
  the wired 14 corrected to 10 + 4 NODUMP (`e62851ac`, rule 11). X1 dispatched. T2 building its RED case under the lock
  (queued behind the owner's other session's `agent-launch-rev` build for issue #69 -- the cap's second builder).**
- **2026-09-26 16:57Z — OPEN.** `sprint-15` off `main` at `200f3287` (Sprint 14 merged as `v0.14.0`, PR #68, 16:55Z); the GitHub
  milestone `Sprint 15` (#5) opened at the Sprint 14 close; R282-R289 issued for D1-D8. The Sprint 14 assumptions, one line
  each: the fourth leg landed (E2: 12/12 in the close chain's record, `s14_close1`); gate freshness landed (E4: exit 5 on a
  stale exe); the re-derivation job landed (E3: `recomp-ref` green on the branch, red once on a plant); the merged chain
  template landed (W2: two ALL GREEN chains on 2026-09-26); the WIP cap landed (W1: live, its two-night watch continues
  here -- every exit 4 to this Log); the four skills landed (I2); the `reviewer` definition landed (G4). Nothing fell back.
  LATER checked: `docs/LATER.md` on this branch holds the non-audio walk-list rows with a confidence and a trigger or a
  dismissal, and VU1 at row 5 with its experiment; the markers came off this pair. The next free research number is 68.
- **2026-09-26 — RE-CUT, still PROPOSED** on the owner's word ("order it by perceived value and time"): audio first,
  the drag freeze beside it, #59 and #32 if time allows, about three loop days; the walk list's other rows to LATER
  (D8); not open; Sprint 14's Task 99 first.
- **2026-09-26 — PROPOSED** from the never-run cloud handoff, rewritten for the owner's machine (no cloud session
  from this day; Sprint 14 D8); not open; Sprint 14's Task 99 first.
