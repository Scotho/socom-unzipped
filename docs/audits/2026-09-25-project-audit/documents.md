# Audit area: documents — 2026-09-25

Scope: every file under `docs/` (research/, superpowers/, archive/ only as far as their indexes and citations),
`README.md`, `CONTRIBUTING.md`, `SECURITY.md`, `THIRD_PARTY_NOTICES.md`, and the registry and rules in
`docs/DOC_MAINTENANCE.md`. Read on `C:\projects\wt-s12` (branch `sprint-12` at `eb190a42` plus the uncommitted
Sprint 12 close). Line numbers are this worktree's. The Sprint 12 close's rewrite of CURRENT_SPRINT's header and its
two CLOSED blocks are not re-reported, except where a field inside the header survived the rewrite stale.

External state read with read-only commands on 2026-09-25: `git ls-remote --tags origin` lists `v0.10.0` as the
newest `v0.x` tag (no `v0.11.0`); `git merge-base --is-ancestor origin/sprint-11 origin/main` fails (Sprint 11 is
**not** in `main`); `origin/main` is `e63f9ba9` "Merge pull request #44"; `gh pr list --state all` shows no Sprint 11
PR; `gh release list` shows one release, "SOCOM Unzipped v0.10.0 Draft". `gh issue list --label known-issue` shows
21 open issues (#25–#48), none labelled `help wanted` or `good first issue`.

## 1. Findings

| # | item | class | who | cost | evidence | why it matters |
|---|---|---|---|---|---|---|
| 1 | Four live documents say Sprint 11 is merged to `main` as `v0.11.0`. It is not: no `v0.11.0` tag exists on origin, `origin/sprint-11` is not an ancestor of `origin/main`, and there is no Sprint 11 PR. | REVISION | LOCKFREE-AGENT | S | `docs/HUMAN_TASKS.md:42` "Sprint 11 is merged to `main` as `v0.11.0`"; `docs/STATUS.md:5` "Sprint 11 CLOSED and merged to `main` as `v0.11.0`"; `docs/HANDOFF.md:56` "Sprint 11 is CLOSED and on `main` as `v0.11.0`"; `docs/GIT_STRATEGY.md:100` "(`v0.9.0`, `v0.10.0`, `v0.11.0`)"; the pushed `origin/sprint-11:docs/CURRENT_SPRINT.md:56` "merged to `main` as `v0.11.0`" | The documents record the one step of the close that has not happened yet, so nobody is prompted to do it |
| 2 | STATUS's newest dated entry still contains an unfilled template placeholder. | REVISION | LOCKFREE-AGENT | S | `docs/STATUS.md:46` "the \"Sprint 12 — CLOSED\" block), {COUNTS_AND_R0004} and the PR to `main`" | A literal `{...}` template token is about to be committed into the project's log |
| 3 | DEVELOPING says `build.sh test` runs no Python tests. `build.sh` does run them, and the same file says so 140 lines later. | REVISION | LOCKFREE-AGENT | S | `docs/DEVELOPING.md:120-121` "It runs NO Python tests: # python -m unittest discover ... (by hand, for now)"; `build.sh:140` `python -m unittest discover -s tools_py/tests -t . -v`; `docs/DEVELOPING.md:261` "`./build.sh test --no-runner` runs both suites"; `docs/KNOWN.md:476` "~~`build.sh test` runs zero Python tests.~~ Fixed Sprint 5" | The developer reference contradicts itself and the script on the most-run command |
| 4 | DEVELOPING gives the guest-clock default backwards. KNOWN says wall time is now the default and `=1` restores the exclusion for an A/B. DEVELOPING says the default (1) excludes and `=0` stops it. | REVISION | LOCKFREE-AGENT | S | `docs/DEVELOPING.md:442-445` "`PS2X_CLOCK_EXCLUDE=0` stops subtracting ... the default (1) is the 2026-09-08 behaviour"; `docs/KNOWN.md:117` "the scheduler counts wall time by default now (`PS2X_CLOCK_EXCLUDE=1` for an A/B)" | Someone following the knob text inverts the one A/B that explains online freezes |
| 5 | Two paragraphs in DEVELOPING are spliced together. The `--rounds N` flag sentence breaks off, the window-size knob and the whole launcher paragraph are inserted, and "(default 4) plays N rounds" resumes 13 lines later. | REVISION | LOCKFREE-AGENT | S | `docs/DEVELOPING.md:453` "`online_match_ours.py` flags from the Sprint 5 engagement ladder (Amendment A): `--rounds N`" → `:454` `PS2X_WINDOW_SIZE=...` → `:467` "(default 4) plays N rounds on one lobby success" | The ladder's flag reference cannot be read as written |
| 6 | DEVELOPING's knob paragraph still lists a knob that was removed, and still says knobs are found "under `getenv`", which Q2 abolished. | REVISION | LOCKFREE-AGENT | S | `docs/DEVELOPING.md:314-316` "other behaviour-changing knobs exist under `getenv(\"PS2X_` ... `PS2X_GS_TEX_FROM_CPU`"; `docs/HANDOFF.md:315-316` "Five names are gone ... `PS2X_GS_TEX_FROM_CPU`" | It sends a contributor looking for a knob and an access pattern that no longer exist |
| 7 | DEVELOPING's recomp timing is off by a factor of about 30, and the same file measures it elsewhere. | REVISION | LOCKFREE-AGENT | S | `docs/DEVELOPING.md:113` "`./build.sh recomp` ... (~10 s)"; `docs/DEVELOPING.md:268` "`build.sh recomp` **352 s**"; `docs/KNOWN.md:24` "`./build.sh recomp` 273 s" | A newcomer reads a correct run as a hang |
| 8 | DEVELOPING's launcher paragraph describes the pre-redesign launcher. It names "Copy diagnostics", "Native / Sharp (2x) / Sharper (3x)" and cards in `game/disc/mc0`, while the launcher has had a rail, SAVE DIAGNOSTICS and `cards/<profile>` since Sprint 8/9. | REVISION | LOCKFREE-AGENT | S | `docs/DEVELOPING.md:464` "Copy diagnostics puts the last log and `config.json` under `diagnostics/`"; `:513` "memory cards live in `game/disc/mc0`"; `docs/INSTALL.md:67-68` (the rail), `:166` SAVE DIAGNOSTICS | The developer reference disagrees with the player page on the same program |
| 9 | HANDOFF's first hour points a new controller at the closed Sprint 11 plan, and at `sprint-11`'s CI. | REVISION | LOCKFREE-AGENT | S | `docs/HANDOFF.md:113` "`gh run list --branch sprint-11 --limit 3`"; `:124-125` "begin at the first open item of the current plan's task list (`docs/superpowers/plans/2026-09-23-sprint-11.md`)" | The file every new controller reads first starts them on a finished plan |
| 10 | HANDOFF rule 4 hard-codes two sprint numbers in the same sentence that forbids hard-coding one. | REVISION | LOCKFREE-AGENT | S | `docs/HANDOFF.md:180-181` "`sprint-10` through its close, `sprint-11` after it). Never hard-code a sprint number here" | This is the rotting pattern the rule was written against |
| 11 | HANDOFF §2's "Plays" and state bullets are two sprints stale: "Sprint 11 is open ... closing 2026-09-25", Sprint 9's detail, and "Landed but not closed out: Goal 8 ... see P5". | REVISION | LOCKFREE-AGENT | S | `docs/HANDOFF.md:40-46` "Sprint 11 is open on branch `sprint-11` and closing 2026-09-25 ... Landed but not closed out: Goal 8" | §2 is "where it stands", and it contradicts the LATEST bullet 15 lines below it |
| 12 | Three HANDOFF bullets are headed "Where the loop is now", dated 09-25, 09-23 and 09-20. The stack is also not in date order (09-20 evening, then 09-21 03:00, then 09-20 19:40, then "09:00 UTC" with no date). | REVISION | LOCKFREE-AGENT | S | `docs/HANDOFF.md:55` "is now (2026-09-25 morning, LATEST)", `:58` "is now (2026-09-23 05:10Z)", `:104` "is now (2026-09-20 19:40 UTC)", `:105` "at 09:00 UTC" | A reader skimming for "now" finds three answers |
| 13 | HANDOFF says nobody else is in the tree "as of 2026-09-19", and says there is no ledger, while §8 of the same file describes the agent worktrees and their `progress.md` ledgers. | REVISION | LOCKFREE-AGENT | S | `docs/HANDOFF.md:106` "Nobody else is known to be in the tree as of 2026-09-19"; `:290-291` "There is no scheduler and no ledger ... `.superpowers/sdd/` holds only a `.gitignore`"; `:382` "`.superpowers/sdd/<plan>/progress.md` is the ledger" | Contradictory orientation facts in the start-here file |
| 14 | HANDOFF §4 "The work, in order" still lays out Sprint 9's milestones P and Q as the order, and §10 lists decisions to make "before the playtest". The playtest happened on 2026-09-22 and Sprints 9–12 are closed. | REVISION | LOCKFREE-AGENT | M | `docs/HANDOFF.md:131-146` "Sprint 9, milestone P ... ends in the tag `playtest-1`"; `:415` "What the owner should decide before the playtest"; `:109` "A playtest by the owner is planned" | Most of §4 and §10 describes the past as instructions |
| 15 | The rows of HANDOFF §9 are stale against HANDOFF's own later text: "Next: the persona measurement" (withdrawn by R175 in §10), "confirm at P5", "Scheduled", and "Designed and scheduled; early files landed" for the public flip (done 2026-09-20). | REVISION | LOCKFREE-AGENT | S | `docs/HANDOFF.md:400` "Landed; confirm at P5"; `:410` "Next: the persona measurement, then the launcher's default"; `:412` "Scheduled"; `:413` "Designed and scheduled; early files landed"; `:422` "~~The persona switch~~ -- withdrawn 2026-09-19" | The owner's-words table is the audit trail of their asks, and a third of it is stale |
| 16 | HANDOFF trap 6 and a KNOWN §4 hazard say the raw-address preset stays on offer as a fallback. KNOWN §1 and the code say it was removed on 2026-09-20. | REVISION | LOCKFREE-AGENT | S | `docs/HANDOFF.md:276-277` "which is why the raw address stays on offer as a fallback"; `docs/KNOWN.md:254` "(b) Keeping the raw address as a visible fallback preset is worth doing"; `docs/KNOWN.md:40` "until the owner had it removed on 2026-09-20"; `launcher_config.h:106` "`unzipped-ip` ... removed 2026-09-20" | The DNS-failure mitigation the hazard relies on no longer exists; only Custom remains |
| 17 | HANDOFF rule 8 and HUMAN_TASKS say the community preset stores PSRewired's address. In the code the preset's address is a placeholder. | REVISION | LOCKFREE-AGENT | S | `docs/HUMAN_TASKS.md:698` "the launcher's community preset only stores the address (67.222.156.250)"; `docs/HANDOFF.md:201` "The community server (PSRewired) preset stores an address"; `launcher_config.h:95` `"COMMUNITY_SERVER_ADDRESS_TBC"`; `docs/HUMAN_TASKS.md:529` "the community server's (`COMMUNITY_SERVER_ADDRESS_TBC`)" | Two documents disagree with the source, and HUMAN_TASKS disagrees with itself |
| 18 | Several HANDOFF facts are stale: research notes "01-34" (they run to 61); the archive recipe "once Goal 3 lands" (it landed 2026-09-21); STATUS "2400 lines" (2504); trap 13, the Goal 3 plan's drift (the plan ran). | REVISION | LOCKFREE-AGENT | S | `docs/HANDOFF.md:301` "`docs/research/` (tracked, notes 01-34)"; `:445` "`docs/research/01-34`"; `:350-351` "once Goal 3 lands"; `:285` "2400 lines"; `:295` "The Goal 3 plan was written against the tree at `8e5d778`" | Small errors, but in the file a stranger is told is complete |
| 19 | HANDOFF §11 calls PLAYTEST, LOOP_PROMPT, GIT_STRATEGY and DOC_MAINTENANCE "Live". The registry classes them as C. | REVISION | LOCKFREE-AGENT | S | `docs/HANDOFF.md:440-443` "**Live, kept in step by the controller:** ... `docs/PLAYTEST.md` ... `docs/LOOP_PROMPT.md`, `docs/GIT_STRATEGY.md`, `docs/DOC_MAINTENANCE.md`"; `docs/DOC_MAINTENANCE.md` §3 rows ("**C**") | Two homes for the class of four documents, and they disagree |
| 20 | CURRENT_SPRINT's header block still carries a `sprint 10:` field about the Sprint 10 opening ("Goal 1 built, first run tonight"). The Sprint 12 close's header rewrite left it in. | REVISION | LOCKFREE-AGENT | S | `docs/CURRENT_SPRINT.md:32-37` "sprint 10: OPENED 2026-09-20 ... (Goal 1 built, first run tonight)" | The header is "the only sprint pointer in the project" (`docs/LOOP_PROMPT.md:75-76`) |
| 21 | CURRENT_SPRINT's "Standing rules" still say to push to `sprint-9`. | REVISION | LOCKFREE-AGENT | S | `docs/CURRENT_SPRINT.md:922` "Push to `origin sprint-9` and check CI." | This duplicates HANDOFF §5 and contradicts it |
| 22 | CURRENT_SPRINT says research/40 lives only on `agent/upstream`. It is in the tree, and KNOWN cites it as merged. | REVISION | LOCKFREE-AGENT | S | `docs/CURRENT_SPRINT.md:356-358` "R243 cites `docs/research/40-upstream-divergence.md`, which lives on branch `agent/upstream` ... has not been merged forward"; `:430` "Its citation ... is on `agent/upstream`"; file exists; `docs/KNOWN.md:22` "`agent/upstream` merged at 45e5fce" | A resolved citation is recorded as dangling |
| 23 | CURRENT_SPRINT's worktree paragraph and HANDOFF §8 disagree on the same three worktrees. `git worktree list` sides with HANDOFF. | REVISION | LOCKFREE-AGENT | S | `docs/CURRENT_SPRINT.md:220` "`C:/Projects/wt-cherry` (`agent/cherry` ...), `C:/Projects/wt-ci-fix` (`agent/ci-fix`, the red-suite fix, live)"; `docs/HANDOFF.md:383-384` "`wt-cherry` (`agent/picks-keep`, merged ...), `wt-ci-fix` (`agent/ci-fix`, merged)"; `git worktree list`: `wt-cherry f7a7e206 [agent/picks-keep]` | The same fact has two homes, and one of them is wrong |
| 24 | CURRENT_SPRINT cites KNOWN by line number (`KNOWN.md:110(e)`, `:110(c)`, `:110 a, b, d`). Line 110 is now the glyph-atlas row. The review items are at line 150. | REVISION | LOCKFREE-AGENT | S | `docs/CURRENT_SPRINT.md:464` "`KNOWN.md:110(e)`", `:483` "`KNOWN.md:110(c)`", `:489` "`KNOWN.md:110` a, b, d"; `docs/KNOWN.md:150` "(a) ... (e) `pcmStreamStart`"; `docs/KNOWN.md:189` "Cite item names and dated entries, never `file:NN`" | KNOWN's own rule is broken, and the pointer lands on an unrelated row |
| 25 | ROADMAP (class N) cites KNOWN and STATUS by line number, and every such line has moved. | REVISION | LOCKFREE-AGENT | S | `docs/ROADMAP.md:32` "`STATUS.md:474` and `:550`"; `:33` "`KNOWN.md:119`" (the transition-strip row is now `KNOWN.md:133`); `:34` "`KNOWN.md:120`" (now `:134`); `:114` "`KNOWN.md:206`" (now a harvest hazard) | The audit table that justified the rewrite now points at the wrong evidence |
| 26 | Several parts of CURRENT_SPRINT's historical blocks still read as live: "run 2 in flight", "running", "queued", "Next, in order: Task 6 Step 3" (done 2026-09-25), and "stands at 2/3" (3/3 by 16:30Z). | REVISION | LOCKFREE-AGENT | S | `docs/CURRENT_SPRINT.md:555` "run 2 in flight"; `:563` "running"; `:567` "queued"; `:244` "**Next, in order:** **Task 6 Step 3**" | These are the "live-looking" rows the archive split (§2) would retire |
| 27 | KNOWN's audit stamp is two days old, although rows were edited and a row was promoted at both 2026-09-25 closes. | REVISION | LOCKFREE-AGENT | S | `docs/KNOWN.md:8` "Last audited: **2026-09-23 (Sprint 10's close ...)**"; `docs/KNOWN.md:17` "(Promoted from section 2 to section 1 on 2026-09-25 at the Sprint 11 close" | KNOWN is "the one that wins", and its freshness line understates it |
| 28 | Two KNOWN §4 hazards end with "No issue: not yet on the stack -- it needs one", but their own headlines cite issues opened at the Sprint 11 close. | REVISION | LOCKFREE-AGENT | S | `docs/KNOWN.md:207` headline "*(issue #45)*" … end "*No issue: not yet on the stack -- it deserves one of its own*"; `docs/KNOWN.md:500` "*(issue #46)*" … `:505` "*No issue: not yet on the stack -- it needs one*"; `docs/CURRENT_SPRINT.md:115` "three at the close (#45 ... #46 ...)" | The issue audit and a reader get contradictory answers from one row |
| 29 | A KNOWN §2 "scope" row still says the recompile step is Windows-only. Sprint 12 ran `ps2_recomp` on Linux. | REVISION | LOCKFREE-AGENT | S | `docs/KNOWN.md:140` "What is still Windows-only after the Linux port ...: the recompile step"; `docs/CURRENT_SPRINT.md:71` "`ps2_recomp` on Linux in 52 s" | A Linux contributor is told they cannot do what the cloud session did |
| 30 | KNOWN §2 rows name defects that Sprint 10 Q7 fixed as still open: the Linux `version.txt` test, and review items (b) and (d). | REVISION | LOCKFREE-AGENT | S | `docs/KNOWN.md:147` "the Linux packaging branch's `version.txt` has no test"; `docs/KNOWN.md:150` "(b) `micFramesNeeded` consumes one input frame too many ... (d) the card's 8000-cluster size is not enforced"; `docs/CURRENT_SPRINT.md:565` "the Linux packaging branch tested ... the mic feed's lost frame ... fixed; the card's 8000-cluster cap enforced" | The believed list overstates what is open |
| 31 | Four KNOWN §4 hazards describe states since fixed and are not superseded: the gate pins "only the binary" (Q1b fixed it), `PS2X_LAUNCHER_API_BASE` "not yet counted" (R205), the gate "host-load sensitive" because waits are excluded (R81), and "Fix planned in ROADMAP §6 item 5 ... cause still open" for the water (fixed). | REVISION | LOCKFREE-AGENT | S | `docs/KNOWN.md:258` "The gate records WHICH BINARY it ran and nothing else"; `:249` "a `PS2X_*` name that Goal 3's inventory does not yet count"; `:377` "(back-pressure excludes wait time from guest time, R41)"; `:268-271` "(cause still open ...) ... Fix planned in ROADMAP §6 item 5" | They are hazards that no longer bite, filed in the section the loop is told to heed |
| 32 | A KNOWN §4 bullet still states that the gate's mission stage "now FAILS on the current tree ... Fix in progress (R35)". Only its headline is struck. | REVISION | LOCKFREE-AGENT | S | `docs/KNOWN.md:398` "**The gate mission stage now FAILS on the current tree** ... Fix in progress (R35, bounded backpressure)"; `docs/KNOWN.md:567` "Striking a claim's headline leaves its consequences standing" | This is KNOWN's own named anti-pattern, inside KNOWN |
| 33 | A KNOWN §1 row says its second launch is "pending", eleven days later. | REVISION | LOCKFREE-AGENT | S | `docs/KNOWN.md:43` "second launch `s6_ladder9` pending for the plan's 2-of-2 bar" | A proven row carries an open-looking tail |
| 34 | Several documents point to "§1's first row" for the endpoint A/B. It is now row 8 of §1. | REVISION | LOCKFREE-AGENT | S | `docs/FAQ.md:237` "is now `docs/KNOWN.md` §1's first row"; `docs/KNOWN.md:37` "(the first row of this section)"; `:107` "promoted to §1's first row"; `:234` "(§1's first row)"; the row is `docs/KNOWN.md:23` | A positional pointer rotted the day a row was added above it |
| 35 | README's "Not yet" cell ties the public download to the r0004 distribution decision. The public download waits on D2, the legal position. | REVISION | LOCKFREE-AGENT | S | `README.md:58` "the download waits on an owner decision (`docs/HUMAN_TASKS.md`, the r0004 distribution)"; `docs/HUMAN_TASKS.md:106` "D2 \| Legal position on shipping `socom2.exe` + `socom2_game.elf` \| as today; no public download until answered"; `docs/GIT_STRATEGY.md:125-126` | The public front page names the wrong blocker |
| 36 | README says "two players have finished online rounds against each other". Every online result is two harness-driven instances on one host, and STORY says no two humans have played. | REVISION | LOCKFREE-AGENT | S | `README.md:28` "two players have finished online rounds against each other on the hosted server"; `docs/STORY.md:767` "No two humans have played each other"; `docs/KNOWN.md:55` "Still unexercised: two DIFFERENT networks" | It overstates the project on the page a stranger reads first |
| 37 | README's frame-rate cell is undated, came from an online round on 2026-09-16, and disagrees with HANDOFF on the menus (52-60 against 58-60). DOC_MAINTENANCE uses exactly this sentence as its example of a claim that needs a date. | REVISION | LOCKFREE-AGENT | S | `README.md:59` "43-45 fps in a mission and 52-60 in the menus"; `docs/KNOWN.md:41` (online Frostfire, "43–45 fps", 2026-09-16); `docs/HANDOFF.md:39` "58-60 fps on the menus under load"; `docs/DOC_MAINTENANCE.md:238` "\"43-45 fps in a mission, measured on <date>\"" | The public number is stale and its own schema says so |
| 38 | README's developer block runs the Python suite twice (`build.sh test` already runs it), and its comment says `build.sh test` is "the C++ suite and the VU1 replay goldens". | REVISION | LOCKFREE-AGENT | S | `README.md:98-99` "`./build.sh test` # the C++ suite and the VU1 replay goldens" / "`python -m unittest discover ...` # the Python suite"; `build.sh:140` | A newcomer's first hour costs an extra ~5 minutes, and they learn the wrong model of the test step |
| 39 | Documents give three durations for the gate: "about 15 minutes" (README, DEVELOPING) and ~17 minutes (HANDOFF's per-stage figures). | REVISION | LOCKFREE-AGENT | S | `README.md:100` "about 15 minutes"; `docs/DEVELOPING.md:287` "about 15 min"; `docs/HANDOFF.md:331` "title about 3 min, transition about 3, mission about 11" | A duplicated number with no home |
| 40 | GIT_STRATEGY's opening line says its NOW parts are in force on `sprint-11`, and its §1 inventory stops at `v0.10.0`'s draft. Its own NOW line says no sprint branch is open. | REVISION | LOCKFREE-AGENT | S | `docs/GIT_STRATEGY.md:4` "The parts marked **NOW** are in force on `sprint-11`"; `:10-12` "the tags `v0.9.0` and `v0.10.0`; **one draft Release, `v0.10.0`**"; `:36` "no sprint branch is open" | A contract document carrying live state rots the way the registry says it should not |
| 41 | GIT_STRATEGY §6's first bullets still prescribe a CODEOWNERS review and "the Windows check once it exists". Both were superseded (R182; `build-windows` required since 2026-09-21), and the bullets are not blockquoted. | REVISION | LOCKFREE-AGENT | S | `docs/GIT_STRATEGY.md:132-133` "require the `linux` check (and the Windows check once it exists), require a CODEOWNERS review"; `:147-150` "One deviation ... no CODEOWNERS review"; "`build-windows` joined the required set on 2026-09-21" | The rule and its override sit 15 lines apart with no supersede mark |
| 42 | GIT_STRATEGY §2 says `main` is pushed by "today the controller" until protection is on. Rulesets have required a PR since 2026-09-20. | REVISION | LOCKFREE-AGENT | S | `docs/GIT_STRATEGY.md:30` "Nobody directly once protection is on (**AT PUBLIC**); today the controller, at sprint close-out only." | The permissions statement is out of date in the public contract |
| 43 | LOOP_PROMPT's close procedure assumes the next sprint's spec "is already drafted". None is, and the next sprint is unnamed. | REVISION | LOCKFREE-AGENT | S | `docs/LOOP_PROMPT.md:74` "then open the next sprint: its spec is already drafted"; `docs/CURRENT_SPRINT.md:22` "next sprint:  not yet planned" | A controller following the loop looks for a document that does not exist |
| 44 | PLAYTEST (class C) still presents the 2026-09-21 build as "Ready" for a sitting. The owner played it on 2026-09-22. | REVISION | LOCKFREE-AGENT | S | `docs/PLAYTEST.md:8` "build: 2026-09-21 (evening) ... commit: acbc693"; `:25` "**Ready.**"; `docs/CURRENT_SPRINT.md:594` "The owner played tonight's portable build" (2026-09-22) | A contract document holds a live, stale build block |
| 45 | ROADMAP (class N) holds a live ladder count that is wrong (4 of 7; the ledger says 7 of 7) and a knob count that is wrong (150/123 Dev; KNOBS says 152/125). | REVISION | LOCKFREE-AGENT | S | `docs/ROADMAP.md:52` "the scheduled ladder's streak stands at **4 of 7**"; `docs/LADDER.md` "**Clean streak:** 7 of 7"; `docs/ROADMAP.md:43` "**150 registered names** (18 Shipping, 1 Switch, 123 Dev, 8 Test)"; `docs/KNOBS.md:5` "152 names: 18 Shipping, 1 Switch, 125 Dev, 8 Test." | This is exactly the live-state-in-N rot the ROADMAP rewrite was written to stop |
| 46 | ROADMAP's narrative stops at "Sprint 10 (09-20 → open)" and names "the road to `v0.10.0`". Sprints 11 and 12 do not appear in §1. | REVISION | LOCKFREE-AGENT | S | `docs/ROADMAP.md:78` "**Sprint 10 (09-20 → open)**"; `:14` "(what is being worked, and the road to `v0.10.0`)" | The narrative layer lags two sprints |
| 47 | ROADMAP says online results include "the owner's two machines". The two-machine match has never been run. | REVISION | LOCKFREE-AGENT | S | `docs/ROADMAP.md:143-144` "Every online result so far is either two instances on one host or the owner's two machines"; `docs/HUMAN_TASKS.md:544-546` "Every online result so far is two instances on one PC" | This is a false claim of a result |
| 48 | ROADMAP §6 blockquotes its old text, but the paragraph below the blockquotes ("Sprint 11 is drafted ... should run first") is plain text and reads as current. | REVISION | LOCKFREE-AGENT | S | `docs/ROADMAP.md:182-184` "> Superseded 2026-09-25 ... this paragraph is kept"; `:186-193` "**Sprint 11 is drafted, in two independent specs** ... is blocked on nothing and should run first" | A superseded instruction without its blockquote reads as live |
| 49 | The Current-state block in STATUS is 30 KB of 22 dated bullets under a heading that says "keep it short". Sprint 11's close record exists only as one of those bullets (there is no dated entry), although HANDOFF points to "STATUS's 2026-09-25 entry" for it. | REVISION | LOCKFREE-AGENT | M | `docs/STATUS.md:3` "## Current state (keep it short ...)"; `sed -n 3,33p docs/STATUS.md \| wc -c` = 30110; dated headings jump from `:35` (Sprint 12, 2026-09-25) to `:55` (2026-09-23); `docs/HANDOFF.md:56` "The close's own record: `docs/STATUS.md`'s 2026-09-25 entry" | The only live block of the 2,504-line log is itself a log |
| 50 | HUMAN_TASKS opens with a banner from 2026-09-22 saying nothing below is waited on "tonight". It says the morning block is "immediately below", but two newer sections now sit between them. | REVISION | LOCKFREE-AGENT | S | `docs/HUMAN_TASKS.md:7-12` "2026-09-22 evening — parked ... it is immediately below, `## Start here (2026-09-23 morning)`"; `:14`, `:40` newer sections | The owner's queue opens with an obsolete note |
| 51 | HUMAN_TASKS still asks the owner for things that were withdrawn or superseded: the church route (issue #29 closed by the owner), the lobby channel (R244: no run needed), "a domain name for the server ... decide before strangers log in" (it exists; R175), the persona test "Next is mine" (withdrawn), and "the r0004 package from your own memory card" (obtained 2026-09-23). | REVISION | LOCKFREE-AGENT | S | `docs/HUMAN_TASKS.md:198` "A route to the church"; `docs/KNOWN.md:110` "the church-route A/B above is no longer owed"; `docs/HUMAN_TASKS.md:195` "The lobby channel"; `:674-675` "(1) a domain name for the server"; `:682` "Next is mine: whether saved personas follow the name"; `:688` "The r0004 package from your own memory card" | Owner time spent on answered questions is the most expensive waste in the project |
| 52 | 20 unchecked items sit in HUMAN_TASKS "## Open", dated 2026-09-17 to 2026-09-21 (4–8 days old). At least 9 of them are superseded by later rounds, fixes or the 2026-09-22 playthrough: the Sprint 7 title/free-play listens, the Sprint 9 Goal 10 listen, the fade/save-prompt item (the save is fixed), the Sprint 7 re-listen, "second look" with `_new.exe`, the Task 8b pad item, the Sprint 8 launcher look, and "expect to be unheard" (the headset opens since Sprint 8). | REVISION | LOCKFREE-AGENT | S | `grep -c '^- \[ \]' docs/HUMAN_TASKS.md` = 20; `:496` "(Task 6c Step 4)", `:507`, `:512`, `:613`, `:630`, `:517` "`dist/socom_unzipped_launcher_new.exe`", `:518`, `:658`, `:601-605` "the IOP headset module still answers \"no headset\""; `docs/KNOWN.md:149` (headset opens), `:111` (save fixed `152579a`) | The owner cannot tell the live asks from the dead ones |
| 53 | Of HUMAN_TASKS' asks, these are agentic and could be done or prepared without the owner: the r0004 sidecar gate (a window, "nothing to watch"); the quiet-endpoint capture (the script refuses a contaminated run by itself); the story's missing days (writing cited entries); a dossier per engine routine for the five-routine hand review; building the release archives short of `gh release upload`; the Linux no-audio-device LAST RUN check (possible in the VM); the two site-session relays and the site's `secret-scan.mjs` literal. | TASK | LOCK-AGENT | M | `docs/HUMAN_TASKS.md:27-30` "about an hour of the machine ... nothing to watch. Name the window and the loop runs it"; `:63-82`; `:33-36`; `:50` "Half an hour with the disassembly"; `:180-188`; `:516` "(c) On the real Linux box, with the audio device disabled"; `:464-472`, `:419-421` | About half of the owner's queue needs only a window or a reader, not the owner |
| 54 | The story has five days missing (three Sprint 11 days and two Sprint 12 days), carried twice and now an owner question. Writing cited entries is agent work; the owner only rules keep or drop. | NEGLECTED | LOCKFREE-AGENT | M | `docs/STORY.md:771-776` "Three days are missing ... carried explicitly as a Sprint 12 item ... and carried a second time at Sprint 12's close"; `docs/HUMAN_TASKS.md:33` "The story's missing days, carried twice" | The public narrative ends at 2026-09-22 and omits the r0004 result |
| 55 | Three ruling numbers were issued twice with different decisions: R107, R109 and R110 each name two unrelated rulings in two Sprint 8 plans. | REVISION | LOCKFREE-AGENT | S | `docs/superpowers/plans/2026-09-18-sprint-8-linux-client.md:2216` "**R107** ... the Linux audio bar"; `2026-09-18-sprint-8-menu-render-cost.md:914` "**R107** (Task 1): the per-call trace"; `menu-render-cost.md:918` "**R109** ... `PS2X_GS_TESTS_GL`" vs `2026-09-19-sprint-8-hosted-server.md:81` "**R109** ... default preset"; `menu-render-cost.md:920` "**R110** ... login-screen window" vs `hosted-server.md:82` "**R110** ... The static IP" | "R107" in HANDOFF trap 9 (`docs/HANDOFF.md:284`) is ambiguous; the ledgers' "no collisions" claim covers only R181–R263 |
| 56 | R114, R116 and R124 have no findable ruling text. They appear only in ranges or as citations. R112 appears only as "the one structural rule" in a step. | REVISION | LOCKFREE-AGENT | S | `docs/superpowers/plans/2026-09-19-sprint-8-menu-cost-2b.md:912` "the hosted-server and voice plans for R111-R116" (the voice plan defines R115 only, `:410`); the menu-cost rulings list jumps "**R123**" (`:924`) → "**R125**" (`:926`); R124 is cited in `docs/KNOBS.md:86` "(R124)" and `docs/CURRENT_SPRINT.md:938` | A cited ruling with no text cannot be overturned or checked |
| 57 | Two research notes share the number 43, and number 35 was never used. HANDOFF still says the notes run 01-34. | REVISION | LOCKFREE-AGENT | S | `ls docs/research`: `43-r0004-capsule.md`, `43-what-changed-in-r0004.md`; no `35-*`; `docs/ROADMAP.md:60` "`docs/research/35-*.md` does not exist"; `docs/HANDOFF.md:301` | "research/43" is ambiguous in every citation (`docs/KNOWN.md:17` "research/43 §2", `:19`) |
| 58 | SECURITY and HANDOFF cite different hashes for the profile-is-a-path fix. Both commits exist with the identical subject (a duplicated commit). | REVISION | LOCKFREE-AGENT | S | `SECURITY.md:22` "`c81b17a`"; `docs/HANDOFF.md:409` "(`f5809c8`, the profile was a path)"; `git log -1 c81b17a` / `f5809c8`: both "fix(config): the profile is a name, not a path" | STORY's citation checker requires reachability, and one of the two may not be on `main` |
| 59 | CONTRIBUTING says `help wanted` marks the issues a disc-less contributor can take. No open issue carries it. | UNFINISHED | LOCKFREE-AGENT | S | `CONTRIBUTING.md:74` "`help wanted` marks the ones that need no disc"; `gh issue list --label known-issue`: 21 open, labels only area/`known-issue`/`carried`/`needs-disc-gate`; `docs/DOC_MAINTENANCE.md:258-259` step 6 "Put `help wanted` on what a stranger without a disc could take" | The public on-ramp the review procedure promises does not exist |
| 60 | The next-ruling counter has two homes (HANDOFF and the CURRENT_SPRINT header). The code checks both, but HANDOFF calls itself "the one checked home". | REVISION | LOCKFREE-AGENT | S | `docs/HANDOFF.md:204` "which is the one checked home for it"; `docs/CURRENT_SPRINT.md:39` "next ruling:  R265"; `tools_py/docmaint.py:72` `COUNTER_DOCS = ("docs/HANDOFF.md", "docs/CURRENT_SPRINT.md")` | A small untruth in the rule that exists because two homes once disagreed |
| 61 | The Sprint 11 decision table (D2–D6), the Sprint 12 defaults (D1–D7) and the r0004 spec's D1–D4 reuse the same D-numbers for different decisions in one file. Sprint 11's D5 is signing; Sprint 12's D5 is the hand review. | REVISION | LOCKFREE-AGENT | S | `docs/HUMAN_TASKS.md:109` "D5 \| Signing"; `:20` "The seven defaults D1–D7"; `docs/CURRENT_SPRINT.md:186` "D5's hand review"; `docs/HUMAN_TASKS.md:112` "r0004 D1-D4" | "D5" in a reply from the owner is ambiguous |
| 62 | `docs/process-audit.md` and `docs/AUDIT-2026-09-17.md` are dated snapshots that still sit in `docs/`. The registry said to move them "at the next tidy", and three closes have passed since. | NEGLECTED | LOCKFREE-AGENT | S | `docs/DOC_MAINTENANCE.md` §3 row "`docs/process-audit.md` \| **S** ... it should move to `docs/audits/` at the next tidy"; `docs/audits/` holds the three newer audits | A stranger sees two stray audits beside the live files |
| 63 | The Codex task-allocation audit (six recommended assignments) is referenced by no document and was never triaged against the backlog. | NEGLECTED | LOCKFREE-AGENT | S | `docs/audits/2026-09-23-codex-task-allocation.md:54` "## Recommended assignments, in order" (six headings `:56-186`); `grep -rl 2026-09-23-codex-task-allocation` finds no other file | An owner-requested review whose outputs went nowhere |
| 64 | The Sprint 7–10 specs and plans (4 specs and 25 plans, all closed) are still under `docs/superpowers/`. The Sprint 1–6 set was moved to `docs/archive/sprints-1-6/`, with check 6 as the stated reason the move is now safe. | NEGLECTED | LOCKFREE-AGENT | M | `ls docs/superpowers/plans` = 32 files, 27 of them dated ≤ 2026-09-22; `docs/archive/README.md` "`sprints-1-6/` ... check 6 is what made the move safe" | A stranger opening `docs/superpowers/plans/` sees 27 finished plans beside the live ones |
| 65 | The archive README still says Sprint 11 Goal 1 "decides" whether the archive is published under `docs/dev/`. D4's default ("nothing moves to `docs/dev/`") has stood since 2026-09-23. | REVISION | LOCKFREE-AGENT | S | `docs/archive/README.md:6-7` "Sprint 11 Goal 1 decides whether this directory is published (under `docs/dev/`), kept private, or deleted"; `docs/HUMAN_TASKS.md:108` "D4 ... nothing moves to `docs/dev/`" | A decided question reads as open |
| 66 | HANDOFF tells the reader to leave an "orphan directory" `C:\projects\wt-issues` "until someone identifies it". It is empty. | TASK | OWNER | S | `docs/HANDOFF.md:385-386` "an orphan directory `C:\projects\wt-issues` that is not a registered worktree; leave it until someone identifies it"; `ls /c/projects/wt-issues` prints nothing | A standing caveat for an empty folder |
| 67 | DEVELOPING (class L) is mostly a dated history of when knobs landed, but its class says it must be true now. DOC_MAINTENANCE's §5 reading cannot cover 515 lines of prose "kept as written". | RISK | LOCKFREE-AGENT | M | `docs/DEVELOPING.md:3` "It is kept as written by the sprints that added each entry; the dates in it are when a behaviour landed"; findings 3–8 above | Six REVISIONs in one L file show that the class and the content disagree |
| 68 | KNOWN §2 ("Believed, unconfirmed") is about half settled records: 26 of its 50 rows carry a settled, fixed, closed or superseded marker. | RISK | LOCKFREE-AGENT | M | `awk 'NR>=101&&NR<=150' docs/KNOWN.md \| grep -c '^\|'` = 50; 26 rows match `~~\|SETTLED\|FIXED\|CLOSED\|SUPERSEDED\|DROPPED\|landed result\|Done\.`; e.g. `:115` "~~Requiem~~ SETTLED", `:142` "*No issue: a landed result*" | The open-question list is diluted by answered ones; §1 or §3 is their home |
| 69 | The HUMAN_TASKS "Sprint 11 close" and HANDOFF text call `v0.11.0`'s release archives the owner's. The draft for `v0.11.0` cannot exist, because the tag does not. | UNFINISHED | OWNER | S | `docs/HUMAN_TASKS.md:47` "and now `v0.11.0`'s (the tag's workflow makes a draft ...)"; `gh release list`: only `v0.10.0` Draft | The merge PR and tag step (finding 1) is the real outstanding item |

## 2. Structure notes

**CURRENT_SPRINT.md has outgrown its purpose.** It is 958 lines and 189,136 bytes. The brief's ~1,300 lines is
wrong; the lines are long. Measured per block (`awk` over the `##`/`###` headings):

| Block | Lines | Bytes |
|---|---|---|
| Header (fields, `next ruling`, baselines) | 1–60 | ~7 KB |
| Sprint 12 CLOSED | 61–87 | 8.3 KB |
| Sprint 11 CLOSED + ledger R245–R263 + S12 cross-table | 88–192 | 24.1 KB |
| Sprint 11 record (task-by-task, timestamps) | 193–262 | 17.2 KB |
| Sprint 10 close + CLOSED block + standing backlog + ledger R181–R244 | 263–448 | 21.2 KB |
| Sprint 9 order, milestones P and Q | 449–495 | 23.1 KB |
| Sprint 10 REORGANIZED (H1–H8, chunk table, road to v0.10.0) | 496–591 | 35.5 KB |
| The playthrough 2026-09-22, R236–R240, R181–R183 text | 592–785 | 19.5 KB |
| Sprint 10 and Sprint 11 drafts | 786–888 | 12.0 KB |
| Rulings R174–R178 | 889–914 | 9.1 KB |
| Standing rules (stale, finding 21) + Sprint 9 record | 915–958 | 11.7 KB |

By sprint: Sprint 12 ≈ 8 KB, Sprint 11 ≈ 46 KB, Sprint 10 ≈ 88 KB, Sprint 9 ≈ 43 KB. No sprint is open, so the
live content is the header, the carry lists of the two CLOSED blocks and the standing backlog: about 12% of the file.
**An archive split under DOC_MAINTENANCE's own rules** (§1 class A, §5 step 5 "Anything superseded this sprint moves
to S or A with a banner"; precedent `docs/archive/CURRENT_SPRINT-to-sprint-8.md`):
- Move lines 193–958, less the standing backlog (327–354), verbatim to
  `docs/archive/CURRENT_SPRINT-sprints-9-to-11.md`. Give it a first-15-lines banner (check 5) and a registry row <!-- docmaint: future -->
  (§2 requires a row for every `docs/archive/*.md`), and add an entry to `docs/archive/README.md`'s table.
- Keep in the live file: the header, the two CLOSED blocks with their carries, the two ruling ledgers (both say "this
  table is the one home"; alternatively move them and leave a one-line pointer), and the standing backlog.
  DOC_MAINTENANCE's first-review lesson (1) says the sprint file must keep a filler list. Delete the "Standing
  rules" block rather than archive it: it duplicates HANDOFF §5 and is wrong.
- The **ruling counter survives only if the highest number stays live.** `tools_py/docmaint.py:129`
  `max_ruling()` scans L documents, STATUS, `docs/superpowers/plans/` and `docs/archive/sprints-1-6/`, **not**
  `docs/archive/*.md`. R264 sits in the Sprint 12 block, which stays, so the check holds today. If the ledgers
  move, add the new archive file to that scan. Otherwise a future close that archives the newest ledger lets the
  counter walk backwards (the failure `docmaint.py:125-127` warns about).
- Check 6 citations: the files that cite blocks by name ("the R237 block": `docs/KNOWN.md:106`; "the 14:30Z
  block": `docs/CURRENT_SPRINT.md:129`) must be re-pointed in the same commit.
- Result: roughly 230 lines and 45 KB live, and the next close adds one block instead of growing the stack.

**HANDOFF §2 is a growing stack.** It holds 10 "Where the loop is/was" bullets (`docs/HANDOFF.md:55, 56, 57, 58, 65, 76,
88, 103, 104, 105`) plus 2 "Picking up after ..." blockquotes (`:26`, `:28`): 12 pick-up points in 87 lines and
14.9 KB. Three are labelled "is now", and they are not in date order. Rule: keep exactly one "now" bullet and move the rest
into STATUS's dated entries (they are already duplicated there) or into an archived handoff. §4 (Sprint 9's order)
and §10 (the pre-playtest decisions) are the same problem in other sections.

**N documents holding live state** (DOC_MAINTENANCE §1 "No live state, no live numbers, no task lists"):
ROADMAP holds the ladder streak, knob counts, fps figures, the tag `v0.10.0` as "the road", and line-number citations
(findings 25, 45, 46). STORY's closing section holds instructions and carry pointers. They are dated blockquotes,
which is acceptable, but "Sprint 11 is open" (`docs/STORY.md:765`) and "carried explicitly as a Sprint 12 item" are
sprint state in a narrative file. HOW_IT_WAS_BUILT is clean: it points instead of counting (`docs/HOW_IT_WAS_BUILT.md:189`).
Two class-C files hold live state too: PLAYTEST's build block (finding 44) and GIT_STRATEGY's NOW line and tag
inventory (finding 40). GIT_STRATEGY is at least self-aware about it (`:21-23`).

**Duplicated facts, the one home, and the copies:**
- Suite counts: home `docs/DEVELOPING.md:285-286` (881 / 2553). The copies elsewhere are dated historical lines, so
  check 3 is satisfied.
- Knob counts: home `docs/KNOBS.md:5` (152). Copies: `docs/ROADMAP.md:43` (150, wrong) and
  `docs/CURRENT_SPRINT.md:557` (149, dated history).
- Ladder streak: home `docs/LADDER.md` (7 of 7). Copies: `docs/ROADMAP.md:52` (4 of 7, wrong) and
  `docs/CURRENT_SPRINT.md:308` (7 of 7, dated).
- Next ruling: `docs/HANDOFF.md:52` and `docs/CURRENT_SPRINT.md:39`. Both are checked by the code, but HANDOFF
  claims to be the only one (finding 60).
- Server address `3.143.65.100`: in 7 files (`grep -l`: CURRENT_SPRINT, HANDOFF, HUMAN_TASKS, KNOWN, LADDER,
  PLAYTEST, STATUS). No home is assigned; README correctly uses the name `socom.scotho.com` (`README.md:45`).
- The Sprint 12 proof (1,771 names, exe `804dd172…`, 14,882 files): in README, CURRENT_SPRINT, HANDOFF,
  HUMAN_TASKS, KNOWN and STATUS. It is dated everywhere, but six copies of one proof is the pattern §6 warns about;
  KNOWN §1 row `:16` should be the home.
- fps 43–45 / 58–60 / 52–60: in 6, 6 and 3 files, and the copies disagree (finding 37). KNOWN `:41` and `:137` should
  be the home.
- Gate duration: 15 or 17 minutes (finding 39). Worktree list: CURRENT_SPRINT `:220` against HANDOFF `:383`
  (finding 23). Workflow/tag inventory: GIT_STRATEGY §1 against reality (finding 40).

**A stranger's path, README to a running game.** Every link resolves (`docs/INSTALL.md`, `docs/FAQ.md`,
`.github/workflows/{linux,windows,secrets}.yml`, `LICENSE`, `LICENSES/`, `server/horizon-server/LICENSE`,
`scripts/install_hooks.sh`, `bootstrap_windows.sh`, `disc_to_elf.sh`, `run.sh`, `ghidra_scripts/`). `docs/INSTALL.md`
contains **no build commands at all**: it is the player page and there is no download (`:34`). So the path for a
stranger is README's developer block. Its commands match `build.sh`'s usage (`build.sh:3` `[tools|recomp|runtime|release|test|all] [--no-runner]`);
the defects are the double Python run and the mislabelled test step (finding 38), plus the gate time (39).
INSTALL's quoted launcher strings were spot-checked against the source and match: `launcher_config.h:52` "needs the
r0004 game update -- planned", `page_controller.cpp:252` "Menus and typing only ...". FAQ's exit-code sentences
match `exit_codes.h:28-37` word for word, 65–74. The stranger's first real contradiction is README's Status table
(findings 35–37). DEVELOPING's first-hour table is accurate (`:283-287`); its `build.sh test` comment (`:120`) is
not.

**What is well organised.** The registry and its six mechanical checks work (every covered file has a row). KNOWN's
supersede-in-place discipline is visible throughout. FAQ is exact and current. INSTALL quotes the product rather than
describing it. The ruling ledgers R181–R263 are a model: every number has a row, a home and a status, and R229's
vacancy is explained. `docs/archive/README.md` maps every old path. HOW_IT_WAS_BUILT keeps to its class.

**What is not.** Four files carry the bulk of the rot, and all four grow by appending: CURRENT_SPRINT, HANDOFF §2,
the STATUS Current-state block, and HUMAN_TASKS (699 lines with seven "Start here" generations: `:54`, `:204`, `:273`,
`:345`, `:355`, plus the two close sections). Nothing retires a block. DOC_MAINTENANCE §5 asks "is anything the
sprint closed still listed as open?". The reviewer answers that for the newest block and never reads the older ones
back. Two review passes on 2026-09-25 applied 75 rows (`docs/CURRENT_SPRINT.md:86`, `:115`), and this audit still
finds 69 findings, most of them in blocks older than the reviewed sprint. The missing rule is "at close, archive every
block older than the previous sprint", with a check (for example, a byte ceiling on CURRENT_SPRINT, HANDOFF §2 and the
STATUS Current state) so it cannot be skipped. Naming that misleads a stranger: `docs/process-audit.md` and
`docs/AUDIT-2026-09-17.md` sit beside live files; there are two notes numbered 43; and there are three "D5"s.

## 3. Top five

1. **Finding 1: Sprint 11 is recorded as merged and tagged, but it is neither.** Five documents close a step that
   has not happened. Nobody will open the PR, and every later statement about `main` inherits the error.
2. **Findings 9–14: the start-here file points a new controller at a closed plan and a closed branch, and offers three
   "now"s.** HANDOFF is read first by every session, so this is the costliest place for stale text.
3. **The CURRENT_SPRINT/HANDOFF/STATUS/HUMAN_TASKS archive rule (Structure notes).** One split plus a size check
   retires most of findings 11–26 and 49–52 at once and stops them recurring. The counter caveat in `max_ruling()`
   is the one trap to handle.
4. **Findings 51–53: the owner's queue is about half obsolete or agentic.** Pruning the dead asks and handing the
   agentic ones (the r0004 gate window, the quiet capture, the story days, the routine dossiers, the archives) to the
   loop saves the scarcest resource: the owner's evenings.
5. **Findings 35–37 and 3–4: the two documents a stranger reads, README and DEVELOPING, state wrong facts.** They get
   the download's blocker, "two players", undated fps, `build.sh test` running no Python, and the clock knob's
   default backwards. These are cheap to fix and costly in credibility.
