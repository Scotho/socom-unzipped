# Audit: the product as a stranger meets it (player, contributor, tester)

Auditor area: `stranger-player`. Worktree `C:\projects\wt-s12`, branch `sprint-12` at `eb190a42`, 2026-09-25.
Read-only. `gh` reads were made against `github.com/Scotho/socom-unzipped` on 2026-09-25. The default branch a
stranger lands on is `main` at `e63f9ba9` (2026-09-24), which does **not** hold Sprint 11 or 12. Several findings
turn on that one fact.

> Added 2026-09-25 (Sprint 13 Task R4): `docs/HUMAN_TASKS.md` became one table that day. Every `HUMAN_TASKS.md` line cited below is the old file, now `docs/archive/HUMAN_TASKS-to-2026-09-25.md` -- its banner gives the line offset, the quoted text finds each line, and its table gives each item's disposition.

## 1. Findings

| # | item | class | who | cost | evidence | why it matters |
|---|---|---|---|---|---|---|
| **The player: download and first run** |||||||
| 1 | A player has nothing to download. The only release is one draft with no assets, and the tags v0.11.0/v0.12.0 do not exist | UNFINISHED | OWNER | M | `gh release list`: "SOCOM Unzipped v0.10.0 Draft v0.10.0 2026-09-23"; `gh release view v0.10.0` → `"assets":[]`; `git ls-remote origin 'refs/tags/v0.1*'` → only `refs/tags/v0.10.0` | every player path in INSTALL/FAQ is unreachable by anyone the owner does not hand a zip |
| 2 | Docs claim Sprint 11 is on `main` as `v0.11.0`. No PR, merge or tag exists | REVISION | LOCKFREE-AGENT | S | `docs/CURRENT_SPRINT.md:88` "merged to `main` as `v0.11.0`"; `docs/HANDOFF.md:56` "on `main` as `v0.11.0`"; `docs/HUMAN_TASKS.md:42` "Sprint 11 is merged to `main` as `v0.11.0`"; `docs/STATUS.md:5`; but `git log origin/main -1` = `e63f9ba9 Merge pull request #44` and `gh pr list --state all` shows no Sprint 11 PR | the controller's and owner's picture of what strangers can see is wrong |
| 3 | A stranger on GitHub's default branch sees no player guide: `docs/INSTALL.md` and `docs/FAQ.md` exist only on the sprint branches | UNFINISHED | OWNER | S | `git diff --stat origin/main HEAD` → "docs/FAQ.md \| 249 +++", "docs/INSTALL.md \| 185 +++" (whole files added); main's README "the download and its page are Sprint 11" (`git show origin/main:README.md`, Status table) | the "For players" path on the public page ends at a dead promise |
| 4 | HUMAN_TASKS asks the owner to attach archives to a `v0.11.0` draft that was never created | REVISION | LOCKFREE-AGENT | S | `docs/HUMAN_TASKS.md:48` "the `v0.10.0` draft's, and now `v0.11.0`'s (the tag's workflow makes a draft…)"; `gh release list` shows only v0.10.0 | an owner task points at an object that does not exist |
| 5 | The legal question behind any download (D2: may recompiled code and the decrypted ELF be shipped?) is still open. It blocks every release | RISK | OWNER | S | `docs/HUMAN_TASKS.md:106` "D2 \| Legal position on shipping `socom2.exe` + `socom2_game.elf` \| as today; no public download until answered"; draft checklist "The legal position on distributing recompiled code (Sprint 11 decision D2) stands as decided" (`gh release view v0.10.0`) | this is the actual gate on "a stranger plays", and it has no date |
| 6 | README says "two players have finished online rounds against each other". STORY says no two humans ever have | REVISION | LOCKFREE-AGENT | S | `README.md:28` "two players have finished online rounds against each other on the hosted server"; `docs/STORY.md:763` "No stranger has played yet. No two humans have played each other." | the headline claim is stronger than the evidence (two automated instances on one machine) |
| 7 | README's frame-rate figure "43-45 fps in a mission" comes from a 2026-09-16 online round measured before the guest-clock fix | REVISION | LOCK-AGENT | S | `README.md:59` "43-45 fps in a mission and 52-60 in the menus"; only source `docs/STATUS.md:325` "`ours_control_frostfire_clutfix` … 43–45 fps, guest clock 0.74 s/s" | the most player-visible number is unmeasured on the current build |
| 8 | No issue tracks the mission frame rate, the defect a player notices before any other. Only the menus' upload cost (#32) is on the stack | TASK | LOCKFREE-AGENT | S | `gh issue list` render label: only #32 "The menus re-upload one atlas a frame…"; README:59 lists frame rate under "Not yet" | the top visible defect has no closing bar and no owner |
| 9 | INSTALL and FAQ never mention the GAME VERSION row that PLAY and ONLINE both draw. That row includes a greyed "r0004 (community update)" cell | REVISION | LOCKFREE-AGENT | S | `ps2xLauncher/src/ui/pages.h:176-188` draws "GAME VERSION" on both pages; `launcher_config.h:44-45` `{"r0004", "r0004 (community update)", "socom2_r0004.exe"}`; `docs/INSTALL.md:109` "The PLAY page is four rows — DISC, VIDEO, CONTROLLER, ONLINE" | a player meets an unexplained greyed control on the first page |
| 10 | LAUNCH's blocked reason says "that file is not SOCOM II" when the path does not exist. The DISC page says "cannot open the file" for the same state | TASK | LOCKFREE-AGENT | S | `ps2xLauncher/src/ui/focus.cpp:631-632` `if (!discOk) return "that file is not SOCOM II (NTSC, r0001)";`; `main.cpp:140` `st.message = "cannot open the file";` | a moved ISO is reported as the wrong disc |
| 11 | Exit 66 ("disc not found") cannot be reached from the launcher, because LAUNCH greys out first. PLAYTEST step 4 asks the owner to produce it that way | REVISION | LOCKFREE-AGENT | S | `docs/PLAYTEST.md:54` "Point DISC at a path that does not exist, press LAUNCH: LAST RUN should say the disc was not found"; `focus.cpp:631` blocks LAUNCH on `!discOk` | the owner's script contains a step that cannot pass |
| 12 | The launcher, README and INSTALL describe voice three different ways: "does not send your voice yet", "untested end to end" and "untested" | REVISION | LOCKFREE-AGENT | S | `ps2xLauncher/src/ui/page_microphone.cpp:61` "The game does not send your voice yet (that is a later sprint)"; `README.md:60` "Voice chat is untested end to end"; `docs/INSTALL.md:28` "Voice is untested end to end"; `docs/KNOWN.md:149` "No pad button talks" | a player with a headset is told it might work; the code's own page says it does not |
| 13 | The CONTROLLER page lists the keyboard as "arrows, Enter, Backspace, Z/X/C/V", yet the default crouch hint sends fire mode to the keyboard's 2 key, which the default map binds to L3 | REVISION | LOCKFREE-AGENT | S | `page_controller.cpp:252` "Menus and typing only: arrows, Enter, Backspace, Z/X/C/V."; `launcher_config.cpp:518` "Fire mode moves to the keyboard's 2 key."; `mapping.cpp:30` `{'2', kPs2L3}` | two sentences on one page disagree about what the keyboard does |
| 14 | ABOUT's "BUILT FROM" credits SDL2, which the notices say does not ship. It omits Dear ImGui, libjxl, libwebp and Brotli, which do | REVISION | LOCKFREE-AGENT | S | `page_about.cpp:28-32` `{"SDL2", "zlib"}`; `THIRD_PARTY_NOTICES.md:35` "SDL2 … no (not in the release closure)"; `:28,31-33` ImGui/jxl/webp/brotli "yes" | the in-app credits disagree with the legal inventory |
| 15 | The launcher's sample reference ids ("BR-20260919-A1B2C3", "ABC123") fail the launcher's own id check, which wants lowercase hex. These ids appear in the screenshots used for docs | REVISION | LOCKFREE-AGENT | S | `main.cpp:1971` `report.id = "BR-20260919-A1B2C3"`; `bug_report.h:108` "BR-20260919-ABC123"; `bug_report.cpp:357-367` regex `/^BR-\d{8}-[0-9a-f]{6}$/`; the issue template example is `BR-20260920-abc123` | a tester compares a real id with the pictured one and sees a different format |
| 16 | The contact field promises "only if you want an answer", but the triage policy is never to reply. The two words were asked of the owner on 2026-09-24 and not yet given | UNFINISHED | OWNER | S | `page_report.cpp:58` "only if you want an answer"; `docs/HUMAN_TASKS.md:168` "promises a reply the triage routine forbids (your G7 reply policy: no)" | the product makes a promise the process refuses to keep |
| 17 | After SEND, the launcher invites a public GitHub issue for every report. That includes security reports, which SECURITY.md says must never become public issues | UNFINISHED | OWNER | S | `bug_report.h:123-124` "Contributors can also open an issue at github.com/Scotho/socom-unzipped and quote this id."; `docs/HUMAN_TASKS.md:168` item (1); `SECURITY.md:5` | a player can be steered into disclosing an exploit in public |
| 18 | The report pipeline has never produced a public issue. No issue carries `from-launcher`, and every issue ever opened is the owner's | NEGLECTED | OWNER | S | `gh issue list --state all --label from-launcher` → `[]`; authors → `{"Scotho":22}` | the documented tester loop (BR id → issue) is untested end to end |
| 19 | Two relays owed to the site session are unconfirmed, including dropping a "keyboard/mouse support" claim the product contradicts | UNFINISHED | OWNER | S | `docs/HANDOFF.md:388-390` "Relays owed to the site session, not yet confirmed done: (1) drop the 'keyboard/mouse support' claim from s2u.scotho.com … (2) … quote the `BR-` id" | the public site says what the launcher's CONTROLLER page denies ("Playing needs a controller") |
| 20 | Players get no account or data page: nothing says what the hosted server stores (persona, password, IP), how to reset a forgotten password, or how to delete an account | TASK | OWNER | M | `grep -i "forgot\|reset.*password\|delete.*account\|privacy"` over `docs/INSTALL.md docs/FAQ.md SECURITY.md` → no hits; `docs/INSTALL.md:134-137` covers only the local `config.json` | a stranger hands a password to a server run by one person, with no statement of what happens to it |
| 21 | The password the launcher prefills is kept in plain-text `config.json`. The underlying defect (#27, the game forgets its own saved password) sits under the `recomp` label, not `online` | RISK | LOCK-AGENT | M | `docs/INSTALL.md:136` "stored in plain text in `config.json`"; `gh issue list`: "27 … saved password does not (W10) recomp,known-issue" | the workaround every player is told to use is the insecure one; the fix is filtered out of an `online` search |
| 22 | The multiplayer warning has said "the reporter's confirmation is still pending" since 2026-09-23. No owner step chases it | NEGLECTED | OWNER | S | `README.md:19` "No mechanics are published, and the reporter's confirmation is still pending."; `SECURITY.md:32` "the moderator's confirmation pending" | the one security claim players rely on has no closing date |
| 23 | Two believed player-facing items still wait on the owner's hands: the Explorer double-click lets go of its console window, and the no-audio notice reaches LAST RUN | NEGLECTED | OWNER | S | `docs/KNOWN.md:147` "Believed, not proven (Goal 1): the Explorer double-click lets go of its console window (R132) and the no-audio-device notice reaches LAST RUN -- both need a person" | both are first-run behaviours, unproven since 2026-09-20 |
| 24 | `docs/PLAYTEST.md` is stale in every part. It names a 2026-09-21 build (`acbc693`), asks for decisions already taken ("The repository going public needs six answers") and was last edited 2026-09-21 | NEGLECTED | LOCKFREE-AGENT | S | `docs/PLAYTEST.md:8` "build: 2026-09-21 … commit: acbc693"; `:102` "The repository going public needs six answers from you"; `git log -1 -- docs/PLAYTEST.md` → `ffebba2d 2026-09-21`; `docs/HANDOFF.md:109` "A playtest by the owner is planned" | the owner's one-evening script would test a build two sprints old |
| 25 | PLAYTEST step 11 admits the connection failure is invisible: "nothing on screen says that is what went wrong" when the name will not resolve. No task exists to make the launcher say so | TASK | LOCKFREE-AGENT | M | `docs/PLAYTEST.md:75-78` "pick Custom, type `3.143.65.100` … because nothing on screen says that is what went wrong" | a player whose DNS fails sees a silent online failure |
| 26 | FAQ exit 67 describes only the disc revision. The LAUNCH check stops a wrong disc earlier, so a player can meet 67 only by double-clicking `socom2.exe` | TASK | LOCKFREE-AGENT | S | `docs/FAQ.md:38-42`; `focus.cpp:631-632` | small: the FAQ could say where each code is reachable from |
| **Online, as a player meets it** |||||||
| 27 | The community preset carries the literal placeholder address `COMMUNITY_SERVER_ADDRESS_TBC`. Its note promises "planned" while D1/Goal F (the r0004 distribution) are the owner's open questions | UNFINISHED | OWNER | S | `ps2xShared/include/launcher/launcher_config.h:95` `"COMMUNITY_SERVER_ADDRESS_TBC"`; `:51` `"needs the r0004 game update -- planned"`; `docs/HUMAN_TASKS.md:44-45` D1 and Goal F open | "planned" is a public promise nobody has decided to keep |
| 28 | r0001 and r0004 clients bounce silently from each other's games. The launcher's mismatch line covers only a mismatch between server and build | RISK | OWNER | S | `docs/FAQ.md:183-185` "the client's own token filter bounces the join silently"; `pages.h:241-244` `revisionWarning(serverPreset, gameRevision)` | once r0004 ships, mixed rooms fail with no message |
| 29 | Online freeze candidate #34 (up to 10 s EE block) has no run scheduled. It is the only `online`-labelled defect | NEGLECTED | LOCK-AGENT | M | `gh issue view 34`: "Online instances have frozen for 3-17 s under host load … Research note 29's shape 2 is still a live candidate" | a 3-17 s freeze in a round is what a player remembers |
| **The contributor** |||||||
| 30 | CONTRIBUTING says "`help wanted` marks the ones that need no disc". No issue has that label or `good first issue` | REVISION | LOCKFREE-AGENT | S | `CONTRIBUTING.md:74` "`help wanted` marks the ones that need no disc"; `gh issue list --label "help wanted"` → 0 lines; `--label "good first issue"` → 0 | a stranger looking for work finds nothing marked for them |
| 31 | Several open issues could be done without a disc and could carry `help wanted`: #39, #40, #46, #48 (tooling). Their bodies cite no build-machine logs | TASK | LOCKFREE-AGENT | S | scan of `## Evidence` sections: #39, #40, #46, #48 → `logs-in-evidence=0`, `disc/gate/lock-mentions` 0-2 | cheap to make the tracker usable by an outsider |
| 32 | 12 of the 21 open issues rest their Evidence on git-ignored `logs/` files on the owner's machine, which a contributor cannot open | RISK | LOCKFREE-AGENT | M | #25, #26, #27, #28, #30, #35, #36, #37, #38, #42, #45 `logs-in-evidence=1`; e.g. #25 "`logs/chain9b_test.log` … on the build machine (git-ignored)" | issue bodies are not self-contained for anyone but the owner |
| 33 | About a third of the public tracker (#35, #36, #37, #45) is the owner's internal loop lock and gate harness, which no stranger can run | NEGLECTED | OWNER | S | `gh issue list`: "#36 The loop lock has no queue", "#35 loop_lock.sh --wait N is a retry count", "#37 ladder_job.sh checks the lock…", "#45 gate --accept-pins…" | the public stack reads as a private to-do list; a label or milestone could separate them |
| 34 | `sprint-12`, the branch meant to merge to main as v0.12.0, is red on CI: Linux and Windows both fail 6 Python tests. The close record says `build.sh test` exit 0 | RISK | LOCKFREE-AGENT | M | `gh run list --branch sprint-12`: "windows completed failure eb190a42", "linux completed failure eb190a42"; failed-log "FAILED (failures=6, skipped=152)" in `test_gate_accept_pins`, `test_gate_exe_line`; `docs/HANDOFF.md:36` "`./build.sh test` exit 0 on the renamed tree" | the required checks (`build`, `build-windows`) will refuse the v0.12.0 PR; a fork of this branch starts red |
| 35 | Four Dependabot PRs have been open and failing since 2026-09-21. On the public PR tab they read as neglect | NEGLECTED | LOCKFREE-AGENT | S | `gh pr list --state open`: #7-#10 created "2026-09-21T04:40", status "FAILURE,FAILURE,SUCCESS…" | the first thing a would-be contributor sees on the PR tab |
| 36 | DEVELOPING says "Everything below this line works on a fresh clone with no disc at all", then lists five commands of which recomp, runtime with the game and the gate need the disc | REVISION | LOCKFREE-AGENT | S | `docs/DEVELOPING.md:255` "Everything below this line works on a fresh clone with no disc at all."; `:280-288` rows 1 `./build.sh recomp`, 5 `python -m tools_py.parity.gate` | a disc-less contributor is told the wrong steps will work |
| 37 | README's disc recipe runs the Python suite twice: `./build.sh test` already runs it, then a separate `python -m unittest` line follows (about 280 s each) | REVISION | LOCKFREE-AGENT | S | `README.md:98-99`; `build.sh:139-140` `python -m unittest discover -s tools_py/tests -t . -v` inside `test_step` | wasted minutes on every first run |
| 38 | DEVELOPING's Python count "Ran 2553 tests on 2026-09-25" is below what CI ran the same day on sprint-12 (2797) | REVISION | LOCKFREE-AGENT | S | `docs/DEVELOPING.md:286` "`Ran 2553 tests` on 2026-09-25"; CI linux log "Ran 2797 tests in 253.537s" | the one source for counts is already behind (the doc says OK is the bar, so the impact is low) |
| 39 | CODEOWNERS says "Every change is reviewed by the owner", but the `main` ruleset needs 0 approvals and no code-owner review | REVISION | OWNER | S | `.github/CODEOWNERS:1`; `gh api …/rulesets/23746017`: `"required_approving_review_count":0,"require_code_owner_review":false` | a stranger's reading of the review guarantee is wrong |
| 40 | Dependabot security updates are disabled although version updates open PRs. SECURITY.md's list of protections does not mention this | RISK | OWNER | S | `gh api repos/…` `"dependabot_security_updates":{"status":"disabled"}`; `SECURITY.md:11-14` | a known-vulnerable action pin gets no automatic fix |
| 41 | The repository has no homepage URL, no topics and no Discussions. A stranger asking a question has nowhere to go but a bug template | TASK | OWNER | S | `gh repo view`: `"homepageUrl":"","repositoryTopics":null,"hasDiscussionsEnabled":false`; `.github/ISSUE_TEMPLATE/config.yml` `blank_issues_enabled: false` | questions either turn into malformed bug reports or go unasked |
| 42 | README's badge caption says the gate's "stamps are in the release notes". No release notes have ever been published | REVISION | LOCKFREE-AGENT | S | `README.md:10-11` "the parity gate … its stamps are in the release notes"; draft body "the stamp is named here: `________`" | the promise points at an empty field |
| 43 | README's status paragraph cites "`docs/superpowers/plans/2026-09-24-sprint-12.md` on that branch" under a `docmaint: future` marker. After the merge the phrase goes stale | REVISION | LOCKFREE-AGENT | S | `README.md:54` "on that branch): <!-- docmaint: future -->" | a small edit to fold into the v0.12.0 PR |
| **Public surface: security, licences, story** |||||||
| 44 | SECURITY.md cites the profile-path fix as `c81b17a`, a pre-rewrite hash that no branch contains. The live commit is `f5809c84` | REVISION | LOCKFREE-AGENT | S | `SECURITY.md:22` "a profile name that was really a path, `c81b17a`"; `git branch -a --contains c81b17a \| wc -l` → 0; `git merge-base --is-ancestor f5809c84 origin/main` → on main | the one security fix it cites cannot be opened on GitHub |
| 45 | KNOWN still lists the Sprint 8 review's item (c), "the launcher's profile name reaches `PS2X_MC_DIR` unsanitised", as open. It was fixed on 2026-09-19 | REVISION | LOCKFREE-AGENT | S | `docs/KNOWN.md:150` "(c) the launcher's profile name reaches `PS2X_MC_DIR` unsanitised"; `launcher_config.cpp:445-460` `normalizeProfile` refuses separators; `f5809c84` "the profile is a name, not a path" | a security-sounding open item that is closed |
| 46 | SECURITY.md promises "an acknowledgement within a week". No process or owner step backs that SLA | RISK | OWNER | S | `SECURITY.md:7` "Expect an acknowledgement within a week." | a public promise with no mechanism behind it |
| 47 | The Linux tarball ships about 76 distro libraries in `lib/` with no row in THIRD_PARTY_NOTICES. The notices test checks only `.dll` files, and only when `dist-release/` exists | RISK | LOCKFREE-AGENT | M | `scripts/make_portable.sh:44` `mkdir -p "$PKG/lib"`; `docs/KNOWN.md:143` "lib/ 116 -> 76"; `tools_py/tests/test_third_party_notices.py:91-99` `.endswith(".dll")` + `skipTest("no dist-release/ here")`; `THIRD_PARTY_NOTICES.md` has no `.so` row | a distro FFmpeg build may carry GPL codecs; the Linux archive's licence inventory is unverified. The test that keeps the list complete is `tools_py/tests/test_third_party_notices.py` |
| 48 | The story's frames and gameplay video are not in the disc-derived-bytes audit, although PICTURES.md calls itself that audit's input | RISK | OWNER | S | `ls docs/story/img` → 22 files, `du` 15M, incl. `2026-09-21-online-kill.mp4`; `grep -i story docs/audits/2026-09-21-disc-derived-bytes.md` → no hits; `docs/story/PICTURES.md:5-6` "also the input to Sprint 11 Goal 1's disc-derived decision table" | the largest block of game-rendered imagery in the public repo has no recorded decision |
| 49 | STORY's missing days (three from Sprint 11, two from Sprint 12) have been carried twice and now wait on the owner | NEGLECTED | OWNER | M | `docs/STORY.md:766-775` "the record stops short … carried a second time"; `docs/HUMAN_TASKS.md:31-35` item 5 | the page "written for people who want to play" stops before r0004 and the security fix |
| 50 | STORY's last narrative sentence ("Sprint 11 is open") predates its own closing note | REVISION | LOCKFREE-AGENT | S | `docs/STORY.md:760-761` "Sprint 10 is closed. Sprint 11 is open, and the specs became a plan" | a reader meets a stale present tense directly above the note that explains it |
| **Linux** |||||||
| 51 | INSTALL's Linux claims hold (it says Linux is not a released platform), but no row proves Linux on a real GPU, in a mission at speed, or online. The owner's real-Linux / Steam Deck task has been unchecked since 2026-09-18 | NEGLECTED | OWNER | M | `docs/INSTALL.md:18-20` "the client boots and draws in the project's test VM, but Linux is not a released platform yet"; `docs/KNOWN.md:64,67` (VM title stage 19/23 at "~3 fps on software GL"); `docs/HUMAN_TASKS.md:644` "- [ ] **Run the Linux tarball on a real Linux machine or a Steam Deck**" | CONTRIBUTING:57 asks strangers for Steam Deck results the project never produced |
| 52 | README calls the VM-rebuilt Linux client "the playable build". Nothing shows it has been played beyond the title stage | REVISION | LOCKFREE-AGENT | S | `README.md:61` "the playable build was rebuilt from wiped trees in the VM on 2026-09-23"; KNOWN §1 Linux rows stop at boot and the title stage (`:64`, `:67`) | "playable" overclaims |
| 53 | The VM's suites are still not green (#25, carried). The Python half's cause is believed fixed, but no VM run has confirmed it | UNFINISHED | LOCK-AGENT | M | `gh issue view 25` comment "Pending a VM run to show it. The closing bar … stands unchanged"; `docs/KNOWN.md:103` | Linux cannot be called proven while its own suites fail there |
| 54 | FAQ's Linux dependency list names zenity but not curl, which REPORT A BUG needs on Linux. It also does not say the pad window switch is X11-only | REVISION | LOCKFREE-AGENT | S | `docs/FAQ.md:146-148` "**Optional:** `zenity`"; `ps2xLauncher/src/posix_glue.cpp:476-478` "curl is not installed, so nothing can be sent from here"; `:342` "the window switch is Linux (X11) only in this build" | a Wayland or minimal-install player loses two features with no warning |
| 55 | KNOWN's "Windows-only after the Linux port" row still lists the recompile step, but Sprint 12 ran `ps2_recomp` on Linux in 52 s | REVISION | LOCKFREE-AGENT | S | `docs/KNOWN.md:140` "the recompile step (the owner's disc through ps2_recomp; the generated code travels to Linux as files)"; `docs/CURRENT_SPRINT.md:71` "`ps2_recomp` on Linux in 52 s" | a Linux contributor is told they cannot recompile when they probably can (disc_to_elf.sh on Linux is still unproven, `CONTRIBUTING.md:19`) |
| 56 | The Linux HUMAN_TASKS item tells the owner to set `PS2X_AUDIO_DUMP` before Launch. That knob is dev-mode-only since 2026-09-21 | REVISION | LOCKFREE-AGENT | S | `docs/HUMAN_TASKS.md:654` "with `PS2X_AUDIO_DUMP=/tmp/title.wav` in the environment before Launch"; `SECURITY.md:47-49` "a Dev knob reads as unset without `--dev` / `PS2X_DEV=1`" | the owner's Linux run would silently produce no WAV |
| **What the player meets that is wrong, in the order met** (menus and first mission before the lobby) |||||||
| 57 | 1st, boot/menus: the last four menu screens have scored 60-88 on every gate since Sprint 9 (#30). Nobody has looked at what changed | NEGLECTED | LOCKFREE-AGENT | S | `gh issue view 30` "the same four every time: `s19`..`s22`, the last four menu screens, at 60-88"; its bar only needs saved captures compared | agentic without the owner: the saved PNGs exist, so the comparison needs no game run (only the re-reference needs the lock) |
| 58 | 2nd, menus: 80-133 ms/s of menu render time goes to 1 KB tile uploads (#32) | UNFINISHED | LOCK-AGENT | M | `gh issue view 32` "The login, universe and lobby screens upload 7-11k textures a second at 80-133 ms/s" | agentic (stats line + gate), no ears needed |
| 59 | 3rd, loading screens: every strip of a streamed full-screen image goes to the same DBP (#31, labelled `recomp`, not `render`) | UNFINISHED | LOCK-AGENT | M | `gh issue view 31` "so every strip of a streamed loading-screen image goes to the sam[e DBP]"; its bar includes a unit test in `ps2_gs_tests.cpp` | the unit-test half is LOCKFREE; mislabelled, so a `render` search misses it |
| 60 | 4th, first mission: frame rate (item 8), then the music. About a dozen 50 ms dropouts per mission (#42) and degradation over time (#28) | UNFINISHED | LOCK-AGENT | L | `README.md:60` "about a dozen 50 ms dropouts a mission"; `gh issue view 42`, `28` | #42's instrument is agentic; #28's verdict and the fix's acceptance need the owner's ears (`docs/HUMAN_TASKS.md` "Ten quiet minutes") |
| 61 | 5th, online: saved password lost (#27, item 21), then the freeze candidate (#34, item 29), then the chat traversal (#26), then voice (item 12) | UNFINISHED | LOCK-AGENT | M | `gh issue list` #27, #34, #26 | #27's bar (a clean-exit relaunch) is fully agentic; #34 needs PCSX2 on the owner's box |
| 62 | No `input` or `launcher` issue is open, but the launcher defects in items 9-17 and the keyboard/crouch wording (item 13) have no issue | TASK | LOCKFREE-AGENT | S | `gh issue list` shows no `input`/`launcher` labels; the labels exist (`gh label list`: "launcher … its pages, its settings, the disc check, REPORT A BUG") | the stack reads "launcher: nothing wrong" while this audit finds a dozen defects |

## 2. Structure notes

- **Two public faces, and the default one is behind.** `main` (what GitHub shows) has no INSTALL/FAQ and a README
  that says "the download and its page are Sprint 11". `sprint-12` has both and a README that says Sprint 11 is
  merged. The docs describe the branch the owner works on, not the page a stranger reads (findings 2, 3). Every
  "merged to main" sentence should be written by the merge commit itself, not ahead of it.
- **Player documentation is well built where it exists.** INSTALL, FAQ and the launcher agree string for string on the
  disc verdicts (`main.cpp:134-163` vs `INSTALL.md:79-84`), the three LAUNCH reasons (`focus.cpp:627-632` vs
  `INSTALL.md:114-116`) and all exit sentences (`exit_codes.h:25-37` vs `FAQ.md:18-98`, which reads the same table). This
  is the strongest part of the area. The gaps are features added since the docs were written (GAME VERSION,
  ADVANCED) and cross-surface wording (voice, keyboard, credits).
- **`docs/PLAYTEST.md` has outgrown its purpose.** It was a one-evening script for `playtest-1` (Sprint 9). It now
  mixes that sitting, a second build's notes and decisions already taken. Its generic name and its place in `docs/`
  suggest it is for any tester, yet `:3` says "For the owner". Either regenerate it per build or archive it.
- **`docs/DEVELOPING.md` "Run it (players)"** (`:32-36`) repeats README/INSTALL in a developer file, and
  `README.md:67-76` repeats INSTALL §2-§4. Three places describe the first run, and only INSTALL is maintained
  against the code.
- **KNOWN §2 ("Believed, unconfirmed") is mostly not believed rows.** It holds struck and settled rows (`:105`,
  `:107-110`) and outright proven rows (`:142` "The launcher reports bugs", `:143` "A smaller, checkable download",
  `:145` crouch, `:146` "A failure explains itself"). A stranger told that KNOWN "wins" (`FAQ.md:4-5`) cannot tell
  which §2 rows are live defects.
- **Research numbering misleads.** `docs/research/` has two notes numbered 43 (`43-r0004-capsule.md`,
  `43-what-changed-in-r0004.md`) and none numbered 35. `59-what-a-stranger-sees.md` is about generated names, not the
  stranger path this audit covers.
- **The issue tracker mixes public defects with the owner's machine.** Its labels are well designed (`gh label list`:
  area labels, `needs-disc-gate`, `needs-repro`, `from-launcher`), but three of them (`help wanted`, `good first issue`,
  `from-launcher`) have never been used, and a third of the open issues are about the loop lock.
- **The report path is split across three places a stranger cannot see together:** the launcher
  (`ps2xShared/src/bug_report.cpp`, the scrubber in `diagnostics.cpp:105`), the site (`../scotho`, outside the
  repository; `bug_report.h:4` and `KNOWN.md:142` even give two different paths for `report.ts`) and a git-ignored
  reader skill (`HANDOFF.md:361`). `tools_py/release/` holds only the leak check; nothing there scrubs reports.
- **Rulesets and CI are sound for a contributor.** Required `build`, `build-windows` and `leakcheck`; private
  vulnerability reporting on (`gh api …/private-vulnerability-reporting` → `{"enabled":true}`); issue templates that
  keep game data out.

## 3. Top five

1. **#1 + #5: no download, and the legal decision D2 that gates one.** Every other player-facing item is moot until the owner answers D2 and a release is published.
2. **#2 + #3: `main` is not what the docs say it is.** Sprint 11 was never merged or tagged, so the public default branch has no INSTALL/FAQ. The controller's records claim otherwise in four documents.
3. **#34: `sprint-12` is red on both required CI jobs** (6 Python failures) while the close record says green. The v0.12.0 PR will be refused as it stands, and anyone forking the newest branch starts from red.
4. **#30-#32: the tracker offers a contributor nothing.** No `help wanted`/`good first issue` despite CONTRIBUTING promising them, and 12 of 21 issues rest on logs only the owner has. Labelling #39/#40/#46/#48 costs minutes.
5. **#6-#8 and #12: the README overclaims what a player gets.** "Two players" were two automated instances, the "43-45 fps in a mission" figure is a week-old online-round number with no issue behind it, and voice is described three ways. These are cheap text fixes, plus one issue for the frame rate.
